/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.integration.dropbox;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.dropbox.core.DbxRequestConfig;
import com.dropbox.core.oauth.*;
import com.dropbox.core.v2.DbxClientV2;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.TemplateHelper;
import com.ragic.s3.postoffice2.ConfigItem;
import com.ragic.s3.postoffice2.IntegrationManager;
import com.ragic.s3.postoffice2.MessageFacade;
import com.ragic.s3.service.schedule2.RagicAccountJobs;
import com.ragic.s3.util.*;
import org.json.JSONArray;
import org.json.JSONObject;
import java.io.File;
import java.util.*;

public final class exec_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("com.dropbox.core.oauth");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.TemplateHelper");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.IntegrationManager");
    _jspx_imports_classes.add("com.ragic.s3.service.schedule2.RagicAccountJobs");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.dropbox.core.v2.DbxClientV2");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.dropbox.core.DbxRequestConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.MessageFacade");
    _jspx_imports_classes.add("org.json.JSONArray");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.ConfigItem");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  User user=(User)session.getAttribute("USER");
  final String apname=request.getParameter("a");
  final String dir=request.getParameter("dir");
  final boolean create="true".equals(request.getParameter("create"));

  //create folder
  String accessToken=(String)session.getAttribute("DROPBOX_ACCESS_TOKEN");
  long accessTokenExpirationTime = session.getAttribute("DROPBOX_ACCESS_TOKEN_EXPIRATION") == null? -1L: (long)session.getAttribute("DROPBOX_ACCESS_TOKEN_EXPIRATION");
  String refreshToken = (String)session.getAttribute("DROPBOX_REFRESH_TOKEN");
  if(create){
    if(accessToken==null || accessTokenExpirationTime == -1L || refreshToken == null){
      
      out.write("<script>location='/");
      out.print(apname);
      out.write("/apps/dropbox/index';</script>");

      return;
    }
    try{
      DbxRequestConfig.Builder dbxBuilder = DbxRequestConfig.newBuilder("text-edit/0.1");
      dbxBuilder.withUserLocaleFrom(Locale.getDefault());
      DbxRequestConfig requestConfig = dbxBuilder.build();
      DbxCredential dbxCredential = new DbxCredential(accessToken, accessTokenExpirationTime, refreshToken, Const.DROPBOX_KEY, Const.DROPBOX_SECRET);
      DbxClientV2 client = new DbxClientV2(requestConfig, dbxCredential);
      client.files().createFolderV2(dir);
    }
    catch(Exception e){
      System.out.println("not creating folder:"+dir);
    }
  }

  //install the app (include appInstall.jsp)
  boolean ifZH="zh-TW".equals(bundle.getString("langv5")) || "zh-CN".equals(bundle.getString("langv5"));
  boolean ifNewTemplatesV2=true;//License.isDev()?true:false;
  final String xId=Const.XID_DROPBOX;

  String installedPath=null;
  int installedSheetIndex=0;
  List<String> installed=new ArrayList<>();
  if(ifNewTemplatesV2){
    TemplateHelper.Template t=new TemplateHelper(apname,bundle).getTemplate(ifZH?"00048":"00049");
    JSONObject installedJO=t.install(user);
    for(Iterator i=installedJO.keys();i.hasNext();){
      installedPath=(String)i.next();
      JSONObject pathJO=installedJO.optJSONObject(installedPath);
      JSONObject sheetJO=pathJO.optJSONArray("sheets").optJSONObject(0);
      installedSheetIndex=Encoder.parseInt(sheetJO.optString("sheetIndex"));
      installed.add(installedPath+"/"+installedSheetIndex);
      break;
    }

  }
  else{
    AppInstallation instApp= StoreAppCache.getInstance().getAppInstallation(ifZH?23:3,bundle.getString("langv5"));//AppManager.getAppById(appId);
    installed= AppManager.installApplication(apname, instApp, true);//last argument for install in the same tab
    AppProperty aprop=new AppProperty();
    aprop.loadProperties(new File("cust/def/app/"+apname+"/app.properties"));
//  boolean ifReinstall=aprop.getProperties(appId).length()==0;
//  aprop.setScope(instApp.getApname().replace("zhtw", ""));
    aprop.setScope("bridge");

    //get the installed destination
//  List<String> installedPathSheetList=aprop.getKey(instApp.getPath()+"/"+instApp.getSheets(instApp.getPath()).get(0));
    String installedPathSheet=installed.get(0);//should be only one
    String[] installedPathSheetSplit=installedPathSheet.split("/");
    installedPath="/"+installedPathSheetSplit[1];
    installedSheetIndex=Encoder.parseInt(installedPathSheetSplit[2]);

    //rename installation
    String[] dirsplit=dir.split("/");
    String sheetName=dirsplit[dirsplit.length-1];
    AppManager.renameSheet(apname,installedPath,installedSheetIndex,sheetName,session);
  }



  //change access right to the installation according to initial config
  Map<String,Integer> rights=new HashMap<String, Integer>();
  String installAns=(String)session.getAttribute("INSTALL_ANS");
  JSONArray ja=new JSONArray(installAns);
  Integer right=ja.optInt(3);
  boolean external="e".equals(ja.optString(2));
  rights.put("USER",right);
  if(external) rights.put("EVERYONE",right);
  else rights.put("EVERYONE",Const.NORIGHT);
  AppManager.setSheetPermission(apname,installedPath,installedSheetIndex,rights,session);

  //reset cache
  Account.getInstance(apname).resetAppCache();

  //record installation
//  SimpleDateFormat sdf=new SimpleDateFormat("yyyy/MM/dd HH:mm");
//  new Log("install.log").log(sdf.format(new Date())+","+instApp.getApplicationName()+","+instApp.getInstallTab()+","+apname+","+instApp.getAppId());

  //setup for install animation
  ja=new JSONArray();
  for(String s:installed){
    ja.put(s);
  }

  //config the IntegrationManager
  ConfigItem ci=new ConfigItem();
  ci.setApname(apname);
  ci.setPath(installedPath);
  ci.setSheetIndex(installedSheetIndex);
  ci.setDbxAccessToken(accessToken);
  ci.setDbxAccessTokenExpirationTime(accessTokenExpirationTime);
  ci.setDbxRefreshToken(refreshToken);
  ci.setxId(xId);
  ci.setDirection(ConfigItem.BI);
  ci.setSetupDate(Encoder.formatDate(new Date(), "yyyy/MM/dd HH:mm"));
  ci.setSetupUser(user.getUserId());
  ci.setSetupUserName(user.getUserIdText(apname));
  ci.setEnabled(true);
  //config the dropbox folder
  if(ifNewTemplatesV2) ci.setProperties("version","2");
  ci.setProperties("dir",dir);
  ci.setProperties("userId",(String)session.getAttribute("DROPBOX_USER"));

  IntegrationManager im=IntegrationManager.getInstance(apname);
  if(im==null) return;
  im.addConfigItem(ci);
  im.save();

  //reset progress
  ProgressMessenger.getInstance().reset(apname);

  //do job now
  final String fInstalledPath=installedPath;
  final int fInstalledSheetIndex=installedSheetIndex;
  Thread t=new Thread(new Runnable(){
    public void run() {

      if(create){
        //upload sample file
        //removed because it's a little annoying adding this file every time we sync a folder
//        ProgressMessenger.getInstance().setStatus(apname,"Uploading sample files...",1);
//        DbxRequestConfig requestConfig = new DbxRequestConfig("text-edit/0.1", Locale.getDefault().toString());
//        final DbxClient client = new DbxClient(requestConfig, accessToken);
//        File file=new File("web/integration/dropbox/RagicDocsManual.pdf");
//        BufferedInputStream bis = null;
//        try {
//          bis = new BufferedInputStream(new FileInputStream(file));
//          client.uploadFile(dir+"/"+file.getName(), DbxWriteMode.force(), -1, bis);
//          bis.close();
//        } catch (FileNotFoundException e) {
//          e.printStackTrace();
//        } catch (IOException e) {
//          e.printStackTrace();
//        } catch (DbxException e) {
//          e.printStackTrace();
//        }
      }

      //do sync
      MessageFacade.checkInMessages(apname, fInstalledPath, fInstalledSheetIndex, xId,true,false);
    }
  });
  t.start();


  //register user id to apname relation
  SystemData.getInstance().setProperty(xId+"USER"+session.getAttribute("DROPBOX_USER"),apname);

  //record the ap has installed this ap
  //Account.getInstance(apname).getPaymentStatus().updateApp("_"+Const.XID_DROPBOX,Const.XID_DROPBOX);
  //user.updateInstallRedirect("_"+Const.XID_DROPBOX,Const.XID_DROPBOX);

  //start SystemTaskProgress first
  SystemTaskProgress.setStatus(apname,Const.STP_DROPBOX, "Initializing sync...", -1, SystemTaskProgress.Status.PROCESSING);

  //go to the installed app
  out.println("localStorage.setItem('INSTALL_ANIMATE','"+ja.toString()+"');");
  out.println("localStorage.setItem('DROPBOX_INSPATH','"+installedPath+"');");
  out.println("localStorage.setItem('DROPBOX_INSSHEETINDEX','"+installedSheetIndex+"');");
  out.println("location='/"+apname+installedPath+"/"+installedSheetIndex+"';");
  
  //Add po2 job [issue id: 05057]
  RagicAccountJobs.getInstance().addJob(apname, Const.JOB_PO2);
  

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
