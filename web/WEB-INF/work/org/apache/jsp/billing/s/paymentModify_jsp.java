/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.billing.s;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.License;
import com.ragic.s3.util.Log;
import com.ragic.s3.util.SystemData;
import com.ragic.s3.service.notification.Mail2;
import com.ragic.s3.service.payment.stripe.StripeManager;
import com.stripe.Stripe;
import com.stripe.exception.CardException;
import java.util.Date;

public final class paymentModify_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.stripe.exception.CardException");
    _jspx_imports_classes.add("com.ragic.s3.service.payment.stripe.StripeManager");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.util.License");
    _jspx_imports_classes.add("com.stripe.Stripe");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemData");
    _jspx_imports_classes.add("com.ragic.s3.service.notification.Mail2");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  //environment info
  User user=(User)session.getAttribute("USER");
  RequestParam rp=(RequestParam) request.getAttribute("REQ");
  if(user==null) return;
  //parameters
  String apname=rp.getApname();

  if(apname==null) {
    return;
  }

  String edition=request.getParameter("e");
  String quantity=request.getParameter("quantity");
  String months=request.getParameter("months");
  String coupon=request.getParameter("coupon");
  String email=request.getParameter("email");
  String info=request.getParameter("info");
  String token=request.getParameter("stripeToken");
  String nameOnCard=request.getParameter("nameOnCard");
  String subId=request.getParameter("subId");
  boolean ifOnPremise = Account.ONPREMISE.equals(edition) || Account.CONPROF_ONPREMISE.equals(edition) || Account.CONENT_ONPREMISE.equals(edition);
  RagicBundle bundle=(RagicBundle)session.getAttribute("RAGIC_BUNDLE");

  boolean ifGhosted=SystemData.getInstance().getProperty("GHOSTED_"+apname)!=null || session.getAttribute("GHOSTED")!=null;;
  if(ifGhosted){
    CardException cex=new CardException("Your card was declined.","req","FU","","","",-1,null);
    request.setAttribute("PAYMENT_RESULT", cex);
    return;
  }

  //plan restart
  if(edition!=null && quantity!=null && months!=null && token!=null && nameOnCard!=null){
    System.out.println("paymentModify.jsp, plan restart, token:"+token+" nameOnCard:"+nameOnCard);

    StripeManager sm=new StripeManager(apname,user,request,bundle);
    sm.setCoupon(coupon);
    if (ifOnPremise) sm.createPlanOp(edition,quantity,months,email,info);
    else sm.restartSubscription(edition,quantity,months,token,nameOnCard);
    return;
  }

  //plan modify
  if(edition!=null && quantity!=null && months!=null){
    new Log("log/planModify.log").setIfDisplayTime(true).info(apname+","+user.getUserId()+","+edition+","+quantity+","+months+","+email);
    StripeManager sm=new StripeManager(apname,user,request,bundle);
    sm.setCoupon(coupon);
    if (ifOnPremise) {
      if (subId != null) sm.modifyPlanOp(edition,quantity,months,email,info,subId);
      else sm.createPlanOp(edition,quantity,months,email,info);
    }
    else sm.modifyPlan(edition,quantity,months,email,info);
  }

  //sms package
  String smsPackages=request.getParameter("smsPackages");
  if(smsPackages!=null){

    //auto ghosting sms communication when the account upgrades and immediately buys SMS package, likely pattern of spammer
    Date trialStartDate=Account.getInstance(apname).getPaymentStatus().getTrialStartDate();
    if(trialStartDate==null) trialStartDate=new Date(0L);
    long trialSince=new Date().getTime() - trialStartDate.getTime();
    if(trialSince<48L*Const.HOUR_MILLI){
      SystemData.getInstance().setProperty("GHOSTED_"+apname, "Automatic "+ Encoder.formatAsStandardDate(new Date(),true));
      new Mail2().compose("jeff@ragic.com",null,"support@ragic.com","SMS Watchdog","Auto ghosted account:"+apname,
              "Buying SMS package of $"+smsPackages+" in "+(trialSince/Const.HOUR_MILLI)+" hours of getTrialStartDate.").send();
    }
    else{
      StripeManager sm=new StripeManager(apname,user,request,bundle);
      sm.addSmsPackage(smsPackages);
    }
  }

  //cancel account
  if("1".equals(request.getParameter("confirmCancel"))){
    String reason=request.getParameter("reason");
    StripeManager sm=new StripeManager(apname,user,request,bundle);
    if (subId != null) sm.cancelSubscriptionOp(reason,subId);
    else sm.cancelSubscription(reason);
  }


  //update card
  if(token==null && nameOnCard!=null){
    request.setAttribute("PAYMENT_RESULT", "Your card number was not sent, please try turning off your browser extensions and try again, or use another browser. If you still experience problem, please e-mail support@ragic.com");
  }
  else if(token!=null && nameOnCard!=null){
    StripeManager sm=new StripeManager(apname,user,request,bundle);
    sm.setCoupon(coupon);
    sm.updateCard(token,nameOnCard);
  }

  //do extra file storage
  //we bill them manually
//  String fileStorge=request.getParameter("fileStorage");
//  String dbStorage=request.getParameter("dbStorage");
//  if(fileStorge!=null && dbStorage!=null){
//    aps.modifyExtraFileStorage(Encoder.parseInt(fileStorge));
//    aps.modifyExtraStorage(Encoder.parseInt(dbStorage));
//    new Mail2().compose("billing@ragic.com",null,"billing@ragic.com","Ragic Billing",
//            "storage modify",rp.getApname()+","+user.getUserId())
//            .send();
//  }



    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
