/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:40 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.external.saml;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.authentication.Authentication;
import com.ragic.s3.authentication.HTTPAuthentication;
import com.ragic.s3.authentication.exception.AuthenticationFailedException;
import com.ragic.s3.authentication.exception.AuthenticationNotCompleteException;
import com.ragic.s3.authentication.logger.AuthenticationLogger;
import com.ragic.s3.authentication.saml.*;
import com.ragic.s3.nui.Account;
import net.shibboleth.utilities.java.support.component.ComponentInitializationException;
import net.shibboleth.utilities.java.support.xml.XMLParserException;
import org.apache.jasper.tagplugins.jstl.Util;
import org.opensaml.core.xml.io.UnmarshallingException;
import java.io.File;
import java.text.MessageFormat;
import java.nio.file.Paths;
import com.ragic.s3.nui.helper.ExceptionManager;
import com.ragic.s3.util.Log;
import com.ragic.s3.nui.helper.URLCaller;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.AccountSetting;
import com.ragic.s3.util.Const;
import com.ragic.s3.nui.Plan.PricingPlan;
import com.ragic.s3.util.License;

public final class samlLogin_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("com.ragic.s3.authentication.saml");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.authentication.Authentication");
    _jspx_imports_classes.add("com.ragic.s3.authentication.logger.AuthenticationLogger");
    _jspx_imports_classes.add("net.shibboleth.utilities.java.support.component.ComponentInitializationException");
    _jspx_imports_classes.add("com.ragic.s3.authentication.exception.AuthenticationFailedException");
    _jspx_imports_classes.add("com.ragic.s3.util.License");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("com.ragic.s3.nui.Plan.PricingPlan");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
    _jspx_imports_classes.add("java.text.MessageFormat");
    _jspx_imports_classes.add("com.ragic.s3.authentication.exception.AuthenticationNotCompleteException");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("org.opensaml.core.xml.io.UnmarshallingException");
    _jspx_imports_classes.add("com.ragic.s3.nui.AccountSetting");
    _jspx_imports_classes.add("org.apache.jasper.tagplugins.jstl.Util");
    _jspx_imports_classes.add("java.nio.file.Paths");
    _jspx_imports_classes.add("net.shibboleth.utilities.java.support.xml.XMLParserException");
    _jspx_imports_classes.add("com.ragic.s3.authentication.HTTPAuthentication");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.URLCaller");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    //Logger logger = LoggerFactory.getLogger("/api/external/saml/samlLogin.jsp");
    Log log=new Log("log/saml.log");
    boolean ignoreSSL=true;


    String apname = request.getParameter("a");
    String csrfToken=request.getParameter("t");

    if(!User.getCSRFToken(session).equals(csrfToken)){
        out.println("SAML login fail. Reason: Invalid token");
        return;
    }

    apname=Encoder.sanitizeParameter(apname);

    if (apname == null) {
        out.println("SAML login fail. Reason: missing parameter apname");
        // Currently not support
        return;
    }

    if (!OpenSAMLUtils.hasAuthorityToUseSingleSignOn(apname)) {
        out.println("SAML login fail. Reason: account("+apname+") don't have access right for sso");
        return;
    }

    Account account = Account.getInstance(apname);

    if (account == null) {
        // we need to use double single quote due to
        // https://docs.oracle.com/javase/8/docs/api/java/text/MessageFormat.html
        out.println("SAML login fail. Reason: couldn't get account instance from account "+apname);
        return;
    }

    //æª¢æ¥æ¯å¦çºä¼æ¥­çï¼æ«ææé¤ç§æä¸»æ©!ç±æ¼å¤æ·ä¸åºç§æä¸»æ©çä¼æ¥­ç
    Boolean ifPlanAllowSAML=account.getPricingPlan().isIfSAMLIntegration();
    if(!ifPlanAllowSAML&& !License.isDev()&&!License.isDistr()){
        out.println("SAML login fail. Reason: Single sign-on is only available for databases on the enterprise plan.");
        return;
    }

    String ssoIDP=account.getSSOIDP();
    String ssoIDPApplicationId=account.getSSOIDPApplicationId();

    log.setIfDisplayTime(true).info("[SSOIDP]:"+ssoIDP+"[ssoIDPUrl]:"+account.getSSOIDPUrl()+"[ssoIDPApplicationId]:"+ssoIDPApplicationId);

    boolean isAD=Const.IDP_ACTIVE_DIRECTORY.equals(ssoIDP);

    String metadataUrl = OpenSAMLUtils.getMetadataUrl(isAD,account.getSSOIDPUrl());

    //æª¢æ¥å¸³èæ¯å¦åè¨±SAMLç»å¥
    AccountSetting setting=account.getAppSetting();
    if(setting.getDeniedLoginType().contains(Const.LOGINTYPE_SAML)){
        out.print("SAML login fail. Reason: account "+apname+" did not allow SAML login");
        return;
    }


    boolean isUrl = Util.isAbsoluteUrl(metadataUrl);
    if (!isUrl) {
        out.print("SAML login fail. Reason: sso (saml) settings (metadata url) for account("+apname+") is not valid");
        return;
    }

    // create IdentityProvider instance from metadata
    IdentityProvider identityProvider = null;
    String metadataPath = Paths.get(System.getProperty("java.io.tmpdir"), "metadata_" + apname + ".xml").toAbsolutePath().toString();
    //File metadataFile = new File(metadataPath);
    try {
        //avoid meta data changing resulting in hard to purge cache
//        if (metadataFile.exists()) {
//            try {
//                if (!OpenSAMLUtils.isMetadataFileStale(metadataPath)) {
//                    // create identityProvider instance from metadata file
//                    out.print("Create identityProvider instance from [metadata file="+metadataPath+"]");
//                    identityProvider = new IdentityProviderBase(metadataPath);
//                }
//            } catch (XMLParserException | UnmarshallingException | ComponentInitializationException e) {
//                log.error(MessageFormat.format("Create identityProvider instance from [metadata file={0}] failed", metadataPath));
//                ExceptionManager.getInstance().processException(e);
//            }
//        }

        // create identityProvider instance from metadata url
        if (identityProvider == null) {

            if(ignoreSSL) URLCaller.ignoreSSL();

            out.println("Create identityProvider instance from [metadata url="+metadataUrl+"]");
            identityProvider = new IdentityProviderBase(metadataUrl, metadataPath);
        }
    } catch (AuthenticationFailedException e) {
        // we need to use double single quote due to
        // https://docs.oracle.com/javase/8/docs/api/java/text/MessageFormat.html
        ExceptionManager.getInstance().processException(e);
        out.println("SAML login fail. Reason: couldn't create identityProvider instance. apname="+apname+", message="+e.getMessage());
        return;
    }

    // Start SAML Request
    HTTPAuthentication httpAuthentication = new HTTPAuthentication(request, response, SAMLStrategy.class);
    ServiceProvider serviceProvider = new RagicServiceProvider(ssoIDP,ssoIDPApplicationId);
    ProviderPair providerPair = new ProviderPair(serviceProvider, identityProvider);
    httpAuthentication.setParameter(SAMLStrategy.PARAMETER_APNAME, apname);
    httpAuthentication.setParameter(SAMLStrategy.PARAMETER_PROVIDER_PAIR, providerPair);
    httpAuthentication.setParameter(SAMLStrategy.PARAMETER_IDP, ssoIDP);
    httpAuthentication.setParameter(SAMLStrategy.PARAMETER_APPLICATIONID, ssoIDPApplicationId);
    session.setAttribute("authentication", httpAuthentication);

    Authentication authentication = new AuthenticationLogger(httpAuthentication);
    try {
        session.setAttribute(HTTPAuthentication.AUTHENTICATION_RETURN_TO_URL, session.getAttribute("LOGIN_REFERER"));
        authentication.authenticate();
        httpAuthentication.returnUrl();
    }
    catch (AuthenticationNotCompleteException authenticationNotCompleteException) {
        // Ignore, waiting for Authentication Done
    }
    catch (Exception e) {
        // guess the Authentication Fail
        ExceptionManager.getInstance().processException(e);
        out.println("SAML Authentication fail. apname="+apname+", message="+e.getMessage());
        httpAuthentication.returnUrl();
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
