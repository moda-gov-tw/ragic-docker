/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.http;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.AppManager;
import org.json.JSONObject;
import java.util.Map;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.service.api.actionButton.ButtonExecutorFactory;
import com.ragic.s3.service.api.actionButton.ButtonExecutor;
import com.ragic.s3.sdb.data.bdb.*;
import org.json.JSONArray;
import com.ragic.s3.service.workflow.WorkflowApprovalAgent;
import com.ragic.s3.util.Const;
import com.ragic.s3.service.workflow.Workflow;
import java.util.Set;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.nui.helper.ExceptionManager;
import com.ragic.s3.nui.*;
import java.util.Collection;

public final class actionButton_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.sdb.data.bdb");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.Workflow");
    _jspx_imports_classes.add("com.ragic.s3.service.api.actionButton.ButtonExecutorFactory");
    _jspx_imports_classes.add("java.util.Collection");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.WorkflowApprovalAgent");
    _jspx_imports_classes.add("com.ragic.s3.service.api.actionButton.ButtonExecutor");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("application/json; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  RequestParam rp = (RequestParam) request.getAttribute("REQ");
  String apname = rp.getApname();
  String path = rp.getPath();
  int si = rp.getSheetIndex();
  int ni = rp.getNodeId();
  String buttonId=request.getParameter("bId");
  if(buttonId.contains("_")){
    buttonId=buttonId.substring(0,buttonId.indexOf("_"));
  }
  int bi = Encoder.parseInt(buttonId, -1);
  boolean ifApproval = request.getParameter("approval") != null;
  String rmlc = request.getParameter("rmlc");

  JSONObject result = new JSONObject();
  result.put("status", "SUCCESS");

  try {
      if(bi < -1 || si == -1 || path.isEmpty() || apname.isEmpty()) {
          response.setStatus(HttpServletResponse.SC_BAD_REQUEST);
          result.put("status", "ERROR");
          result.put("msg", "Invalid parameters.");
          return;
      }

      User user = (User) session.getAttribute("USER");
      RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");

      NUIForm form;
      if(ni > -1) {
          form = AppManager.getFormPage(apname, path, si);
      } else {
          form = AppManager.getListingPage(apname, path, si);
      }

      if(form == null) {
          response.setStatus(HttpServletResponse.SC_BAD_REQUEST);
          result.put("status", "ERROR");
          result.put("msg", "Form not found");
          return;
      }

      if(user == null) {
          response.setStatus(HttpServletResponse.SC_BAD_REQUEST);
          result.put("status", "ERROR");
          result.put("msg", "User not found");
          return;
      }

      Map<String,ActionButton> actionButtons = form.getActionButtons();

       if(actionButtons == null) {
          response.setStatus(HttpServletResponse.SC_BAD_REQUEST);
          result.put("status", "ERROR");
          result.put("msg", "actionButtons not found");
          return;
      }

      Map.Entry<String,ActionButton> matchedActionButton = actionButtons.entrySet().stream().filter(
              actionButton -> String.valueOf(bi).equals(actionButton.getKey())
              ).findAny().orElse(null);

      if(matchedActionButton == null) {
          response.setStatus(HttpServletResponse.SC_BAD_REQUEST);
          result.put("status", "ERROR");
          result.put("msg", "Button not found: " + bi);
          return;
      }

      ActionButton actionButton = matchedActionButton.getValue();

      if(actionButton == null) {
          response.setStatus(HttpServletResponse.SC_BAD_REQUEST);
          result.put("status", "ERROR");
          result.put("msg", "ActionButtonInfo not found");
          return;
      }

      if(ifApproval) {
          String userId = user.getUserId();
          String userName = user.getUserName(apname);
          user = new UserSuper(userId, userName);
      }

      boolean needApproval = actionButton.ifNeedApproval();
      if(needApproval && ni != Const.NONODE) {
          WorkflowApprovalAgent approvalAgent = new WorkflowApprovalAgent(form, ni, user, bundle);
          Workflow wf = approvalAgent.getCurrentWorkflow();
          if(wf == null || !wf.getStatus().equals(Workflow.FINISH)) {
              response.setStatus(HttpServletResponse.SC_FORBIDDEN);
              result.put("status", "ERROR");
              result.put("msg", "This action button is only executable when approved");
              return;
          };
      }

      // [issue #15171, #15141]
//      if(!(ifApproval && ni != Const.NONODE)) {
//           boolean hideButton = actionButton.ifHideButton();
//          if(hideButton) {
//              response.setStatus(HttpServletResponse.SC_FORBIDDEN);
//              result.put("status", "ERROR");
//              result.put("msg", "This action button is hidden.");
//              return;
//          }
//      }
      
      boolean checkIfUserCanEdit = actionButton.ifCheckIfUserCanEdit();
      if(checkIfUserCanEdit && !user.ifApManager(apname)) {
    	  boolean canEdit = true;
    	  if(ni >= 0) {
    		  // Check if this record is belong to this user
    		  BDBWorker worker = new BDBWorker(apname);
              worker.setWhoami("api/http/actionButton.jsp check if can edit");
              try {
                  canEdit = worker.checkAccessToNode(form.getRootDomainId(), ni, user);
              } catch(Exception ignored) {
              } finally {
                  worker.commit();
              }
    	  }
    	  
          if(!canEdit) {
        	  int rightLevel = form.checkRight(user);
              switch(rightLevel) {
              case Const.NORIGHT: // no right
              case Const.VIEWER: // read all records, read only
                  canEdit = false;
                  break;
              case Const.USER: // can only read + edit his own records
              case Const.MANAGER: // can read all records, can only edit his own records
                  // Already checked
                  break;
              default:
                  canEdit = true;
              }
          }
          
          if(!canEdit) {
        	  response.setStatus(HttpServletResponse.SC_FORBIDDEN);
              result.put("status", "ERROR");
              result.put("msg", "This action button is only available for users who can edit this entry.");
              return;
          }
      }

      boolean ifAllowed = actionButton.ifUserAllowed(apname,path,si,user);
      if(!ifAllowed) {
          response.setStatus(HttpServletResponse.SC_FORBIDDEN);
          result.put("status", "ERROR");
          result.put("msg", "This action button is not available for your groups");
          return;
      }

      ButtonExecutor be = ButtonExecutorFactory.getInstance(actionButton, ni, user, bundle, form, rmlc);
      result = be.work();
      //System.out.println(result.toString(2));
  } catch (Exception e) {
      ExceptionManager.getInstance().processException(e);
      result.put("status", "ERROR");
      result.put("msg", e.getClass().getName()+":"+e.getMessage());
  } finally{
      out.println(result.toString(4));
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
