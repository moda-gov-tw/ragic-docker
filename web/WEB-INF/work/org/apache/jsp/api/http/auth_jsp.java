/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.http;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.User;
import com.ragic.s3.util.Util;
import org.json.JSONArray;
import org.json.JSONObject;
import java.util.*;
import java.util.Set;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.helper.RemoteAccountManager;
import com.ragic.s3.nui.helper.UserManager;
import com.ragic.s3.util.Const;

public final class auth_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.RemoteAccountManager");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserManager");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  //logged in via the LoginFilter
  try{
    User user=(User)session.getAttribute("USER");
    RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    String callback=request.getParameter("callback");
    boolean logout=request.getParameter("logout")!=null;
    boolean status=request.getParameter("status")!=null;
    boolean json=request.getParameter("json")!=null;
    boolean apikey=request.getParameter("apikey")!=null || request.getParameter("APIKey")!=null;
    boolean ifRagicSupport= UserManager.ifRagicSupport(user.getUserId());
    int version= Encoder.parseInt(request.getParameter("v"),1);
    if(logout){
      Util.clearCookieSession(request, response);
      session.invalidate();
      out.println("Logged out.");
      return;
    }
    if(status){
      if(user!=null) out.println(user.getUserId());
      else out.println("no user");
      return;
    }

    if(user==null || !user.isValid()){
        //[BugId:09019]
        if(version==2){
          JSONObject jo=new JSONObject();
          String loginMsg=(String)session.getAttribute("LOGIN_MSG");
          if(loginMsg!=null){
            jo.put("code",1);
            jo.put("message",loginMsg);
          }
          else{
            jo.put("code",2);
            jo.put("message",bundle.getString("incorrectLogin"));
          }
          out.println(jo.toString());
          return;
        }
        //version 1
        else {
          out.print("-1");
          return;
        }
    }

    if(json){
      int MAX_AP_NUMBER=100;
      JSONObject jo=new JSONObject();
      if(user!=null){
        jo.put("sid",session.getId());
        jo.put("email", user.getUserId());
        if(apikey) jo.put("apikey", RemoteAccountManager.main_getAPIKey(user.getUserId(), user.isPassivelyAddedUser()));

        JSONObject _2faInfo=new JSONObject();
        _2faInfo.put("is2faLogin", user.is2faLogin());
        if(user.is2faLogin()) {
          _2faInfo.put("phone", user.getPhone());
          _2faInfo.put("method", user.get_2faMethodValue());
          _2faInfo.put("authyId", user.getAuthyId());
        }
        jo.put("2fa", _2faInfo);

        //only returns the default ap, generally we don't want them to retrieve info on all aps
        JSONObject apdat=new JSONObject();
        Map<String, User.UserAp> map=user.getUapMap();
        int count=0;
        for(Map.Entry<String,User.UserAp> e:map.entrySet()){
          if(count>MAX_AP_NUMBER) break;
          count++;
          apdat.put("account",e.getKey());
          apdat.put("ragicId",e.getValue().getUserNodeId());
          apdat.put("username",e.getValue().getUserName());
          apdat.put("external",e.getValue().isExternalv2());
          Set<String> groups=user.getGroups(e.getKey());
          Set<String> upGroups=new LinkedHashSet<>();
          for(String s:groups) upGroups.add(s.toUpperCase());
          if(groups!=null) {
              apdat.put("groups",upGroups);
              break;
          }
        }
        jo.put("accounts",apdat);

        //returns all ap
        if(apikey){
          JSONArray allAccounts=new JSONArray();
          count=0;
          for(String apname:user.getAuthenticatedApnames().keySet()){
            if(count>MAX_AP_NUMBER) break;
            count++;
            JSONObject apjo=new JSONObject();
            apjo.put("account",apname);
            User.UserAp uap=user.initUap(apname);
            apjo.put("ragicId",uap.getUserNodeId());
            apjo.put("username",uap.getUserName());
            apjo.put("external",uap.isExternalv2());
            Set<String> upGroups=new LinkedHashSet<>();
            Set<String> groups=null;
            if(ifRagicSupport){
              upGroups.add(Const.G_ADMIN);
            }
            else{
              groups=user.getGroups(apname);
              for(String s:groups) upGroups.add(s.toUpperCase());
            }
            if(groups!=null) apjo.put("groups",upGroups);
            
        	AbstractMap.SimpleEntry<Boolean, User.PasswordChangeType> needChangePwd = Util.checkUserNeedChangePassword(user, apname);
            if(needChangePwd.getKey()) {
                apjo.put("needChangePassword", needChangePwd.getValue().getDescription(bundle));
            }
            
            allAccounts.put(apjo);
          }
          jo.put("allAccounts",allAccounts);
        }


      }
      if(callback!=null) out.print(callback+"('");
      out.print(jo.toString(2));
      if(callback!=null) out.print("');");
    }
    else{
      //System.out.println("AUTH?"+user);
      if(user!=null){
        if(callback!=null) out.print(callback+"('");
        out.print(session.getId());
        if(callback!=null) out.print("');");
      }
      else{
        if(callback!=null) out.print(callback+"(");
        out.print("-1");
        if(callback!=null) out.print(");");
      }
    }
  }
  catch(java.lang.IllegalStateException e){}


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
