/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.http;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.RequestParam;
import com.ragic.s3.nui.Sheet;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.sdb.search.FTSDocument;
import com.ragic.s3.sdb.search.FTSRequest;
import com.ragic.s3.sdb.search.FTSResponse;
import com.ragic.s3.sdb.search.FTSearch;
import org.json.JSONArray;
import org.json.JSONObject;
import java.util.List;
import com.ragic.s3.nui.NUIFilter;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.util.Const;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.nui.NUIFilterSet;
import java.util.Set;
import java.util.HashSet;

public final class search_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


  public boolean ifMatchFixedFilter(String apname, String path, String sheet, int rootNodeId, User user, RagicBundle bundle, boolean isMVP, String[] filterV2) {
    if(apname == null || apname.trim().isEmpty() ||
            path == null || path.trim().isEmpty() ||
            sheet == null || sheet.trim().isEmpty() ||
            rootNodeId == Const.NONODE) return false;

    NUIForm listingPage = AppManager.getListingPage(apname, path, sheet);
    NUIForm formPage = AppManager.getListingPage(apname, path, sheet);
    if(listingPage==null) return false;

    List<NUIFilter> fixedFilters = listingPage.getFixedFilters();
    if((fixedFilters == null || fixedFilters.size() == 0) && (!isMVP || filterV2 == null)) return true;

    APIQuery q = new APIQuery(apname, path, sheet);
    q.setUpdateMode();
    if(fixedFilters != null) {
      for (NUIFilter fixedFilter : fixedFilters) {
        for (int fixedFilterDomain : fixedFilter.getDomainIds())
          q.addFetchDomains(fixedFilterDomain); // for performance
      }
    }

    NUIFilterSet fset = new NUIFilterSet();
    if(fixedFilters != null) {
      for (NUIFilter fixedFilter : fixedFilters) {
        fset.addFilter(fixedFilter);
      }
    }

    if(isMVP && filterV2 != null && filterV2.length > 0) {
      q.addFetchDomains(formPage.getTitleField());

      //apply filters on the mvp block now
      for (String s : filterV2) {
        NUIFilter filter = listingPage.createNUIFilterV2(s);
        if (filter == null) continue;
        fset.addFilter(filter);
        for (int filterDomain : filter.getDomainIds()) {
          q.addFetchDomains(filterDomain); // for performance
        }
        q.addFetchDomains(filter.getOriginalDomainId());
      }
    }

    APIEntry e = q.getAPIEntry(rootNodeId);
    if(e == null) return false;

    return e.checkFilterSet(fset, user, bundle);
  }

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTSResponse");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIFilter");
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTSRequest");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIFilterSet");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.HashSet");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTSDocument");
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTSearch");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.Sheet");
    _jspx_imports_classes.add("com.ragic.s3.nui.RequestParam");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  RequestParam rp=(RequestParam) request.getAttribute("REQ");
  User user=(User) session.getAttribute("USER");
  String apname=rp.getApname();
  String qt=request.getParameter("q");
  String path=rp.getPath();
  String sheet=rp.getSheet()!=null?rp.getSheet().getFormName():null;
  boolean ifMvp = request.getParameter("mvp") != null;
  boolean keywordFixedToSheet=path!=null && path.length()>0 && sheet!=null && sheet.length()>0;
  if(apname==null || qt==null) return;
  qt=qt.trim().toLowerCase();
  if(qt.length()==0) return;


  FTSRequest req;
  boolean ifSheetSearch=path!=null && sheet!=null;
  if(ifSheetSearch) {
    req=new FTSRequest(user,apname,qt,path,sheet);
    req.setKeywordFixedToSheet(keywordFixedToSheet);
  }
  else {
    req=new FTSRequest(user,apname,qt);
  }
  req.setMaxDocs(20);
  FTSResponse res=null;
  if(Encoder.ifPureEnglish(qt)) res=FTSearch.prefixSearch(req);
  else res=FTSearch.search(req);

  res.setCheckUserRights(true);


  JSONObject result=new JSONObject();
  JSONArray jaKeywords=new JSONArray();
  result.put("k",jaKeywords);



  JSONArray ja=new JSONArray();
  result.put("i",ja);
  List<FTSDocument> list=res.getDoclist(apname,user);
  Set<String> matchedKeyword=new HashSet<>();
  int keyWordNum = req.getMaxKeywords();

  for(FTSDocument doc:list){
    if(doc==null) continue;
    if("kb".equals(apname) && !doc.getPath().equals("/document")) continue;
    if(doc.isNoRights() || doc.isInvalid()) continue;
    if("/ragic-setup".equals(doc.getPath())) continue;
    String url=doc.getUrl();
    if(url!=null && url.startsWith("/ragic-setup")) continue;

    boolean valid = ifMatchFixedFilter(apname, doc.getPath(), doc.getSheet(), doc.getNodeId(), user, bundle, ifMvp, request.getParameterValues("fv2"));
    if(!valid) continue;

    if(matchedKeyword.size() <= keyWordNum) {
      String keyword = doc.getMatchedTerm();
      if(!matchedKeyword.contains(keyword)) {
        matchedKeyword.add(keyword);

        JSONObject keywordJo = new JSONObject();
        if(keyword !=null && !keyword.isEmpty()) {
          keywordJo.put("d",qt);
          keywordJo.put("w",keyword);
          jaKeywords.put(keywordJo);
        }
      }
    }

    JSONObject jo=new JSONObject();
    jo.put("ragicId",doc.getNodeId());
    jo.put("path",doc.getPath());
    jo.put("sheetIndex",doc.getSheetIndex());
    jo.put("name",doc.getEntryName());
    jo.put("url",url);

    //creating term display, highlight matched part
    String matchedPart=doc.getMatchedPart(apname, user);
    if(matchedPart==null || matchedPart.length()==0) jo.put("term","");
    else jo.put("term",matchedPart);

    //generating form name
    String[] sec=url.split("/");
    Sheet sh=AppManager.getSheetBySheetIndex(apname,"/"+sec[1],Integer.parseInt(sec[2]));
    if(sh!=null) {
      jo.put("fn", sh.getName());
      ja.put(jo);//only return as result if we can find sheet, if not able to find it, it's probably deleted
    }
  }

  out.print(result.toString(2));

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
