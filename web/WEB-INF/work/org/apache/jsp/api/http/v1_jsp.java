/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.http;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.service.workflow.ScriptResponse;
import org.json.JSONObject;
import java.io.PrintWriter;
import java.io.StringWriter;

public final class v1_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.io.PrintWriter");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.io.StringWriter");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ScriptResponse");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/javascript; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  RequestParam rp=(RequestParam) request.getAttribute("REQ");
  //RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");

  String apname=rp.getApname();
  String path=rp.getPath();
  int sheetIndex=rp.getSheetIndex();//eg: 1_test

  //actions
  int pageDelta=0;
  try {
    pageDelta=Integer.parseInt(request.getParameter("pd"));
  }
  catch (Exception e) {
  }

  //paging for crawler
  int pageUrl=-1;
  try {pageUrl=Integer.parseInt(request.getParameter("pg"));}
  catch (Exception e) {}


  //text filters
  //filter domain
  int filterDomainId=0;
  try {
    filterDomainId=Integer.parseInt(request.getParameter("fd"));
  }
  catch (Exception e) {}
  //textfilter
  String textFilter=request.getParameter("tf");
  boolean exactMatch=request.getParameter("em")!=null;
  //nodefilter
  String filterNodeIds=request.getParameter("fni");
  //clearing filter
  int clearDomainId=0;
  try {
    clearDomainId=Integer.parseInt(request.getParameter("cl"));
  }
  catch (Exception e) {}

  //filter 2.0
  String[] filterv2=request.getParameterValues("fv2");

  //sorting
  int sortDomainId=-1;
  try {
    sortDomainId=Integer.parseInt(request.getParameter("sdi"));
  }
  catch (Exception e) {}

  //defaulting
//  int defaultDomain=-1;
//  try {
//    defaultDomain=Integer.parseInt(request.getParameter("dd"));
//  }
//  catch (Exception e) {}
//  int defaultNodeId=-1;
//  try {
//    defaultNodeId=Integer.parseInt(request.getParameter("dni"));
//  }
//  catch (Exception e) {}



  /*
  *
  * Execution on NUIFormSet
  *
  */


  //retrieve the query parameter
  NUIFormSet set=null;
  try{
    Sheet sheet=AppManager.getSheetBySheetIndex(rp.getApname(),rp.getPath(),sheetIndex);
    set=new NUIFormSet(apname, path, sheet.getFormName(), session);//no session stored NUIFormSet for stateless browsing
    request.setAttribute("NUIFORMSET",set);

    if(rp.getNodeId()>=0){
      //set the form
      String newFileName=set.getFormName()+"_index.nui";
      if (!newFileName.equals(set.getForm().getFileName())) {//don't go to the same form
        set.goForm(newFileName);
      }
      set.setCurrentNodeId(rp.getNodeId());
    }


    NUIForm form=set.getForm();
    if (form.getType()==NUIForm.NUI_TABLE) pageDelta*=set.getTableListingEntryLimit();

    //run the actions on parameter
    set.addItemDelta(pageDelta);
    set.setPureInsert(false);

    //paging from URL
    if(pageUrl!=-1) set.setBeginItem(pageUrl);

    //filters
    if (textFilter!=null && filterDomainId!=0) {
      if(textFilter.length()>0) {
        NUIFilter f=new NUIFilter(filterDomainId,exactMatch?NUIFilter.OPERAND_EQ:NUIFilter.OPERAND_LK,textFilter);
        set.addFilter(f);
      }
      set.setBeginItem(0);
    }
    if (filterNodeIds!=null && filterNodeIds.length()>0 && filterDomainId!=0) {
      NUIFilter f=new NUIFilter(filterDomainId,NUIFilter.OPERAND_EQN,filterNodeIds);
      set.addFilter(f);
      set.setBeginItem(0);
    }
    if (clearDomainId!=0) {
      if(clearDomainId==-11) set.removeFilter();
      else set.removeFilter(clearDomainId);
      set.setBeginItem(0);
    }

    //filter 2.0
    if(filterv2!=null){
      if(filterv2.length>0) set.removeFilter();
      for(String s:filterv2){
        try{
          NUIFilter filter=NUIFilter.createNUIFilterV2(s, form).orElse(null);
          if(filter==null) continue;
          set.addFilter(filter);
          set.setBeginItem(0);
        }
        catch(Exception e){
          continue;
        }
      }
    }

    //sorting
/*    if (sortDomainId!=-1){
      set.sort(sortDomainId);
    }*/
//    if (defaultDomain!=-1 && defaultNodeId!=-1){
//      set.setUrlDefault(defaultDomain,defaultNodeId);
//      //we set it as pure insert
//      set.setPureInsert(true);
//    }
  }
  catch(Exception e){
    JSONObject err=new JSONObject();
    err.put("status", ScriptResponse.ERROR);
    err.put("msg","Error retrieving data from your form:"+e.getMessage());
    StringWriter sw=new StringWriter();
    e.printStackTrace(new PrintWriter(sw));
    err.put("detail",sw.toString());
    out.print(err.toString(3));
    return;
  }



  /*
  *
  * The output
  *
  */
  String mode=request.getParameter("mode");

  //specify whether the API uses JSON or raw CSV as Ragic Builder UI uses
  boolean raw="RAW".equals(session.getAttribute("API_MODE"));
  if("raw".equalsIgnoreCase(mode)) {
    session.setAttribute("API_MODE","RAW");
    raw=true;
  }
  else if("json".equalsIgnoreCase(mode)){
    session.setAttribute("API_MODE","JSON");
    raw=false;
  }

  if(raw){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "getraw.jsp", out, false);

  }else{
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "getjson.jsp", out, false);

  }


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
