/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.internal;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.translate.*;
import com.ragic.s3.util.Const;
import org.json.JSONObject;
import java.util.*;
import com.ragic.s3.service.report.Report;
import com.ragic.s3.service.report.ReportConfig;
import com.ragic.s3.util.Util;
import com.ragic.s3.nui.menu.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.*;

public final class local_005faccountInfo_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("com.ragic.s3.nui.menu");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.nui.translate");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.report.Report");
    _jspx_imports_classes.add("com.ragic.s3.service.report.ReportConfig");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


	//  if(!com.ragic.s3.nui.helper.Util.ifInternalIp(request.getRemoteAddr())) return;

  String userEmail=request.getParameter("e");
  String apname=request.getParameter("a");
  String transKey=request.getParameter("trans");
  User user= new User();
  user.setUserId(userEmail);
  JSONObject jo=new JSONObject();
  int accessFormCount = 0;
  AccountTranslation trans = Account.getInstance(apname).getAccountTranslation();

  List<MenuSet> mslist=Account.getInstance(apname).getMenuConfiguration().getMenuSets();
  int tabSeq=1;
  int menusetIndex=0;
  for(MenuSet ms:mslist){
    if(ms==null) continue;
    for(MenuTab tab:ms.listTabs(user,Const.USER,true,false)){

      if(tab==null) continue;
      if(!tab.isRagicSetup()) {
        accessFormCount++;
      };

      JSONObject insObject=new JSONObject();
      JSONObject insChildren=new JSONObject();
      jo.put(tab.getPath(),insObject);
      String applicationName = tab.getName();
      Locale transToLocale = null;
      if(transKey != null) {
        transToLocale = trans.keyToLocale(transKey);
        boolean ifTemplateApp = tab.getPath().startsWith("/ragic") && !tab.isRagicSetup();
        applicationName = trans.translate(applicationName, transToLocale, ifTemplateApp, AccountTranslation.EncodingType.NO_ENCODING);
      }
      insObject.put("name",applicationName);
      insObject.put("seq",tabSeq++);
      insObject.put("children",insChildren);

      insObject.put("menuset",ms.getName());
      insObject.put("menusetIndex",menusetIndex);


      MenuConfiguration mc = Account.getInstance(apname).getMenuConfiguration();
      MenuTab menuTab = mc.getMenuTab(tab.getPath());

      if(menuTab != null) {
        int count = 0;
        List<MenuItem> items = menuTab.getItems();
        synchronized (items){
          for(MenuItem item : items) {
            JSONObject itemJson = item.getJSON();
            String type = itemJson.optString("type");


            if(item instanceof MenuSheet) {
              int rightsLevel = user.getRightsLevel(item.getApname(), ((MenuSheet)item).getPath(), item.getRights());
              if(rightsLevel < Const.USER) continue;
            }

            switch (type) {
              case "sheet":
                int rightsLevel = user.getRightsLevel(item.getApname(), ((MenuSheet)item).getPath(), item.getRights());
                if(rightsLevel < Const.USER) break;

                JSONObject formObject=new JSONObject();
                insChildren.put("" + itemJson.optInt("si"),formObject);
                formObject.put("name",item.getName());
                formObject.put("type", type);
                formObject.put("seq",count);
                break;
              case "url":
                JSONObject urlObject=new JSONObject();
                insChildren.put("" + (count + 1) * -1, urlObject);
                urlObject.put("name",item.getName());
                urlObject.put("type", type);
                urlObject.put("id", itemJson.optInt("id"));
                String url=itemJson.optString("url");
                urlObject.put("url", url);
                //[BugId:15432]
                if(url!=null && url.contains("/action/")){
                  String[] sections=url.split("/");
                  NUIForm form=AppManager.getFormPage(apname,"/"+sections[2], Encoder.parseInt(sections[3]));
                  if(form!=null){
                    ActionButton button=form.getActionButton(sections[5]);
                    if(button!=null){
                      urlObject.put("actionButtonName",button.getName());
                      urlObject.put("type", "action");
                    }
                  }
                }
                urlObject.put("seq",count);
                break;
              case "report":
                JSONObject reportObject=new JSONObject();
                insChildren.put("" + (count + 1) * -1, reportObject);
                reportObject.put("name",item.getName());
                reportObject.put("type", type);
                reportObject.put("reportId", itemJson.optInt("reportId", -1));
                reportObject.put("seq",count);
                break;
            }

            count++;
          }
        }
      }
    }
    menusetIndex++;
  }

  //[BugId:12248] Report tab
  JSONObject reportTabJO=new JSONObject();
  jo.put("reportTab",reportTabJO);
  ReportConfig rc=Account.getInstance(apname).getReportConfig();
  int index=0;
  for(Report r:rc.listReports()){
    boolean access = r.access(user, Const.USER, apname);
    if(access) {
      JSONObject reportJSON=new JSONObject();
      reportJSON.put("name",r.getName());
      reportJSON.put("seq",++index);
      reportJSON.put("reportId",r.getId());
      reportTabJO.put(""+index,reportJSON);
    }
  }

  Collection<String> groups = new ArrayList<>();
  Set<String> userGroups=user.getGroups(apname);
  groups.addAll(userGroups);
  for(String g:userGroups) groups.add(g.toUpperCase());
  jo.put("groups", groups);
  boolean realEmpty = AppManager.ifEmpty(apname);
  jo.put("access authority", !(!realEmpty && accessFormCount == 0));

  boolean additionalInfo = request.getParameter("additionalInfo") != null;
  if(additionalInfo) {
    String lang = request.getParameter("lang");
    RagicBundle bundle = null;

    switch (lang) {
      case "zh-TW":
        bundle = Const.BUNDLE_ZHTW;
        break;
      case "zh-CN":
        bundle = Const.BUNDLE_ZHCN;
        break;
      case "ja-":
        bundle = Const.BUNDLE_JA;
        break;
      case "es-":
        bundle = Const.BUNDLE_ES;
        break;
      default:
        bundle = Const.BUNDLE_EN;
        break;
    }
    JSONObject additionalPart = new JSONObject();
    int userNodeId = user.getUserNodeId(apname);
    if(userNodeId > -1) {
      additionalPart.put("ragicId", user.getUserNodeId(apname));
      additionalPart.put("username", user.getUserName(apname));
      additionalPart.put("external", user.getIfExternalv2(apname));
      AbstractMap.SimpleEntry<Boolean, User.PasswordChangeType> needChangePwd = Util.checkUserNeedChangePassword(user, apname);
      if(needChangePwd.getKey()) {
        additionalPart.put("needChangePassword", needChangePwd.getValue().getDescription(bundle));
      }
      jo.put("additionalInfo", additionalPart);
    }
  }


  String callback=request.getParameter("callback");
  if(callback!=null) out.print(callback+"(");
  out.print(jo.toString(3));
  if(callback!=null) out.print(");");

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
