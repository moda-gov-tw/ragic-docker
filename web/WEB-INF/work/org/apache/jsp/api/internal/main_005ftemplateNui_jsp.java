/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:40 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.api.internal;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.helper.AppManager;
import java.io.File;
import java.io.BufferedReader;
import java.io.FileInputStream;
import java.io.InputStreamReader;
import java.io.IOException;
import com.ragic.s3.nui.HomeConfig;
import com.ragic.s3.nui.Account;
import org.json.JSONObject;
import com.ragic.s3.service.report.ReportConfig;
import java.nio.file.Files;
import java.nio.charset.Charset;
import java.util.zip.Adler32;
import java.util.zip.CheckedOutputStream;
import java.util.zip.ZipOutputStream;
import java.util.zip.ZipEntry;
import java.util.HashSet;
import java.util.Set;
import java.nio.file.Paths;
import java.nio.file.Path;
import java.nio.file.FileVisitResult;
import java.nio.file.SimpleFileVisitor;
import com.ragic.s3.nui.User;
import java.util.Date;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.nui.EntryAnalyticResult;
import com.ragic.s3.util.EntryNodeNLinkAnalyser;
import java.util.List;
import com.ragic.s3.util.CSVCreator;
import java.io.FileWriter;
import java.nio.file.attribute.BasicFileAttributes;
import com.ragic.s3.nui.helper.ExceptionManager;

public final class main_005ftemplateNui_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.zip.ZipOutputStream");
    _jspx_imports_classes.add("com.ragic.s3.service.report.ReportConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.EntryAnalyticResult");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.util.EntryNodeNLinkAnalyser");
    _jspx_imports_classes.add("java.util.HashSet");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("java.nio.charset.Charset");
    _jspx_imports_classes.add("java.util.zip.Adler32");
    _jspx_imports_classes.add("java.util.zip.ZipEntry");
    _jspx_imports_classes.add("java.nio.file.Path");
    _jspx_imports_classes.add("java.nio.file.SimpleFileVisitor");
    _jspx_imports_classes.add("com.ragic.s3.nui.HomeConfig");
    _jspx_imports_classes.add("java.nio.file.Files");
    _jspx_imports_classes.add("java.io.FileWriter");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("java.io.IOException");
    _jspx_imports_classes.add("java.io.FileInputStream");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.util.CSVCreator");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("java.nio.file.attribute.BasicFileAttributes");
    _jspx_imports_classes.add("java.io.InputStreamReader");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("java.nio.file.FileVisitResult");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("java.util.zip.CheckedOutputStream");
    _jspx_imports_classes.add("java.nio.file.Paths");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("java.io.BufferedReader");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    String apname = request.getParameter("ap");
    String path = request.getParameter("p");
    String type = request.getParameter("type");

//    if (!"standardzhtw".equals(apname) && !"standard".equals(apname)) return;
    NUIForm form;
    if("formPage".equals(type)) {
        int sheetIndex = Encoder.parseInt(request.getParameter("si"), -1);
        if(sheetIndex==-1) return;

        form = AppManager.getFormPage(apname,path,sheetIndex);
        if(form == null || !form.fileExists()) return;
        form.outputHeaders(out, null, false, true, false, null);
    } else if("listingPage".equals(type)) {
        int sheetIndex = Encoder.parseInt(request.getParameter("si"),-1);
        if(sheetIndex==-1) return;

    	form = AppManager.getListingPage(apname,path,sheetIndex);
        if(form == null || !form.fileExists()) return;
        form.outputHeaders(out, null, false, true, false, null);
    } else if("config".equals(type)) {
        File file = new File("cust/def/app/" + apname + path + "/config.xml");
        try (BufferedReader bfr = new BufferedReader(new InputStreamReader(new FileInputStream(file), "UTF-8"))) {
            String configLine = bfr.readLine();
            while(configLine != null) {
                out.println(configLine);
                configLine = bfr.readLine();
            }
        } catch (IOException e) {
            ExceptionManager.getInstance().processException(e);
        }
    } else if("bulkDownload".equals(type)) {
        String listStr = request.getParameter("list");
        String[] downloadList = listStr.split(",");

        response.setHeader("Cache-Control", "no-cache"); //HTTP 1.1
        response.setHeader("Pragma", "no-cache"); //HTTP 1.0
        response.setContentType("application/zip");

        try(ZipOutputStream zout = new ZipOutputStream(new CheckedOutputStream(response.getOutputStream(), new Adler32()), Charset.forName("UTF-8"))) {
            Set<String> check = new HashSet<>();
            for(String name : downloadList) {
                String[] parts = name.split("/");
                if(parts.length >= 3) {
                    String pathPart = parts[1];
                    String si = parts[2];
                    String formPageName = si + "_Sheet" + si + "_index.nui";
                    String listingPageName = si + "_Sheet" + si + "_index_t.nui";
                    File formPageNuiFile = new File("cust/def/app/" + apname + "/" + pathPart + "/" + formPageName);
                    File listingPageNuiFile = new File("cust/def/app/" + apname + "/" + pathPart + "/" + listingPageName);
                    if(formPageNuiFile.exists()) {
                        zout.putNextEntry(new ZipEntry(pathPart +"\\"+ formPageNuiFile.getName()));
                        Files.copy(formPageNuiFile.toPath(), zout);
                        zout.closeEntry();
                    }
                    if(listingPageNuiFile.exists()) {
                        zout.putNextEntry(new ZipEntry(pathPart +"\\"+ listingPageNuiFile.getName()));
                        Files.copy(listingPageNuiFile.toPath(), zout);
                        zout.closeEntry();
                    }
                    if(check.add(pathPart)) {
                        File configFile = new File("cust/def/app/" + apname + "/" + pathPart + "/config.xml");
                        if(configFile.exists()) {
                            zout.putNextEntry(new ZipEntry(pathPart +"\\"+ configFile.getName()));
                            Files.copy(configFile.toPath(), zout);
                            zout.closeEntry();
                        }
                    }
                }
            }
            Path templateJs = Paths.get("cust", "def", "app", apname, "template.js");
            if(Files.exists(templateJs)) {
                zout.putNextEntry(new ZipEntry("template.js"));
                Files.copy(templateJs, zout);
                zout.closeEntry();
            }
            Path templateDailyJs = Paths.get("cust", "def", "app", apname, "templateDaily.js");
            if(Files.exists(templateDailyJs)) {
                zout.putNextEntry(new ZipEntry("templateDaily.js"));
                Files.copy(templateDailyJs, zout);
                zout.closeEntry();
            }
            Path importConfig = Paths.get("cust", "def", "app", apname, "importConfig.json");
            if(Files.exists(importConfig)) {
                zout.putNextEntry(new ZipEntry("importConfig_template.json"));
                Files.copy(importConfig, zout);
                zout.closeEntry();
            }
            Path jobConfig = Paths.get("cust", "def", "app", apname, "jobSchedules.json");
            if(Files.exists(jobConfig)) {
                zout.putNextEntry(new ZipEntry("jobSchedules_template.json"));
                Files.copy(jobConfig, zout);
                zout.closeEntry();
            }
            Files.list(Paths.get("cust", "def", "app", apname))
            .filter(filePath -> {
                File file = filePath.toFile();
                if(!file.exists() || file.isDirectory() || !file.getName().endsWith(".properties")) return false;
                return true;
            })
            .forEach(filePath -> {
                try {
                	zout.putNextEntry(new ZipEntry(filePath.toFile().getName()));
                    Files.copy(jobConfig, zout);
                    zout.closeEntry();
                } catch(Exception e) {
                    ExceptionManager.getInstance().processException(e);
                }
            });
        } catch (Exception e) {
            ExceptionManager.getInstance().processException(e);
        }
    } else if("mailMergeTemplate".equals(type)) {
        String fn = request.getParameter("fn");
        String decodeFn = Encoder.decodeURIComponent(fn);

        Path p = Paths.get("cust", "def", "app", apname, path, decodeFn);
        if(Files.exists(p)) {
            response.setHeader("Cache-Control", "no-cache"); //HTTP 1.1
            response.setHeader("Pragma", "no-cache"); //HTTP 1.0
            response.setContentType("application/octet-stream");
            response.setHeader("Content-Disposition",
                    "attachment;filename="+decodeFn);
            Files.copy(p, response.getOutputStream());
        } else {
            System.out.println("不存在: " + p.toFile().getAbsolutePath());
        }

    }

    else if("tabIcon".equals(type)) {
        HomeConfig standardHc = Account.getInstance(apname).getHomeConfig();
        JSONObject tabIconList = standardHc.getTabIconList();
        out.println(tabIconList.toString());
    } else if("report".equals(type)) {
        ReportConfig reportConfig = Account.getInstance(apname).getReportConfig();
        out.println(reportConfig.getJson().toString());
    } else if("csv".equals(type)) {
        Path csvPath = null;
        response.setHeader("Cache-Control", "no-cache"); //HTTP 1.1
        response.setHeader("Pragma", "no-cache"); //HTTP 1.0
        response.setContentType("application/zip");
        try (ZipOutputStream zout = new ZipOutputStream(new CheckedOutputStream(response.getOutputStream(), new Adler32()), Charset.forName("UTF-8"))) {
            int si = -1;
            String pathSheets = request.getParameter("pathSheets");
            String email = request.getParameter("email");
            if(email == null) email = "support@ragic.com";
            String pathName = email + "_" + new Date().getTime();
            if(pathSheets == null) {
                return;
            }

            String[] pathSheetsArr = pathSheets.split(",");

            ZipEntry zipEntry = new ZipEntry("record.csv");
            zout.putNextEntry(zipEntry);
            csvPath = Paths.get("cust", "data", apname, "tmp", pathName, "record.csv");
            Files.createDirectories(csvPath.getParent());

            for(String pathSheet : pathSheetsArr) {
                String[] pathSheetPart = pathSheet.split("/");
                path = "/" + pathSheetPart[1];
                si = Encoder.parseInt(pathSheetPart[2], -1);

            form = AppManager.getFormPage(apname, path, si);

            int kf = form.getKeyField();

            BDBWorker worker = new BDBWorker(apname);
            worker.setWhoami("main_templateNui.jsp");
            try {
                SDBQuery q = new SDBQueryFull(worker);
                q.go(kf);
                SDBNode n = null;
                while ((n = q.next()) != null) {
                    int nodeId = n.getId();
                    User user = new User();
                    user.setUserId(email);

                    EntryAnalyticResult result = new EntryNodeNLinkAnalyser(apname).analyseEntry(path, si, kf, nodeId, false, user, EntryNodeNLinkAnalyser.AnalyticPolicy.TEMPLATE_COPY);

                    List<String[]> record = result.getRecord();

                        try (CSVCreator c = new CSVCreator(new FileWriter(csvPath.toFile(), true))) {
                        c.writeAll(record);
                    }
                }
            } catch (Exception e) {
                ExceptionManager.getInstance().processException(e);
            } finally {
                worker.commit();
            }
            }
            Files.copy(csvPath, zout);
            zout.closeEntry();
        } catch (Exception e) {
            ExceptionManager.getInstance().processException(e);
        } finally {
            try {
                if (csvPath != null) {

                    Files.walkFileTree(csvPath.getParent(), new SimpleFileVisitor<Path>() {
                        @Override
                        public FileVisitResult visitFile(Path file, BasicFileAttributes attrs){
                            try {
                                Files.delete(file);

                            } catch (Exception e) {
                                ExceptionManager.getInstance().processException(e);
                            }
                            return FileVisitResult.CONTINUE;
                        }
                    });
                    Files.delete(csvPath.getParent());
                }
            } catch (Exception e) {
                ExceptionManager.getInstance().processException(e);
            }
        }
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
