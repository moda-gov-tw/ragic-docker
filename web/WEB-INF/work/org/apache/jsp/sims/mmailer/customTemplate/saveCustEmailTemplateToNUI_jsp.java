/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.mmailer.customTemplate;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.*;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.util.Log;
import org.json.JSONArray;
import org.json.JSONObject;

public final class saveCustEmailTemplateToNUI_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.nui.helper");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  Log logger = new Log().setIfDisplayLevel(true).setIfDisplayTime(true).setLevel(Log.ERROR);
  logger.debug("Save cust email template...");
  //currently only for ap owner
  User user=(User)session.getAttribute("USER");
  String apname=request.getParameter("ap");
  String path=request.getParameter("path");
  if(apname==null || path==null || user==null || !user.ifApManager(apname,path)) return;

  int sheetIndex=Integer.parseInt(request.getParameter("si"));
  String type = request.getParameter("type");
  logger.debug("type=" + type);
  String key = request.getParameter("key");
  logger.debug("key=" + key);
  
  APIQuery q = new APIQuery(apname, path, sheetIndex);
  NUIForm form = q.getForm();
  
  if(form == null){
      return;
  }
  form.setUser(user); // [issue#05914]
  
  String content = request.getParameter("content").replaceAll("\n", "");
  String subject = request.getParameter("subject");
  String receiverList = request.getParameter("receiverList");
  String ccList = request.getParameter("ccList");
  String bccList = request.getParameter("bccList");
  String replyTo = request.getParameter("replyTo");
  boolean attLink = Encoder.parseBoolean(request.getParameter("attLink"), false);
  boolean attExcel = Encoder.parseBoolean(request.getParameter("attExcel"), false);
  boolean attPdf = Encoder.parseBoolean(request.getParameter("attPdf"), false);
  boolean attMagicLinkReadOnly = Encoder.parseBoolean(request.getParameter("attMagicLinkReadOnly"), false);
  boolean attMagicLinkEditable = Encoder.parseBoolean(request.getParameter("attMagicLinkEditable"), false);
  String attMM = request.getParameter("attMM");
  String attField = request.getParameter("attField");
  String attFile = request.getParameter("attFile");
  String pdfDownloadParams = request.getParameter("pdfDownloadParams");
  logger.debug("content=" + content);
  logger.debug("subject=" + subject);
  logger.debug("receiverList=" + receiverList);
  logger.debug("ccList=" + ccList);
  logger.debug("bccList=" + bccList);
  logger.debug("replyTo=" + replyTo);
  logger.debug("attLink=" + attLink);
  logger.debug("attExcel=" + attExcel);
  logger.debug("attPdf=" + attPdf);
  logger.debug("attMagicLinkReadOnly=" + attMagicLinkReadOnly);
  logger.debug("attMagicLinkEditable=" + attMagicLinkEditable);
  logger.debug("attMM=" + attMM);
  logger.debug("attField=" + attField);
  logger.debug("attFile=" + attFile);
  logger.debug("pdfDownloadParams=" + pdfDownloadParams);
  
  JSONObject newTemplate = null;
  JSONObject queryAttributes = new JSONObject();
  queryAttributes.put("type", type);
  if(key != null) queryAttributes.put("key", key);
  newTemplate = form.getEmailTemplate(queryAttributes);
  logger.debug("oldTemplate=" + newTemplate);
  if(newTemplate == null) {
	  newTemplate = new JSONObject();
      newTemplate.put("attributes", queryAttributes);
      
      JSONArray att = new JSONArray();
      newTemplate.put("att", att);
      
      newTemplate.put("subject", subject);
      newTemplate.put("content", content);
      if(receiverList != null) newTemplate.put("receiverList", receiverList);
      if(ccList != null) newTemplate.put("ccList", ccList);
      if(bccList != null) newTemplate.put("bccList", bccList);
      if(replyTo != null) newTemplate.put("replyTo", replyTo);
      newTemplate.put("attLink", attLink);
      newTemplate.put("attExcel", attExcel);
      newTemplate.put("attPdf", attPdf);
      if(pdfDownloadParams != null) newTemplate.put("pdfDownloadParams", pdfDownloadParams);
      newTemplate.put("attMagicLinkReadOnly", attMagicLinkReadOnly);
      newTemplate.put("attMagicLinkEditable", attMagicLinkEditable);
      if(attMM != null && !attMM.trim().isEmpty()) newTemplate.put("attMM", attMM);
      if(attField != null && !attField.trim().isEmpty()) newTemplate.put("attField", attField);
      if(attFile != null && !attFile.trim().isEmpty()) newTemplate.put("attFile", attFile);
      
      form.addEmailTemplate(newTemplate);
  } else {
      int id = newTemplate.optInt("id", -1);
      if(id == -1) return;
      newTemplate.put("subject", subject);
      newTemplate.put("content", content);
      if(receiverList != null) {
    	  newTemplate.put("receiverList", receiverList);
      } else {
    	  newTemplate.remove("receiverList");
      }
      if(ccList != null) {
    	  newTemplate.put("ccList", ccList);
      } else {
    	  newTemplate.remove("ccList");
      }
      if(bccList != null) {
          newTemplate.put("bccList", bccList);
      } else {
          newTemplate.remove("bccList");
      }
      if(replyTo != null) {
    	  newTemplate.put("replyTo", replyTo);
      } else {
    	  newTemplate.remove("replyTo");
      }
      newTemplate.put("attLink", attLink);
      newTemplate.put("attExcel", attExcel);
      newTemplate.put("attPdf", attPdf);
      if(pdfDownloadParams != null){
          newTemplate.put("pdfDownloadParams", pdfDownloadParams);
      }else{
          newTemplate.remove("pdfDownloadParams");
      }
      newTemplate.put("attMagicLinkReadOnly", attMagicLinkReadOnly);
      newTemplate.put("attMagicLinkEditable", attMagicLinkEditable);
      if(attMM != null && !attMM.trim().isEmpty()){
          newTemplate.put("attMM", attMM);
      }else{
          newTemplate.remove("attMM");
      }
      if(attField != null && !attField.trim().isEmpty()){
          newTemplate.put("attField", attField);
      }else{
          newTemplate.remove("attField");
      }
      if(attFile != null && !attFile.trim().isEmpty()) newTemplate.put("attFile", attFile);
      
      form.setEmailTemplate(id, newTemplate);
  }
  logger.debug("newTemplate=" + newTemplate);
  form.saveFile(apname, path,true);

  //sync with builder mode
  NUIFormSetInfo.resetForms(apname, path, session);

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
