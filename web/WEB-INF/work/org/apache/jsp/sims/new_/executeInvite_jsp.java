/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.new_;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.NUIFormSetInfo;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.RagicConfig;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.RemoteAccountManager;
import com.ragic.s3.sdb.data.bdb.BDBSmartWorker;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.util.*;
import java.text.SimpleDateFormat;
import java.util.Date;
import com.ragic.s3.nui.helper.ExceptionManager;
import com.ragic.s3.nui.helper.UserCache;
import com.ragic.s3.proxy.ProxyUpdater;

public final class executeInvite_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.RemoteAccountManager");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBSmartWorker");
    _jspx_imports_classes.add("com.ragic.s3.proxy.ProxyUpdater");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIFormSetInfo");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserCache");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  //getting the params
  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String apname=request.getParameter("a");
  String sig=request.getParameter("sig");
  boolean existing=request.getParameter("existing")!=null;
  boolean external=request.getParameter("external")!=null;
  int invitedUserNodeId= Encoder.parseInt(request.getParameter("i"), Const.NONODE);
  String username=request.getParameter("u");
  String password=request.getParameter("p");
  if(apname==null || username==null || password==null || invitedUserNodeId==Const.NONODE) return;

  //avoid duplicated execution
  if(session.getAttribute("INVITE_EXECUTING")!=null) return;
  session.setAttribute("INVITE_EXECUTING",true);
  //make sure whenever there's a return, there is a session.removeAttribute("INVITE_EXECUTING");

  //check user access right
  //get user email
  String email=null;
  BDBWorker worker=new BDBWorker(apname);
  worker.setWhoami("executeInvite.jsp");
  try{
    SDBNode node=worker.getNode(DConst.USER_USERID_EMAIL, invitedUserNodeId);
    email=node.getValue();
  }
  finally{
    worker.commit();
  }

  //check sig
  User user=null;
  if(existing){
    String decrypt=new DES().decrypt(sig);
    if(decrypt==null || !decrypt.equals("RINVITE"+email)){
      out.println("Your invite link has expired. Please ask your Ragic account admin to invite you again.");
      session.removeAttribute("INVITE_EXECUTING");
      return;
    }
    user=RemoteAccountManager.main_userInfo(email);
  }
  else{
    user=RemoteAccountManager.main_authenticateUser(email,sig,false);
    if(user==null){
      out.println("Invalid signature.");
      session.removeAttribute("INVITE_EXECUTING");
      return;
    }
    else{
      user.setIp(request.getRemoteAddr());
    }
  }
  if(user==null){
    out.println("Your invite link is invalid, the user is probably deleted. Please ask your Ragic account admin to save your user again.");
    session.removeAttribute("INVITE_EXECUTING");
    return;
  }

  //set user name
  if(!"".equals(username)){
    APIQuery aq=new APIQuery(apname,"/ragic-setup",external?5:1);
    aq.setUpdateMode();
    APIEntry e=aq.getAPIEntry(invitedUserNodeId);
    e.setFieldValue(DConst.USER_NAME,username);
    
    //update the user
    try{
      e.save(user,bundle);
    }
    catch(Exception ex){
      ExceptionManager.getInstance().processException(ex);
    }
  }

  //set password
  //RemoteAccountManager.main_updatePassword(0, null, "@"+email, sig, password);
  // [issue#06511]
  // For re-invited users, i.g. existing users, the value of sig is new DES().encryptWeb("RINVITE"+email).
  // Using sig as old password will make main_updatePassword failed.
  // Here we use "2" as old password to skip old password checking.
  // Old password "2" will not ask user to change password. [issue#09434]
  if(!"".equals(password)){
    RemoteAccountManager.main_updatePassword(0, null, "@"+email, "2", password);
  }

  //set language, might not be using the same language as the inviting user
  try{
    BDBSmartWorker bbs=new BDBSmartWorker(Const.AP_RAGIC);
    bbs.setFieldSelectionValue(DConst.ROOT_USER_EMAIL,(user.isPassivelyAddedUser()?"@":"")+user.getUserId(),DConst.ROOT_USER_LANG,bundle.getLangv5());
    new ProxyUpdater().updateUser(user.getUserId());
    UserCache.getInstance().updateUser(user.getUserId());
  }
  catch(Exception ex){
    //will end up in exception on ap2...etc
  }
  
  // Update join time
  RemoteAccountManager.main_updateUserJoinTime(email);
  
  //log in as this user
  if(user!=null) user.setIp(request.getRemoteAddr());
  session.setAttribute("USER",user);
  //update NUIFORMSET
  NUIFormSetInfo.setUser(session, user);
  //do cookie auto log in for the future
  Util.setCookieSession(Const.userId, email,response);
  Util.setCookieSession(Const.sigature, existing?sig:Hash.hash(password),response, RagicConfig.getInstance().ifProperty("SSL_ENABLED"));

  //record a register date, used to determine if a new on-boarding feature should be applied
  SimpleDateFormat sdf=new SimpleDateFormat("yyyyMMdd");
  user.saveSharedProperties("regdate",sdf.format(new Date()));

  session.removeAttribute("INVITE_EXECUTING");

  //redirect to tutorial

      out.write("<script>location.href='/");
      out.print(apname);
      out.write("#gotutorial000");
      out.print(external?"c":"b");
      out.write("';</script>");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
