/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.command.formula.*;
import com.ragic.s3.postoffice2.MessageFacade;
import com.ragic.s3.postoffice2.RagicMessageV2;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.service.execute.ServiceExecuteEngine;
import com.ragic.s3.service.execute.custom.CustomSaveActionProcessor;
import com.ragic.s3.service.execute.custom.CustomSaveActionRequest;
import com.ragic.s3.service.execute.custom.CustomSaveActionResponse;
import com.ragic.s3.service.execute.queue.*;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import com.ragic.s3.util.SystemTaskProgress;
import org.json.JSONArray;
import org.json.JSONObject;
import java.util.List;
import com.ragic.s3.nui.ActionButton;
import java.util.Collection;

public final class delv6_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.nui.helper.command.formula");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("com.ragic.s3.service.execute.queue");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.execute.custom.CustomSaveActionProcessor");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Collection");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.MessageFacade");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemTaskProgress");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.RagicMessageV2");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.ActionButton");
    _jspx_imports_classes.add("com.ragic.s3.service.execute.custom.CustomSaveActionRequest");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.service.execute.custom.CustomSaveActionResponse");
    _jspx_imports_classes.add("com.ragic.s3.service.execute.ServiceExecuteEngine");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


	//System.out.println("delv6.jsp?"+request.getQueryString());
  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String apname=request.getParameter("a");
  int domainId=Encoder.parseInt(request.getParameter("d"), Const.NONODE);
  int nodeId=Encoder.parseInt(request.getParameter("n"), Const.NONODE);
  String path=request.getParameter("p");
  String formName=request.getParameter("fn");
  String rpNodeId=request.getParameter("rpn");//node id of the entry, to determine if have privilege to delete from this entry
  String rootDomainId=request.getParameter("rd");
  String deleteSetting=request.getParameter("ds");
  String toolButtonSeq = request.getParameter("toolButtonSeq");
  boolean isReserveReport = request.getParameter("reserve_report")!=null;
  
  if(apname==null || domainId==Const.NODOMAIN || nodeId==Const.NONODE) return;

  User user=(User)session.getAttribute("USER");
  if(user==null) return;

  NUIForm form= AppManager.getFormPage(apname, path, formName);

  boolean ignorePrivilegeCheck = false;
  if(toolButtonSeq!= null && form.getActionButtonBySeq(toolButtonSeq) != null) {
      boolean pass = true;
    ActionButton toolButton = form.getActionButtonBySeq(toolButtonSeq);
    String action = toolButton.getAction();
    if(!"deleteEntry".equals(action)) pass = false;

    if(pass) {
      boolean groupCheckSuccess = toolButton.ifUserAllowed(apname,path,form.getSheetIndexInt(),user);
      if(!groupCheckSuccess) pass = false;
    }

    if(pass) ignorePrivilegeCheck = true;

    System.out.println("ignorePrivilegeCheck: " + ignorePrivilegeCheck);
  }

  if(isReserveReport) ignorePrivilegeCheck = true;
  int right=form.checkRight(user);
  boolean canDelete=false;
  APIEntry deletedEntry = null;
  boolean deleteSuccess = false;
  BDBWorker worker=new BDBWorker(apname);
  worker.setWhoami("delv6.jsp check access to node");
  try{
    switch(right){
      case Const.NORIGHT:
        break;
      case Const.USER://only edit self or new, if can see, then he can edit
        if(worker.checkAccessToNode(Integer.parseInt(rootDomainId),Integer.parseInt(rpNodeId),user)){
          canDelete=true;
        }
        break;
      case Const.VIEWER:
        break;
      case Const.MANAGER:
        if(worker.checkAccessToNode(Integer.parseInt(rootDomainId),Integer.parseInt(rpNodeId),user)){
          canDelete=true;
        }
        break;
      case Const.ADMIN:
        canDelete=true;
        break;
    }
    String status="E";
    List<String[]> delRecord;
    if(ignorePrivilegeCheck) canDelete = true;
    
    if(canDelete) {
        CustomSaveActionRequest checkRequest = new CustomSaveActionRequest(form, nodeId, bundle, CustomSaveActionRequest.DELETE);
        CustomSaveActionResponse cres=new CustomSaveActionProcessor().preCheck(checkRequest);
        canDelete = cres.isSuccess();
        if(cres.hasJsResponse()) {
            out.println(cres.getJsReponse());
        }
    }
    if(canDelete) {
      APIQuery aq=new APIQuery(apname,path,formName);
      aq.setIfEidNaming(true);
      aq.setIfIncludeInfo(true);
      aq.setUser(user);
      aq.openFTS();
      aq.setBundle(bundle);
      
      // Get cascading recal info before delete
      ServiceExecuteEngine see = new ServiceExecuteEngine(form.getApname(), form);
      CascadingRecalculateEntriesInfo cascadingRecalMap = new CascadingRecalculateEntriesInfo(see.getCascadingLimit());
      deletedEntry = aq.getAPIEntry(nodeId);
      if(form.getCascadingFormulaRecal()) {
          if(deletedEntry != null) {
              SystemTaskProgress.setStatus(apname,Const.STP_DELETE, bundle.getString("entry_cascading_formula_recal_start"), 0, SystemTaskProgress.Status.PROCESSING);
              try {
                  cascadingRecalMap = see.getCascadingRecalculateEntries(deletedEntry);
              } catch (TooManyCascadingRecalculateEntriesException e) {
                  System.out.println("Cascading node number over limit");
              }
          }
      }
      
      try{
        SystemTaskProgress.setStatus(apname,Const.STP_DELETE, "Initing deletion...", -1, SystemTaskProgress.Status.PROCESSING);
        //delete or unlink relative fields
        //status=aq.deleteEntry(Encoder.parseInt(domainId),Encoder.parseInt(nodeId),false,false,Encoder.parseInt(deleteSetting));
        String reserveReportErrMsg = "";
        if(nodeId>=0){
          if (isReserveReport && aq.getAPIEntry(nodeId) == null) {
            reserveReportErrMsg = bundle.getString("reserve_new_update_error") + bundle.getString("reserve_deleted_data_error");
          }
          else{
            delRecord=aq.deleteEntry(domainId,nodeId,false,false,Encoder.parseInt(deleteSetting));
            status = delRecord==null?"E":delRecord.get(0)[0];
          }
        }
        if("E".equals(status)) {
          if (isReserveReport) {
            out.println("floatWinError(\"" + reserveReportErrMsg + "\")");
          }
          else out.println("floatWin(null,'There were some problem deleting this record, please try again if you still see the record.');");
        }
        else if("C".equals(status)) out.println("floatWin(null,'"+bundle.getString("deleteConflict")+"');");
        //S
        else {
          long transId = aq.getTransId();

          SystemTaskProgress.setStatus(apname,Const.STP_DELETE, "Done!", -1, SystemTaskProgress.Status.DONE, "recoverEntry('"+apname+"','"+transId+"')", bundle.getString("undoDelete"));
            out.println("floatWinSuccess('" + (isReserveReport?bundle.getString("reserve_cancel"):bundle.getString("deleteOK")) + "',true);");
            deleteSuccess = true;
        }
      } finally {
        aq.closeFTS();

      }
      
      // Do cascading formula recal after record deleted
      if(form.getCascadingFormulaRecal() && deleteSuccess) {
          if(cascadingRecalMap.getCount() > see.getCascadingLimit()) {
              System.out.println("Cascading recalculation over limit: " + cascadingRecalMap.getCount());
//              SystemTaskProgress.setStatus(apname,  bundle.getString("entry_cascading_formula_recal_overlimit"), cascadingRecalMap.getCount(), true);
          } else {
              see.cascadingFormulaRecalculate(cascadingRecalMap, user, bundle, nodeId, RagicMessageV2.Event.delete);
//              SystemTaskProgress.setStatus(apname,  bundle.getString("entry_cascading_formula_recal_finish"), cascadingRecalMap.getCount(), true);
          }
      }
      
      // Delete related reminder tasks
      if(deletedEntry != null && deleteSuccess) {
          ExecQueue.getIndexQueue().execute(new RemoveReminderTasksWhenRecordRemoved(form,deletedEntry));
      }
    }
  }
  finally{
    worker.commit();
  }

  CustomSaveActionRequest saveActionRequest = new CustomSaveActionRequest(form, nodeId, bundle, CustomSaveActionRequest.DELETE);
  saveActionRequest.setUser(user);

  //sheet custom processing
  CustomSaveActionResponse cres=new CustomSaveActionProcessor().process(saveActionRequest);

  if(cres.hasJsResponse()){
    out.println(cres.getJsReponse());
  }

  //out messages
  if(deletedEntry != null && deleteSuccess) {
	  if(MessageFacade.needDoOutMessage(apname, form.getPath(), Encoder.parseInt(form.getSheetIndex(), -1))) {
	      RagicMessageV2 rm = MessageFacade.createMessage(apname, path, Encoder.parseInt(form.getSheetIndex(), -1), deletedEntry);
          if(rm != null) {
              rm.setEvent(RagicMessageV2.Event.delete);
        	  MessageFacade.processOutMessage(rm);
          }
	  }
      out.println("setTimeout(updateSystemTaskProgress, 100);");
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
