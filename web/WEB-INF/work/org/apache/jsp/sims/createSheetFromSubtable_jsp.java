/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.sdb.search.FTIndex;
import java.util.Map;

public final class createSheetFromSubtable_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTIndex");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String apname=request.getParameter("a");
  String path=request.getParameter("p");
  String formName=request.getParameter("fn");
  String domainId=request.getParameter("d");
  int myStf=Encoder.parseInt(request.getParameter("mystf"));
  if(domainId!=null){
    NUIForm formPage=(NUIForm)session.getAttribute("CREATE_FORM_FROM_SUBTABLE");
    if(formPage==null) return;
    apname=formPage.getApname();
    path=formPage.getPath();

    final String fApname=apname;
    final String fPath=path;

    //find the field name of the first field of the subtable to use as the new form name
    CellLocation keyLocation=formPage.getCellLocation(Encoder.parseInt(domainId));
    Integer subtableKey=formPage.getSubtableKey(keyLocation);
    String subtableName=null;
    if(subtableKey!=null){
      subtableName=formPage.getSubtableName(subtableKey);
    }
    if(subtableName==null) {
      CellInfo info=formPage.getField(Encoder.parseInt(domainId));
      if(info!=null){
        subtableName=info.getValue();
      }
    }
    if(subtableName==null){
      subtableName="New Sheet";
    }

    NUIForm newForm=formPage.autoFormGen(Integer.parseInt(domainId),formPage.getRootDomainId(),myStf,path+"|"+formPage.getFormName());

    final String newFormname= Util.generateNewFormName(apname,path);
    newForm.setRealname(subtableName);
    newForm.setFileName(newFormname + "_index.nui");
    newForm.saveFile(apname,path);
    newForm.init();
    NUIFormSetInfo.resetForms(apname,path,session);
    Account.getInstance(apname).resetAppCache();

    //re-index the newly created form asynchrounously
    Thread t=new Thread(new Runnable(){
      public void run() {
        FTIndex.index(fApname, fPath, newFormname,false);
      }
    });
    t.setName("Create new sheet from subtable full text index:"+fApname+fPath+"/"+newFormname);
    t.start();

    int sheetIndex=Integer.parseInt(newFormname.split("_")[0]);
    out.print("/"+apname+path+"/"+sheetIndex);
    return;
  }


      out.write("<div id=\"createSheetFromSubtable\">\r\n");



  //Sheet sheet=AppManager.getSheetBySheetIndex(apname,path,Integer.parseInt(sheetIndex));
  NUIForm formPage=new NUIForm(apname,path,formName+"_index.nui",true);
  session.setAttribute("CREATE_FORM_FROM_SUBTABLE",formPage);

  int cellsStarted=0;
  boolean anySubtableFound=false;
  int count=0;
  for(Map.Entry<CellLocation, CellInfo> e : formPage.getFields().entrySet()){
    CellLocation loc=e.getKey();
    CellInfo info=e.getValue();

    if(formPage.isFirstTableCell(loc)){
      //we now allow generating subtables for references subtables, because original sheets might be deleted too
      //skip reference subtables
//      if(formPage.getSubtableParentAlias(loc)!=null){
//        continue;
//      }

      anySubtableFound=true;
      if(cellsStarted>0) {
        for(int j=0;j<3;j++){
          out.print("</tr><tr>");
          for(int i=0;i<cellsStarted;i++){
            out.print("<td style='background:#f8f8f8;'>&nbsp;</td>");
          }
        }
        out.print("</tr></table><br/>");
      }
      out.print("<br/><a id=\"createSheetFromSubtableLink"+count+"\" href='javascript:void(0);' onclick=\"newSheetFromSubtable("+info.getDomainId()+","+count+");\">"+bundle.getString("createNewSheet")+"</a> <span style='padding-left:8px;'>"+bundle.getString("fromThisSubtable")+":</span><br/>");
      out.print(""+bundle.getString("newSheetLink")+": ");
      out.print("<select id='mystf"+count+"'>");
      int mySelectTitleField=formPage.determineTitleField().getDomainId();
      for(CellInfo myinfo:formPage.getSelectableSelectTitleFields()){
        out.println("<option value='"+myinfo.getDomainId()+"' "+(mySelectTitleField==myinfo.getDomainId()?"selected":"")+">"+myinfo.getValue()+"</option>");
      }
      out.print("</select>");
      out.print("<table style='clear:both;padding-top: 0;'><tr><td>"+Encoder.encjs(info.getValue())+"</td>");
      cellsStarted=1;
      count++;
    }
    else if(formPage.isRestTableCell(loc)){
      //we now allow generating subtables for references subtables, because original sheets might be deleted too
      //skip reference subtables
//      int tableHeaderDomain=formPage.getTableHeaderDomain(loc,info.getDomainId());
//      if(formPage.getSubtableParentAlias(tableHeaderDomain)!=-1){
//        continue;
//      }

      out.print("<td>"+Encoder.encjs(info.getValue())+"</td>");
      cellsStarted++;
    }
  }
  if(cellsStarted>0) {
    for(int j=0;j<3;j++){
      out.print("</tr><tr>");
      for(int i=0;i<cellsStarted;i++){
        out.print("<td style='background:#f8f8f8;'>&nbsp;</td>");
      }
    }
    out.print("</tr></table><br/>");
  }

  if(!anySubtableFound){
    out.print(bundle.getString("linkDigest_newSheetFromSubtable_noSubtable"));
  }


      out.write("</div>\r\n\r\n\r\n");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
