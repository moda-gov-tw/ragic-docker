/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.sdb.data.bdb.RelationKey;
import com.ragic.s3.sdb.data.bdb.BDBMgr;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.sleepycat.bind.tuple.TupleBinding;
import java.util.*;
import com.sleepycat.je.*;
import com.ragic.s3.sdb.data.bdb.ValueKey;
import java.nio.charset.StandardCharsets;

public final class linkNNodeChecker_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


    private int searchLinkNum(String apname, int d1, int n1, int d2, List<Map.Entry<String, String>> results) {
        BDBWorker worker = new BDBWorker(apname);
        DatabaseEntry theKey = new DatabaseEntry();
        DatabaseEntry foundData = new DatabaseEntry();
        Database relationDB = worker.getRelationDB();

        RelationKey rKey = new RelationKey(d1, n1, d2);
        BDBMgr.getRelationDBKeyTupleBinding().objectToEntry(rKey, theKey);
        Cursor dbc = relationDB.openCursor(null, null);

        int count = 0;
        try {
            TupleBinding intBinding = TupleBinding.getPrimitiveBinding(Integer.class);
            if(n1 > -1) {
                OperationStatus status = dbc.getSearchKey(theKey, foundData, LockMode.READ_UNCOMMITTED);
                while(status == OperationStatus.SUCCESS) {
                    count++;
                    if(results.size() < 100) {
                        Integer toNodeId = (Integer) intBinding.entryToObject(foundData);
                        results.add(new AbstractMap.SimpleEntry<>(String.valueOf(n1),
                        String.valueOf(toNodeId)));
                    }
                    status = dbc.getNextDup(theKey, foundData, LockMode.READ_UNCOMMITTED);
                }
            } else {
                OperationStatus status = dbc.getSearchKeyRange(theKey, foundData, LockMode.READ_UNCOMMITTED);

                while(status == OperationStatus.SUCCESS) {
                    RelationKey result = (RelationKey) BDBMgr.getRelationDBKeyTupleBinding().entryToObject(theKey);

                    Integer toNodeId = (Integer) intBinding.entryToObject(foundData);
                    RelationKey changedRKey = (RelationKey) BDBMgr.getRelationDBKeyTupleBinding().entryToObject(theKey);
                    if(d1 != changedRKey.getDomainId1() || d2 != changedRKey.getDomainId2()) {
                    } else {
                        if(results.size() < 100) {
                            results.add(new AbstractMap.SimpleEntry<>(String.valueOf(result.getNodeId1()),
                            String.valueOf(toNodeId)));
                        }
                        count++;
                    }

                    while(status == OperationStatus.SUCCESS) {
                        status = dbc.getNextDup(theKey, foundData, LockMode.READ_UNCOMMITTED);
                        if(status == OperationStatus.SUCCESS) {
                            toNodeId = (Integer) intBinding.entryToObject(foundData);
                            if(results.size() < 100) {
                                results.add(new AbstractMap.SimpleEntry<>(String.valueOf(result.getNodeId1()),
                                String.valueOf(toNodeId)));
                            }
                        }
                    }

                    if(result.getDomainId1() == d1) {
                        status = dbc.getNext(theKey, foundData, LockMode.READ_UNCOMMITTED);
                    } else {
                        status = null;
                    }
                }
            }

        } finally {
            if(dbc!=null) dbc.close();
            worker.commit();
        }
        return count;
    }

    private int searchNodeNum(String apname, int d1, List<Map.Entry<String, String>> results) {

        BDBWorker worker = new BDBWorker(apname);

        DatabaseEntry theKey = new DatabaseEntry();
        DatabaseEntry foundData = new DatabaseEntry();
        Database valueDB = worker.getValueDB();

        int searchDomainId = d1;

        ValueKey vkey = new ValueKey(searchDomainId, -1);
        BDBMgr.getValueDBKeyTupleBinding().objectToEntry(vkey, theKey);
        Cursor dbc = valueDB.openCursor(null, null);
        int count = 0;
        try {
            OperationStatus status = dbc.getSearchKeyRange(theKey, foundData, LockMode.READ_UNCOMMITTED);
            while(status == OperationStatus.SUCCESS) {
                ValueKey result = (ValueKey) BDBMgr.getValueDBKeyTupleBinding().entryToObject(theKey);

                if(result.getDomainId() == searchDomainId) {
                    count++;
                    if(results.size() < 100) {
                        results.add(new AbstractMap.SimpleEntry<>(String.valueOf(result.getNodeId()),
                        new String(foundData.getData(), StandardCharsets.UTF_8)));
                    }
                    status = dbc.getNext(theKey, foundData, LockMode.READ_UNCOMMITTED);
                } else {
                    status = null;
                }
            }
        } finally {
            if(dbc!=null) dbc.close();
            worker.commit();
        }
            return count;
        }

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.sleepycat.je");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBMgr");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.RelationKey");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.ValueKey");
    _jspx_imports_classes.add("com.sleepycat.bind.tuple.TupleBinding");
    _jspx_imports_classes.add("java.nio.charset.StandardCharsets");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("application/json; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    List<Map.Entry<String, String>> results = new ArrayList<>();
    String apname = request.getParameter("a");
    int d1 = Encoder.parseInt(request.getParameter("d1"), -1);
    int n1 = Encoder.parseInt(request.getParameter("n1"), -1);
    int d2 = Encoder.parseInt(request.getParameter("d2"), -1);
    String type = request.getParameter("type");

    if("link".equals(type)) {
        int num = searchLinkNum(apname, d1, n1, d2, results);

        out.println("Apname: " + apname);
        out.println("Domain1: " + d1);
        if(n1 > -1) out.println("Node1: " + n1);
        out.println("Domain2: " + d2);
        out.println("Link Num: " + num);

        for(Map.Entry<String, String> result:results) {
            out.println("nodeId 1: " + result.getKey() + "　nodeId 2: " + result.getValue());
        }


    } else if("node".equals(type)) {
        int num = searchNodeNum(apname, d1, results);
        out.println("Apname: " + apname);
        out.println("Domain: " + d1);
        out.println("Node Num: " + num);

        for(Map.Entry<String, String> result:results) {
            out.println("nodeId: " + result.getKey() + "　nodeValue: " + result.getValue());
        }

    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
