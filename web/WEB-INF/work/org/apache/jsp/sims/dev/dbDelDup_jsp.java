/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.util.Const;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.nui.CellInfo;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.nui.CellLocation;
import java.util.stream.Collectors;
import java.util.*;
import com.ragic.s3.sdb.data.bdb.BDBWorkerCommand;
import com.ragic.s3.sdb.data.bdb.BDBWorkerCommandUnlink;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.sdb.data.bdb.BDBWorkerCommandLink;

public final class dbDelDup_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorkerCommand");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorkerCommandLink");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellInfo");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorkerCommandUnlink");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellLocation");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;

      out.write("<div style=\"padding: 20px;\">\r\n");

    String action=request.getParameter("action");
    String recordURL = request.getParameter("record");//   /RegressionTest/forms28/2/3148
    String pd = request.getParameter("pd");

    if (recordURL == null) {
        out.println("<span style=color:red>Invalid Record URL</span> (ex:/support/nobug/1/123) : <form method=GET action='dbDelDup.jsp'><input type='text' name='record' /><input type='submit' /></form>");
        return;
    }
    String[] sec = recordURL.split("/");
    if (sec.length < 4) {
        out.println("<span style=color:red>Invalid Record URL</span> (ex:/support/nobug) : <form method=GET action='dbDelDup.jsp'><input type='text' name='record' /><input type='submit' /></form>");
        return;
    }

      out.write("<div style=\"margin: 20px 0\">\r\n        <button onclick=\"dedup()\">Remove duplicate nodes</button>\r\n    </div>\r\n    <div id=\"startDedup\" style=\"display:none\">\r\n        Start removing duplicated nodes......\r\n    </div>\r\n");

    String apname = sec[1];
    String path = "/" + sec[2];
    int sheetIndex = Encoder.parseInt(sec[3]);
    int recordId = -1;
    if(sec.length == 5) recordId = Encoder.parseInt(sec[4], -1);
    StringBuilder details = new StringBuilder();
    Map<String, Set<String>> bugNodeIdMapping = new HashMap<>();
    Map<String, Set<String>> newNodeIdMapping = new HashMap<>();


    NUIForm form = AppManager.getFormPage(apname, path, sheetIndex);
    BDBWorker worker = new BDBWorker(apname);
    List<Integer> problemDomains = null;
    if(pd != null && !pd.equals("null")) problemDomains = Arrays.stream(pd.split(",")).map(Integer::parseInt).collect(Collectors.toList());

    Set<String> imageValues = new HashSet<>();
    Set<String> insertValue = new HashSet<>();
    List<BDBWorkerCommand> dedups = new ArrayList<>();
    List<BDBWorkerCommand> linkNew = new ArrayList<>();

    if("dedup".equals(action)) {
        try {
            int key = form.getKeyField();
            SDBQuery q = new SDBQueryFull(worker);
            q.go(key);
            if(recordId >= 0) q.filter(recordId);
            SDBNode keyNode;
            int keyNodeId;
            while ((keyNode = q.next()) != null) {
                keyNodeId = keyNode.getId();

                for (CellInfo info : form.getAllFields(false)) {
                    imageValues.clear();
                    insertValue.clear();

                    if(problemDomains != null){
                        if(!problemDomains.contains(info.getDomainId())) continue;
                    }
                    else if(!info.isFileUpload()) continue;

                    List<SDBNode> nodes = q.dipAllNodes(info.getDomainId());
                    for (int i = 0; i < nodes.size(); i++) {
                        SDBNode node = nodes.get(i);
                        int nodeId = node.getId();
                        String nodeValue = node.getValue();

                        insertValue.remove(nodeValue);

                        if (imageValues.contains(nodeValue) || nodeValue.contains("|")) {
                            dedups.add(new BDBWorkerCommandUnlink(key, keyNode.getId(), info.getDomainId(), nodeId));

                            bugNodeIdMapping.putIfAbsent("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId, new LinkedHashSet<>());
                            bugNodeIdMapping.get("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId).add(info.getValue()+"("+info.getDomainId()+")");
                        }

                        if(nodeValue.contains("|")){
                            String[] nodeValues = nodeValue.split("\\|");
                            for(String v : nodeValues){
                                String[] nodeValues2 = v.split(", ");
                                for(String v2 : nodeValues2) {
                                    if (!imageValues.contains(v2) && v2.trim().length() > 0) {
                                        insertValue.add(v2);
                                    }
                                }
                            }
                        }
                        else if(nodeValue.trim().length() > 0) imageValues.add(nodeValue);

                        //empty value
                        if(imageValues.size() > 0 && nodeValue.trim().length() == 0) {
                            dedups.add(new BDBWorkerCommandUnlink(key, keyNode.getId(), info.getDomainId(), nodeId));

                            bugNodeIdMapping.putIfAbsent("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId, new LinkedHashSet<>());
                            bugNodeIdMapping.get("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId).add(info.getValue()+"("+info.getDomainId()+")");
                        }
                    }

                    for(String v : insertValue) {
                        int newNodeId = worker.addNode(info.getDomainId(), v, false);
                        linkNew.add(new BDBWorkerCommandLink(key, keyNode.getId(), info.getDomainId(), newNodeId));

                        newNodeIdMapping.putIfAbsent("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId + "/" + info.getValue(), new LinkedHashSet<>());
                        newNodeIdMapping.get("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId + "/" + info.getValue()).add(v);
                    }
                }

                //do subtables
                Map<CellLocation, Integer> subtableKeys = form.getSubtableKey();
                for (Map.Entry<CellLocation, Integer> e : subtableKeys.entrySet()) {
                    Integer subtableKey = e.getValue();
                    List<CellInfo> subtableCellInfoList = form.getSubtableFields(subtableKey);
                    Integer parentAlias = form.getSubtableParentAlias(e.getKey());
                    q.go(subtableKey);
                    if (parentAlias != null) {
                        q.setAliasParent(parentAlias);
                    }
                    SDBNode subtableKeyNode = null;
                    while ((subtableKeyNode = q.next()) != null) {
                        for (CellInfo info : subtableCellInfoList) {
                            imageValues.clear();
                            insertValue.clear();

                            if(problemDomains != null){
                                if(!problemDomains.contains(info.getDomainId())) continue;
                            }
                            else if(!info.isFileUpload()) continue;

                            List<SDBNode> nodes = q.dipAllNodes(info.getDomainId());
                            for (int i = 0; i < nodes.size(); i++) {
                                SDBNode node = nodes.get(i);
                                int nodeId = node.getId();
                                String nodeValue = node.getValue();

                                insertValue.remove(nodeValue);

                                if (imageValues.contains(nodeValue) || nodeValue.contains("|")) {
                                    dedups.add(new BDBWorkerCommandUnlink(subtableKey, subtableKeyNode.getId(), info.getDomainId(), nodeId));

                                    bugNodeIdMapping.putIfAbsent("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId, new LinkedHashSet<>());
                                    bugNodeIdMapping.get("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId).add(info.getValue()+"("+info.getDomainId()+")");
                                }

                                if(nodeValue.contains("|")){
                                    String[] nodeValues = nodeValue.split("\\|");
                                    for(String v : nodeValues){
                                        String[] nodeValues2 = v.split(", ");

                                        for(String v2 : nodeValues2) {
                                            if (!imageValues.contains(v2) && v2.trim().length() > 0) {
                                                insertValue.add(v2);
                                            }
                                        }
                                    }
                                }
                                else if(nodeValue.trim().length() > 0) imageValues.add(nodeValue);


                                //empty value
                                if(imageValues.size() > 0 && nodeValue.trim().length() == 0) {
                                    dedups.add(new BDBWorkerCommandUnlink(subtableKey, subtableKeyNode.getId(), info.getDomainId(), nodeId));

                                    bugNodeIdMapping.putIfAbsent("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId, new LinkedHashSet<>());
                                    bugNodeIdMapping.get("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId).add(info.getValue()+"("+info.getDomainId()+")");
                                }
                            }

                            for(String v : insertValue) {
                                int newNodeId = worker.addNode(info.getDomainId(), v, false);
                                linkNew.add(new BDBWorkerCommandLink(subtableKey, subtableKeyNode.getId(), info.getDomainId(), newNodeId));

                                newNodeIdMapping.putIfAbsent("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId + "/" + info.getValue(), new LinkedHashSet<>());
                                newNodeIdMapping.get("/" + apname + path + "/" + sheetIndex + "/" + keyNodeId + "/" + info.getValue()).add(v);
                            }
                        }
                    }
                    q.goback();
                }

                imageValues.clear();
                insertValue.clear();
            }
        } finally {
            worker.commit();
        }

        if(problemDomains!=null) {
            String problemFields = problemDomains.stream().map(id -> "<b>" + form.getField(id).getValue() + "</b>(" + id + ")").collect(Collectors.joining(", "));
            out.println("<h3>Problem Fields: " + problemFields + "</h3>");
        }
        worker = new BDBWorker(apname);

        if (dedups.size() == 0) {
//            out.println("No duplicated links");
        } else {
            try {
                out.println("Dedup removing " + dedups.size() + " links for " + apname + path + "/" + sheetIndex + "/" + (recordId >= 0 ? recordId : ""));
                out.println("<br>");
                for (BDBWorkerCommand c : dedups) {
                    c.execute(worker);
                }
                out.println("Dedup removed " + dedups.size() + " links for " + apname + path + "/" + sheetIndex + "/" + (recordId >= 0 ? recordId : ""));
                out.println("<br><br>");

                out.println("Dedup adding " + linkNew.size() + " links for " + apname + path + "/" + sheetIndex + "/" + (recordId >= 0 ? recordId : ""));
                out.println("<br>");
                for (BDBWorkerCommand c : linkNew) {
                    c.execute(worker);
                }
                out.println("Dedup added " + linkNew.size() + " links for " + apname + path + "/" + sheetIndex + "/" + (recordId >= 0 ? recordId : ""));
            } finally {
                worker.commit();
            }
        }

        if(dedups.size() == 0){
            details.append("No duplicated Links<br>");
        }
        else {
            for (Map.Entry e : bugNodeIdMapping.entrySet()) {
                details.append(e.getKey() + ": " + e.getValue().toString() + "<br>");
            }
        }

        details.append("<br><br><br>");

        if(linkNew.size() == 0){
            details.append("No new nodes were added.");
        }
        else {
            details.append("<b>Add New Node In:</b><br>");

            for (Map.Entry<String, Set<String>> e : newNodeIdMapping.entrySet()) {
                details.append(e.getKey() + ": " + new ArrayList<>(e.getValue()).stream().collect(Collectors.joining("  ，   ")) + "<br>");
            }
        }

      out.write("<div style=\"margin-top: 30px; padding: 20px; background-color: aliceblue\">");
      out.print(details.toString());
      out.write("</div>\r\n");

    }

      out.write("</div>\r\n\r\n<script>\r\n    function dedup(){\r\n        document.getElementById(\"startDedup\").style.display = \"\";\r\n        postPromise('/sims/dev/dbDelDup.jsp', {'record':'");
      out.print(recordURL);
      out.write("','action':'dedup', 'pd': '");
      out.print(pd);
      out.write("'}).then(function (res){\r\n            document.body.innerHTML = res;\r\n        });\r\n    }\r\n\r\n    function postPromise(url, data) {\r\n        // Turn the data object into an array of URL-encoded key/value pairs.\r\n        data = data || {};\r\n        let urlEncodedDataPairs = Object.entries(data).reduce(function (accumulator, pair) {\r\n            const key = pair[0], value = pair[1];\r\n            if (Array.isArray(value)) {\r\n                return accumulator.concat(value.map(function (subValue) {\r\n                    return encodeURIComponent(key) + '=' + encodeURIComponent(subValue);\r\n                }));\r\n            } else {\r\n                accumulator.push(encodeURIComponent(key) + '=' + encodeURIComponent(value));\r\n                return accumulator;\r\n            }\r\n        }, []);\r\n\r\n        // x-www-form-urlencoded issue.\r\n        // Combine the pairs into a single string and replace all %-encoded spaces to\r\n        // the '+' character; matches the behaviour of browser form submissions.\r\n        let urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');\r\n");
      out.write("\r\n        return createPromiseRequest({\r\n            \"method\": \"POST\",\r\n            \"url\": url,\r\n            \"headers\": {'Content-Type': 'application/x-www-form-urlencoded'},\r\n            \"data\": urlEncodedData\r\n        });\r\n    }\r\n\r\n    function createPromiseRequest(options) {\r\n        return new Promise(function (resolve, reject) {\r\n            let xhr = new XMLHttpRequest();\r\n            xhr.open(options.method, options.url);\r\n            xhr.onload = function () {\r\n                if (this.status >= 200 && this.status < 300) {\r\n                    resolve(xhr.response);\r\n                } else {\r\n                    reject({\r\n                        status: this.status,\r\n                        statusText: xhr.statusText,\r\n                        response: xhr.response\r\n                    });\r\n                }\r\n            };\r\n            xhr.onerror = function () {\r\n                reject({\r\n                    status: this.status,\r\n                    statusText: xhr.statusText\r\n                });\r\n            };\r\n");
      out.write("\r\n            if (options.hasOwnProperty(\"headers\")) {\r\n                Object.keys(options.headers).forEach(function (key) {\r\n                    xhr.setRequestHeader(key, options.headers[key]);\r\n                });\r\n            }\r\n\r\n            if (options.hasOwnProperty(\"responseType\")) {\r\n                xhr.responseType = options.responseType;\r\n            }\r\n\r\n            let data = options.data || null;\r\n            xhr.send(data);\r\n        });\r\n    }\r\n</script>\r\n\r\n");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
