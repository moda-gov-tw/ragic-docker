/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.util.Const;
import com.stripe.Stripe;
import java.util.*;
import com.stripe.model.*;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.util.DConst;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.nui.helper.Encoder;
import java.io.File;
import com.ragic.s3.util.Util;
import com.ragic.s3.nui.helper.UserRegister;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

public final class expireArchive_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("com.stripe.model");
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.stripe.Stripe");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserRegister");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("java.util.regex.Matcher");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("java.util.regex.Pattern");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html;charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



    Map<String,String> paymentStatus=new LinkedHashMap<>();
    //go through all stripe and record apnames with active payment
    //api info
    Stripe.apiKey = Const.STRIPE_KEY;
    Stripe.apiVersion = Const.STRIPE_API;
/*
    Map<String,Customer> cclist=new LinkedHashMap<>();
    Map<String,Customer> cclistStripeId=new LinkedHashMap<>();
    String lastCustomer=null;
    CustomerCollection cc=null;
    while(cc==null || cc.getHasMore()){
        Map param=new HashMap<String, Object>();
        param.put("limit","100");
        if(lastCustomer!=null) param.put("starting_after",lastCustomer);
        cc= Customer.list(param);
        for(Customer c:cc.getData()){
            String apname=c.getDescription();
            if(cclist.containsKey(apname)){//already have, duplicated
                //out.println("Duplicated stripe customer:"+apname+"<br>");
            }
            cclist.put(c.getDescription(),c);
            cclistStripeId.put(c.getId(),c);
            lastCustomer=c.getId();
        }
        //lastCustomer=cclist.get(cclist.size()-1).getId();
    }
    for(Customer c:cclist.values()) {
        CustomerSubscriptionCollection sc = c.getSubscriptions();
        String apname=c.getDescription();
        List<Subscription> list=sc.getData();
//        if(list!=null && list.isEmpty()){
//            Subscription sub=c.getSubscription();
//            if(sub!=null) list.add(sub);
//        }
//        if(list==null || list.isEmpty()){
//            out.println("list empty:"+apname+"<br>");
//            continue;
//        }
        for (Subscription s : list) {
            if (s == null) {
                out.println("s=null:"+apname+"<br>");
                continue;
            }
            if ("unpaid".equals(s.getStatus())) {
                //System.out.println("unpaid:"+c.getDescription()+"/"+s.getPlan().getId()+"/"+s.getQuantity());
                continue;
            }
            if ("past_due".equals(s.getStatus())) {
                paymentStatus.put(apname,"past_due");
            }
            else{
                paymentStatus.put(apname,"stripe");
            }
        }
    }

    //go through all ragic/adm/1 and save list of apnames, update payment status
    BDBWorker worker=new BDBWorker(Const.AP_RAGIC);
    Set<String> alldbapnames=new LinkedHashSet<>();
    Map<String,String> needFixPS=new LinkedHashMap<>();
    try{
        SDBQuery q=new SDBQueryFull(worker);
        q.go(DConst.ROOT_APNAME);
        SDBNode node;
        long today=new Date().getTime();
        while((node=q.next())!=null){
            String apname=node.getValue();
            String plan=q.dip(DConst.ROOT_ACCOUNT_EDITION);
            String ps=q.dip(DConst.ROOT_PAYMENT_STATUS);

            if(apname!=null && apname.contains("_deleted_")) continue;
            alldbapnames.add(apname);

            //process free
            boolean free="FREE2".equals(plan) || "FREE".equals(plan) || "COMM".equals(plan);
            if(free) {
                String lastActiveDateString=q.dip(DConst.ROOT_AP_LAST_ACTIVE_DATE);
                Date lastActiveDate= Encoder.parseStandardDate(lastActiveDateString);
                boolean active30=lastActiveDate!=null && ((today - lastActiveDate.getTime())<Const.DAY_MILLI*30L);
                if(active30){
                    paymentStatus.put(apname,"free active");
                }
                else{
                    paymentStatus.put(apname,"free inactive");
                }
                if(ps!=null && !ps.isEmpty()){
                    needFixPS.put(apname,"clear ps");
                }
                continue;
            }

            //process manual
            String stripeId=q.dip(DConst.ROOT_STRIPE_ID);
            String subscribeToString=q.dip(DConst.ROOT_ACCOUNT_SUBSCRIBE_TO);
            Date subscribeTo= Encoder.parseStandardDate(subscribeToString);
            boolean manual="m".equals(stripeId);
            boolean trial=subscribeTo!=null && (subscribeTo.getTime() - new Date().getTime()) < 60L*Const.DAY_MILLI;
            if(manual){
                if(paymentStatus.containsKey(apname)){
                    out.println("Manual but also have active payment:"+apname+"<br>");
                }
                else if(subscribeTo==null){
                    out.println("Manual but no subscribe to date:"+apname+"<br>");
                }
                paymentStatus.put(apname,"manual");
                if(ps!=null && !"p".equals(ps)){
                    needFixPS.put(apname,ps+" => p");
                }
                continue;
            }

            //process trial
            if(trial){
                paymentStatus.put(apname,"trial");
                continue;
            }

            //process internal
            boolean expired=subscribeTo!=null && subscribeTo.getTime() < today;
            boolean wontExpire=subscribeTo==null;
            if(!expired && (stripeId==null || stripeId.isEmpty())){

                //check if still active
                String lastActiveDateString=q.dip(DConst.ROOT_AP_LAST_ACTIVE_DATE);
                Date lastActiveDate= Encoder.parseStandardDate(lastActiveDateString);
                boolean active90=lastActiveDate!=null && ((today - lastActiveDate.getTime())<Const.DAY_MILLI*90L);
                if(active90){
                    String preconfirm=q.dip(DConst.ROOT_PRECONFIRM_EMAIL);
                    if(preconfirm==null || preconfirm.isEmpty() || preconfirm.endsWith("@ragic.com")){
                    }
                    else if(wontExpire){
                        out.println("Wont expire:"+apname+"<br>");
                        //com.ragic.s3.nui.Account.getInstance(apname).getPaymentStatus().setSubscribeToDate(Encoder.parseStandardDate("2020/09/15"));
                    }
                    else{
                        //out.println("Long trial:"+apname+":"+subscribeToString+"<br>");
                    }
                    paymentStatus.put(apname,"internal");
                }
                else{
                    //com.ragic.s3.nui.Account.getInstance(apname).getPaymentStatus().setSubscribeToDate(Encoder.parseStandardDate("2020/09/15"));
                    out.println("Internal to be expired:"+apname+"<br>");
                }
                continue;
            }


            //process expired
            if(expired){
                if(paymentStatus.containsKey(apname)){
                    out.println("Expired but has active payment:"+apname+"<br>");
                }
                paymentStatus.put(apname,"expired");
                if(ps!=null && !"c".equals(ps)){
                    needFixPS.put(apname,ps+" => c");
                }
                continue;
            }

            //process renamed
//            if(!cclist.containsKey(apname) && cclistStripeId.containsKey(stripeId)){//renamed
//                Customer c=cclistStripeId.get(stripeId);
//                String fromApname=c.getDescription();
//                c.setDescription(apname);
//                Map<String, Object> params = new HashMap<>();
//                params.put("description", apname);
//                c.update(params);
//                out.println("Renamed:"+fromApname+" => "+apname+"<br>");
//                continue;
//            }


            //active stripe
            if(paymentStatus.containsKey(apname)){
                if(ps!=null && !"p".equals(ps)){
                    needFixPS.put(apname,ps+" => p");
                }
            }
            else{
                paymentStatus.put(apname,"uncategorized");

                if(subscribeTo!=null){
                    //out.println("Cancel but has subscription left:"+apname+":"+subscribeToString+"<br>");
                }
                else{
                    //com.ragic.s3.nui.Account.getInstance(apname).getPaymentStatus().setSubscribeToDate(Encoder.parseStandardDate("2020/09/15"));
                    out.println("Cancel but no expire date:"+apname+"<br>");
                }
                if(ps!=null && !"c".equals(ps)){
                    needFixPS.put(apname,ps+" => c");
                }
            }
        }
    }
    finally {
        worker.commit();
    }
    */

    //go through all cust/data folders
    File d=new File("cust/data");
    Set<String> invalidFolders=new LinkedHashSet<>();
    for(File dir:d.listFiles()){
        if(!dir.isDirectory() || dir.isHidden()) continue;
        String dirname=dir.getName();
        if(Util.isSystemApname(dirname)) continue;
        if(dirname!=null && dirname.toLowerCase().contains("ragic")) continue;
        if(dirname!=null && dirname.toLowerCase().contains("standard")) continue;
        if(dirname!=null && dirname.toLowerCase().contains("template")) continue;


//        if(dirname!=null && dirname.contains("__invalid2_")) {
//            dir.delete();
//        }

        //not contained
        //if(!paymentStatus.containsKey(dirname)){
        //if(!alldbapnames.contains(dirname)){
              //dir.renameTo(new File("cust/data/__invalid2_"+dirname));

//            Pattern p = Pattern.compile("^[0-9a-zA-Z]{2,}$");
//            Matcher m = p.matcher(dirname);
//            if (!m.matches()) {
//                //dir.renameTo(new File("cust/data/__invalid_"+dirname));
//                continue;
//            }
//            invalidFolders.add(dirname);
        //}
    }

    //  if expired then archive


    //display
//    for(String s:invalidFolders){
//        out.println("Invalid folder:"+Encoder.encjs(s,true)+"<br>");
//    }


//    for(Map.Entry<String,String> e:needFixPS.entrySet()){
//        out.println("PS Fix:"+Encoder.encjs(e.getKey(),true)+":"+e.getValue()+"<br>");
//    }
//    for(Map.Entry<String,String> e:paymentStatus.entrySet()){
//        out.println(Encoder.encjs(e.getKey(),true)+":"+e.getValue()+"<br>");
//    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
