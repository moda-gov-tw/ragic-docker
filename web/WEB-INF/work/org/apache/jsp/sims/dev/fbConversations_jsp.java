/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.nui.helper.ExceptionManager;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.nui.helper.AppManager;
import java.util.List;
import com.ragic.s3.bot.BotManager;
import java.util.Set;
import java.util.stream.Collectors;
import java.util.Date;
import java.text.SimpleDateFormat;
import com.ragic.s3.nui.*;
import org.json.JSONObject;
import com.ragic.s3.nui.helper.FileHelper;
import java.io.File;
import com.ragic.s3.bot.mapped.BotConfig;

public final class fbConversations_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.bot.BotManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.FileHelper");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.bot.mapped.BotConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    User user=(User)session.getAttribute("USER");
    if(user == null || !user.getUserId().endsWith("@ragic.com") && !"jeff.kuo@gmail.com".equals(user.getUserId())) {
        return;
    }

    // change bot's active
    String Id = request.getParameter("ID");
    String at = request.getParameter("active");
    if(Id!=null && at!=null){
        BotManager.getInstance().setbotActive(Id,Encoder.parseBoolean(at));
    }

    //BOT Customer's sheet
    final String apname = Const.AP_BOT;

    String conf = request.getParameter("conf");
    if(conf==null) return;
    String platform = "bottw".equals(conf) ? "FB_ragictw" :"FB_ragicdb";
    JSONObject json = new JSONObject(FileHelper.readFileToString(new File("cust/def/app/"+apname+"/"+conf+".json")));
    BotConfig botConfig = new BotConfig(json);

    String customerSheetURL = botConfig.getCustomerSheetURL();
    String[] customerSheetURLArr = customerSheetURL.split("/");
    String path = "/"+customerSheetURLArr[1];
    int sheetIndex = Encoder.parseInt(customerSheetURLArr[2]);

    NUIForm form = AppManager.getFormPage(apname,path,sheetIndex);
    List<CellInfo> fields=form.getAllFields(false);
    int rootDomainId=form.getRootDomainId();

    int lastInteractionDateFieldId = botConfig.getCustomerLastInteractionDateFieldId(); // used to sort customers
    int cuidFieldId = botConfig.getCustomerChatUserIdFieldId();
    int chatLogFileFieldId = botConfig.getCustomerChatLogFileFieldId();
    int contactPlatformFieldId = botConfig.getContactPlatformFieldId();

    //get current time and last week time
    SimpleDateFormat sdf = new SimpleDateFormat("yyyy/MM/dd HH:mm:ss");
    long timeStamp = Encoder.parseLong(request.getParameter("ts"),-1);

    Date currentDate = new Date();
    if(timeStamp != -1) currentDate.setTime(timeStamp);
    long currentTimeStamp = currentDate.getTime();
    String currentTimeStr = sdf.format(currentDate);

    long lastWeekTimeStamp = currentTimeStamp - (7*24*60*60*1000);
    Date lastWeekDate = new Date(lastWeekTimeStamp);
    String lastWeekTimeStr = sdf.format(lastWeekDate);

    //Content
    Set<String> suspendSet = BotManager.getInstance().getSuspendBotCuidSet();
    String suspendFilterString;
    String activeFilterString="";
    BDBWorker worker = new BDBWorker(apname);
    try{
        //Suspended Bot List
        if(suspendSet.size() > 0){
            //setting activeFilterString & suspendFilterString
            suspendFilterString = suspendSet.stream().collect(Collectors.joining("|","(",")")); // (suspendId1|suspendId2|suspendId3|...)
            activeFilterString = suspendSet.stream().collect(Collectors.joining("|","^(?!(",")$)")); // ^(?!(suspendId1|suspendId2|suspendIdId3|...)$)

            out.println("<div style='margin-bottom: 16px;'>");
            out.println("<div style='color:red;font-weight:bold;font-size:24px;'>Suspended Bot:</div>");
            out.println("<table id='suspendTable'><tr>");
            // print suspend Bot table header
            for(CellInfo info:fields){
                out.println("<td style='font-weight:bold;'>"+Encoder.encjs(info.getValue())+"</td>");
            }
            out.println("<td style='font-weight:bold;'>Latest Message of User</td>");
            out.println("<td style='font-weight:bold;'></td>"); //active field
            out.println("</tr>");

            SDBQuery suspendQuery = new SDBQueryFull(worker);
            SDBNode suspendNode = null;
            suspendQuery.go(rootDomainId);
            suspendQuery.filterRegex(cuidFieldId,suspendFilterString);
            suspendQuery.filter(contactPlatformFieldId,platform);
            suspendQuery.sort(lastInteractionDateFieldId,Const.SORTEDREV);

            while ((suspendNode=suspendQuery.next())!=null){
                String currentCuid="";
                out.println("<tr>");
                for(CellInfo info:fields){
                    if(info.getDomainId()==cuidFieldId) currentCuid = suspendQuery.dip(info.getDomainId());
                    if(info.getDomainId()==chatLogFileFieldId)
                        out.println("<td><a href='javascript:void(0);' style='text-decoration:none;' onclick=\"downloadFile('"+Encoder.encjs(apname)+"','"+Encoder.encjs(currentCuid)+"')\">"+Encoder.encjs(suspendQuery.dip(info.getDomainId()))+"</a></td>");
                    else if(info.getDomainId()==lastInteractionDateFieldId)
                        out.println("<td>"+bundle.convertTime(suspendQuery.dip(info.getDomainId()))+"</td>");
                    else
                        out.println("<td>"+Encoder.encjs(suspendQuery.dip(info.getDomainId()))+"</td>");
                }
                out.println("<td>"+Encoder.encjs(BotManager.getInstance().getLastUserMessage(currentCuid))+"</td>");
                out.println("<td style='font-weight:bold;'><a href='javascript:void(0);' onclick=\"passBotStatus('"+Encoder.encjs(currentCuid)+"',true);\">Active</a></td>");
                out.println("</tr>");
            }

            out.println("</table>");
            out.println("</div>");
        }

        //Active Bot List
        out.println("<div>");
        out.println("<div style='color:limegreen;font-weight:bold;font-size:24px;'>Active Bot:</div>");
        out.println("<table><tr>");
        // print active Bot table header
        for(CellInfo info:fields){
            out.println("<td style='font-weight:bold;'>"+Encoder.encjs(info.getValue())+"</td>");
        }
        out.println("<td style='font-weight:bold;'>Latest Message of User</td>");
        out.println("<td style='font-weight:bold;'></td>"); //stop field
        out.println("</tr>");

        SDBQuery activeQuery = new SDBQueryFull(worker);
        SDBNode activeNode = null;

        activeQuery.go(rootDomainId);
        activeQuery.filterRegex(cuidFieldId,"^fb_");
        activeQuery.filterRange(lastInteractionDateFieldId,currentTimeStr,lastWeekTimeStr);
        activeQuery.filter(contactPlatformFieldId,platform);
        if(suspendSet.size()>0) activeQuery.filterRegex(cuidFieldId,activeFilterString);
        activeQuery.sort(lastInteractionDateFieldId,Const.SORTEDREV);

        while ((activeNode=activeQuery.next())!=null){
            String currentCuid="";
            out.println("<tr>");
            for(CellInfo info:fields){
                if(info.getDomainId()==cuidFieldId) currentCuid = activeQuery.dip(info.getDomainId());
                if(info.getDomainId()==chatLogFileFieldId)
                    out.println("<td><a href='javascript:void(0);' style='text-decoration:none;' onclick=\"downloadFile('"+Encoder.encjs(apname)+"','"+Encoder.encjs(currentCuid)+"')\">"+Encoder.encjs(activeQuery.dip(info.getDomainId()))+"</a></td>");
                else if(info.getDomainId()==lastInteractionDateFieldId)
                    out.println("<td>"+bundle.convertTime(activeQuery.dip(info.getDomainId()))+"</td>");
                else
                    out.println("<td>"+Encoder.encjs(activeQuery.dip(info.getDomainId()))+"</td>");
            }
            out.println("<td>"+Encoder.encjs(BotManager.getInstance().getLastUserMessage(currentCuid))+"</td>");
            out.println("<td style='font-weight:bold;'><a href='javascript:void(0);' onclick=\"passBotStatus('"+Encoder.encjs(currentCuid)+"',false);\">Stop</a></td>");
            out.println("</tr>");
        }
        out.println("</table>");
        out.println("</div>");
    }catch (Exception e){
        ExceptionManager.getInstance().processException(e);
    }
    finally {
        worker.commit();
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
