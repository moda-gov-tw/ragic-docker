/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.UserCacheLocal;
import com.google.common.io.BaseEncoding;
import com.ragic.s3.util.DES;
import org.json.JSONObject;
import com.ragic.s3.service.workflow.ScriptResponse;
import org.json.JSONException;
import java.net.URLEncoder;
import java.io.UnsupportedEncodingException;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.RemoteAccountManager;
import com.ragic.s3.nui.helper.ExceptionManager;
import com.ragic.s3.util.Log;

public final class checkUserCache_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserCacheLocal");
    _jspx_imports_classes.add("org.json.JSONException");
    _jspx_imports_classes.add("java.net.URLEncoder");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.google.common.io.BaseEncoding");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.RemoteAccountManager");
    _jspx_imports_classes.add("com.ragic.s3.util.DES");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ScriptResponse");
    _jspx_imports_classes.add("java.io.UnsupportedEncodingException");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("application/json; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    JSONObject result = new JSONObject();
    Log log=new Log("log/checkUserCache.log");
    log.setIfDisplayTime(true);
try {
    String basicAuthHeader=request.getHeader("Authorization");
    boolean force = request.getParameter("forceReload") != null;

    if(basicAuthHeader==null && request.getParameter("embedAPIKey")!=null) {
      basicAuthHeader="Basic "+ BaseEncoding.base64().encode((request.getParameter("embedAPIKey") + ":").getBytes());
    }
    else if(basicAuthHeader==null && request.getParameter("APIKey")!=null) {
      basicAuthHeader="Basic "+request.getParameter("APIKey");
    }

    if(basicAuthHeader!=null && basicAuthHeader.startsWith("Basic ")){
      //strip "Basic "
      try{
        basicAuthHeader=basicAuthHeader.substring(6);

        //base64 decode
        basicAuthHeader=new String(BaseEncoding.base64().decode(basicAuthHeader));

        //DES decode
        basicAuthHeader=new DES().decrypt(basicAuthHeader);
      }
      catch(Exception e){
        basicAuthHeader=null;//if any exception, the header is bad
      }


      log.info("basicAuthHeader: " + basicAuthHeader);

      //retrieve the user and secret key
      String[] split=basicAuthHeader!=null?basicAuthHeader.split("@"):new String[0];
      //bad secret key
      if(basicAuthHeader==null || split.length<=2){
        //this following line will cause calls that includes a basic auth header to fail
//        response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
        JSONObject err=new JSONObject();
        try {
          err.put("status", ScriptResponse.ERROR);
          err.put("code",304);
          err.put("msg","The API key you provided is invalid, or you are providing the API key in the wrong place, or the wrong encoding.");
          request.setAttribute("SKEY_LOGIN_MSG","The API key you provided is invalid, or you are providing the API key in the wrong place, or the wrong encoding.");
        } catch (JSONException e) {
          e.printStackTrace();
        }
        out.println(err.toString(2));
        return;
      }

      String email=split[0]+"@"+split[1];
      String skey=split[2];

        log.info("email: " + email);
      try {
              log.info("before encode skey: " + email);
        skey= URLEncoder.encode(skey, "UTF-8");
            log.info("after encode skey: " + email);
      } catch (UnsupportedEncodingException e) {
        e.printStackTrace();
      }

      //check if the user has logged in already
      try{
        HttpSession currentSession=request.getSession();
        currentSession.setAttribute("API_REQUEST",true);//for User.valueBound to retrive and remove
        User cacheUser=UserCacheLocal.getUser(email);
        if(cacheUser==null || force){
            log.info("cache is null");
            log.info("force: " + force);
          User user= RemoteAccountManager.main_authenticateUser(email, skey, "SKEY");
          if(user!=null){
               log.info("user is not null: " + user.getJSON());
            user.setIp(request.getRemoteAddr());
            currentSession.setAttribute("USER",user);
            request.setAttribute("APIKEY_LOGIN",true);
            if(!user.ifSkey(skey)){
              user.addSkey(skey);
            }
            UserCacheLocal.addUser(email,user);
                 log.info("add user to cache!");
                       log.info(user.getJSON().toString(2));
          } else {
              log.info("user is null");
          }
        }
        else{
              log.info("cache is not null");
          if(cacheUser.ifSkey(skey)){
                log.info("isSkey");
            cacheUser.setIp(request.getRemoteAddr());
            currentSession.setAttribute("USER",cacheUser);
            request.setAttribute("APIKEY_LOGIN",true);
          } else {
              log.info("is not skey");
          }
          log.info(cacheUser.getJSON().toString(2));
        }

        //old mechanism below, replaced with SessionRegistry.getInstance().reuseOldApiSessionUser
//        HttpSession loggedInSession= SessionRegistry.getInstance().getLoggedInSession(email,currentSession,false);
//        if(loggedInSession==null){//not logged in yet
//          User user= RemoteAccountManager.main_authenticateUser(email, pwdhash, "SKEY");
//          currentSession.setAttribute("USER",user);
//        }
//        else{
//          //same session, do nothing
//          if(Objects.equals(loggedInSession.getId(), currentSession.getId())){}
//          //this is a different session, switch user object and invalidate old session
//          else{
//            //unregister the old one
//            SessionRegistry.getInstance().unregister(loggedInSession.getId());
//
//            //set the user in the current session, must come after unregister, or
//            User user=(User)loggedInSession.getAttribute("USER");
//            if(user!=null) currentSession.setAttribute("USER",user);
//
//            //we have retrieved the user, now old session can invalidate
//            loggedInSession.invalidate();
//
//            //switch to the current log in session to this one
//            //this following line should not be needed anymore, becuase register is automatic with SessionBindingListener
//            //if(user!=null) SessionRegistry.getInstance().registerLogInSession(user.getUserId(),currentSession);
//          }
//        }
      }
      catch(java.lang.IllegalStateException e){}//session time out
      catch(Exception e){
        ExceptionManager.getInstance().processException(e,email,null);
      }
    }
} finally{
    out.println(result.toString(2));
}


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
