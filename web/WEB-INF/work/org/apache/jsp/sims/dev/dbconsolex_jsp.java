/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.sdb.query.SDBQuerySingleNode;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.UserManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;

public final class dbconsolex_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuerySingleNode");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserManager");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  //access right check
  User access=(User)session.getAttribute("USER");
  if(access==null || !UserManager.ifRagicSupport(access.getUserId())) {
    if(access!=null) out.println("No access for "+access.getUserId());
    else out.println("Not Logged In.");
    return;
  }

  String apname=request.getParameter("a");
  String domainId=request.getParameter("d");
  boolean noAlias=request.getParameter("noalias")!=null;
  if (apname==null || domainId==null) return;

  //update node
  String updateNodeId=request.getParameter("uni");
  String updateNodeValue=request.getParameter("unv");
  if(updateNodeId!=null && updateNodeValue!=null){
    BDBWorker worker=new BDBWorker(apname);
    try{
      worker.updateNode(Integer.parseInt(domainId),Integer.parseInt(updateNodeId),updateNodeValue);
    }
    finally{
      worker.commit();
    }
  }
  //add node
  String addNodeValue=request.getParameter("anv");
  if(addNodeValue!=null){
    BDBWorker worker=new BDBWorker(apname);
    try{
      worker.addNode(Integer.parseInt(domainId),addNodeValue);
    }
    finally{
      worker.commit();
    }
  }

  //restore node
  String restoreNodeId=request.getParameter("rni");
  String restoreValue=request.getParameter("rv");
  if(restoreNodeId!=null && restoreValue!=null){
    BDBWorker worker=new BDBWorker(apname);
    try{
      worker.restoreNode(Integer.parseInt(domainId),Integer.parseInt(restoreNodeId),restoreValue);
    }
    finally{
      worker.commit();
    }
  }

  //unlink node
  String udomainA=request.getParameter("uda");
  String udomainB=request.getParameter("udb");
  String unodeA=request.getParameter("una");
  String unodeB=request.getParameter("unb");
  if(udomainA!=null && udomainB!=null && unodeA!=null && unodeB!=null){
    BDBWorker worker=new BDBWorker(apname);
    try{
      worker.unlinkNode(Integer.parseInt(udomainA),Integer.parseInt(unodeA),Integer.parseInt(udomainB),Integer.parseInt(unodeB));
      out.println("unlink:"+udomainA+"/"+unodeA+", "+udomainB+"/"+unodeB);
    }
    finally{
      worker.commit();
    }
  }

  //delete node
  String deleteNodeId=request.getParameter("dni");
  if(deleteNodeId!=null){
    BDBWorker worker=new BDBWorker(apname);
    try{
      worker.delNode(Integer.parseInt(domainId),Integer.parseInt(deleteNodeId));
    }
    finally{
      worker.commit();
    }
  }


  //child nodes
  String parentNodeId=request.getParameter("n");
  String childDomainId=request.getParameter("cd");

  //filter nodes
  String constraintDomainId=request.getParameter("cd");
  String constraintNodeId=request.getParameter("cn");

  //check links
  String domainA=request.getParameter("da");
  String domainB=request.getParameter("db");
  String nodeA=request.getParameter("na");
  String nodeB=request.getParameter("nb");
  BDBWorker w=new BDBWorker(apname);
  try {
    if (childDomainId!=null && parentNodeId!=null) {
      SDBQuery q=new SDBQueryFull(w);
      q.go(Integer.parseInt(domainId));
      SDBNode node=null;
      while ((node=q.next())!=null) {
        if (node.getId()==Integer.parseInt(parentNodeId)) break;
      }
      q.go(Integer.parseInt(childDomainId));
      while ((node=q.next())!=null) {
        out.println(node.getId()+":"+node.getValue()+"<br/>");
      }
    } else if (constraintDomainId!=null && constraintNodeId!=null) {
      SDBQuery q=new SDBQueryFull(w);
      q.go(Integer.parseInt(domainId));
      SDBQuery conn=new SDBQuerySingleNode(w,Integer.parseInt(constraintDomainId),Integer.parseInt(constraintNodeId));
      q.filter(conn,false);
      SDBNode node=null;
      while ((node=q.next())!=null) {
        out.println(node.getId()+":"+node.getValue()+"<br/>");
      }
    }
    else if(domainA!=null && domainB!=null && nodeA!=null && nodeB!=null){
      boolean b=w.ifLinksTo(Integer.parseInt(domainA),Integer.parseInt(nodeA),Integer.parseInt(domainB),Integer.parseInt(nodeB));
      out.println(b);
    }
    else if(noAlias){
      SDBNode node;
      int nodeId=0;
      int gaps=10000;
      while((node=w.getNode(Encoder.parseInt(domainId, Const.NODOMAIN),nodeId,true))!=null || gaps>0){
        if(node!=null) out.println(node.getId()+":"+ Encoder.textToHTML(node.getValue())+"<br/>");
        nodeId++;
        gaps--;
      }
    }
    else {
      SDBQuery q=new SDBQueryFull(w);
      q.go(Encoder.parseInt(domainId));
      SDBNode node=null;
      while ((node=q.next())!=null) {
        out.println(node.getId()+":"+ Encoder.textToHTML(node.getValue())+"<br/>");
      }
    }
  } catch (Exception e) {
    e.printStackTrace();
  } finally {
    w.commit();
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
