/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.RagicConfig;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.UserManager;
import com.ragic.s3.util.SessionBindingListener;
import com.ragic.s3.util.SessionRegistry;
import java.text.SimpleDateFormat;
import java.time.Instant;
import java.util.ArrayList;
import java.util.Date;
import java.util.List;

public final class online_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicConfig");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.util.SessionRegistry");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.util.SessionBindingListener");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserManager");
    _jspx_imports_classes.add("java.time.Instant");
    _jspx_imports_classes.add("java.util.ArrayList");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;

      out.write("<html>\r\n<head><title>Online - ");
      out.print(RagicConfig.getInstance().getProperty("SERVER_NAME"));
      out.write("</title></head>\r\n<style type=\"text/css\">\r\ntable,td{font-size:12px;border-collapse:collapse;border:1px solid #cccccc;}\r\ntd{overflow-x:hidden;max-width:300px;text-overflow:ellipsis;}\r\n</style>\r\n<body>");


  //access right check
  User access=(User)session.getAttribute("USER");
  if(access==null || !UserManager.ifRagicSupport(access.getUserId())) {
    if(access!=null) out.println("No access for "+access.getUserId());
    else out.println("Not Logged In.");
    return;
  }

  boolean showBot=request.getParameter("bot")!=null;
  boolean reloadConfig =request.getParameter("rrb")!=null;
  if(reloadConfig) {
    RagicConfig.getInstance().reload();
  }

  List<SessionBindingListener> list=SessionRegistry.getInstance().getListeners();

  List<SessionBindingListener> registered=new ArrayList<>();
  List<SessionBindingListener> other=new ArrayList<>();
  for (SessionBindingListener sl : list) {
    if (sl == null) continue;
    HttpSession s = sl.getSession();
    if (s == null) continue;
    User user = null;
    try{
        user=(User) s.getAttribute("USER");
    }
    catch (Exception e){
        continue;
    }
    if (user != null && user.isValid()) {
      registered.add(sl);
    } else {
      other.add(sl);
    }
  }

  List<SessionBindingListener> newlist= new ArrayList<>();
  newlist.addAll(registered);
  newlist.addAll(other);

  SimpleDateFormat sdf=new SimpleDateFormat("yyyy/MM/dd HH:mm:ss");
  out.println("Total Users:<span id=vu>Counting...</span> (<span id=tu>Counting...</span>/"+newlist.size()+")<br/>");
  out.println("Now:"+sdf.format(new Date()));
  out.println("Testpay:"+session.getAttribute("TESTPAY"));
  out.println(" <a href=/sims/dev/onlineWorker.jsp>Worker</a>");
  out.println(" <a href=/sims/dev/security.jsp>Watchdog</a>");
  out.println(" <a href=/sims/dev/saveSession.jsp>Save Session</a>");
  out.println(" <a href=/sims/dev/online.jsp?rrb>Reload Config</a><br/>");
  out.println("<table border=1><tr><td>User</td><td>AP</td><td>ua</td><td>Idle</td><td>Total</td><td>IP</td><td style='max-width:300px;'>Refer</td><td style='max-width:300px;'>init</td><td style='max-width:300px;'>last</td></tr>");
  int count=0,validUsers=0;
  //we don't use a for-each here because the list is synchronized, using iterator will lock it longer
    for (SessionBindingListener sl : newlist) {
        if (sl == null) continue;
        HttpSession s = sl.getSession();
        if (s == null) continue;
        try{s.getAttribute("USER_AGENT");}catch (Exception e){continue;}
        String ua = (String) s.getAttribute("USER_AGENT");
        String uashort = Encoder.getUserAgentReadable(ua);

        boolean ifBot = ua == null || ua.contains("http:") || s.isNew();
        if (ifBot && !showBot) continue;
        count++;
        User user = (User) s.getAttribute("USER");
        String referer = (String) s.getAttribute("REFERRER");
        if (referer == null) referer = "";
        long idle = (Instant.now().toEpochMilli() - s.getLastAccessedTime()) / 1000;
        long total = (s.getLastAccessedTime() - s.getCreationTime()) / 1000;
        String idleString = "";
        String totalString = "";
        if (idle > 3600) idleString = (idle / 3600) + "hr";
        else if (idle > 60) idleString = (idle / 60) + "min";
        else idleString = idle + "sec";
        if (total > 3600) totalString = (total / 3600) + "hr";
        else if (total > 60) totalString = (total / 60) + "min";
        else totalString = total + "sec";
        String color = "black";
        if (idle < 300) color = "red";
        else if (idle < 600) color = "blue";
        if (user != null) {
            if (user.isValid()) validUsers++;
            String sysmsgrcv = s.getAttribute("SYSMSG_RCV") != null ? "(RCV)" : "";
            String apname = user.getDefaultApname();
            if (apname == null) apname = "";
            out.println("<tr style='color:" + color + "'><td title='" + s.getId() + "'>" + user.getUserId() + "</td>" +
                    "<td>" + apname + sysmsgrcv + "</td>" +
                    //"<td>"+sdf.format(s.getCreationTime())+"</td>"+
                    //"<td>"+sdf.format(s.getLastAccessedTime())+"</td>"+
                    "<td title='" + ua + "'>" + uashort + "</td>" +
                    "<td>" + idleString + "</td><td>" + totalString + "</td>" +
                    //"<td>"+(s.isNew() ? "New" : "Old")+"</td>"+
                    "<td><span title='" + ua + "'>" + s.getAttribute("IP") + "</span></td>" +
                    "<td title='" + referer + "'>" + referer + "</td>" +
                    "<td>" + s.getAttribute("INIT_URL") + "</td>" +
                    "<td>" + s.getAttribute("LAST_URL") + (s.getAttribute("INSERT") != null ? "(inserting)" : "") + "</td>" +
                    "</tr>");
        } else {
            out.println("<tr" + (ifBot ? " style='color:#CCCCCC'" : "") + "><td>Secret Guest</td>" +
                    "<td></td>" +
                    //"<td>"+sdf.format(s.getCreationTime())+"</td>"+
                    //"<td>"+sdf.format(s.getLastAccessedTime())+"</td>"+
                    "<td title='" + ua + "'>" + uashort + "</td>" +
                    "<td>" + idleString + "</td><td>" + totalString + "</td>" +
                    //"<td>"+(s.isNew() ? "New" : "Old")+"</td>"+
                    "<td><span title='" + ua + "'>" + s.getAttribute("IP") + "</span></td>" +
                    "<td title='" + referer + "'>" + referer + "</td>" +
                    "<td>" + s.getAttribute("INIT_URL") + "</td>" +
                    "<td>" + s.getAttribute("LAST_URL") + "</td>" +
                    "</tr>");
        }
    }
  out.println("</table>");
  out.println("<script>document.getElementById('tu').innerHTML='"+count+"';</script>");
  out.println("<script>document.getElementById('vu').innerHTML='"+validUsers+"';</script>");

      out.write("</body>\r\n</html>\r\n");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
