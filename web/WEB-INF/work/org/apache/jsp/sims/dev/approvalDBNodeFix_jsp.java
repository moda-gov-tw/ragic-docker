/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.dev;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.util.DConst;
import com.ragic.s3.service.workflow.Workflow;
import java.util.stream.Collectors;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.sdb.data.bdb.*;
import com.ragic.s3.util.Log;
import java.util.*;

public final class approvalDBNodeFix_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


final Log logger = new Log().setIfDisplayLevel(true).setIfDisplayTime(true).setLevel(Log.DEBUG);  //DEBUG 時可以改成 Log.DEBUG


    private void commitAllCommands(String apname, List<List<BDBWorkerCommand>> allCommands) {
        allCommands.forEach(commands -> commitCommands(apname, commands));
    }
    private void commitCommands(String apname, List<BDBWorkerCommand> commands) {
        BDBWorker worker = new BDBWorker(apname);
        try {
            for (BDBWorkerCommand c : commands) {
                c.execute(worker);
            }
        } finally {
            worker.commit();
        }
    }

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.sdb.data.bdb");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.Workflow");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    String action = request.getParameter("action");
    boolean doFix = Objects.equals(action, "doFix");


    if (!doFix) {

      out.write("<style>\r\n    div {\r\n        margin: 10px 0;\r\n    }\r\n    input[type=text] {\r\n        width: 100%;\r\n    }\r\n    .hidden {\r\n        visibility: hidden;\r\n    }\r\n\r\n</style>\r\n<div style=\"margin: 20px 0\">\r\n    <div>\r\n");
      out.write("\r\n        form url: <input type=\"text\" id=\"url\" name=\"url\" placeholder=\"/apname/path/sheet_index/root_node_id (可以不帶 root node id，這樣就會修正該表單所有資料的簽核)\" value=\"\">\r\n    </div>\r\n    <input type=\"hidden\" id=\"action\" name=\"action\" value=\"doFix\">\r\n    <div>\r\n        <button onclick=\"fixApprovalStatusAndResponse()\">Starting to fix incorrect approval status and response node value</button>\r\n    </div>\r\n\r\n    <div id=\"startApprovalFix\" class=\"hidden\">\r\n        Start fixing ......\r\n    </div>\r\n</div>\r\n");

    }
    else {
        String url = request.getParameter("url");
        if (url == null) return;
        String[] sec = url.split("/");
        if (sec.length < 4) {
            out.println("<span style='color:red'>Error!!! url must be /apname/path/sheet_index/root_node_id</span>");
            return;
        }
        String apname = sec[1];
        String path = "/" + sec[2];
        int sheetIndex = Encoder.parseInt(sec[3]);
        int nodeId = sec.length > 4 ? Encoder.parseInt(sec[4]) : -1;
        NUIForm form = AppManager.getFormPage(apname, path, sheetIndex);

        BDBWorker worker = new BDBWorker(apname);
        int rootDomainId = form.getRootDomainId();

        Set<Integer> errorRootNodeIds = new HashSet<>();
        List<BDBWorkerCommand> toFixUnlinkAllCommands = new ArrayList<>();
        List<BDBWorkerCommand> toFixDeleteCommands = new ArrayList<>();
        List<BDBWorkerCommand> toFixLinkCommands = new ArrayList<>();

        List<List<BDBWorkerCommand>> allCommands = new ArrayList<>();
        Collections.addAll(allCommands, toFixUnlinkAllCommands, toFixDeleteCommands, toFixLinkCommands);

        try {
            SDBQuery q = new SDBQueryFull(worker);
            SDBNode node, wfIDNode, wfHistoryIDNode, statusNode, respNode;
            int newStatusNodeId;
            q.go(rootDomainId);
            if (nodeId != -1) q.filter(nodeId);
            String status, resp;
            List<String> stepsResponse = new ArrayList<>();
            int rootNodeId;

            while ((node = q.next()) != null) {
                rootNodeId = node.getId();
                q.go(DConst.WF_ID);
                while ((wfIDNode = q.next()) != null) {
                    q.go(DConst.WF_HISTORY_ID);
                    stepsResponse.clear();
                    while ((wfHistoryIDNode = q.next()) != null) {
                        stepsResponse.add(q.dip(DConst.WF_HISTORY_RESPONSE));
                    }
                    q.goback();

                    statusNode = q.dipNode(DConst.WF_STATUS);
                    if(statusNode == null) continue;
                    status = statusNode.getValue();

                    respNode = q.dipNode(DConst.WF_RESPONSE);
                    if(respNode == null) continue;
                    resp = respNode.getValue();

                    //status
                    boolean isProcessing, isAccept, isReject;
                    isReject = stepsResponse.stream().anyMatch(res -> res.equals(Workflow.REJECT) || res.equals(Workflow.REJECT_BY_DEPUTY));
                    isAccept = stepsResponse.stream().allMatch(res -> res.equals(Workflow.ACCEPT) || res.equals(Workflow.ACCEPT_BY_DEPUTY));
                    isProcessing = !isReject && !isAccept && stepsResponse.stream().anyMatch(res -> res.equals(Workflow.NEW));

                    if (isProcessing && !status.equals(Workflow.PROCESSING)) {
                        logger.debug("[STATUS] Wrong node: " + rootNodeId + ", Expected: " + Workflow.PROCESSING + ", Get: " + status);
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(rootDomainId, rootNodeId, DConst.WF_STATUS));
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(DConst.WF_ID, wfIDNode.getId(), DConst.WF_STATUS));
//                        toFixDeleteCommands.add(new BDBWorkerCommandDelete(DConst.WF_STATUS, statusNode.getId()));

                        newStatusNodeId = worker.addNode(DConst.WF_STATUS, Workflow.PROCESSING, true);
                        toFixLinkCommands.add(new BDBWorkerCommandLink(rootDomainId, rootNodeId, DConst.WF_STATUS, newStatusNodeId));
                        toFixLinkCommands.add(new BDBWorkerCommandLink(DConst.WF_ID, wfIDNode.getId(), DConst.WF_STATUS, newStatusNodeId));

                        errorRootNodeIds.add(rootNodeId);
                    }
                    else if (isReject && !status.equals(Workflow.REJECT)) {
                        logger.debug("[STATUS] Wrong node: " + rootNodeId + ", Expected: " + Workflow.REJECT + ", Get: " + status);
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(rootDomainId, rootNodeId, DConst.WF_STATUS));
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(DConst.WF_ID, wfIDNode.getId(), DConst.WF_STATUS));
//                        toFixDeleteCommands.add(new BDBWorkerCommandDelete(DConst.WF_STATUS, statusNode.getId()));

                        newStatusNodeId = worker.addNode(DConst.WF_STATUS, Workflow.REJECT, true);
                        toFixLinkCommands.add(new BDBWorkerCommandLink(rootDomainId, rootNodeId, DConst.WF_STATUS, newStatusNodeId));
                        toFixLinkCommands.add(new BDBWorkerCommandLink(DConst.WF_ID, wfIDNode.getId(), DConst.WF_STATUS, newStatusNodeId));

                        errorRootNodeIds.add(rootNodeId);
                    }
                    else if (isAccept && !status.equals(Workflow.FINISH)) {
                        logger.debug("[STATUS] Wrong node: " + rootNodeId + ", Expected: " + Workflow.FINISH + ", Get: " + status);
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(rootDomainId, rootNodeId, DConst.WF_STATUS));
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(DConst.WF_ID, wfIDNode.getId(), DConst.WF_STATUS));
//                        toFixDeleteCommands.add(new BDBWorkerCommandDelete(DConst.WF_STATUS, statusNode.getId()));

                        newStatusNodeId = worker.addNode(DConst.WF_STATUS, Workflow.FINISH, true);
                        toFixLinkCommands.add(new BDBWorkerCommandLink(rootDomainId, rootNodeId, DConst.WF_STATUS, newStatusNodeId));
                        toFixLinkCommands.add(new BDBWorkerCommandLink(DConst.WF_ID, wfIDNode.getId(), DConst.WF_STATUS, newStatusNodeId));

                        errorRootNodeIds.add(rootNodeId);
                    }

                    //reponse
                    if (isProcessing && !resp.equals(Workflow.PROCESSING)) {
                        logger.debug("[RESPONSE] Wrong node: " + rootNodeId + ", Expected: " + Workflow.PROCESSING + ", Get: " + resp);
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(DConst.WF_ID, wfIDNode.getId(), DConst.WF_RESPONSE));
//                        toFixDeleteCommands.add(new BDBWorkerCommandDelete(DConst.WF_RESPONSE, respNode.getId()));
                        toFixLinkCommands.add(new BDBWorkerCommandLink(DConst.WF_ID, wfIDNode.getId(), DConst.WF_RESPONSE, worker.addNode(DConst.WF_RESPONSE, Workflow.PROCESSING, true)));

                        errorRootNodeIds.add(rootNodeId);
                    }
                    else if (isReject && !resp.equals(Workflow.REJECT)) {
                        logger.debug("[RESPONSE] Wrong node: " + rootNodeId + ", Expected: " + Workflow.REJECT + ", Get: " + resp);
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(DConst.WF_ID, wfIDNode.getId(), DConst.WF_RESPONSE));
//                        toFixDeleteCommands.add(new BDBWorkerCommandDelete(DConst.WF_RESPONSE, respNode.getId()));
                        toFixLinkCommands.add(new BDBWorkerCommandLink(DConst.WF_ID, wfIDNode.getId(), DConst.WF_RESPONSE, worker.addNode(DConst.WF_RESPONSE, Workflow.REJECT, true)));

                        errorRootNodeIds.add(rootNodeId);
                    }
                    else if (isAccept && !resp.equals(Workflow.ACCEPT)) {
                        logger.debug("[RESPONSE] Wrong node: " + rootNodeId + ", Expected: " + Workflow.ACCEPT + ", Get: " + resp);
                        toFixUnlinkAllCommands.add(new BDBWorkerCommandUnlinkAll(DConst.WF_ID, wfIDNode.getId(), DConst.WF_RESPONSE));
//                        toFixDeleteCommands.add(new BDBWorkerCommandDelete(DConst.WF_RESPONSE, respNode.getId()));
                        toFixLinkCommands.add(new BDBWorkerCommandLink(DConst.WF_ID, wfIDNode.getId(), DConst.WF_RESPONSE, worker.addNode(DConst.WF_RESPONSE, Workflow.ACCEPT, true)));

                        errorRootNodeIds.add(rootNodeId);
                    }
                }
                q.goback();
            }
        } finally {
            worker.commit();
        }

        out.println(String.format("總共有 %s 筆錯誤的資料<br>", errorRootNodeIds.size()));

        commitAllCommands(apname, allCommands);
//        out.println(errorRootNodeIds.stream().map(String::valueOf).collect(Collectors.joining("<br>")));

        out.println("<br><br>");
        out.println("done !");
        return;
    }

      out.write("<div id=\"result\"></div>\r\n\r\n<script>\r\n    function $(id) {\r\n        return document.getElementById(id);\r\n    }\r\n\r\n    function fixApprovalStatusAndResponse(){\r\n        $(\"startApprovalFix\").classList.remove(\"hidden\");\r\n        let url = $(\"url\").value;\r\n        let action = $(\"action\").value;\r\n        postPromise('/sims/dev/approvalDBNodeFix.jsp', {url, action}).then(function (res) {\r\n            $(\"result\").innerHTML = res;\r\n        });\r\n    }\r\n\r\n    function postPromise(url, data) {\r\n        // Turn the data object into an array of URL-encoded key/value pairs.\r\n        data = data || {};\r\n        let urlEncodedDataPairs = Object.entries(data).reduce(function (accumulator, pair) {\r\n            const key = pair[0], value = pair[1];\r\n            if (Array.isArray(value)) {\r\n                return accumulator.concat(value.map(function (subValue) {\r\n                    return encodeURIComponent(key) + '=' + encodeURIComponent(subValue);\r\n                }));\r\n            } else {\r\n                accumulator.push(encodeURIComponent(key) + '=' + encodeURIComponent(value));\r\n");
      out.write("                return accumulator;\r\n            }\r\n        }, []);\r\n\r\n        // x-www-form-urlencoded issue.\r\n        // Combine the pairs into a single string and replace all %-encoded spaces to\r\n        // the '+' character; matches the behaviour of browser form submissions.\r\n        let urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');\r\n\r\n        return createPromiseRequest({\r\n            \"method\": \"POST\",\r\n            \"url\": url,\r\n            \"headers\": {'Content-Type': 'application/x-www-form-urlencoded'},\r\n            \"data\": urlEncodedData\r\n        });\r\n    }\r\n\r\n    function createPromiseRequest(options) {\r\n        return new Promise(function (resolve, reject) {\r\n            let xhr = new XMLHttpRequest();\r\n            xhr.open(options.method, options.url);\r\n            xhr.onload = function () {\r\n                if (this.status >= 200 && this.status < 300) {\r\n                    resolve(xhr.response);\r\n                } else {\r\n                    reject({\r\n                        status: this.status,\r\n");
      out.write("                        statusText: xhr.statusText,\r\n                        response: xhr.response\r\n                    });\r\n                }\r\n            };\r\n            xhr.onerror = function () {\r\n                reject({\r\n                    status: this.status,\r\n                    statusText: xhr.statusText\r\n                });\r\n            };\r\n\r\n            if (options.hasOwnProperty(\"headers\")) {\r\n                Object.keys(options.headers).forEach(function (key) {\r\n                    xhr.setRequestHeader(key, options.headers[key]);\r\n                });\r\n            }\r\n\r\n            if (options.hasOwnProperty(\"responseType\")) {\r\n                xhr.responseType = options.responseType;\r\n            }\r\n\r\n            let data = options.data || null;\r\n            xhr.send(data);\r\n        });\r\n    }\r\n</script>");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
