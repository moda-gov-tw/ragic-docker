/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.webview.themes;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.output.HTMLWriter;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.util.DConst;
import com.ragic.s3.util.DES;
import java.util.*;
import com.ragic.s3.service.api.APIResultsFull;
import com.ragic.s3.nui.*;
import com.ragic.s3.util.Const;

public final class table_005fview_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


    private String getToken(User user, HttpSession session) {
        return Encoder.encodeURIComponent(new DES().encrypt(user.getUserId() + "|" + session.getId()));
    }
    private String cellValueProcess(CellInfo info, String cellValue) {
        boolean urlMatch =  cellValue.matches("(?:https?):(?:\\/{1,3}|\\\\{1})[-a-zA-Z0-9:;,@#!%&()~_?\\+=\\/\\\\\\.]*");
        boolean isUrl = !cellValue.isEmpty() && (info.getAttribute("url") != null || urlMatch);
        if (isUrl) {
            if (!cellValue.startsWith("http://") && !cellValue.startsWith("https://") && !cellValue.startsWith("//")) cellValue = "https://" + cellValue;
            return "<a target=\"_blank\" rel=\"noopener noreferrer nofollow\" href=\"" + cellValue + "\" onmousedown=\"cancelEventBubble(event);\">" + cellValue + "</a>";
        } else if (info.getAttribute("addr") != null) {
            return "<a target=\"_blank\" rel=\"noopener noreferrer nofollow\" href=\"//maps.google.com/maps?q=" + Encoder.encodeURIComponent(cellValue.replaceAll("'", "%27")) + "\" onmousedown=\"cancelEventBubble(event);\">" + cellValue + "</a>";
        } else if (info.getAttribute("phone") != null) {
            return "<a href=\"tel:" + cellValue + "\" onmousedown=\"cancelEventBubble(event);\">" + cellValue + "</a>";
        } else if (info.getAttribute("sig") != null) {
            return "<img class=\"wvpic\" alt=\"no signature\" src=\"" + cellValue + "\">";
        } else if (info.getAttribute("email") != null) {
            return cellValue.replaceAll("([A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,})+(\\\\?[^\\s><]+)?", "<a href=\"mailto:$1\" onmousedown=\"cancelEventBubble(event);\">$1</a>$2");
        } else if (info.getAttribute("checkbox") != null) {
            return HTMLWriter.checkboxSymbolRenderNoFont(cellValue);
        } else if (info.isMultiFilesUpload()) {
            return "<i class=\"fas fa-clone multiIcon\"></i>";
        } else {
            return cellValue;
        }
    }

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.output.HTMLWriter");
    _jspx_imports_classes.add("com.ragic.s3.util.DES");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResultsFull");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    APIQuery aq = (APIQuery) request.getAttribute("WV_APIQUERY");
    User user = aq.getUser();
    NUIForm form = (NUIForm) request.getAttribute("WV_NUIFORM");
    Boolean webConfigObj = (Boolean) request.getAttribute("WV_CONFIG");
    boolean isExactSearchMode = (boolean) request.getAttribute("WV_EXACTSEARCHMODE");
    boolean isAllDomainChecked = (boolean) request.getAttribute("WV_ALLDOMAINCHECKED");
    boolean webConfig = webConfigObj != null && webConfigObj;
    boolean isDefaultFilter = request.getParameter("defaultView") != null;
    RequestParam rp = (RequestParam) request.getAttribute("REQ");
    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    List<String> passByToken = new ArrayList<>();
    String feature = request.getParameter("feat");
    if (!("view".equals(feature) || "query".equals(feature) || "twoRow".equals(feature) || "fts".equals(feature))) feature = "view";
    String hideSlogan = request.getParameter("hideSlogan") != null ? "&hideSlogan" : "";
    String pageSize = form.getWebviewPageSize();
    try {
        int transfer = Integer.parseInt(pageSize);
        if (transfer <= 0) throw new Exception();
    } catch (Exception e) {
        request.getRequestDispatcher("/sims/webview/saveConfig.jsp?a=" + form.getApname() + "&p=" + form.getPath() + "&si=" + form.getSheetIndex() + "&wvname=pagesize&v=" + "50").include(request, response);
        pageSize = form.getWebviewPageSize();
    }
    //get the listing nuiform
    NUIForm listingForm = AppManager.getListingPage(rp.getApname(), rp.getPath(), rp.getSheet().getFormName());
    String[] widthSplit = listingForm.getWidthString().orElse("W,,").split(",", -1);
    HashMap<Integer, Integer> widthMap = new HashMap<>();
    if (!widthSplit[1].isEmpty()) {
        String[] widths = widthSplit[1].split("&");
        for (String width : widths) {
            String[] pair = width.split("=");
            widthMap.put(Encoder.parseInt(pair[0], -1), Math.round(Float.parseFloat(pair[1])));
        }
    }
    if (isDefaultFilter && listingForm.getDefaultFilter() != null) {
        List<NUIFilter> defaultFilter = listingForm.getDefaultFilter().getFilters();
        for (NUIFilter f : defaultFilter) {
            aq.addFilter(f);
        }
    }
    //get the sort params
    int oDomain = 0;
    int oDir = Const.NODIR;
    if (request.getParameter("oDomain") != null) {
        oDomain = Encoder.parseInt(request.getParameter("oDomain"));
    }
    if (request.getParameter("oDir") != null) {
        oDir = Encoder.parseInt(request.getParameter("oDir"));
    }
    aq.setListingOnly(true);
    if( oDir != Const.NODIR )
        aq.addFilter(oDomain,oDir==Const.SORTED?NUIFilter.OPERAND_SORT_ASC:NUIFilter.OPERAND_SORT_DESC,"x");
    request.setAttribute("aq", aq);
    request.setAttribute("listingForm", listingForm);
    request.setAttribute("showFilter", !("query".equals(feature) || "fts".equals(feature) || "twoRow".equals(feature)));

    //get paging
    String pagingString = request.getParameter("page");
    if (pagingString == null) {
        pagingString = (String) session.getAttribute("EMBEDPAGING" + rp.getApname() + rp.getPath() + "/" + rp.getSheetIndex());
    } else {
        session.setAttribute("EMBEDPAGING" + rp.getApname() + rp.getPath() + "/" + rp.getSheetIndex(), pagingString);
    }

    //do paging
    int paging = Encoder.parseInt(pagingString, 0);
    int CONFIG_PAGE_SIZE = 3;
    int PAGE_SIZE = Integer.parseInt(pageSize);
    int NUM_OF_PAGES = 10;

    aq.setLimitFrom((webConfig ? CONFIG_PAGE_SIZE : PAGE_SIZE) * paging);
    if (isExactSearchMode && !isAllDomainChecked) {
        aq.setLimitSize(0);
    } else {
        aq.setLimitSize((webConfig ? CONFIG_PAGE_SIZE : PAGE_SIZE) * (NUM_OF_PAGES));//todo better paging or just async cache print the results
    }
    StringBuilder fv2qs = new StringBuilder();
    List<Integer> domainIdArray = new ArrayList<>();
    boolean showFieldSummary = request.getQueryString().contains("fieldSummary");

      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "viewSelector.jsp", out, false);
      out.write("<main class=\"table-layout\">\r\n    <table class=\"wvtable\">\r\n        <thead>\r\n        <tr>\r\n            ");

                //print the header first
                for (CellInfo info : listingForm.getFields().values()) {
                    if (Encoder.parseBoolean(info.getAttribute("hide"))) continue;
                    domainIdArray.add(info.getDomainId());
            
      out.write("<th class=\"cell\" onclick=\"changeOrder(");
      out.print(info.getDomainId());
      out.write(',');
      out.print(oDir);
      out.write(");\">\r\n                <div class=\"field-header\">\r\n                    <span>");
      out.print(Encoder.contentEncode(Encoder.contentDecode(info.getValue())));
      out.write("</span>\r\n                    ");

                        if (oDomain == info.getDomainId() && oDir > 0) {
                        String pointing = oDir == 1 ? "\u25b2" : "\u25bc";
                    
      out.write("<span>");
      out.print(pointing);
      out.write("</span>\r\n                    ");

                        }
                    
      out.write("</div>\r\n            </th>\r\n            ");

                }
            
      out.write("</tr>\r\n        </thead>\r\n        <tbody>\r\n        ");

            APIResultsFull results = aq.getAPIResultsFull();
            int count = 0;
            int selectedSize = (webConfig ? CONFIG_PAGE_SIZE : PAGE_SIZE);
            String token = getToken(user, session);
            for (NUIFilter filter : aq.getFilters()) {
                for (String fv2String : filter.getFilterV2String()) {
                    fv2qs.append("&fv2=").append(fv2String);
                }
            }
            for (APIEntry ae : results) {
                count++;
                if (count > selectedSize) continue;
                passByToken.add("/" + rp.getApname() + rp.getPath() + "/" + rp.getSheetIndex() + "/" + ae.getRootNodeId());
                String url = "/" + rp.getApname() + rp.getPath() + "/" + rp.getSheetIndex() + "/" + ae.getRootNodeId() + ".xhtml?wv=" + feature + hideSlogan + "&sid=" + session.getId() + fv2qs + (isExactSearchMode ? "&token=" + token : "");
        
      out.write("<tr class=\"wvtablerow\" onmousedown=\"location.href = '");
      out.print(url);
      out.write("';\">\r\n            ");

                int index = 0;
                for (CellInfo info : listingForm.getFields().values()) {
                    if (Encoder.parseBoolean(info.getAttribute("hide"))) continue;
                    boolean noLineBreak = "true".equals(info.getAttribute("nowrap"));
                    String nowrapStyle = noLineBreak ? "white-space:nowrap;" : "";
                    String widthStyle = widthMap.containsKey(++index) ? "width:" + (100 + widthMap.get(index)) + "px" : "";
                    String value = ae.getFieldValueFormatted(info.getDomainId(),bundle);
                    switch (info.getDomainId()) {
                        case DConst.WF_STATUS:
                        case DConst.WF_NEXT_SIGNER:
                            value = com.ragic.s3.util.Util.sysOptLocalization(info.getDomainId(), value, bundle, rp.getApname());
                            break;
                    }
                    value = Encoder.processBBCode(value);
                    String cellValue = cellValueProcess(info, value);
            
      out.write("<td class=\"cell\">\r\n                <span class=\"field-value wvanchor\" style=\"");
      out.print(nowrapStyle);
      out.print(widthStyle);
      out.write('"');
      out.write('>');
      out.print(cellValue);
      out.write("</span>\r\n            </td>\r\n            ");

                }
            
      out.write("</tr>\r\n        ");

            }
            if (count == 0) {
        
      out.write("<td style=\"text-align:center;\" ");
      out.print("colspan=\"" + listingForm.getFields().size() + "\"");
      out.write(">\r\n            <span>");
      out.print(bundle.getString("noMatchedRecord"));
      out.write("</span>\r\n        </td>\r\n        ");

            }
            request.setAttribute("WV_PAGING", paging);
            request.setAttribute("WV_COUNT", count);
            request.setAttribute("WV_PAGESIZE", PAGE_SIZE);
            if (isExactSearchMode) {
                Map<String, List<String>> tokenMap = new HashMap<>();
                tokenMap.put(token, passByToken);
                session.setAttribute("tokenMap", tokenMap);
            }
            if (webConfig) request.setAttribute("WV_CONFIGPAGESIZE", CONFIG_PAGE_SIZE);
            if (showFieldSummary) {
        
      out.write("<tr class=\"wvtablerow\">\r\n            <td>\r\n                <a href=\"javascript:void(0);\" style=\"font-size:10px;\" onclick=\"doListingPageSummary(this.parentNode);\">\r\n                    <i class=\"fa fa-calculator\"></i>\r\n                    <span style=\"color:#aaaaaa;\">");
      out.print(bundle.getString("calculateAllRecords"));
      out.write("</span>\r\n                </a>\r\n            </td>\r\n        </tr>\r\n        ");

            }
        
      out.write("</tbody>\r\n    </table>\r\n</main>\r\n<script>\r\n    let domainIds = ");
      out.print(domainIdArray);
      out.write(";\r\n    function changeOrder(oDomain, oDir) {\r\n        const queryString = location.search; //e.g. '?webview&webaction=view&oDomain=1000004&oDir=1'\r\n        const params = queryString.substring(1).split('&');\r\n\r\n        if (queryString.indexOf(\"oDomain\") !== -1) {\r\n            for (let i = params.length; i--;) {\r\n                const pair = params[i].split('=');\r\n                if (pair[0] === \"oDomain\") params[i] = \"oDomain=\" + oDomain;\r\n                else if (pair[0] === \"oDir\") {\r\n                    if (oDir === 0) {\r\n                        params[i] = \"oDir=1\";\r\n                    } else if (oDir === 1) {\r\n                        params[i] = \"oDir=2\";\r\n                    } else {\r\n                        params[i] = \"oDir=0\";\r\n                    }\r\n                }\r\n            }\r\n            location.href = location.href.replace(queryString, \"?\" + params.join('&'));\r\n        } else {\r\n            location.href = location.href + \"&oDomain=\" + oDomain + \"&oDir=1\";\r\n        }\r\n    }\r\n    function doListingPageSummary(btn) {\r\n");
      out.write("        const tr = btn.parentNode;\r\n        let fv2 = '");
      out.print(fv2qs.toString());
      out.write("';\r\n        btn.innerHTML='<i class=\"fa fa-spinner fa-spin\"></i>';\r\n        const url = '/sims/webview/mod/getFieldSummary.jsp';\r\n        const params = {\r\n            'a': '");
      out.print(rp.getApname());
      out.write("',\r\n            'p': '");
      out.print(rp.getPath());
      out.write("',\r\n            'fn': '");
      out.print(rp.getSheet().getFormName());
      out.write("',\r\n            fv2,\r\n        };\r\n        getPromise(url, params).then(function (response) {\r\n            rm(tr);\r\n            const jo = JSON.parse(response);\r\n            if (!jo) return;\r\n            domainIds.forEach(function (domainId) {\r\n                const td = document.createElement('td');\r\n                td.className = 'totalCell';\r\n                const fmt = jo && jo[domainId] && jo[domainId].format ? unformatNUIAttr(jo[domainId].format) : '';\r\n                td.textContent = jo && jo[domainId] && jo[domainId].total ? ragic_number_format(jo[domainId].total, fmt) : '-';\r\n                tr.appendChild(td);\r\n            });\r\n        });\r\n    }\r\n</script>");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
