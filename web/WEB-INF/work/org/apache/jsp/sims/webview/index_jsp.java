/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.webview;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.Util;
import java.net.URLEncoder;
import java.util.Map;
import com.ragic.s3.util.License;

public final class index_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  static {
    _jspx_dependants = new java.util.HashMap<java.lang.String,java.lang.Long>(1);
    _jspx_dependants.put("/sims/webview/../googleAnalytics.jsp", Long.valueOf(1668044495807L));
  }

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.net.URLEncoder");
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.util.License");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;

      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "/api/http/basicAuth.jsp", out, false);

  RequestParam rp = (RequestParam) request.getAttribute("REQ");
  RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  User user=(User)session.getAttribute("USER");
  boolean webconfig=request.getParameter("webconfig")!=null;
  String feature=request.getParameter("feat");
  boolean fromQuery="query".equals(feature);
  boolean fromTwoRow="twoRow".equals(feature);

  String action=request.getParameter("webaction");
  if(user==null) user=new User();
  if(rp == null) return;
  if(rp.getSheet()==null && !"fts".equals(action)){
    request.getRequestDispatcher("noWebviewPage.jsp").forward(request, response);
    return;
  }

  boolean noLogout = false;

  NUIForm form = AppManager.getFormPage(rp.getApname(), rp.getPath(), rp.getSheetIndex());
  if(form==null){
    out.println("The <a href='https://www.ragic.com'>Ragic</a> sheet has been moved to another place or deleted.");
    return;
  }
  boolean isExactSearchMode = form.getExactMode();
  int everyone_right = form.getRight(Const.G_EVERYONE);

  boolean accountExpired;
  if(rp.getAccountPaymentStatus()!=null) accountExpired = rp.getAccountPaymentStatus().ifExpired();
  else{
    Account ap=Account.getInstance(rp.getApname());
    rp.setAccountPaymentStatus(ap.getPaymentStatus(false));
    accountExpired = rp.getAccountPaymentStatus().ifExpired();
  }
  if(accountExpired){
    out.println(bundle.getString("expired1")+"<a style='text-decoration:none;' href='https://www.ragic.com/"+rp.getApname()+"/admin'> "+bundle.getString("expired2")+"</a> "+bundle.getString("expired3"));
    return;
  }
  Util.enableCookieAttached(response);

  //query state
  String state=(String)session.getAttribute("EMBEDSTATE"+rp.getApname()+rp.getPath()+"/"+rp.getSheetIndex());
  if(state==null) state="query";

  //changing query state
  if("form".equals(action)){
    state="form";
    if (rp.getUserRight() < Const.USER || rp.getUserRight() == Const.VIEWER || form.ifUserNoCreate(user)) {
      // issue[12836]:權限不足則無法讀取頁面
      if (form.ifUserNoCreate(user)) {
        request.setAttribute("INVALID_REQ", bundle.getString("noAccessCreate") + "<br/>" + String.format(bundle.getString("askSignout"), "url=" + URLEncoder.encode("/" + rp.getApname() + rp.getPath() + "/" + rp.getSheetIndex() + "?" + request.getQueryString(), "UTF-8")));
      }

      //[BugId:13655] 內嵌表單如果沒有開放權限給任何一個群組時 未登入的情況下點開的頁面調整成僅顯示：你沒有權限存取這張表單
      boolean formNoRightsAtAll=true;
      for(Map.Entry<String,Integer> e:form.getRights().entrySet()){
        String group=e.getKey();
        if(group!=null && (group.equalsIgnoreCase(Const.G_MANAGER) || group.equalsIgnoreCase(Const.G_ADMIN))) continue;
        Integer right=e.getValue();
        if (right != null && right.intValue() > 0) {
          formNoRightsAtAll = false;
          break;
        }
      }

      if(user.isLoginComplete()){
        state="noRight";
      }
      else if(formNoRightsAtAll){
        request.setAttribute("INVALID_REQ", bundle.getString("noAccessCreate"));
        state="noRight";
      }
      else{
        state="login";
      }
    }
  }
  else if("query".equals(action)){
    state="query";
    if(rp.getUserRight()< Const.USER && !user.isLoginComplete() && !isExactSearchMode) state="login";
  }
  else if("view".equals(action)){
    state="view";
    if(rp.getUserRight()< Const.VIEWER && !user.isLoginComplete() && !(fromQuery || fromTwoRow)) state="login";
    if("fts".equals(feature))  {
      state="view";
      noLogout=true;
    }
  }
  else if("viewDetail".equals(action)){
    state="viewDetail";
    if(rp.getUserRight()< Const.VIEWER && !user.isLoginComplete()) state="login";
  }
  else if("fts".equals(action)){
    state="fts";
  }
  else if("signup".equals(action)){
    state="signup";
  }
  else if("twoRow".equals(action)) {
    state="twoRow";
    if(rp.getUserRight()< Const.VIEWER && !user.isLoginComplete() && !isExactSearchMode) state="login";
  }
  else if("login".equals(action)){
    state="login";
  }

  session.setAttribute("EMBEDSTATE"+rp.getApname()+rp.getPath()+"/"+rp.getSheetIndex(),state);

//  System.out.println(state);

  if("form".equals(state)){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/form.jsp", out, false);

  }
  else if("query".equals(state)){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/database.jsp", out, false);

  }
  else if("view".equals(state)){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/database.jsp", out, false);

  }
  else if("viewDetail".equals(state)){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/viewDetail.jsp", out, false);

  }
  else if("fts".equals(state)){
    
      if (true) {
        _jspx_page_context.forward("mod/fts.jsp");
        return;
      }

  }
  else if("twoRow".equals(state)){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/database.jsp", out, false);

  }
  else if("login".equals(state)){

      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "/sims/reg/login.jsp?embed" + "&" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("a", request.getCharacterEncoding())+ "=" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode(String.valueOf(rp.getApname()), request.getCharacterEncoding()) + "&" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("url", request.getCharacterEncoding())+ "=" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode(String.valueOf("/"+rp.getApname()+"/"+rp.getFullpath()+"?"+request.getQueryString()), request.getCharacterEncoding()), out, false);

  }
  else if("signup".equals(state)){
    String redirectUrl=request.getParameter("url");
    if(redirectUrl!=null) request.setAttribute("REDIRECT_TO",Util.getServerProtocol()+"://"+Util.getServerNameWithPort()+redirectUrl);
    Sheet sheet= AppManager.getSheetBySheetIndex(rp.getApname(), "/ragic-setup", 6);
    rp.setPath("/ragic-setup");
    rp.setSheet(sheet);
    rp.setSheetIndex(6);
    if(sheet!=null){
      
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/form.jsp", out, false);

    }
  } else if ("noRight".equals(state)) {
  
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "mod/noRight.jsp", out, false);

  }

      out.write("<script>\r\n  function postSize(event){\r\n    var target = parent.postMessage ? parent : (parent.document.postMessage ? parent.document : undefined);\r\n    var isMSIE = /*@cc_on!@*/0;\r\n    var isFireFox = navigator.userAgent.toLowerCase().indexOf('firefox') > -1;\r\n    document.body.style.height = 'auto';\r\n    console.log(event.type, document.body.scrollHeight);\r\n    if (typeof target != \"undefined\" && document.body.scrollHeight){\r\n      //add 20 more pixels to prevent that width overflow generate scroll and influence the height displayed\r\n      if(isMSIE) target.postMessage((document.documentElement ? Math.max(document.body.scrollHeight, document.documentElement.scrollHeight) : document.body.scrollHeight)+20, \"*\");\r\n      else if(isFireFox) target.postMessage(Math.max(document.body.scrollHeight, document.documentElement.scrollHeight)+70, \"*\");\r\n      else target.postMessage(Math.max(document.body.scrollHeight, document.documentElement.scrollHeight)+50, \"*\");\r\n    }\r\n  }\r\n  function postContentChanged(){\r\n    var target = parent.postMessage ? parent : (parent.document.postMessage ? parent.document : undefined);\r\n");
      out.write("    var isMSIE = /*@cc_on!@*/0;\r\n    if (typeof target != \"undefined\" && document.body.scrollHeight){\r\n      //add 20 more pixels to prevent that width overflow generate scroll and influence the height displayed\r\n      if(isMSIE) target.postMessage(-1, \"*\");\r\n      else target.postMessage(-1, \"*\");\r\n    }\r\n  }\r\n  window.addEventListener(\"load\", postSize, false);\r\n  window.addEventListener(\"unload\", postContentChanged, false);\r\n  window.addEventListener('resize', postSize, {once: true}); //issue[15766]: 針對容器一開始被隱藏導致無法計算高度\r\n</script>\r\n<div id=\"loginStatus\">\r\n  ");
 //issue #09252
    if (form.getLoginStatus() && !"login".equals(state) && user.isLoginComplete() && everyone_right == 0 && !noLogout) {
      String logoutUrl = "/sims/reg/logout.jsp?&t="+User.getCSRFToken(session)+"&url=" + URLEncoder.encode("/" + rp.getApname() + rp.getPath() + "/" + rp.getSheetIndex() + "?" + request.getQueryString()+"&t="+User.getCSRFToken(session), "UTF-8");
  
      out.write("<a title=\"");
      out.print(bundle.getString("signout"));
      out.print(user.getUserId());
      out.write("\" href=\"");
      out.print(logoutUrl);
      out.write('"');
      out.write('>');
      out.print(bundle.getString("signout"));
      out.write("</a>\r\n  ");
}
      out.write("</div>\r\n");

  //https://www.ragic.com/tickets/ragic/1/93495 確認是否為ga載入過久的問題
  if (!request.getQueryString().contains("Rtest")) {


  if(!License.isDev() && !License.isDistr()){

      out.write("<script type=\"text/javascript\">\r\n  var _gaq = _gaq || [];\r\n  _gaq.push(['_setAccount', 'UA-494873-6']);\r\n  _gaq.push (['_gat._anonymizeIp']);\r\n  _gaq.push(['_trackPageview']);\r\n  (function() {\r\n    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;\r\n    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';\r\n    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);\r\n  })();\r\n</script>\r\n\r\n\r\n\r\n<!-- New Google Analytics 4 -->\r\n<!-- Google tag (gtag.js) -->\r\n<script async src=\"https://www.googletagmanager.com/gtag/js?id=G-L91LYMGF8W\"></script>\r\n<script>\r\n  window.dataLayer = window.dataLayer || [];\r\n  function gtag(){dataLayer.push(arguments);}\r\n  gtag('js', new Date());\r\n\r\n  gtag('config', 'G-L91LYMGF8W');\r\n</script>\r\n\r\n\r\n\r\n");

  String __ourl=(String)request.getAttribute("ORIGINAL_URI");
  if(__ourl!=null && __ourl.contains("/intl/")) {

      out.write("<script>(function(w,d,t,r,u){var f,n,i;w[u]=w[u]||[],f=function(){var o={ti:\"25033197\"};o.q=w[u],w[u]=new UET(o),w[u].push(\"pageLoad\")},n=d.createElement(t),n.src=r,n.async=1,n.onload=n.onreadystatechange=function(){var s=this.readyState;s&&s!==\"loaded\"&&s!==\"complete\"||(f(),n.onload=n.onreadystatechange=null)},i=d.getElementsByTagName(t)[0],i.parentNode.insertBefore(n,i)})(window,document,\"script\",\"//bat.bing.com/bat.js\",\"uetq\");</script>\r\n");

  }

}
}
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
