/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.webview.mod;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.service.api.APIQuery;
import java.util.Map;
import java.util.HashMap;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import com.ragic.s3.nui.helper.AppManager;

public final class view_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.util.HashMap");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    User user = (User) session.getAttribute("USER");
    RequestParam rp = (RequestParam) request.getAttribute("REQ");
    if (rp == null) return;
    String ap = rp.getApname();
    String path = rp.getPath();
    String formName = rp.getSheet().getFormName();
    int sheetIndex = rp.getSheetIndex();
    NUIForm formPage = AppManager.getFormPage(ap, path, formName);
    boolean webconfig = request.getParameter("webconfig") != null;
    String feature = request.getParameter("feat");
    boolean fromQuery = "query".equals(feature);
    boolean fromTwoRow = "twoRow".equals(feature);
    boolean isExactSearchMode = (fromQuery || fromTwoRow) && formPage.getExactMode();
    String fts = request.getParameter("q");
    String[] filterv2 = request.getParameterValues("fv2");
    APIQuery aq = new APIQuery(ap, path, sheetIndex);
    aq.setIfEidNaming(true);
    aq.setBundle(bundle);

    //if any param remove old ones
    if (fts != null || filterv2 != null) {
        session.removeAttribute("EMBEDFTS" + ap + path + "/" + sheetIndex);
        session.removeAttribute("EMBEDFILTER" + ap + path + "/" + sheetIndex);
        session.removeAttribute("EMBEDPAGING" + ap + path + "/" + sheetIndex);
    }
    //full text search filters
    if (fts == null) {
        fts = (String) session.getAttribute("EMBEDFTS" + ap + path + "/" + sheetIndex);
    }
    if (fts != null && !"".equals(fts)) {
        aq.setFullTextSearch(fts);
        session.setAttribute("EMBEDFTS" + ap + path + "/" + sheetIndex, fts);
    }
    if (filterv2 == null) filterv2 = (String[]) session.getAttribute("EMBEDFILTER" + ap + path + "/" + sheetIndex);

    boolean isAllDomainChecked = filterv2 != null;
    if (filterv2 != null && isExactSearchMode) {
        String displayQueryString = fromQuery ? formPage.getDisplayedQueries() : formPage.getDisplayedQueries(Const.WEBVIEW_TWOROW_DISPLAYEDQUERIES);
        String[] displayQueries = displayQueryString.split("\\|");
        Map<Integer, Integer> checkMap = new HashMap<>();
        for (String displayQuery : displayQueries) {
            int domainId = Encoder.parseInt(displayQuery);
            if (domainId == 0) {
                isAllDomainChecked = false;
                break;
            }

            //count how many filter values for each tyoe of fields
            CellInfo info = formPage.getField(domainId);
            String filterType = info.getFilterType();
            if (Const.FILTER_SELECT.equals(filterType) || Const.FILTER_USER.equals(filterType) || info.getAttribute("im") != null) {
                checkMap.put(domainId, 1);
            } else if (Const.FILTER_NUMBER.equals(filterType)) {
                checkMap.put(domainId, 2);
            } else if (Const.FILTER_DATE.equals(filterType)) {
                checkMap.put(domainId, 2);
            } else if (Const.FILTER_TEXT.equals(filterType)) {
                checkMap.put(domainId, 1);
            } else {
                checkMap.put(domainId, 1);
            }
        }

        //count the field values against the number of filter values needed
        for (String filterString : filterv2) {
            if (filterString.length() == 0) break;
            String domain = filterString.substring(0, filterString.indexOf("|"));
            int domainId = Encoder.parseInt(domain);
            if (checkMap.get(domainId) != null) {
                checkMap.put(domainId, checkMap.get(domainId) - 1);
            }
        }

        //make sure all fields have the correct number of filter values
        for (int value : checkMap.values()) {
            if (value > 0) {
                isAllDomainChecked = false;
                break;
            }
        }
    }

    //issue[11433]
    if (fromQuery || fromTwoRow) aq.setFiltersStringV2(filterv2);
    session.setAttribute("EMBEDFILTER" + ap + path + "/" + sheetIndex, filterv2);

    //user right constraint
    if (isExactSearchMode) {
        if (isAllDomainChecked) {
            aq.setUser(new UserSuper(user.getUserId()));
        } else {
            aq.addFilter(DConst.SYS_DUAL, NUIFilter.SOR_EQN, "" + Const.NULLNODE);
        }
    } else {
        aq.setUser(user);
    }

    //choose a theme
    String webviewTheme = formPage.getWebviewTheme();
    String webviewThemeJSP = "/sims/webview/themes/" + webviewTheme + "_view.jsp";
    request.setAttribute("WV_APIQUERY", aq);
    request.setAttribute("WV_NUIFORM", formPage);
    request.setAttribute("WV_CONFIG", webconfig);
    request.setAttribute("WV_EXACTSEARCHMODE", isExactSearchMode);
    request.setAttribute("WV_ALLDOMAINCHECKED", isAllDomainChecked);

      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, webviewThemeJSP, out, false);

    //paging
    int paging = (Integer)request.getAttribute("WV_PAGING");
    int count = (Integer)request.getAttribute("WV_COUNT");
    int PAGE_SIZE = (Integer)(webconfig ? request.getAttribute("WV_CONFIGPAGESIZE") : request.getAttribute("WV_PAGESIZE"));
    int maxPages = paging + (int)Math.ceil((double)count / PAGE_SIZE);
    int totalPages = paging > 5 ? paging + 5 : 10;
    if (totalPages>maxPages) totalPages = maxPages;
    int startingPage = paging > 5 ? paging - 5 : 0;

if (totalPages > 1 && !webconfig) {
      out.write("<div class=\"webviewPaging flex-center\">\r\n    ");

        String hrefString = "?" + request.getQueryString();
        hrefString = hrefString.replaceAll("(&page=[0-9]+)|(&oDomain=[0-9]+)|(&oDir=[0-9])", "");
        StringBuilder appendQueryString = new StringBuilder(); // keep additional query param [bug id:2580]
        if (request.getParameter("oDomain") != null) appendQueryString.append("&oDomain=").append(request.getParameter("oDomain"));
        if (request.getParameter("oDir") != null) appendQueryString.append("&oDir=").append(request.getParameter("oDir"));
    
if (paging > 0){
      out.write("<a href=\"");
      out.print(hrefString + "&page=0" + appendQueryString);
      out.write("\">\r\n        <div class=\"pageNum\">&lt;&lt;</div>\r\n    </a>\r\n    ");
      out.write("<a href=\"");
      out.print(hrefString + "&page=" + (paging - 1) + appendQueryString);
      out.write("\">\r\n        <div class=\"pageNum\">&lt;</div>\r\n    </a>\r\n    ");
}
for (int num = startingPage; num < totalPages; ++num) {
      out.write("<a href=\"");
      out.print(hrefString + "&page=" + num + appendQueryString);
      out.write("\">\r\n        <div class=\"pageNum");
      out.print((paging == num ? " pageNumNow" : ""));
      out.write('"');
      out.write('>');
      out.print(num + 1);
      out.write("</div>\r\n    </a>\r\n    ");
}
if (paging < totalPages - 1) {
      out.write("<a href=\"");
      out.print(hrefString + "&page=" + (paging + 1) + appendQueryString);
      out.write("\">\r\n        <div class=\"pageNum\">&gt;</div>\r\n    </a>\r\n    ");
}
      out.write("</div>\r\n");
} else {
      out.write("<div style=\"margin:15px auto;color:#aaa;text-align:center;\">No entries found.</div>\r\n");
}
if(webconfig) {
      out.write("<style>\r\n    .wvitem:hover {\r\n        background-color: #e1e8f1;\r\n    }\r\n</style>\r\n");
      out.write("<select id=\"ragic_config_selection\" title=\"ragic config selection\" style=\"position:absolute;display:none;\">\r\n    <option value=\"\">-</option>\r\n    ");
for (CellInfo info : formPage.getFields().values()) {
      out.write("<option value=\"");
      out.print(info.getDomainId());
      out.write('"');
      out.write('>');
      out.print(Encoder.encjs(info.getValue()));
      out.write("</option>\r\n    ");
}
      out.write("</select>\r\n<script>\r\n    var target = parent.postMessage ? parent : (parent.document.postMessage ? parent.document : undefined);\r\n    if (typeof target != \"undefined\") {\r\n        target.postMessage('{\"webviewTheme\":\"");
      out.print(webviewTheme);
      out.write("\", \"pageSize\":");
      out.print(request.getAttribute("WV_PAGESIZE"));
      out.write(", \"showComment\":");
      out.print(formPage.getCommentsPermission());
      out.write(", \"showLogin\":");
      out.print(formPage.getLoginStatus());
      out.write("}', \"*\");\r\n    }\r\n    const fieldSelect = document.getElementById('ragic_config_selection');\r\n    document.addEventListener('mouseover', function (event) {\r\n        const target = event.target.closest('.wvitem');\r\n        if (!target) return;\r\n        const domainId = target.getAttribute('domainid');\r\n        const wvname = target.getAttribute('wvname');\r\n        const position = findPos(target);\r\n        fieldSelect.style.top = (position[1] + target.offsetHeight) + 'px';\r\n        fieldSelect.style.left = position[0] + 'px';\r\n        fieldSelect.style.display = '';\r\n        fieldSelect.dataset.wvname = wvname;\r\n        fieldSelect.selectedIndex = Array.from(fieldSelect.options).findIndex(function (option) {\r\n            return option.value === domainId;\r\n        });\r\n    });\r\n    fieldSelect.addEventListener('change', function () {\r\n        console.log(fieldSelect.value, fieldSelect.dataset.wvname);\r\n        const url = '/sims/webview/saveConfig.jsp';\r\n        const params = {\r\n            'a': ap,\r\n            'p': path,\r\n");
      out.write("            'fn': sheet,\r\n            'wvname': fieldSelect.dataset.wvname,\r\n            'v': fieldSelect.value,\r\n        };\r\n        postPromise(url, params).then(function () {\r\n            location.reload();\r\n        });\r\n    });\r\n</script>\r\n");
}
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
