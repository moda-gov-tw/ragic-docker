/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.webview.mod;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.DataHelper;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.Util;
import org.json.JSONObject;
import java.util.*;

public final class queryItems_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.DataHelper");
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    RequestParam rp = (RequestParam) request.getAttribute("REQ");
    String apname = rp.getApname();
    String path = rp.getPath();
    String formName = rp.getSheet().getFormName();
    User user = (User) session.getAttribute("USER");
    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    boolean fromTwoRow = "twoRow".equals(request.getParameter("webaction"));
    boolean more = request.getParameter("more") != null;
    boolean isConfig = request.getParameter("webconfig") != null;
    //retrieve the form
    NUIForm formPage = AppManager.getFormPage(apname, path, formName);
    NUIForm listingPage = AppManager.getListingPage(apname, path, formName);
    if (formPage == null) return;
    formPage.setBundle(bundle);
    Map<CellLocation, CellInfo> formFields = formPage.getListingQueryItems(bundle, user);
    String displayedqueriesString = fromTwoRow ? formPage.getDisplayedQueries(Const.WEBVIEW_TWOROW_DISPLAYEDQUERIES) : formPage.getDisplayedQueries();
    String querySingleSelect = formPage.getQuerySingleSelect();
    ArrayList<String> displayedqueries = new ArrayList<>(Arrays.asList(displayedqueriesString.split("\\|")));
    String[] filterV2 = request.getParameterValues("fv2");
    if (filterV2 == null) filterV2 = new String[0];
    ArrayList<Integer> displayQueryDomainIds = new ArrayList<>();
    Map<String, String> timeTextMap = new HashMap<>();
    timeTextMap.put("$TOMORROW", "tomorrow");
    timeTextMap.put("$TODAY", "today");
    timeTextMap.put("$YESTERDAY", "yesterday");
    timeTextMap.put("$NEXTWEEK", "nextWeek");
    timeTextMap.put("$THISWEEK", "thisWeek");
    timeTextMap.put("$LASTWEEK", "lastWeek");
    timeTextMap.put("$NEXTMONTH", "nextMonth");
    timeTextMap.put("$THISMONTH", "thisMonth");
    timeTextMap.put("$LASTMONTH", "lastMonth");
    timeTextMap.put("$NEXTQUARTER", "nextQuarter");
    timeTextMap.put("$THISQUARTER", "thisQuarter");
    timeTextMap.put("$LASTQUARTER", "lastQuarter");
    timeTextMap.put("$NEXTYEAR", "nextYear");
    timeTextMap.put("$THISYEAR", "thisYear");
    timeTextMap.put("$LASTYEAR", "lastYear");
    timeTextMap.put("$LASTYEARTODATE", "lastYearToDate");
    timeTextMap.put("$YEARTODATE", "yearToDate");
    timeTextMap.put("$QUARTERTODATE", "quarterToDate");
    timeTextMap.put("$MONTHTODATE", "monthToDate");
    timeTextMap.put("$NEXT7", "next7");
    timeTextMap.put("$NEXT30", "next30");
    timeTextMap.put("$NEXT90", "next90");
    timeTextMap.put("$NEXT365", "next365");
    timeTextMap.put("$NEXT-X", "next-X");
    timeTextMap.put("$NEXTMIN-X", "nextMin-X");
    timeTextMap.put("$PAST7", "past7");
    timeTextMap.put("$PAST30", "past30");
    timeTextMap.put("$PAST90", "past90");
    timeTextMap.put("$PAST365", "past365");
    timeTextMap.put("$PAST-X", "past-X");
    for (String displayedDomainId : displayedqueries) {
        int domainId = Encoder.parseInt(displayedDomainId, -1);
        if (domainId == -1) continue;
        displayQueryDomainIds.add(domainId);
    }
    if (more && formFields.size() > displayedqueries.size()) {
        for (Map.Entry<CellLocation, CellInfo> e : formFields.entrySet()) {
            CellInfo info = e.getValue();
            int domainId = info.getDomainId();
            if (info.getAttribute("sig") != null) continue;
            if (displayQueryDomainIds.contains(domainId)) continue;
            displayQueryDomainIds.add(domainId);
        }
    }

      out.write("<table id=\"v6QueryTable\">\r\n    <tbody>\r\n");

    for (int domainId : displayQueryDomainIds) {
        CellInfo info = formPage.getField(domainId);
        if (info == null) continue;
        String filterType = info.getFilterType();
        boolean isSysField = info.getAttribute("sysField") != null;
        boolean isExactSearchMode = formPage.getExactMode();
        boolean isQuickSetting = false;
        String quickSettingValue = "";
        String value = Encoder.encjs(listingPage.getField(domainId) != null && !isSysField ? listingPage.getField(domainId).getValue() : info.getValue());
        int subtableKey = formPage.getTableHeaderDomain(domainId);
        if (subtableKey == domainId) subtableKey = Const.NODOMAIN;
        boolean hasInterm = false;
        int intermDomain = Const.NODOMAIN;
        if (subtableKey > 0) {
            hasInterm = true;
            intermDomain = subtableKey;
        } else if (info.getAttribute("interm") != null) {
            hasInterm = true;
            intermDomain = Integer.parseInt(info.getAttribute("interm"));
        }
        for (String filterString : filterV2) {
            if (filterString.startsWith(domainId + "|" + NUIFilter.OPERAND_SS)) {
                isQuickSetting = true;
                String[] filterStringPart = filterString.split("\\|");
                if (filterStringPart.length > 2) quickSettingValue = filterStringPart[2];
                break;
            }
        }

      out.write("<tr>\r\n        <td class=\"v6QueryItemName\">");
      out.print(value);
      out.write("</td>\r\n        <td id=\"v6Query");
      out.print(domainId);
      out.write("\" class=\"v6QueryItemInput\" ");
      out.print(isQuickSetting ? "lock" : "");
      out.write(">\r\n            ");

                if (info.isSelection()) {
                    String idValue = domainId + "|";
                    boolean isUser = Const.FILTER_USER.equals(filterType);
                    boolean isMultiple = !querySingleSelect.contains(domainId + "true");
                    BDBWorker worker = new BDBWorker(apname);
                    worker.setWhoami("queryItems.jsp FILTER_SELECT");
                    Account account = Account.getInstance(apname);
                    HashMap<String, HashSet<String>> cascadedOptionMap = new HashMap<>();
                    try {
                        CellInfo selectionField = formPage.getField(domainId);
                        Selection s;
                        if (selectionField.getAttribute("stf") != null) {
                            s = DataHelper.createOption(domainId, worker, Encoder.parseInt(selectionField.getAttribute("stf")), -1, null, false);
                        } else {
                            s = DataHelper.createOption(domainId, worker);
                        }
                        boolean isCascaded = s.isCascaded();
                        List<SDBNode> options = s.getOptions();
                        boolean useTextComp = false;
                        for (SDBNode node : options) {
                            if (node.getId() < 0) {
                                useTextComp = true;
                                break;
                            }
                        }
                        if (isUser) {
                            options.sort((o1, o2) -> {
                                String raw1 = Encoder.unformatNUIAttr(o1.getValue());
                                String raw2 = Encoder.unformatNUIAttr(o2.getValue());
                                return account.getUserName(raw1).compareToIgnoreCase(account.getUserName(raw2));
                            });
                        }
            
if (isQuickSetting) {
      out.write("<label class=\"v6QueryItem-wrapper quickSettings\">\r\n                <input id=\"");
      out.print(domainId);
      out.write("|9\" class=\"specialSetter\" style=\"display: none;\" name=\"v6QueryItem\" value=\"$USER\" data-type=\"usdropdown\">\r\n                <span class=\"userFilter\">");
      out.print(bundle.getString("loginedUser"));
      out.write("</span>\r\n            </label>\r\n            ");
}
      out.write("<label class=\"v6QueryItem-wrapper\" ");
      out.print(isQuickSetting ? "style=\"display:none;\"" : "");
      out.write(">\r\n                <select id=\"");
      out.print(domainId);
      out.write('"');
      out.write(' ');
      out.print(isQuickSetting ? "class=\"hide\"" : "");
      out.write(" style=\"display:none;\" name=\"v6QueryItem\" title=\"query select\" ");
      out.print(isMultiple ? "multiple" : "");
      out.write(">\r\n                    ");
if (!isMultiple){
      out.write("<option value=\"\"></option>\r\n                    ");
}

                        for (SDBNode node : options) {
                            if (isCascaded) {
                                String nodeValue = Encoder.unformatNUIAttr(node.getValue());
                                int pipeIndex = nodeValue.indexOf("|");
                                String cascadedValue = nodeValue.substring(0, pipeIndex);
                                String realValue = nodeValue.substring(pipeIndex + 1);
                                if (cascadedOptionMap.containsKey(realValue)) {
                                    cascadedOptionMap.get(realValue).add(cascadedValue);
                                    continue;
                                } else {
                                    HashSet<String> set = new HashSet<>();
                                    set.add(cascadedValue);
                                    cascadedOptionMap.put(realValue, set);
                                }
                                node.setValue(realValue);
                            }
                            if (isUser) node.setValue(account.getUserName(node.getValue()));
                            String optionValue = Encoder.sanitizeHTML(Encoder.unformatNUIAttr((Util.sysOptLocalization(domainId, node, bundle, apname))));
                            String searchId;
                            int nodeId = node.getId();
                            boolean selected = false;
                            if (!useTextComp) {
                                for (String filterString : filterV2) {
                                    if (filterString.equals(idValue + NUIFilter.OPERAND_EQN + "|" + node.getId())) {
                                        selected = true;
                                        break;
                                    }
                                }
                                searchId = idValue + NUIFilter.OPERAND_EQN + "|" + (hasInterm ? intermDomain + "|" : "") + nodeId;
                            } else {
                                for (String filterString : filterV2) {
                                    if (filterString.equals(idValue + NUIFilter.OPERAND_EQ + "|" + node.getValue())) {
                                        selected = true;
                                        break;
                                    }
                                }
                                searchId = idValue + NUIFilter.OPERAND_EQ + "|" + (hasInterm ? intermDomain + "|" : "") + Encoder.encodeURIComponent(node.getValue());
                            }
                    
      out.write("<option value=\"");
      out.print(searchId);
      out.write('"');
      out.write(' ');
      out.print(selected ? "selected" : "");
      out.write('>');
      out.print(optionValue);
      out.write("</option>\r\n                    ");

                        }
                    
      out.write("</select>\r\n                ");
if (isCascaded) {
      out.write("<div class=\"cascadeDataPack\" data-domain=\"");
      out.print(s.getDomainId());
      out.write("\" data-parentDomain=\"");
      out.print(s.getCascadedDomainId(formPage));
      out.write("\" data-cascadeMap=\"");
      out.print(Encoder.encjs((new JSONObject(cascadedOptionMap)).toString()));
      out.write("\"></div>\r\n                ");
}
      out.write("</label>\r\n            <div class=\"special-wrapper\">\r\n            ");

                if (isConfig && user.ifApManager(apname, path)) {
                    boolean flag = querySingleSelect.contains(domainId+"true");
            
      out.write("<label for=\"singleSelectSetting");
      out.print(domainId);
      out.write("\">\r\n                    <input id=\"singleSelectSetting");
      out.print(domainId);
      out.write("\" type=\"checkbox\" ");
      out.print(flag ? "checked": "");
      out.write(" onchange=\"setSingleSelect('");
      out.print(domainId);
      out.write('\'');
      out.write(',');
      out.write(' ');
      out.print(flag);
      out.write(")\">\r\n                    <span class=\"text-xs ml-1\">");
      out.print(bundle.getString("wvconfig_setAsSingleSelect"));
      out.write("</span>\r\n                </label>\r\n            ");

                } else {
            
      out.write("<div class=\"justBlock\"></div>\r\n            ");

                }
                if (isUser) {
            
      out.write("<div class=\"specialSetter-btn\">\r\n                    <span onclick=\"showSpecialSettingMenu(event, this.parentElement, 'usdropdown');\">");
      out.print(bundle.getString("quickSettings"));
      out.write("</span>\r\n                </div>\r\n            ");

                }
            
      out.write("</div>\r\n            ");

                    } finally {
                        worker.commit();
                    }
                } else if (info.isNumeric() || info.isDate()) {
                    String lowerBoundFilter = domainId + "|" + NUIFilter.OPERAND_GTE + (hasInterm ? "|" + intermDomain : "");
                    String upperBoundFilter = domainId + "|" + NUIFilter.OPERAND_LTE + (hasInterm ? "|" + intermDomain : "");
                    String lowerBoundValue = "";
                    String upperBoundValue = "";
                    boolean isPercent = info.getAttribute("fmt_n") != null && info.getAttribute("fmt_n").contains("%");
                    String dateFormat = info.getAttribute("fmt_d");
                    for (String filterString : filterV2) {
                        if (filterString.startsWith(lowerBoundFilter)) {
                            lowerBoundValue = filterString.substring(lowerBoundFilter.length() + 1);
                        }
                        if (filterString.startsWith(upperBoundFilter)) {
                            upperBoundValue = filterString.substring(upperBoundFilter.length() + 1);
                        }
                        if (!lowerBoundValue.isEmpty() && !upperBoundValue.isEmpty()) break;
                    }
                    if (isPercent) {
                        lowerBoundValue = !lowerBoundValue.isEmpty() ? String.valueOf(Encoder.parseDouble(lowerBoundValue) * 100) : "";
                        upperBoundValue = !upperBoundValue.isEmpty() ? String.valueOf(Encoder.parseDouble(upperBoundValue) * 100) : "";
                    }
                    String classValue = info.isNumeric() ? "v6QueryNumber" : "v6QueryDatebox";
            
if (isQuickSetting) {
      out.write("<label class=\"v6QueryItem-wrapper quickSettings\">\r\n                ");

                    if (quickSettingValue.contains("-X-")) {
                        String fullText = "";
                        String str1 = "";
                        String str2 = "";
                        String time = "";
                        int number = -1;
                        String[] parts = quickSettingValue.split("-");
                        if (parts.length > 2) number = Encoder.parseInt(parts[2], -1);
                        if (quickSettingValue.startsWith("$NEXT-X")) {
                            fullText = bundle.getString("next-X");
                            time = "$NEXT-X";
                        } else if (quickSettingValue.startsWith("$PAST-X")) {
                            fullText = bundle.getString("past-X");
                            time = "$PAST-X";
                        } else if (quickSettingValue.startsWith("$NEXTMIN-X")) {
                            fullText = bundle.getString("nextMin-X");
                            time = "$NEXTMIN-X";
                        }
                        if (fullText.contains("{0}")) {
                            String[] textParts = fullText.split("\\{0}");
                            str1 = textParts[0];
                            str2 = textParts[1];
                        }
                
      out.write("<span class=\"dateFilter\">");
      out.print(str1);
      out.write("</span>\r\n                <input id=\"");
      out.print(domainId);
      out.write("|9\" class=\"xdateInput specialSetter\" name=\"v6QueryItem\" type=\"number\" data-time=\"");
      out.print(time);
      out.write("\" data-type=\"dsdropdown\" value=\"");
      out.print(number);
      out.write("\">\r\n                <span class=\"dateFilter\">");
      out.print(str2);
      out.write("</span>\r\n                ");
} else {
      out.write("<input id=\"");
      out.print(domainId);
      out.write("|9\" class=\"specialSetter\" name=\"v6QueryItem\" style=\"display: none;\" data-type=\"dsdropdown\" value=\"");
      out.print(quickSettingValue);
      out.write("\">\r\n                <span class=\"dateFilter\">");
      out.print(bundle.getString(timeTextMap.get(quickSettingValue)));
      out.write("</span>\r\n                ");
}
      out.write("</label>\r\n            ");
}
      out.write("<label class=\"v6QueryItem-wrapper\" ");
      out.print(isQuickSetting ? "style=\"display: none;\"" : "");
      out.write(">\r\n                <input id=\"");
      out.print(lowerBoundFilter);
      out.write("\" class=\"");
      out.print(classValue);
      out.print(isQuickSetting ? " hide" : "");
      out.write("\" name=\"v6QueryItem\" type=\"text\" autocomplete=\"off\" value=\"");
      out.print(lowerBoundValue);
      out.write('"');
      out.write(' ');
      out.print(isPercent ? "data-numbertype=\"percent\"" : "");
      out.print(info.isDate() ? "onclick=\"datePickerClick(this, '" + dateFormat + "');\"" : "");
      out.write(">\r\n                ");
if (isPercent) {
      out.write("<span>%</span>");
}
      out.write("<span style=\"width:21px;text-align:center;display:inline-block;\">~</span>\r\n                <input id=\"");
      out.print(upperBoundFilter);
      out.write("\" class=\"");
      out.print(classValue);
      out.print(isQuickSetting ? " hide" : "");
      out.write("\" name=\"v6QueryItem\" type=\"text\" autocomplete=\"off\" value=\"");
      out.print(upperBoundValue);
      out.write('"');
      out.write(' ');
      out.print(isPercent ? "data-numbertype=\"percent\"" : "");
      out.print(info.isDate() ? "onclick=\"datePickerClick(this, '" + dateFormat + "');\"" : "");
      out.write(">\r\n                ");
if (isPercent) {
      out.write("<span>%</span>");
}
      out.write("</label>\r\n            ");
if (info.isDate()){
      out.write("<div class=\"special-wrapper\">\r\n                <div class=\"justBlock\"></div>\r\n                <div class=\"specialSetter-btn\">\r\n                    <span onclick=\"showSpecialSettingMenu(event, this.parentElement, 'dsdropdown');\">");
      out.print(bundle.getString("quickSettings"));
      out.write("</span>\r\n                </div>\r\n            </div>\r\n            ");
}

                } else if (info.isText()) {
                    String LKFilter = domainId + "|" + NUIFilter.OPERAND_LK + (hasInterm ? "|" + intermDomain : "");
                    String EQFilter = domainId + "|" + NUIFilter.OPERAND_EQ + (hasInterm ? "|" + intermDomain : "");
                    String idValue = domainId + (hasInterm ? "|" + intermDomain : "");
                    String textValue = "";
                    boolean exactMatch = false;
                    for (String filterString : filterV2) {
                        if (filterString.startsWith(LKFilter)) {
                            textValue = filterString.substring(LKFilter.length() + 1);
                            break;
                        } else if (filterString.startsWith(EQFilter)) {
                            textValue = filterString.substring(EQFilter.length() + 1);
                            exactMatch = true;
                            break;
                        }
                    }
                    boolean useRegex = !textValue.isEmpty() && !(textValue.startsWith("\\Q") && textValue.endsWith("\\E"));
                    if (!textValue.isEmpty() && !useRegex && textValue.length() >= 4) {
                        textValue = Encoder.encjs(textValue.substring(2, textValue.length() - 2));
                    } else {
                        textValue = Encoder.encjs(textValue);
                    }
            
      out.write("<label class=\"v6QueryItem-wrapper\">\r\n                <input id=\"");
      out.print(LKFilter);
      out.write("\" class=\"v6QueryTextbox\" name=\"v6QueryItem\" type=\"text\" autocomplete=\"off\" value=\"");
      out.print(textValue);
      out.write("\"\r\n                    ");
if (!isExactSearchMode && !formPage.getTextAutoComplete()) {
      out.write("\r\n                        onfocus=\"$('tExactDiv");
      out.print(idValue);
      out.write("').style.display = 'flex';\" onblur=\"hideV6TextQueryOpts('");
      out.print(idValue);
      out.write("');\"\r\n                    ");
}
      out.write("\r\n                >\r\n            </label>\r\n            <div class=\"queryAutoCompleteList\"></div>\r\n            <label id=\"tExactDiv");
      out.print(idValue);
      out.write("\" class=\"v6QueryTextLabel\" style=\"");
      out.print(exactMatch && !isExactSearchMode ? "display: flex;" : "");
      out.write("align-items:center;\">\r\n                <input id=\"tExact");
      out.print(idValue);
      out.write("\" type=\"checkbox\" autocomplete=\"off\" ");
      out.print(isExactSearchMode || exactMatch ? "checked" : "");
      out.write(">\r\n                <span>");
      out.print(bundle.getString("exactMatch"));
      out.write("</span>\r\n            </label>\r\n            ");

                }
            
      out.write("</td>\r\n    </tr>\r\n");

    }

      out.write("</tbody>\r\n</table>\r\n");

    if (isConfig && user.ifApManager(apname, path)) {

      out.write("<script>\r\n    function setSingleSelect(domainId, flag) {\r\n        let querySingleSelect = '");
      out.print(querySingleSelect);
      out.write("';\r\n        if (querySingleSelect.indexOf(domainId) === -1) {\r\n            querySingleSelect += (querySingleSelect.length === 0 ? '' : '|') + domainId + !flag;\r\n        } else {\r\n            querySingleSelect = querySingleSelect.replace(domainId + flag, domainId + !flag);\r\n        }\r\n        updateConfig('querySingleSelect', querySingleSelect);\r\n    }\r\n    function updateConfig(wvName, value) {\r\n        const url = '/sims/webview/saveConfig.jsp';\r\n        const params = {\r\n            'a': ap,\r\n            'p': path,\r\n            'fn': sheet,\r\n            'wvname': wvName,\r\n            'v': value\r\n        };\r\n        postPromise(url, params).then(function () {\r\n            location.reload();\r\n        });\r\n    }\r\n</script>\r\n");

    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
