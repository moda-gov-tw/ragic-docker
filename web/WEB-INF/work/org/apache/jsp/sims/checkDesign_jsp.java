/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.nui.Account;
import com.ragic.s3.nui.CellInfo;
import com.ragic.s3.nui.CellLocation;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.NUIFormSetInfo;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.Log;
import org.json.JSONObject;
import java.io.File;
import java.util.ArrayList;
import java.util.Map;
import java.util.Set;
import java.util.List;

public final class checkDesign_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIFormSetInfo");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellInfo");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("java.util.ArrayList");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellLocation");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html;charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


User user = (User)session.getAttribute("USER");
    RagicBundle bundle = (RagicBundle)session.getAttribute("RAGIC_BUNDLE");
    String apname = request.getParameter("a");
    String path = request.getParameter("p");
    int sheetIndex = Encoder.parseInt(request.getParameter("si"), -1);

    JSONObject jo = new JSONObject();
    if (apname == null || path == null || sheetIndex == -1) {
        jo.put("status", "error");
        out.print(jo);
        return;
    }

    Account account = Account.getInstance(apname);
    NUIForm form = AppManager.getFormPage(apname, path, sheetIndex);
    if (form == null) {
        jo.put("status", "error");
        out.print(jo);
        return;
    }
    Map<CellLocation, CellInfo> fields = form.getFields();
    int rootDomainId = form.getRootDomainId();
    String msg = "";
    boolean formAnyChange = false;
    List<CellInfo> changedCells = new ArrayList<>();
    Log logger = Log.createStandardLog().setFilePath("log/checkDesign.log");
    
    for (Map.Entry<CellLocation, CellInfo> e : fields.entrySet()) {
        CellLocation loc = e.getKey();
        CellInfo info = e.getValue();
        boolean isSingleCell = form.isSingleCell(loc);
        String mvpString = info.getAttribute("mvp");
        int stf = Encoder.parseInt(info.getAttribute("stf"), Const.NODOMAIN);
        String msgOfField = "";
        boolean sourceSheetValid = true, stfExist = true, sourceFieldValid = true;
        boolean listenFieldExist = false;
        
        if (mvpString != null) {
            NUIForm sourceForm = AppManager.getFormPage(apname, mvpString, false);
            if (sourceForm == null) {
                sourceSheetValid = false;
            }
            if (stf == Const.NODOMAIN) {
                stfExist = false;
            }
            else if (stf != Const.NODOMAIN && sourceForm != null) {
                CellInfo sourceField = sourceForm.getField(stf);
                if (sourceField == null) {
                    sourceFieldValid = false;
                }
            }
        }
        if (stf != Const.NODOMAIN) {
            if (mvpString == null) {
                sourceSheetValid = false;
            }
        }
        
        // check if linked field still exists
        boolean checkLinkedField = false;
        if (form.getRootDomainId() == 3000606 && (info.getDomainId() == 3001594 || info.getDomainId() == 3001864 || info.getDomainId() == 3001865)) {
            //ignore checking of some template domainIds [issue:12431]
        }
        else if (info.getAttribute("l") != null && info.getAttribute("interm") == null) {
            checkLinkedField = true;
            if (!isSingleCell) {
                //If loaded field is a subtable field, check whether the subtable that contains this loaded field is referenced from other sheet,
                //or the subtable has already been created new sheet(s) of it. If so, the linked field existence could be unnecessary
                CellLocation firstTableCellLoc = form.getFirstTableCell(loc);
                Integer subtableKey = form.getSubtableKey(firstTableCellLoc);
                Integer parentAlias = form.getSubtableParentAlias(firstTableCellLoc);
                if (parentAlias != null) {
                    checkLinkedField = false;
                }
                else {
                    List<NUIForm> newSheetsFromSubtable = account.getCopiedNUIForms(subtableKey);
                    if (newSheetsFromSubtable.size() > 0) {
                        checkLinkedField = false;
                    }
                }
            }
            
            if (checkLinkedField) {
                //also check in multiple versions
                List<NUIForm> copiedForms = account.getCopiedNUIForms(rootDomainId);
                
                //if this sheet is External User, we should add this sheet into copiedForms manually
                //because External User is included in Account.EXCLUSIVE_ROOTDOMAIN_URL, it can't be reached by Account.getCopiedNUIForms [BugId:12281]
                if (path.equals("/ragic-setup") && sheetIndex == 5) {
                    NUIForm externalUserForm = AppManager.getFormPage(apname, path, sheetIndex);
                    copiedForms.add(externalUserForm);
                }
                
                for (NUIForm copiedForm : copiedForms) {
                    CellLocation linkedFieldLoc = null;
                    if (copiedForm.getPath().equals(path) && Encoder.parseInt(copiedForm.getSheetIndex()) == sheetIndex) {
                        //this form
                        linkedFieldLoc = form.getListenFieldLoc(loc, info);
                    }
                    else {
                        //multiple versions
                        //find the same loaded field in multiple versions
                        for (Map.Entry<CellLocation, CellInfo> _e : copiedForm.getFields().entrySet()) {
                            CellLocation mvLoc = _e.getKey();
                            CellInfo mvInfo = _e.getValue();
                            if (mvInfo.getDomainId() == info.getDomainId() &&
                                    isSingleCell == copiedForm.isSingleCell(mvLoc)) {
                                linkedFieldLoc = copiedForm.getListenFieldLoc(mvLoc, mvInfo);
                                break;
                            }
                        }
                    }
                    
                    if (linkedFieldLoc != null) {
                        listenFieldExist = true;
                        break;
                    }
                }
            }
        }
        
        if (!sourceSheetValid) {
            msgOfField += bundle.getString("linkedSourceFormInvalid") + "<br>";
        }
        if (!stfExist) {
            int newStf = info.getStf(apname);
            if (newStf != Const.NODOMAIN) {
                info.setAttribute("stf", "" + newStf);
                logger.info(apname + path + "/" +sheetIndex + ", " + info.getDomainId() + ", set stf:" + newStf);
                changedCells.add(info);
                formAnyChange = true;
            } else {
                logger.info(apname + path + "/" +sheetIndex + ", " + info.getDomainId() + ", cannot find a proper stf.");
            }
        } else if (!sourceFieldValid) {
            msgOfField += bundle.getString("linkedSourceFieldInvalid") + "<br>";
        }
        if (checkLinkedField && !listenFieldExist) {
            msgOfField += bundle.getString("correspondingLinkedFieldInvalid") + "<br>";
        }
        if (msgOfField.length() > 0) {
            msgOfField = "<div style='margin-bottom:7px;line-height:1.5;'><b>" + info.getValue() + "</b> (" + info.getDomainId() + "):<br>" + msgOfField + "</div>";
        }
        
        msg += msgOfField;
    }
    
    if (msg.length() > 0) {
        msg = "<div style='margin-bottom:10px;max-width:850px;line-height:1.5;'>" + bundle.getString("designIssueContactSupport") + "</div>" + msg;
    }
    
    if (formAnyChange) {
        //making sure field attributes (only stf for now) are the same in each nui with the same domainId
        File home = new File(Util.getApEnvPath(apname));
        for (File dir : home.listFiles()) {
            if (!dir.isDirectory() || dir.isHidden() || dir.getName().endsWith(".del")) continue;
            for (File f : dir.listFiles()) {
                String name = f.getName();
                if (name.endsWith(".del")) continue;
                if (name.endsWith(".nui") && !(name.equals(form.getFileName()) && ("/" + dir.getName()).equals(path))) {
                    
                    NUIForm otherForm = new NUIForm(apname, "/" + dir.getName(), name, true);
                    if (!otherForm.isInited() || !otherForm.isUsable()) continue;

                    boolean anyChange = false;
                    for (CellInfo savedInfo : changedCells) {
                        for (CellInfo otherInfo : otherForm.getFields().values()) {
                            if (savedInfo.getDomainId() == otherInfo.getDomainId()) {
                                boolean ifChanged = otherInfo.setAttribute("stf", savedInfo.getAttribute("stf"));
                                if (ifChanged) {
                                    logger.info(apname + otherForm.getPath() + "/" + otherForm.getSheetIndex() + ", fileName:" + name + ", " + otherInfo.getDomainId() + ", sync stf:" + savedInfo.getAttribute("stf"));
                                    anyChange = true;
                                }
                            }
                        }
                    }
                    
                    if (anyChange) {
                        otherForm.setUser(user);
                        otherForm.saveFile(apname, "/" + dir.getName());
                    }
                }
            }
        }
        
        form.setUser(user);
        form.saveFile(apname, path);
        Account.getInstance(apname).clearNUIFormCache();
        NUIFormSetInfo.resetForms(apname, path, session);
    }
    
    
    jo.put("status", "success");
    jo.put("msg", msg);
    out.print(jo);

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
