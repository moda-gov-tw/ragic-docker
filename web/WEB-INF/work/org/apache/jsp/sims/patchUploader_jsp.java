/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.util.License;
import com.ragic.s3.util.SIMSMultipartRequest;
import com.ragic.s3.util.UploadedFile;
import java.io.*;
import java.nio.charset.StandardCharsets;
import java.nio.file.*;
import java.nio.file.Files;
import java.nio.file.attribute.BasicFileAttributes;
import java.util.Collection;
import java.util.Comparator;
import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;
import java.util.stream.Collectors;
import java.util.stream.Stream;
import java.util.zip.Adler32;
import java.util.zip.CheckedInputStream;
import java.util.zip.ZipEntry;
import java.util.zip.ZipInputStream;
import com.ragic.s3.util.Log;

public final class patchUploader_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("java.io");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("java.nio.file");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.zip.CheckedInputStream");
    _jspx_imports_classes.add("java.util.zip.ZipInputStream");
    _jspx_imports_classes.add("java.nio.file.Files");
    _jspx_imports_classes.add("java.util.Collection");
    _jspx_imports_classes.add("com.ragic.s3.util.License");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("java.nio.file.attribute.BasicFileAttributes");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("java.nio.charset.StandardCharsets");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("java.util.regex.Matcher");
    _jspx_imports_classes.add("java.util.stream.Stream");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("java.util.zip.Adler32");
    _jspx_imports_classes.add("com.ragic.s3.util.UploadedFile");
    _jspx_imports_classes.add("com.ragic.s3.util.SIMSMultipartRequest");
    _jspx_imports_classes.add("java.util.regex.Pattern");
    _jspx_imports_classes.add("java.util.Comparator");
    _jspx_imports_classes.add("java.util.zip.ZipEntry");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html;charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;

      out.write("<script> let patchStatus = parent.document.getElementById('patchStatus'); </script>\r\n");

    if (!License.isDistr()) return;  // only open for private server.
    String apname = request.getParameter("a");
    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    User user = (User) session.getAttribute("USER");
    if (user == null || apname == null || !user.ifApManager("default")) return;
    boolean restart = "true".equals(request.getParameter("restart"));
    if (restart) {
        System.exit(0);
        return;
    }

    //process the upload request
    SIMSMultipartRequest mreq = new SIMSMultipartRequest(request, "cust/tmp", Integer.MAX_VALUE, "UTF-8");
    Collection files = mreq.getFile("patch");
    final String[] value = new String[files.size()];
    int count = 0;
    for (Object file1 : files) {
        UploadedFile file = (UploadedFile) file1;
        if (file == null) continue;
        value[count] = new String(file.getFilesystemName().getBytes(StandardCharsets.UTF_8), StandardCharsets.ISO_8859_1);
        count++;
    }
    if (value.length == 0) return;

    Pattern jettyPattern = Pattern.compile("^lib(?:.)jetty(\\d+).*");
    int jettyVersion = 9; // jetty 9 as the basic version
    boolean isJettyDetected = false;

    //process the uploaded file sync
    boolean libCleared = false;
    Path zipfile = Paths.get("cust/tmp/" + value[0]);

    //issue#15411
    //check zip no exception
    boolean fileChecked=true;

    try (InputStream in = Files.newInputStream(zipfile);
         CheckedInputStream checksum = new CheckedInputStream(in, new Adler32());
         ZipInputStream zis = new ZipInputStream(new BufferedInputStream(checksum))) {
        ZipEntry entry = null;

        while (fileChecked) {
            try {
                entry = zis.getNextEntry();
            } catch (java.io.EOFException e) {//problem reading this file
                fileChecked=false;
                new Log("log/patchUpload.log").setIfDisplayTime(true).log("[ERROR]["+ entry.getName()+"]Patch invalid content EOFException:"+e.getMessage());
                e.printStackTrace();
            } catch (IOException e) {
                fileChecked=false;
                new Log("log/patchUpload.log").setIfDisplayTime(true).log("[ERROR]["+ entry.getName()+"]Patch invalid content IOException:"+e.getMessage());
                e.printStackTrace();
            } catch (Exception e) {
                fileChecked=false;
                new Log("log/patchUpload.log").setIfDisplayTime(true).log("[ERROR]["+ entry.getName()+"]Patch invalid content Exception:"+e.getMessage());
                e.printStackTrace();
            }
            if (entry == null) break;
        }
    } catch (IOException e) {
        fileChecked=false;
        new Log("log/patchUpload.log").setIfDisplayTime(true).log("[ERROR]Patch invalid content IOException:"+e.getMessage());
        e.printStackTrace();

    }


    if(fileChecked){
        try (InputStream in = Files.newInputStream(zipfile);
             CheckedInputStream checksum = new CheckedInputStream(in, new Adler32());
             ZipInputStream zis = new ZipInputStream(new BufferedInputStream(checksum))) {
            ZipEntry entry = null;
            boolean problemEncountered = false;
            while (true) {
                try {
                    entry = zis.getNextEntry();
                } catch (java.io.EOFException e) {//problem reading this file
                    if (!problemEncountered) {
                        System.out.println("Patch invalid content encountered.");
                    }
                    problemEncountered = true;
                    continue;
                } catch (IOException e) {
                    e.printStackTrace();
                } catch (Exception e){
                    e.printStackTrace();
                }

                if (entry == null) break;

                String entryName = entry.getName();
                //if(entryName.contains("..")) break;//this looks like malicious content

                //we only update some specific dir
                if (!entryName.startsWith("web/") &&
                        !entryName.startsWith("lib/") &&
                        // conf should never be overwritten, unless it's templates
                        !entryName.startsWith("conf/template") &&
                        !entryName.startsWith("conf/version") &&
                        !entryName.startsWith("cust/def/")
                    //bin should also not be overwritten because it's always edited when installed
//                  !entryName.startsWith("bin/"
                ) continue;

                if (!isJettyDetected) {
                    Matcher matcher = jettyPattern.matcher(entryName);
                    if (matcher.matches()) {
                        jettyVersion = Integer.parseInt(matcher.group(1));
                        isJettyDetected = true;
                    }
                }

                //If has any file from the lib directory, should clear the lib directory. From now on (2019/9/20) all patches will include full lib of .jar files
                if (!libCleared && entryName.startsWith("lib/")) {
                    Files.walk(Paths.get("lib"))
                            .skip(1)
                            .sorted(Comparator.reverseOrder())
                            .forEach(path -> { try { Files.delete(path); } catch (IOException ignored) { } });
                    libCleared = true;
                }
                try {
                    if (entry.isDirectory()) {
                        Path dir = Paths.get(entryName);
                        if (!Files.exists(dir)) {
                            Files.createDirectories(dir);
                        }
                    } else {
                        Path entryFile = Paths.get(entryName);
                        if (Files.exists(entryFile)) {
                            Files.delete(entryFile);
                            Files.createFile(entryFile);
                        }
                        try (OutputStream os = Files.newOutputStream(entryFile);
                             BufferedOutputStream dest = new BufferedOutputStream(os, 2048)) {
                            byte data[] = new byte[2048];
                            int ziscount;
                            while ((ziscount = zis.read(data, 0, 2048)) != -1) {
                                dest.write(data, 0, ziscount);
                            }
                        }
                    }
                } catch (IOException e) {
                    e.printStackTrace();
                }
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
        new Log("log/patchHistory.log").setIfDisplayTime(true).warn("Patch uploaded by "+user.getUserId()+" zip size "+zipfile.toFile().getTotalSpace());
        try {
            Files.delete(zipfile);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }else{
        try {
            Files.delete(zipfile);
        } catch (IOException e) {
            e.printStackTrace();
        }
        out.print("<script>alert('Upload Patch fail! For details, please see patchUpload.log');</script>");
        return;
    }

    // update lib setting
//    boolean isWindows = System.getProperty("os.name").toLowerCase().startsWith("win");
//    String bashStart = "bin/ragic.sh";
//    String bashSetCp = "bin/setclasspath.sh";
//
//    if (isWindows) {
//        bashStart = "bin/ragic.bat";
//        bashSetCp = "bin/setclasspath.bat";
//    }
//
//    String mainClass;
//    if (jettyVersion == 8) {
//        mainClass = "com.ragic.s3.nui.RagicJettyServer";
//    } else {
//        mainClass = "com.ragic.s3.nui.RagicJetty" + jettyVersion + "Server";
//    }
//
//    try{
//        Path bashStartPath = Paths.get(bashStart);
//        String bashStartContent = new String(Files.readAllBytes(bashStartPath), StandardCharsets.UTF_8);
//        bashStartContent = bashStartContent.replaceAll("com.ragic.s3.nui.RagicJetty(\\w+)", mainClass);
//        Files.write(bashStartPath, bashStartContent.getBytes(StandardCharsets.UTF_8));
//    }
//    catch (Exception e){
//        e.printStackTrace();
//    }

//    String delimiter = isWindows ? ";" : ":";
//    Path bashSetCpPath = Paths.get(bashSetCp);
//    StringBuilder libSetting = new StringBuilder();
//
//    boolean dbJarExist = false;
//    try (Stream<String> lines = Files.lines(bashSetCpPath, StandardCharsets.UTF_8)) {
//        // user must upgrade existed db library manually,
//        // so we keep his original db path if it exist.
//        Pattern dbPattern = Pattern.compile("lib/db/[^*]*\\*");
//        String classpath = lines.filter(s -> s.startsWith("CLASSPATH")).findAny().orElse("");
//        Matcher matcher = dbPattern.matcher(classpath);
//
//        while (matcher.find()) {
//            String dbPath = matcher.group();
//            dbJarExist = Files.exists(Paths.get(dbPath.replace("/*", "")));
//            if (!dbJarExist) {
//                libSetting.setLength(0);
//                break;
//            }
//            libSetting.append(dbPath + delimiter);
//        }
//    } catch (IOException e) {
//        e.printStackTrace();
//    }
//    boolean _dbJarExist = dbJarExist;
//    Files.walkFileTree(Paths.get("lib"), new SimpleFileVisitor<Path>() {
//        @Override
//        public FileVisitResult preVisitDirectory(Path dir, BasicFileAttributes attrs) throws IOException {
//            if (_dbJarExist && dir.getFileName().toString().equals("db")) {
//                return FileVisitResult.SKIP_SUBTREE;
//            }
//            libSetting.append(dir.toString().replace("\\", "/") + "/*" + delimiter);
//            return super.preVisitDirectory(dir, attrs);
//        }
//    });
//
//    try (Stream<String> lines = Files.lines(bashSetCpPath, StandardCharsets.UTF_8)) {
//        List<String> replaced = lines
//                .map(line -> {
//                    if (line.startsWith("CLASSPATH=")) {
//                        return "CLASSPATH=" + libSetting + ".";
//                    }
//                    return line;
//                })
//                .collect(Collectors.toList());
//        Files.write(bashSetCpPath, replaced);
//    } catch (IOException e) {
//        e.printStackTrace();
//    }

      out.write("<script>\r\n    function restartRagic() {\r\n        parent.ajaxget(\"/sims/patchUploader.jsp\", {\"restart\": \"true\", \"a\": \"default\"});\r\n        location.href = \"/\";\r\n        parent.$(\"patchStatus\").innerHTML = \"");
      out.print(bundle.getString("restarting"));
      out.write("...\";\r\n    }\r\n\r\n    parent.restartRagic = restartRagic;\r\n    patchStatus.innerHTML = '");
      out.print(bundle.getString("licenseUploadSuccess"));
      out.write("<a href=\"javascript:void(0);\" onclick=\"restartRagic();\">");
      out.print(bundle.getString("clickHereRestart"));
      out.write("</a>';\r\n    patchStatus.style.color = '#07c';\r\n</script>");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
