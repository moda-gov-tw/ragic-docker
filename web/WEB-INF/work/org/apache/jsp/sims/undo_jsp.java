/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.Account;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.postoffice2.ConfigItem;
import com.ragic.s3.postoffice2.IntegrationManager;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.service.history.History;
import com.ragic.s3.service.lock.*;
import com.ragic.s3.service.lock.sheet.*;
import com.ragic.s3.util.Log;
import com.ragic.s3.util.SystemTaskProgress;
import com.ragic.s3.util.Util;
import java.nio.file.Path;
import java.util.Date;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import com.ragic.s3.util.Const;
import com.ragic.s3.nui.menu.MenuConfiguration;
import com.ragic.s3.nui.menu.MenuSet;
import com.ragic.s3.nui.menu.MenuTab;
import com.ragic.s3.nui.HomeConfig;
import com.ragic.s3.nui.menu.MenuSheet;
import com.ragic.s3.nui.Plan.PricingPlan;

public final class undo_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.service.lock");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("com.ragic.s3.service.lock.sheet");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.IntegrationManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuTab");
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("com.ragic.s3.nui.HomeConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuSheet");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.service.history.History");
    _jspx_imports_classes.add("java.util.HashMap");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemTaskProgress");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuConfiguration");
    _jspx_imports_classes.add("com.ragic.s3.nui.Plan.PricingPlan");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.nio.file.Path");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.ConfigItem");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuSet");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    String apname = request.getParameter("a");
    if (apname == null) return;
    User user = (User) session.getAttribute("USER");
    if (user == null) user = new User();
    String transId = request.getParameter("t");
    String historyId = request.getParameter("hid");
    String result = "";
    /*
        Recover deleted entries
     */
    if (transId != null) {
        SystemTaskProgress.setStatus(apname, Const.STP_MASSROLL, "Recycle bin restore running...", -1, SystemTaskProgress.Status.PROCESSING);
        //Register running service
        RecordRecoveryRunningService rs = new RecordRecoveryRunningService();
        RunningServiceRegister.getInstance().addService(apname, rs);
        try {
            APIQuery aq = new APIQuery(apname, Long.parseLong(transId));
            aq.setUser(user);
            result = aq.recoverEntry();
            result = Util.getServerUrl() + result;
        } catch (Exception e) {
            e.printStackTrace();
        } finally {
            RunningServiceRegister.getInstance().removeService(apname, rs);
        }
        SystemTaskProgress.setStatus(apname, Const.STP_MASSROLL, "Recycle bin restore complete.", -1, SystemTaskProgress.Status.DONE);
    }
    /*
        Recover tab folder or sheets
     */
    else if (historyId != null) {
        try {
            result = History.recoverMenu(apname, Encoder.parseInt(historyId), user);
            if (!result.isEmpty()) {
                String path = "/" + result.split("/")[2];

                // Recover sheet
                if (result.split("/").length > 3) {
                    String formNumber = result.split("/")[3];
                    if (formNumber.contains("##")) {
                        formNumber = formNumber.split("##")[0];
                    }
                    NUIForm form = AppManager.getFormPage(apname, path, formNumber + "_Sheet" + formNumber);
                    if (form != null && form.init()) {    //form
                        AppManager.saveFormToConfig(apname, path, Encoder.parseInt(form.getSheetIndex()), form.getRealname(), form.getRights());
                        MenuTab tab=Account.getInstance(apname).getMenuTab(path);
                        tab.addSheet(new MenuSheet(form.getRealname(),path,Encoder.parseInt(form.getSheetIndex()),tab.getConfig()));
                        tab.save();

                        //update custom sheets
                        Account.getInstance(apname).getHomeConfig().addCustomizedSheet(apname,path,Encoder.parseInt(form.getSheetIndex()), true);
                        tab.save();

                        result = Util.getServerUrl() + result;

                        // Re-enable sync for recovered sheet
                        try {
                            IntegrationManager im = IntegrationManager.getInstance(apname);
                            if (im != null) {
                                List<ConfigItem> itemList = im.getSheetItems(form.getPath(), Encoder.parseInt(form.getSheetIndex(), -1), true);
                                // Find latest configs
                                Map<String, ConfigItem> m_key_latestConfig = new HashMap<>();
                                for (ConfigItem item : itemList) {
                                    ConfigItem item_latest = m_key_latestConfig.get(item.getKey());
                                    if (item_latest == null) {
                                        m_key_latestConfig.put(item.getKey(), item);
                                    } else {
                                        Date setupDate = Encoder.parseDate(item.getSetupDate(), "yyyy/MM/dd HH:mm");
                                        if (setupDate == null) continue;
                                        Date setupDate_latest = Encoder.parseDate(item_latest.getSetupDate(), "yyyy/MM/dd HH:mm");
                                        if (setupDate_latest == null) {
                                            m_key_latestConfig.put(item.getKey(), item);
                                            continue;
                                        }
                                        if (!setupDate.before(setupDate_latest)) {
                                            m_key_latestConfig.put(item.getKey(), item);
                                        }
                                    }
                                }
                                // Enable latest configs
                                for (Map.Entry<String, ConfigItem> entry : m_key_latestConfig.entrySet()) {
                                    entry.getValue().setEnabled(true);
                                }
                                // Save to aic.json
                                im.save();
                            }
                        } catch (Exception e) {
                            Log logger = Log.createStandardLog();
                            logger.warn("Disable config items failed: " + e.getMessage());
                            logger.warn(e);
                        }
                    } else {    //report
                        String reportName = result.split("##")[1];
                        AppManager.saveReportToConfig(apname, path, Encoder.parseInt(formNumber), reportName);
                        result = Util.getServerUrl() + "/" + apname;
                    }
                }

                // Recover tab
                else {
                    // Re-enable sync for recovered tab
                    try {
                        IntegrationManager im = IntegrationManager.getInstance(apname);
                        if (im != null) {
                            // Find latest configs
                            Map<String, ConfigItem> m_key_latestConfig = new HashMap<>();
                            for (Path nuiPath : com.ragic.s3.nui.helper.Util.getApActiveNUIPathList(apname)) {
                                String fullSheetName = nuiPath.toFile().getName();
                                String NUIPath = "/" + nuiPath.getParent().toFile().getName();

                                NUIForm form = new NUIForm(apname, NUIPath, fullSheetName, true);
                                if (!form.getPath().equals(path)) continue;
                                List<ConfigItem> itemList = im.getSheetItems(form.getPath(), Encoder.parseInt(form.getSheetIndex(), -1), true);
                                for (ConfigItem item : itemList) {
                                    ConfigItem item_latest = m_key_latestConfig.get(item.getKey());
                                    if (item_latest == null) {
                                        m_key_latestConfig.put(item.getKey(), item);
                                    } else {
                                        Date setupDate = Encoder.parseDate(item.getSetupDate(), "yyyy/MM/dd HH:mm");
                                        if (setupDate == null) continue;
                                        Date setupDate_latest = Encoder.parseDate(item_latest.getSetupDate(), "yyyy/MM/dd HH:mm");
                                        if (setupDate_latest == null) {
                                            m_key_latestConfig.put(item.getKey(), item);
                                            continue;
                                        }
                                        if (!setupDate.before(setupDate_latest)) {
                                            m_key_latestConfig.put(item.getKey(), item);
                                        }
                                    }
                                }
                            }

                            // Enable latest configs
                            for (Map.Entry<String, ConfigItem> entry : m_key_latestConfig.entrySet()) {
                                entry.getValue().setEnabled(true);
                            }

                            im.save();
                        }
                    } catch (Exception e) {
                        Log logger = Log.createStandardLog();
                        logger.warn("Disable config items failed: " + e.getMessage());
                        logger.warn(e);
                    }
                }
            }
            else {
                if(Account.getInstance(apname).getPricingPlan().getMaxNumOfSheets()<=5){
                    out.println("quickBillChangePlan();");
                }
                else{
                    out.println("floatWin(null,'The item that you are restoring is no longer available (probably is restored from another backup or has been moved). Please contact support@ragic.com if needed.');");
                }
            }
        } catch (Exception e) {
            System.out.println("restore design failed");
            e.printStackTrace();
        }
    }
    if (!result.isEmpty()) {

      out.write("\r\nlocation.href='");
      out.print(result);
      out.write("';\r\n");
}
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
