/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.Account;
import com.ragic.s3.nui.CellInfo;
import com.ragic.s3.nui.CellLocation;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import java.util.Collection;
import java.util.LinkedHashSet;
import java.util.Map;
import org.json.JSONObject;
import org.json.JSONArray;
import java.util.Set;
import java.util.stream.Collectors;
import com.ragic.s3.service.workflow.ApprovalFieldPickerAutoGen;

public final class getSubtableFromSubtableGen_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ApprovalFieldPickerAutoGen");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Collection");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellInfo");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellLocation");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.util.LinkedHashSet");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String apname = request.getParameter("a");
  String path = request.getParameter("p");
  String formName = request.getParameter("fn");
  String childPath = request.getParameter("cp");
  String childFormName = request.getParameter("cfn");
  int linkToDomain = Encoder.parseInt(request.getParameter("ltd"), Const.NODOMAIN);
  int parentAlias = Encoder.parseInt(request.getParameter("pa"), Const.NODOMAIN);
  if (linkToDomain == parentAlias) parentAlias = Const.NODOMAIN;//this is adding reference subtable for deleted subtable in create new sheet from subtable [BugId:
  boolean filterIntermFields = request.getParameter("filterInterm")!=null;
  JSONObject result = new JSONObject();
  boolean subtableIsSameForm = formName != null && formName.equals(childFormName);

  if (linkToDomain == Const.NODOMAIN) {
    result.put("errorMsg", "key field of this sheet is missing");
    out.println(result);
    return;
  }
  
  NUIForm formPage = AppManager.getFormPage(apname, path, formName);
  NUIForm childFormPage = AppManager.getFormPage(apname, childPath, childFormName);
  NUIForm childListingPage = AppManager.getListingPage(apname, childPath, childFormName);
  
  Map<CellLocation, CellInfo> formFields = formPage.getFields();
  Map<CellLocation, Integer> subtableKeys = formPage.getSubtableKey();
  Map<CellLocation, Integer> subtableAliasParents = formPage.getSubtableAliasParent();
  Collection<CellInfo> childListingFields = childListingPage.getFields().values();
  Collection<CellInfo> selectedFields = new LinkedHashSet<>();
  Collection<CellInfo> availableFields = new LinkedHashSet<>();
  int subtableKey = childFormPage.getKeyField();

  
  CellLocation firstTableCellLoc = null;
  for (CellLocation loc : subtableKeys.keySet()) {
    int sk = subtableKeys.get(loc);
    if (sk == subtableKey) {
      if (parentAlias > 0 && subtableAliasParents.get(loc) != null && subtableAliasParents.get(loc) != parentAlias) {
        continue;
      }
      firstTableCellLoc = loc;
      break;
    }
  }
  
  // collect subtable fields of this form to selectedFields
  if (firstTableCellLoc != null) {
    CellLocation tempLoc = firstTableCellLoc;
    CellInfo info;
    while ((info = formFields.get(tempLoc)) != null) {
      if(info.getDomainId()==linkToDomain) {}
      else if(filterIntermFields && info.getAttribute("interm")!=null) {}
      else selectedFields.add(info);
      tempLoc = new CellLocation(tempLoc.getX()+1, tempLoc.getY());
    }
  }
  
  
  // collect listing page fields
  //I think we don't need to collect listing page fields since they are all in form page too
  /*for(Map.Entry<CellLocation, CellInfo> e : childListingPage.getFields().entrySet()){
    CellLocation loc = e.getKey();
    CellInfo info = e.getValue();
    
    if(linkToDomain==Encoder.parseInt(info.getAttribute("s"),Const.NODOMAIN) && info.getDomainId() == parentAlias ||
         linkToDomain==Encoder.parseInt(info.getAttribute("vdn"),Const.NODOMAIN)
        ) {
      continue;
    }
    
    //added subtable should not have a field domain id same as the keyfield of the form
    if(info.getDomainId()==linkToDomain) continue;
    if(filterIntermFields && info.getAttribute("interm")!=null) continue;
    
    boolean inSelectedFields = false;
    for(CellInfo selectedInfo : selectedFields){
      if(selectedInfo.getDomainId() == info.getDomainId() &&
            info.getDomainId()!=linkToDomain) {
        inSelectedFields = true;
        break;
      }
    }

    if(!inSelectedFields) {
      availableFields.add(info);
    }
  }*/
  
  
  // collect form page fields
  for(Map.Entry<CellLocation, CellInfo> e : childFormPage.getFields().entrySet()){
    CellLocation loc = e.getKey();
    CellInfo info = e.getValue();
    // parentAlias will be used in insert reference subtable
    //softlinking to linkedToTarget, need to set parentAlias
    if(childFormPage.isSingleCell(loc) && //single field
            (linkToDomain == Encoder.parseInt(info.getAttribute("s"), Const.NODOMAIN) && info.getDomainId() == parentAlias ||
             linkToDomain == Encoder.parseInt(info.getAttribute("vdn"), Const.NODOMAIN))
            ) {
      continue;
    }

    //only show single fields for selection
    if(!childFormPage.isSingleCell(loc)) continue;
            
    //added subtable should not have a field domain id same as the keyfield of the form
    if(info.getDomainId()==linkToDomain) continue;
    if(filterIntermFields && info.getAttribute("interm")!=null) continue;

    //check if the field in form page is already in listing
    /* boolean inListing = false;
    for (CellInfo listingInfo : childListingFields) {
      if(listingInfo.getDomainId() == info.getDomainId() &&
              info.getDomainId() != linkToDomain) {
        inListing = true;
        break;
      }
    }
    
    if (inListing) continue; */
    
    //check if the field is already in selected subtable
    boolean isSelected = false;
    for(CellInfo selectedInfo : selectedFields){
      if(selectedInfo.getDomainId() == info.getDomainId() &&
            info.getDomainId() != linkToDomain) {
        isSelected = true;
        break;
      }
    }
    
    if(!isSelected) {
      CellInfo copy = info.copy();
      if (copy.getAttribute("ag") != null) {
        copy.setAttribute("ag", copy.getAttribute("ag").replace("{RAGIC_ID}", "{SUBTABLE_RAGIC_ID}"));
      }
      availableFields.add(copy);
    }
  }

  //check if system field "approve" is included in listingFields
  if(!Const.NO_APPROVAL.equals(formPage.getApprovalType())) {
//    boolean approveResultInListing = false;
//    for(CellInfo listingInfo:childListingFields){
//      if(listingInfo.getDomainId()==DConst.WF_STATUS) {
//        approveResultInListing=true;
//        break;
//      }
//    }
//    if(!approveResultInListing){
//      CellInfo approvalInfo = new CellInfo(bundle.getString("approval"));
//      approvalInfo.setDomainId(DConst.WF_STATUS);
//      approvalInfo.setType(Const.SELECT);
//      approvalInfo.setAttribute("ro", "true");
//      availableFields.add(approvalInfo);
//    }
//
//    boolean approveNextInListing = false;
//    for(CellInfo listingInfo:childListingFields){
//      if(listingInfo.getDomainId()==DConst.WF_NEXT_SIGNER) {
//        approveNextInListing=true;
//        break;
//      }
//    }
//    if(!approveNextInListing){
//      CellInfo approvalNext = new CellInfo(bundle.getString("approval_nextSigner"));
//      approvalNext.setDomainId(DConst.WF_NEXT_SIGNER);
//      approvalNext.setType(Const.MSELECT);
//      approvalNext.setAttribute("ro", "true");
//      availableFields.add(approvalNext);
//    }
    Set<CellInfo> autoGenApprovalFormulaListingFields = childListingFields.stream().filter(listingField -> listingField.getAttribute("aaf") != null).collect(Collectors.toSet());
    Set<CellInfo> autoGenApprovalFormulaLeftFields = availableFields.stream().filter(leftField -> leftField.getAttribute("aaf") != null).collect(Collectors.toSet());
    ApprovalFieldPickerAutoGen fpa = new ApprovalFieldPickerAutoGen(apname, bundle);

    //auto-gen approval formula fields
    Set<CellInfo> allAutoApprovalFormulaFields = fpa.genAllFields();

    for(CellInfo info : allAutoApprovalFormulaFields) {
      String formula = info.getAttribute("f");
      if(autoGenApprovalFormulaListingFields.parallelStream().noneMatch(listingField -> listingField.getAttribute("aaf").equals(formula)) &&
              autoGenApprovalFormulaLeftFields.parallelStream().noneMatch(leftField -> leftField.getAttribute("aaf").equals(formula))) {
        availableFields.add(info);
      }
    }
  }

  //display warning if there's no backlink to the current designing page on the page that you're adding as a subtable of this page
  //check for hardlink or softlink(s,vdn)
  boolean backlinkFound=false;
  for(CellInfo info : childFormPage.getFields().values()){
    if(info.getDomainId()==linkToDomain ||
            Encoder.parseInt(info.getAttribute("s"))==linkToDomain ||
            Encoder.parseInt(info.getAttribute("vdn"))==linkToDomain){
      backlinkFound=true;
      break;
    }
  }
  if(!backlinkFound){
    String source=request.getParameter("source");
    result.put("errorMsg", "<div style='margin:0 0 12px 0;color:#08c;'><p style='margin:0 0 5px 0;'>The sheet that you are linking to has no field linked to this form.</p>" +
            "<p>You will need to add a linked field on <b>"+formPage.getRealname()+"</b>, so when user creates a new <b>"+formPage.getRealname()+"</b> they can choose a <b>"+source+"</b>. " +
            "This way the new <b>"+formPage.getRealname()+"</b> will be shown in this subtable.</p></div>");
    out.println(result);
    return;
  }
  
  
  JSONArray availableFieldsJSONArray = new JSONArray();
  for(CellInfo info : availableFields) {
    JSONObject cell = new JSONObject();
    cell.put("domainId", info.getDomainId());
    cell.put("attr", info.getAttributeStringForCopy(subtableIsSameForm));
    if (info.isSelection()) {
        cell.put("lov", info.getLOVString(apname, "`"));
    }
    cell.put("it", info.getType());
    cell.put("displayedFieldName", info.getValue());
    
    int cascadedDomainId = Account.getInstance(apname).getLOVS2RecNoAlias(info.getDomainId());
    if (cascadedDomainId != Const.NODOMAIN) {
        cell.put("cascaded", cascadedDomainId);
    }
    
    availableFieldsJSONArray.put(cell);
  }
  
  JSONArray selectedFieldsJSONArray = new JSONArray();
  for(CellInfo info:selectedFields) {
    JSONObject cell = new JSONObject();
    cell.put("domainId", info.getDomainId());
    cell.put("attr", info.getAttributeStringForCopy(subtableIsSameForm));
    if (info.isSelection()) {
        //lov string probably doesn't need to be encode again [BugId:11271]
        //because when setting the "value" attribute of the textarea, the browser automatically escapes the characters
        //and lov string has already been encoded in NUI file 
        cell.put("lov", info.getLOVString(apname, "`"));
    }
    cell.put("it", info.getType());
    cell.put("displayedFieldName", info.getValue());
    
    selectedFieldsJSONArray.put(cell);
  }
  
  result.put("availableFields", availableFieldsJSONArray);
  result.put("selectedFields", selectedFieldsJSONArray);
  
  out.print(result);


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
