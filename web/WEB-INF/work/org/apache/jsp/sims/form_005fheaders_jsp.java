/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.postoffice2.MessageFacade;
import com.ragic.s3.util.Const;
import java.net.URLDecoder;
import java.util.LinkedHashMap;
import java.util.Map;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.*;
import org.json.JSONArray;
import org.json.JSONObject;
import com.ragic.s3.util.SystemCache;

public final class form_005fheaders_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.LinkedHashMap");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("java.net.URLDecoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.MessageFacade");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("org.json.JSONArray");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemCache");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  User user=(User)session.getAttribute("USER");
  RagicBundle bundle = (RagicBundle)session.getAttribute("RAGIC_BUNDLE");
  String apname=request.getParameter("a");
  if (apname==null) return;
  String path=request.getParameter("p");
  if (path==null) path="";
  String formName=URLDecoder.decode(request.getParameter("fn"), "UTF-8");//eg: 1_test
  if (formName==null) return;
  formName = Encoder.encjs(formName);// Prevent XSS
  boolean reset=request.getParameter("reset")!=null;

  if(SystemCache.getInstance().ifApBlocked(apname)) {
    return;
  }

  //System.out.println("form_headers.jsp?"+request.getQueryString());

  //do reset if specified in param
  if(reset){
    //we don't reset this any more here, because this will lead to aic.json concurrent read write problems
    //IntegrationManager.removeInstance(apname);//re-read configuration to support manual changes to aic.json
    int sheetIndex= Encoder.parseInt(formName.split("_")[0]);
    MessageFacade.checkInMessages(apname,path,sheetIndex, Const.XID_DROPBOX,true,false);
    NUIFormSetInfo.resetForms(apname, path, session);
  }

  //go form action
  String domainId=request.getParameter("dm");
  //deciding which parent domain and node
  String drillNodeParentDomainIdString=request.getParameter("dnpd");
  String drillNodeParentNodeIdString=request.getParameter("dnpn");
  String domainNodeSeq=request.getParameter("dns");
  int iDomainNodeSeq=Encoder.parseInt(domainNodeSeq,-1);
  int iNodeId=Encoder.parseInt(request.getParameter("ni"),-1);
  //url drilling node if filter
  String realDomainId=request.getParameter("rdm");//for url drilling dm=index
  String nodeIdInUrl=request.getParameter("niu");//for url filtering by nodeid

  //we need to set this so that the url will trigger goForm
  //make sure it's level 2
  if(iNodeId>=0 && "-1".equals(drillNodeParentDomainIdString)){
    domainId="index";
  }

  boolean ifgo=domainId!=null;
  boolean mvp=request.getParameter("mvp")!=null;
  boolean mvpCreate=request.getParameter("mvpcreate")!=null;

  //go back action
  boolean goback=request.getParameter("gb")!=null;
  boolean gobackTop=request.getParameter("gbt")!=null;


  /*
  *
  * Execution on NUIFormSet
  *
  */
  String[] formNameSplit=formName.split("_");
  session.setAttribute("LAST_URL","/"+apname+path+"/"+formNameSplit[0]);

  //retrieve the query parameter
  NUIFormSet set=null;
  if(mvp) {
    set=NUIFormSetInfo.loadMVPFormSet(session, apname, path, formName, mvpCreate);
    set.goBack();
  }
  else set=NUIFormSetInfo.loadBrowsingFormSet(session, apname, path, formName);

  if(set==null) return;

  if(!mvp && !mvpCreate && request.getParameter("rmlc") != null) {
    String originFileName = set.getForm().getFileName();
    String formPageFileName = set.getFormName() + "_index.nui";
    if (!formPageFileName.equals(originFileName) && originFileName.endsWith("_index_t.nui")) {//don't go to the same form
      set.goForm(formPageFileName);  //rmlc should always go to form page
    }
  }

  //execute the form action
  if (goback) {
    set.goBack();
  }
  else if(gobackTop){
    set.goBackTop();
  }
  else if (ifgo) {
    //retrieve beginItem count for page up and page down
    int originalBeginItem=0;
    if("index".equals(domainId)) originalBeginItem=set.getBeginItem();

    //set the form
    String newFileName=set.getFormName()+"_"+domainId+".nui";
    if (!newFileName.equals(set.getForm().getFileName())) {//don't go to the same form
      set.goForm(newFileName);
    }

    if (nodeIdInUrl!=null) set.setUrlNodeIdFilter(Integer.parseInt(realDomainId), Integer.parseInt(nodeIdInUrl));
    else if(iNodeId>=0) {
      set.setCurrentNodeId(iNodeId);
    }

    if(iDomainNodeSeq>=0){
      set.setLastCurrentNodeIdSeq(iDomainNodeSeq+originalBeginItem);
    }

    //set the parameters of the new form
    int drillNodeParentDomainId=Encoder.parseInt(drillNodeParentDomainIdString,-1);
    int drillNodeParentNodeId=Encoder.parseInt(drillNodeParentNodeIdString,-1);
    set.setParentDomainId(drillNodeParentDomainId);
    set.setParentNodeId(drillNodeParentNodeId);
    //System.out.println(iNodeId+","+drillNodeParentDomainId+","+drillNodeParentNodeId);
  }

  /*
   *
   * The output
   *
   */



  try{
    set.outputHeader(out,request.getParameter("lo")!=null,false,true, bundle);
  }
  catch(Exception e){
    System.out.println(e.getMessage());
    e.printStackTrace();
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
