/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.auto;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.UserManager;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.sdb.data.bdb.BDBMgr;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.data.bdb.BDBWorkerRegister;
import com.ragic.s3.service.notification.NotificationCache;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.List;
import com.ragic.s3.service.health.HealthChecker;
import java.util.Queue;
import com.ragic.s3.nui.Account;
import com.ragic.s3.util.*;
import java.util.Enumeration;

public final class notification_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.Enumeration");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.service.health.HealthChecker");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBMgr");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("com.ragic.s3.service.notification.NotificationCache");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorkerRegister");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.util.Queue");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserManager");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/javascript; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;

try{
  boolean getCount=request.getParameter("count")!=null;
  String apname=request.getParameter("a");
  if("DelivereeID".equalsIgnoreCase(apname)) return;
  if(apname==null) {
    out.println("{\"code\":\"no account specified\"}");
    return;
  }
  User user=(User)session.getAttribute("USER");
  if(user==null){
    out.println("{\"code\":\"no user specified\"}");
    return;
  }
  if(!user.isValid()){
    out.println("{\"code\":\"guest user:"+user.getUserId()+"\"}");
    return;
  }
  if(Util.apnameArchived(apname)){
    out.println("{\"code\":\"archived account\"}");
    return;
  }

  //no init db just because of notification.jsp
  //going forward will init db
  //if the db is not in the list, then it's not inited already
  if(!BDBMgr.ifHasInstance(apname)){
    out.println("{'\"code\":\"instance inactive: bdbmgr\"}");
    return;
  }
  if(!Account.hasInstance(apname)){
    out.println("{'\"code\":\"instance inactive: account\"}");
    return;
  }

  //ignore getCount requests when system is busy
  if(getCount){
    //busy with other BDBWorker
    if(new HealthChecker().getSYSCPU()>50) {
      out.println("{'\"code\":\"system load high\"}");
      return;
    }

    //blocked by some other actions
    if(SystemCache.getInstance().ifApBlocked(apname)) {
      out.println("{\"code\":\"blocked by other job\"}");
      return;
    }
  }

  //ignore request when exporting data, don't want it to affect session
  if(session.getAttribute("NO_NOTIFICATION")!=null){
      out.println("{\"code\":\"notification temporarily turned off\"}");
      return;
  }

    //do concurrent waiting check for concurrent user accounts
    //the person is in the concurrent user waiting queue, page should reload to reflect this
    boolean concurrentUserWaiting=SessionRegistry.getInstance().ifConcurrentUserWait(session,apname);
    //is full
    if(concurrentUserWaiting){
        //try kicking idle
        User allowed=SessionRegistry.getInstance().kickIdleConcurrent(apname);
        if(allowed!=null && allowed.getUserId().equalsIgnoreCase(user.getUserId())){
            new Log("log/concurrent.log").setIfDisplayTime(true).log(apname+" allowed "+allowed.getUserId()+" waiting users in notif "+user.getUserId(),Log.INFO);
        }
        //check again
        concurrentUserWaiting=SessionRegistry.getInstance().ifConcurrentUserWait(session,apname);
        //still full, keep waiting
        if(concurrentUserWaiting && !License.isProd()){
            out.print("{\"cmd\":\"goWaiting\"}");
            session.setAttribute("CONCURRENT_WAITING",true);
        }
        return;
    }
    //not full
    else{
        //if was waiting, reload
        if(session.getAttribute("CONCURRENT_WAITING")!=null){
            out.print("{\"cmd\":\"reload\"}");
            session.removeAttribute("CONCURRENT_WAITING");
        }
    }

    //update heartbeat
    boolean tabHidden="false".equals(request.getParameter("vis"));
    if(!tabHidden) user.updateHeartbeat();

  /*
  * Current uses of this jsp
  *
  * 0. Duplicated log in check
  * 1. Web notify that there is unread notification (unread number, no mark unread)
  * 2. Click web notify to show 5 notifications (list, mark unread)
  * 3. Show 20 notifications as notification listing (list, mark unread)
  *
  */
  //session check
  Object duplicatedSession=session.getAttribute("DUPLICATED_SESSION");
  //the user is logged out by another user
  //a duplicated session cannot log another session out
  if(duplicatedSession!=null){
      if(!getCount){
        SimpleDateFormat sdf=new SimpleDateFormat("yyyy/MM/dd HH:mm:ss");

        //logging
        if(user!=null && UserManager.ifRagicAdmin(user.getUserId())){//no record for Ragic special admin
        }
        else {
        	String logFilePath = Log.MaintenanceLog.RAGIC_MONITOR.getFilePath(apname);
            Log ragic_monitor = new Log(logFilePath);
            ragic_monitor.info("D "+ request.getRemoteAddr()+" "+user.getUserId()+" "+sdf.format(new Date()));
        }

        //log out response
        out.print("{\"cmd\":\"logout\",\"message\":\""+duplicatedSession+"\",\"t\":\""+User.getCSRFToken(session)+"\"}");//old -999
        return;
      }
  }

  //page update check
  long since=Encoder.parseLong(request.getParameter("since"),-1L);
  int checkNewDomain=Encoder.parseInt(request.getParameter("checkNew"),Const.NODOMAIN);
  if(checkNewDomain!=Const.NODOMAIN){
    if(BDBMgr.ifHasInstance(apname) && since>0){
        BDBMgr mgr=BDBMgr.getInstance(apname);
        if(mgr==null) return;
        boolean ifRecordCreated=mgr.ifRecordCreated(checkNewDomain,since);
        if(ifRecordCreated){
            out.print("{\"cmd\":\"pup\"}");
            return;
        }
    }
  }
  for(Enumeration<String> e=request.getParameterNames();e.hasMoreElements();){
      String name=e.nextElement();
      if(name!=null && name.startsWith("key_")){
          String domainIdString=name.substring(4);
          int domainId=Encoder.parseInt(domainIdString,Const.NODOMAIN);
          if(domainId!=Const.NODOMAIN){
            int[] rootNodeIds=Encoder.parseMultiInt(request.getParameter(name),",");

            if(BDBMgr.ifHasInstance(apname) && rootNodeIds!=null && since>0){
                BDBMgr mgr=BDBMgr.getInstance(apname);
                if(mgr==null) return;
                boolean ifUpdate=mgr.ifRecordUpdated(domainId,rootNodeIds,since);
                if(ifUpdate){
                    out.print("{\"cmd\":\"pup\"}");
                    return;
                }
            }
          }
      }
  }



  if(!getCount){
    out.print("{}");
    return;
  }


  //get the number of notifications
  int notifCount=NotificationCache.getInstance().getUnreadCount(apname,user.getUserId());
  int taskCount=apname.startsWith("Deliveree")?0:NotificationCache.getInstance().getUnfinishedTaskCount(apname,user.getUserId());
  if(user.getGlobalTaskApname()!=null && !user.getGlobalTaskApname().equals(apname)){
    taskCount+=NotificationCache.getInstance().getUnfinishedTaskCount(user.getGlobalTaskApname(),user.getUserId());
  }

  out.println("{\"n\":"+notifCount+",\"t\":"+taskCount+"}");
  }
  catch(java.lang.IllegalStateException e){}
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
