/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.CellLocation;
import com.ragic.s3.nui.CellInfo;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.DConst;
import java.util.Map;

public final class listFields_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellLocation");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellInfo");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String apname=request.getParameter("a");
  String path=request.getParameter("p");
  int sheetIndex= Encoder.parseInt(request.getParameter("si"));
  String currentValue=request.getParameter("current");
  String prefix=request.getParameter("prefix");
  if(prefix==null) prefix="";
  prefix=Encoder.encjs(prefix); //Prevent XSS
  
  boolean nosubtable="true".equals(request.getParameter("nosubtable"));
  boolean freeTextOnly = Encoder.parseBoolean(request.getParameter("freeTextOnly"));
  boolean selectOnly = Encoder.parseBoolean(request.getParameter("selectOnly"));
  boolean dateOnly = Encoder.parseBoolean(request.getParameter("dateOnly"));
  boolean numberOnly = Encoder.parseBoolean(request.getParameter("numberOnly"));
  boolean ignoreReadOnly = Encoder.parseBoolean(request.getParameter("ignoreReadOnly"), false);
  boolean subtableOnly = Encoder.parseBoolean(request.getParameter("subtableOnly"), false);
  boolean noMultiSelect = Encoder.parseBoolean(request.getParameter("noMultiSelect"), false);
  boolean noInterm = Encoder.parseBoolean(request.getParameter("noInterm"), false);
  boolean noMultiLink = Encoder.parseBoolean(request.getParameter("noMultiLink"), false); // TODO: remove after mass update multi link supported
  String defaultText=request.getParameter("df")!=null?request.getParameter("df"):"";
  int defaultValue=Encoder.parseInt(request.getParameter("dv"),-1);
  String sameSubtableWith = request.getParameter("sameSubtableWith");

  NUIForm form=AppManager.getFormPage(apname,path,sheetIndex);
  if(form==null) {
    out.println("<option>-- Sheet deleted --</option>");
    return;
  }

  CellLocation sameSubtableWithLoc = null;
  if (sameSubtableWith != null) {
      String[] split = sameSubtableWith.split(",");
      if (split.length > 1) {
          sameSubtableWithLoc = new CellLocation(Encoder.parseInt(split[0], -1), Encoder.parseInt(split[1], -1));
      }
  }

  //default first option
  if(dateOnly) out.println("<option value='"+ DConst.ENTRY_CREATE_DATE+"'>"+bundle.getString("createDate")+"</option>");
  else out.println("<option value='"+defaultValue+"'>"+defaultText+"</option>");

  //rest of the options
  Map<CellLocation, CellInfo> allFields = form.getFields();
  for(Map.Entry<CellLocation, CellInfo> e : allFields.entrySet()){
      CellLocation loc = e.getKey();
      CellInfo info = e.getValue();
      boolean isSingleCell = form.isSingleCell(loc);
      if(nosubtable && !isSingleCell) continue;
      if(dateOnly && !info.isDate()) continue;
      if(freeTextOnly && !info.isFreeText()) continue;
      if(numberOnly && !info.isNumeric()) continue;
      if(ignoreReadOnly && info.getAttribute("ro") != null) continue;
      if(noMultiSelect && info.isMultipleSelect()) continue;
      if(noInterm && info.getAttribute("interm") != null) continue;
      if(noMultiLink && info.getAttribute("mvp") != null && info.isMultipleSelect()) continue;
      if(selectOnly && !info.isSelection()) continue;

    String value=prefix+info.getDomainId();

    String dateFormat = info.getAttribute("repeat") == null? info.getAttribute("fmt_d"): null; //[issue#05989]
    if(dateFormat != null) {
        if ((dateFormat.contains("yyyy") || Encoder.isRagicMinguoDateFormat(dateFormat) || Encoder.isRagicJapaneseDateFormat(dateFormat)) && dateFormat.contains("M") && dateFormat.contains("d")) {
            dateFormat = "D";
        } else {
            dateFormat = "T";
        }
    }
    String optionDisplayText = info.getValue();
    optionDisplayText = Encoder.encjs(optionDisplayText);
    if(isSingleCell) {
        if(subtableOnly) continue;
        out.println("<option value='"+value+"' "+(value.equals(currentValue)?"selected":"")+ ((dateFormat == null)? "": " dateFormat='" + dateFormat + "'") + "title='" + bundle.getString("externalId") + ": " + info.getDomainId() + "'>"+optionDisplayText+"</option>");
    } else {
        if (sameSubtableWithLoc != null && !form.isSameSubtable(sameSubtableWithLoc, loc)) continue;
        int subtableKey = form.getTableHeaderDomain(info.getDomainId());
        out.println("<option value='"+value+"' "+(value.equals(currentValue)?"selected":"")+ ((subtableKey == -1)? "":" subtableKey='" + subtableKey + "'") + ((dateFormat == null)? "": " dateFormat='" + dateFormat + "'") + "title='" + bundle.getString("externalId") + ": " + info.getDomainId() + "'>"+optionDisplayText + "(" + bundle.getString("newSubtableDefault") + " " + form.getSubtableName(subtableKey) + ")"+"</option>");
    }
    
    
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
