/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.OptionHelper;
import com.ragic.s3.nui.output.HTMLWriter;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import com.ragic.s3.util.Util;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import org.apache.poi.ss.usermodel.Cell;

public final class queryItemBar_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.nui.output.HTMLWriter");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.OptionHelper");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("java.util.HashSet");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Cell");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    RequestParam rp = (RequestParam) request.getAttribute("REQ");
    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    User user = (User) session.getAttribute("USER");
    if(user==null) user=new User();
    boolean more = false;
    int displayLimit = 15;  //limit 0 = no limit
    int selectLimit = displayLimit;
    if (request.getParameter("more") != null) {
        more = true;
        displayLimit = 0;
    }
    String apname, path, formName;
    if (rp != null) {
        if (rp.getSheet() == null) return;
        apname = rp.getApname();
        path = rp.getPath();
        formName = rp.getSheet().getFormName();
    } else {
        apname = request.getParameter("a");
        path = request.getParameter("p");
        formName = request.getParameter("n");
    }

    //retrieve the form
    NUIForm formPage = AppManager.getFormPage(apname, path, formName,true);
    NUIForm listingPage = AppManager.getListingPage(apname, path, formName,true);

    if (formPage == null || listingPage == null) return;
    Account account = Account.getInstance(apname);

    boolean isCust = formPage.hasCustQueryItems();

    Map<CellLocation, CellInfo> formFields = formPage.getListingQueryItems(bundle, user);

    //[bug id 940]
    int defaultDisplayNum = 0;
    int defaultSelectNum = 0;
    for (CellInfo info : formFields.values()) {
        if (info.getAttribute("sig") != null) continue;
        boolean isDefaultDisplay = formPage.isDefaultDisplayQueryItems(info);
        if (isDefaultDisplay) defaultSelectNum++; // defaultSelectNum is always effective in cust query item selections
        if (!more && !isDefaultDisplay)
            continue; // defaultDisplayNum is only effective in restricted mode (!more = true)
        defaultDisplayNum++;
    }

    StringBuilder outString = new StringBuilder(256);
    if (user != null && formPage.checkRight(user) >= Const.ADMIN) {

      out.write("<div style=\"margin:0 0 3px -3px;\" title=\"");
      out.print(bundle.getString("custQueryItem") );
      out.write("\">\r\n    <a id=\"selectQueryItems\" style=\"font-size:12px;color:#888;cursor:pointer;\"\r\n       onclick=\"floatWin(function(){return '/sims/listingCustQueryItemsSelector.jsp?a=");
      out.print(apname);
      out.write('&');
      out.write('p');
      out.write('=');
      out.print(path);
      out.write("&fn=");
      out.print(formName);
      out.write("&limit=");
      out.print(selectLimit );
      out.write("&def=");
      out.print(defaultSelectNum );
      out.write("'},null,'");
      out.print(bundle.getString("custQueryItem") );
      out.write("',true);\"><i\r\n            class=\"fa fa-cog\"></i></a>\r\n</div>\r\n");

    }
    formFields = formPage.getListingQueryItems(bundle, user, displayLimit, defaultDisplayNum);

    int subtableKey;
    int lastGroup = formPage.getRootDomainId();
    for (Map.Entry<CellLocation, CellInfo> e : formFields.entrySet()) {
        CellLocation loc = e.getKey();
        CellInfo info = e.getValue();
        if (info.getAttribute("sig") != null) continue;
        int _domainId = info.getDomainId();
        boolean isSysField = info.getAttribute("sysField") != null;
        boolean isUTCTime = false;
        boolean isPulledInSysField = _domainId == DConst.ENTRY_CREATE_DATE || _domainId == DConst.ENTRY_UPDATE_DATE;
        String value = Encoder.encjs(Encoder.contentDecode(listingPage.getField(_domainId) != null && (!isSysField || isPulledInSysField) ? listingPage.getField(_domainId).getValue() : info.getValue())),
                filterType = info.getFilterType(),
                id, id2, filterDateFormat=null;

        /* for issue #11422
        in some situation , the data from database will be HTML Entities
        in this situation , data will be encoded twice
        so we need to decode the repeated situation */
        value = Encoder.replaceRepeatedHtmlEntities(value);


        subtableKey = formPage.getTableHeaderDomain(_domainId);
        if (subtableKey == _domainId) subtableKey = Const.NODOMAIN; // no interm domain for self of course
        if (filterType == Const.FILTER_DATE) {
            CellInfo dateInfo = listingPage.getField(_domainId) != null && (!isSysField || isPulledInSysField) ? listingPage.getField(_domainId) : info;

            if (dateInfo.getAttribute("fmt_d") != null) {
                filterDateFormat = dateInfo.getAttribute("fmt_d");
                isUTCTime = dateInfo.getAttribute("utcTime") != null;
            }
        }

        boolean hasInterm = false;
        int intermDomain = Const.NODOMAIN;
        if (subtableKey > 0) {
            hasInterm = true;
            intermDomain = subtableKey;
        } else if (info.getAttribute("interm") != null) {
            hasInterm = true;
            intermDomain = Integer.parseInt(info.getAttribute("interm"));
        }

        boolean selectionFilter = Const.FILTER_SELECT.equals(filterType) || Const.FILTER_USER.equals(filterType) || info.getAttribute("im") != null;

        if (!isCust) {
            if (formPage.isFirstTableCell(loc))
                outString.append("<hr/>\n");  //When a subtable begin to be displayed, it will start with HR as segmentation
        } else {
            // set main table field && sysfield group id = root domain id
            int thisGroup = formPage.getTableHeaderDomain(_domainId) == -1 ||
                    formPage.getTableHeaderDomain(_domainId) == 0 ||
                    formPage.getTableHeaderDomain(_domainId) == _domainId ?
                    formPage.getRootDomainId() : formPage.getTableHeaderDomain(_domainId);
            if (thisGroup != lastGroup) {
                outString.append("<hr/>\n");
                lastGroup = thisGroup;
            }
        }
        outString.append("<div class='v6QueryItemName' onclick=\"toggleV6Query(" + _domainId + ");\">")
                 .append("<span class='v6QueryArrow' id='v6QueryArrow" + _domainId + "'>▶</span>")
                 .append(value)
                 .append("</div>\n")
                 .append("<div id='v6Query" + _domainId + "' class='v6QuerySection clearfix' style='display:none;' filtertype='" + filterType + "'>\n");

        if (Const.FILTER_TEXT.equals(filterType) || selectionFilter) {
            String dId = _domainId + (hasInterm ? "|" + intermDomain : "");
            id = _domainId + "|" + NUIFilter.OPERAND_LK + (hasInterm ? "|" + intermDomain : "");

            if (selectionFilter) {  //selection will has a wrapper for its text search shortcut
                outString.append("<div id='v6QueryTextToggle" + _domainId + "' class='v6QueryToggleBtn specialSetter' onclick='toggleV6QueryTextInput(this);'>" + bundle.getString("filterByText") + "</div>")
                         .append("<i class=\"far fa-ellipsis-v v6QueryQuickSearchIcon besideToggle\" onclick=\"quickSearchTemplate('" + dId + "')\"></i><div></div>")
                         .append("<div id='v6QuerySelToggle" + _domainId + "' class='v6QueryToggleBtn specialSetter' style='display:none;' onclick='toggleV6QueryTextInput(this);'>" + bundle.getString("filterBySelection") + "</div>")
                         .append("<div style='display:none;'>");
            }

            outString.append("<input class='v6QueryTextbox' title='"+bundle.getString("accessibility_v6QueryTextbox")+"'")
                     .append(" onfocus=\"showV6TextQueryOpts('" + dId + "');\"")
                     .append(" onblur=\"hideV6TextQueryOpts('" + dId + "');\"")
                     .append("  name='v6QueryItem' id='" + id + "' type='text' autocomplete='off'/>\n")
                     .append(" <i class=\"far fa-ellipsis-v v6QueryQuickSearchIcon\" onclick=\"quickSearchTemplate('" + dId + "')\"></i>")
                     .append("<div class='v6QueryTextOption' id='tExactDiv" + dId + "'>")
                     .append("<input title='"+bundle.getString("accessibility_v6QueryTextbox")+"' type=checkbox onchange=\"toggleV6TextParentNode(this);\" id='tExact" + dId + "' />")
                     .append("<label for='tExact" + dId + "'><span class='v6QueryTextLabel'>" + bundle.getString("exactMatch") + "</span></label>")
                     .append("</div>");

            if (selectionFilter) {
                outString.append("</div>");
            }
        }

        if (selectionFilter) {
            boolean isUserDomain = (_domainId == DConst.ENTRY_ORIGINAL_CREATE_USER) || (_domainId == DConst.ENTRY_ENTRY_MANAGER);
            boolean isUserSelection = Const.FILTER_USER.equals(filterType);

            BDBWorker worker = new BDBWorker(apname);
            worker.setWhoami("queryItemBar.jsp");
            Set<String> cascadedOptionSet = new HashSet<>();
            try {
                //check for stf
                // if cust, formPage.getField(108) may be null
                Selection s;
                OptionHelper oh = new OptionHelper(_domainId, worker);
                oh.setMaxOptionNumber(Const.SELECT_OPTION_MAX_FOR_FILTER_LOV);
                if (info.getAttribute("stf") != null) {
                    oh.setDisplayDomainId(Encoder.parseInt(info.getAttribute("stf")));
                }
                s = oh.createOption();

                int count = Const.SELECT_OPTION_MAX_FOR_FILTER_LOV;
                boolean _isCascaded = s.isCascaded();

                List<SDBNode> options = s.getOptionsFilterBy(apname, info);
                if (isUserSelection) {
                    options.sort((o1, o2) -> {
                        String raw1 = Encoder.unformatNUIAttr(o1.getValue());
                        String raw2 = Encoder.unformatNUIAttr(o2.getValue());
                        return account.getUserName(raw1).compareToIgnoreCase(account.getUserName(raw2));
                    });
                }

                for (SDBNode node : options) {

                    //cascaded selection would need to trim the parent option value
                    if (_isCascaded) {
                        String _nval = Encoder.unformatNUIAttr(node.getValue()),
                                realVal = _nval.substring(_nval.indexOf("|") + 1);

                        if (cascadedOptionSet.contains(realVal)) continue;
                        cascadedOptionSet.add(realVal);
                        node.setValue(realVal);
                    }

                    //first selection option will be given a margin-top value to separate text input shorcut
                    String rawOption = Encoder.unformatNUIAttr(Util.sysOptLocalization(_domainId, node, bundle, apname));
                    String optionString;
                    if (info.getAttribute("checkbox") != null) {
                        optionString = HTMLWriter.checkboxSymbolRender(rawOption);
                    } else if (isUserDomain || isUserSelection) {
                        optionString = account.getUserName(rawOption).replaceAll("<", "&#60;").replaceAll(">", "&#62;").replaceAll("%7C", "|");
                    } else {
                        optionString = Encoder.sanitizeHTML(Encoder.processBBCode(rawOption.replaceAll("<", "&#60;").replaceAll(">", "&#62;").replaceAll("%7C", "|")));
                    }

                    //[BugId:13022] For special processing of multiple level linked field, other js code may check if this id exists to see if this type of special processing is needed
                    // We don't use this -1 operand for most cases because there may be multiple linked record with this name.
                    // But for multiple level linked records, it is much easier to query using -1, the trade off being if there are multiple records of the same value in this field, only one of them will be returned as result
                    if(info.isMultipleLevelLinkedField(apname)){
                        id = _domainId + "|" + NUIFilter.OPERAND_EQN + "|" +node.getId();
                    }
                    else{
                        id = _domainId + "|" + NUIFilter.OPERAND_EQ + "|" + (hasInterm ? intermDomain + "|" : "") +
                            Encoder.encodeURIComponent((!isUserDomain && isUserSelection) ? optionString : node.getValue());
                    }

                    outString.append("<div " + (count == Const.SELECT_OPTION_MAX_FOR_FILTER_LOV ? "style='margin-top:6px;'" : "") + " class='v6QueryOption'><input title='"+bundle.getString("accessibility_v6QueryTextbox")+"' type=checkbox name='v6QueryItem' value='' id=\"" + id + "\" />")
                            .append("<label for='" + id + "'> ").append(Encoder.sanitizeHTML(optionString)).append("</label></div>\n");
                    count--;
                    if (count <= 0) break;
                }
            } finally {
                worker.commit();
            }
            if (Const.FILTER_USER.equals(filterType)) {
                outString.append("<div style='margin-top:3px;margin-left:12px;'>")
                        .append("<span class='specialSetter' style='clear:left;float:left;' onclick=\"showSpecialSettingMenu(event, this, 'usdropdown');\">")
                        .append("<i class='fas fa-cog'></i></span></div>");
            }
        } else if (Const.FILTER_NUMBER.equals(filterType)) {
            id = _domainId + "|" + NUIFilter.OPERAND_GTE + (hasInterm ? "|" + intermDomain : "");
            id2 = _domainId + "|" + NUIFilter.OPERAND_LTE + (hasInterm ? "|" + intermDomain : "");
            String numberType, extraMark;
            if (info.getAttribute("fmt_n") != null && info.getAttribute("fmt_n").contains("%")) {
                numberType = "percent";
                extraMark = "%";
            } else {
                numberType = "normal";
                extraMark = "";
            }

            outString.append("<input title='"+bundle.getString("accessibility_v6QueryTextbox")+"'id='" + id + "' type='text' class='v6QueryDatebox' data-numbertype='" + numberType + "' onfocus=\"outNUI();\" onblur=\"ctrl.outOfNUI=false;\" name='v6QueryItem' autocomplete='off' />")
                     .append(extraMark)
                     .append("<div style='margin:1px 2px'>" + bundle.getString("to") + "</div>")
                     .append("<input title='"+bundle.getString("accessibility_v6QueryTextbox")+"' id='" + id2 + "' type='text' class='v6QueryDatebox' data-numbertype='" + numberType + "' onfocus=\"outNUI();\" onblur=\"ctrl.outOfNUI=false;\" name='v6QueryItem' autocomplete='off' />")
                     .append(extraMark);
        } else if (Const.FILTER_DATE.equals(filterType)) {

            // For Date field text search
            String dId = _domainId + (hasInterm ? "|" + intermDomain : "");
            outString.append("<div id='v6QueryTextToggle" + _domainId + "' class='v6QueryToggleBtn specialSetter' onclick='toggleV6QueryDateTextInput(this,\"text\");'>" + bundle.getString("filterByText") + "</div>")
                     .append("<i class=\"far fa-ellipsis-v v6QueryQuickSearchIcon besideToggle\" onclick=\"quickSearchTemplate('" + dId + "')\"></i><div></div>")
                     .append("<div id='v6QuerySelToggle" + _domainId + "' class='v6QueryToggleBtn specialSetter' style='display:none;' onclick='toggleV6QueryDateTextInput(this, \"date\");'>" + bundle.getString("filterByCalendar") + "</div>")
                     .append("<div class='textFilterPart' style='display:none;'>");
            id = _domainId + "|" + NUIFilter.OPERAND_LK + (hasInterm ? "|" + intermDomain : "");
            outString.append("<input class='v6QueryTextbox' title='"+bundle.getString("accessibility_v6QueryTextbox")+"'")
                     .append(" onfocus=\"showV6TextQueryOpts('" + dId + "');\"")
                     .append(" onblur=\"hideV6TextQueryOpts('" + dId + "');\"")
                     .append("  data-date='true' " + (filterDateFormat!=null? "data-fmtd='" + filterDateFormat + "'" : "") + (isUTCTime ? "data-utctime='true'" : "") + "name='v6QueryItem' id='" + id + "' type='text' autocomplete='off' />\n")
                     .append(" <i class=\"far fa-ellipsis-v v6QueryQuickSearchIcon\" onclick=\"quickSearchTemplate('" + dId + "')\"></i>")
                     .append("<div class='v6QueryTextOption' id='tExactDiv" + dId + "'>")
                     .append("<input title='"+bundle.getString("accessibility_v6QueryTextbox")+"'type=checkbox onchange=\"toggleV6TextParentNode(this);\" id='tExact" + dId + "' />")
                     .append("<label for='tExact" + dId + "'><span class='v6QueryTextLabel'>" + bundle.getString("exactMatch") + "</span></label>")
                     .append("</div>")
                     .append("</div>");

            // For Date field calendar search
            id = _domainId + "|" + NUIFilter.OPERAND_GTE + (hasInterm ? "|" + intermDomain : "");
            id2 = _domainId + "|" + NUIFilter.OPERAND_LTE + (hasInterm ? "|" + intermDomain : "");

//            String dateFormat = info.getAttribute("fmt_d");
            outString.append("<div class='dateFilterPart'>")
                     .append("<input title='"+bundle.getString("accessibility_v6QueryTextbox")+"'id='" + id + "' type='text' class='v6QueryDatebox' onclick=\"datePickerClick(this, '" + filterDateFormat + "', true, false);\" onfocus=\"outNUI();this.click();\" onblur=\"ctrl.outOfNUI=false;\" oninput=\"hideTaskDatePicker();\" name='v6QueryItem' data-date='true' " + (filterDateFormat!=null ? "data-fmtd='" + filterDateFormat + "'" : "") + (isUTCTime ? "data-utctime='true'" : "") + " autoComplete='off' />")
                     .append("<div style='margin:1px 2px'>" + bundle.getString("to") + "</div>")
                     .append("<input title='"+bundle.getString("accessibility_v6QueryTextbox")+"'id='" + id2 + "' type='text' class='v6QueryDatebox' onclick=\"datePickerClick(this, '" + filterDateFormat + "', false, true);\" onfocus=\"outNUI();this.click();\" onblur=\"ctrl.outOfNUI=false;\" oninput=\"hideTaskDatePicker();\" name='v6QueryItem' data-date='true' " + (filterDateFormat!=null ? "data-fmtd='" + filterDateFormat + "'" : "") + (isUTCTime ? "data-utctime='true'" : "") + " autoComplete='off' />")
                     .append("</div>")
                     .append("<div class='specialSetterContent' style='margin-top:3px;'><span class='specialSetter' style='clear:left;float:left;' onclick=\"showSpecialSettingMenu(event, this, 'dsdropdown');\">")
                     .append("<i class='fa fa-cog'></i></span></div>");
        }

        outString.append("\n</div>\n");
    }
    out.println(outString);

    if (!isCust) {
        if (more) {
            out.println("<a href='javascript:void(0);' class='sideMorelink' onclick='v6MoreFilter()' >Less</a>");
        } else {
            out.println("<a href='javascript:void(0);' class='sideMorelink' onclick='v6MoreFilter(true)'>More</a>");
        }
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
