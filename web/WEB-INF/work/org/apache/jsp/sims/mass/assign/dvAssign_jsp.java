/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.mass.assign;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.CellInfo;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.LongTermTask;
import com.ragic.s3.nui.helper.ThreadManager;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.service.api.APIResults;
import com.ragic.s3.service.api.dv.*;
import com.ragic.s3.service.history.History;
import com.ragic.s3.service.history.HistoryRecord;
import com.ragic.s3.service.lock.*;
import com.ragic.s3.service.lock.sheet.*;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.License;
import com.ragic.s3.util.SystemTaskProgress;
import org.json.JSONArray;
import org.json.JSONException;
import java.time.LocalDateTime;
import java.time.temporal.ChronoUnit;
import java.util.*;
import com.ragic.s3.sdb.search.FTIndex;
import com.ragic.s3.nui.helper.ExceptionManager;
import com.ragic.s3.service.api.APIResultsFull;

public final class dvAssign_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.service.api.dv");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.service.lock");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("com.ragic.s3.service.lock.sheet");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.LongTermTask");
    _jspx_imports_classes.add("java.time.LocalDateTime");
    _jspx_imports_classes.add("com.ragic.s3.util.License");
    _jspx_imports_classes.add("com.ragic.s3.service.history.History");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ThreadManager");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResultsFull");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemTaskProgress");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellInfo");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.ExceptionManager");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResults");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("org.json.JSONException");
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTIndex");
    _jspx_imports_classes.add("java.time.temporal.ChronoUnit");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.service.history.HistoryRecord");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  final RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  final User user=(User)session.getAttribute("USER");
  final String apname=request.getParameter("a");
  final String path=request.getParameter("p");
  final int sheetIndex= Encoder.parseInt(request.getParameter("si"));
  //final int domainId=Encoder.parseInt(request.getParameter("d"));
  final String executeContent=request.getParameter("c");
  final HttpSession finalSession=session;
  final boolean ifProd= License.isProd();

  final List<Integer> domainIds = new ArrayList<>();
  JSONArray ja = null;
  if (executeContent != null) {
    try{
      ja = new JSONArray(executeContent);
    } catch(JSONException ignored) {
      return;
    }
  } else {
    return;
  }
  
  if (ja != null) {
    for (int i = 0; i < ja.length(); i++) {
      domainIds.add(ja.optInt(i));
    }
  }

  if(apname==null || path==null || sheetIndex<=0 || domainIds.size()==0){
    out.println("Please make sure the default value field is saved in the form design before you do this.");
    return;
  }

  Thread t=new Thread(new Runnable(){
    public void run() {
      APIQuery aq=new APIQuery(apname,path,sheetIndex);
      aq.setIfIgnoreFixedFilter(true);//[BugId:15441]
      NUIForm form = aq.getForm();
      //Register running service
      DefaultValueAssignRunningService rs = new DefaultValueAssignRunningService(aq.getForm().getUrl());
      RunningServiceRegister.getInstance().addService(apname, rs);
      LongTermTask ltt= ThreadManager.getInstance().addTask(Thread.currentThread(),apname,"Default value assign:"+aq.getForm().getUrl());
      LocalDateTime start=LocalDateTime.now();
      try{

        SystemTaskProgress.setStatus(apname, Const.STP_MASSEDIT, "Initializing...", 0, SystemTaskProgress.Status.PROCESSING);
        new com.ragic.s3.util.Log("log/m_rootNodeId_lastModifiedHistory.log").setIfDisplayTime(true).info(apname+path+"/"+sheetIndex+" "+executeContent);


        Map<Integer, LastModifiedInfoRetriever> m_rootNodeId_lastModifiedInfoRetriever = new HashMap<>();
        boolean needLastModifiedInfo = domainIds.stream().anyMatch(domainId -> {
        	CellInfo cell = form.getCellInfoByDomainId(domainId);
        	if(cell == null) return false;
        	String dv = cell.getAttribute("dv");
        	if(dv == null) return false;
        	return dv.startsWith("#") || "$SEQ".equals(dv);
        });
        if(needLastModifiedInfo) {
        	try {
                m_rootNodeId_lastModifiedInfoRetriever = DefaultValueHelper.retrieveLastModifiedInfoForDvAssign(apname, aq.getForm().getRootDomainId(), domainIds.size() == 1? domainIds.get(0): Const.NODOMAIN, bundle);
            } catch(Exception e) {
                ExceptionManager.getInstance().processException(e);
            }
            new com.ragic.s3.util.Log("log/m_rootNodeId_lastModifiedHistory.log").setIfDisplayTime(true).info("Size "+m_rootNodeId_lastModifiedInfoRetriever.size());
        }

        aq.setUser(user);
        aq.setBundle(bundle);
        aq.setUpdateMode();
        aq.setIfIncludeInfo(true);
        aq.setIfIgnoreFixedFilter(true);
        aq.setMaxRetrieveFieldSize(1024);

        //NUIFormSet set= NUIFormSetInfo.loadBrowsingFormSet(finalSession, apname, aq.getPath(), aq.getForm().getFormName());
//        List<NUIFilter> filters=set.getCurrentFilter();
//        for(NUIFilter filter:filters){
//          aq.addFilter(filter.getDomainId(),filter);
//        }

        APIResultsFull ar=aq.getAPIResultsFull();
        int count=0;
        for(APIEntry ae:ar){
          for(CellInfo info:form.getAllFields(true)){
            String dv = info.getAttribute("dv");
            if(dv!=null && domainIds.contains(info.getDomainId())){
              String currentValue=ae.getFieldValue(info.getDomainId());
              if(currentValue==null || currentValue.length()==0){
                //we save the whole entry because the default value might be in subtable, it's harder using BDBSmartWorker. For better performance should use BDBSmartWorker in the future
                ae.loadDefaultValue(info.getDomainId(), user, bundle, true, Optional.ofNullable(m_rootNodeId_lastModifiedInfoRetriever.get(ae.getRootNodeId())), false);

                ae.setMassUpdateMode();
                ae.setExecuteWorkflow(false);
                ae.setIgnoreEmptyCheck(true);
                ae.setDoPostOffice(false);
                ae.setCreateHistory(false);
                ae.setIfFullTextIndex(false);
                ae.setDateValueIsUTC(false);

                ae.save(user,bundle);
                SystemTaskProgress.setStatus(apname, Const.STP_MASSEDIT, "Updated: ", count, SystemTaskProgress.Status.PROCESSING);
                count++;
              }
            }
          }
        }
        new com.ragic.s3.util.Log("log/m_rootNodeId_lastModifiedHistory.log").setIfDisplayTime(true).info("Count "+count);

        //should not be necessary since it's making the same change to all records, does not help searching for records
        //FTIndex.index(apname, path, aq.getForm().getFormName(), false);
        long diff= ChronoUnit.SECONDS.between(start, LocalDateTime.now());
        SystemTaskProgress.setStatus(apname, Const.STP_MASSEDIT, "Done! Updated "+count+" records, took "+diff+"s.", -1, SystemTaskProgress.Status.DONE);
      }
      catch(Exception e){
        ExceptionManager.getInstance().processException(e);
      } finally {
    	  RunningServiceRegister.getInstance().removeService(apname, rs);
        ThreadManager.getInstance().removeTask(ltt);
      }
    }
    
  });
  t.setName("Default value assign:"+apname+path+"/"+sheetIndex);
  t.start();

  out.println("updateSystemTaskProgress();");



    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
