/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.menu.MenuSet;
import com.ragic.s3.nui.menu.MenuSheet;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.*;
import com.ragic.s3.util.Const;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import java.util.*;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.sdb.query.SDBNode;
import org.json.JSONArray;
import com.ragic.s3.nui.menu.MenuTab;

public final class fieldAnalyzer_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


public class HistoryLine {
    private Date date;
    private String line;
    public HistoryLine(Date date, String line){
        this.date = date;
        this.line = line;
    }
    public Date getDate() {
        return this.date;
    }
    public String getLine() {
        return this.line;
    }
}

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuTab");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuSheet");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("org.json.JSONArray");
    _jspx_imports_classes.add("com.ragic.s3.nui.menu.MenuSet");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



    RagicBundle bundle=(RagicBundle)session.getAttribute("RAGIC_BUNDLE");
    String apname=request.getParameter("a");
    int domainId= Encoder.parseInt(request.getParameter("d"),Const.NODOMAIN);
    int rootDomainId=Encoder.parseInt(request.getParameter("rd"),Const.NODOMAIN);
    int nodeId=Encoder.parseInt(request.getParameter("ni"),Const.NONODE);
    String path=request.getParameter("p");
    int sheetIndex=Encoder.parseInt(request.getParameter("si"));

    //privilege check
    User user=(User)session.getAttribute("USER");
    if(user==null || !user.ifApManager(apname)) return;
    
    NUIForm form = AppManager.getFormPage(apname, path, sheetIndex);
    if(form==null) return;
    CellInfo info = form.getField(domainId);
    if(info==null) return;
    String currentMvpString = path+"|"+form.getFormName();
    Set<Integer> parentFormRootDomainIds = Account.getInstance(apname).getParentFormRootDomainIds(rootDomainId);
    List<String> linkedByOtherFieldMsg = new ArrayList<>();
    List<String> loadedByOtherFieldMsg = new ArrayList<>();

    //show all versions of configurations
    out.println("<div class='fieldAnalyzerDiv'>"+
            "<div style='margin:0 0 12px 0;'>"+bundle.getString("externalId")+": "+domainId+"</div>"+
            bundle.getString("versionComparison")+
            "<table>");
    out.println("<tr><th>"+bundle.getString("sheetName")+"</th><th>"+bundle.getString("fieldName")+"</th><th>"+bundle.getString("fieldConfig")+"</th><th>"+bundle.getString("linkingRelationship")+"</th></tr>");
    Collection<MenuTab> tabs=Account.getInstance(apname).getMenuConfiguration().getMenuSetFullSheets();
    for(MenuTab tab:tabs){
        for(MenuSheet sheet:tab.listSheets(user, Const.ADMIN)){
            NUIForm otherForm=sheet.getFormPage();
            if(otherForm==null) {
                otherForm=sheet.getListingPage();
                if(otherForm!=null){
                    otherForm=otherForm.autoFormGen();
                }
                if(otherForm==null) continue;
            }
            boolean isParentForm = parentFormRootDomainIds!=null && parentFormRootDomainIds.contains(otherForm.getRootDomainId());
            boolean isMultipleVersion = otherForm.getRootDomainId()==rootDomainId && !otherForm.getUrl().equals(form.getUrl());
            JSONArray replaceArr = new JSONArray();
            replaceArr.put(otherForm.getRealname());

            for(Map.Entry<CellLocation, CellInfo> e : otherForm.getFields().entrySet()){
                CellLocation otherLoc = e.getKey();
                CellInfo otherInfo = e.getValue();
                boolean isOtherFieldSingle = otherForm.isSingleCell(otherLoc);
                if(otherInfo.getDomainId()==domainId){
                    out.println("<tr><td><a href='/"+otherForm.getUrl()+"' target='_blank'>"+otherForm.getRealname()+"</td><td>"+otherInfo.getValue());
                    if (!isOtherFieldSingle) {
                        out.println("<span style='font-size:12px;color:#aaa;margin-left:3px;'>("+bundle.getString("subtableField")+")</span>");
                    }
                    out.println("</td><td>"+otherInfo.getAttributeSummaryString(apname,bundle,otherForm)+"</td><td style='min-width:50px;'>");
                    if (isParentForm) {
                        out.println(bundle.getString("parentSheet"));
                        if (info.getAttribute("interm") != null) {
                            out.println("<a href='javascript:void(0);' style='margin-left:2px;' onclick='showSingleTooltip(this,\"intermDesc\",false,true,false,true,false,"+replaceArr+");' class='tooltipQ'><i class='fas fa-question'></i></a>");
                        }
                    }
                    if (isMultipleVersion) {
                        out.println(bundle.getString("multipleVersionSheet"));
                    }
                    out.println("</td></tr>");
                }

                //if linked by other fields
                if(Encoder.parseInt(otherInfo.getAttribute("stf"))==domainId && currentMvpString.equals(otherInfo.getAttribute("mvp"))){
                    linkedByOtherFieldMsg.add("<tr><td><a href='/"+otherForm.getUrl()+"' target='_blank'>"+otherForm.getRealname()+"</a></td><td>"+otherInfo.getValue()+
                            "</td><td style='min-width:50px;'>"+bundle.getString("linked")+"</td></tr>");
                }

                //if loaded by other fields
                if(Encoder.parseInt(otherInfo.getAttribute("vd"))==domainId){
                    CellInfo listenField=otherForm.getField(Encoder.parseInt(otherInfo.getAttribute("l")));
                    if(listenField!=null && currentMvpString.equals(listenField.getAttribute("mvp"))){
                        loadedByOtherFieldMsg.add("<tr><td><a href='/"+otherForm.getUrl()+"' target='_blank'>"+otherForm.getRealname()+"</a></td><td>"+otherInfo.getValue()+
                                "</td><td style='min-width:50px;'>"+bundle.getString("loaded")+"</td></tr>");
                    }
                }
            }
        }
    }
    out.println("</table></div>");
    
    
    //show if this field is linked or loaded by any other fields [BugId:09295]
    if (linkedByOtherFieldMsg.size() > 0 || loadedByOtherFieldMsg.size() > 0) {
        out.println("<div class='fieldAnalyzerDiv'>"+bundle.getString("linkedOrLoadedByFollowing")+"<table><tr><th>"+bundle.getString("sheetName")+"</th><th>"+bundle.getString("fieldName")+"</th><th>"+bundle.getString("linkingRelationship")+"</th></tr>");
        for (String msg : linkedByOtherFieldMsg) {
            out.println(msg);
        }
        for (String msg : loadedByOtherFieldMsg) {
            out.println(msg);
        }
        out.println("</table></div>");
    }


    //show history versions of configurations, not including multiple versions
    out.println("<div class='fieldAnalyzerDiv' id='fieldDesignHistoryContainer' data-di='"+domainId+"' data-rdi='"+rootDomainId+"'>");
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "fieldDesignHistory.jsp" + "?" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("all", request.getCharacterEncoding())+ "=" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("false", request.getCharacterEncoding()), out, false);

    out.println("</div>");

    //Detect multiple nodes linking to the same key field for non multi selection or upload files
    if(!Const.MSELECT.equals(info.getType()) && !Const.GRAPHIC.equals(info.getType()) && !Const.FILE.equals(info.getType()) ){
        BDBWorker worker=new BDBWorker(apname);
        int duplicateLinkCount=0;
        int keyfieldHasMultipleNodes=0;
        try{
            Set<Integer> listOfLinkedNodeIds= new HashSet<>();
            SDBQuery q=new SDBQueryFull(worker);
            CellLocation loc=form.getCellLocation(info.getDomainId());
            int parentDomainId=form.isSingleCell(loc)?form.getRootDomainId():form.getTableHeaderDomain(info.getDomainId());
            q.go(parentDomainId);
            SDBNode keyNode;
            while((keyNode=q.next())!=null){
                List<SDBNode> nodesFree=q.dipAllNodes(info.getDomainId());
                if(nodesFree==null) continue;
                if(nodesFree.size()>1){
                    keyfieldHasMultipleNodes++;
                }

                for(SDBNode nodeFree:nodesFree){
                    if(listOfLinkedNodeIds.contains(nodeFree.getId())) {
                        duplicateLinkCount++;
                    }
                    listOfLinkedNodeIds.add(nodeFree.getId());
                }
            }
            if(Const.TEXT.equals(info.getType())){
                if(duplicateLinkCount>0){
                    out.println("<div class='fieldAnalyzerDiv'> Warning: Text field with multiple parent nodes: "+duplicateLinkCount+" records. <a href='javascript:void(0);' onclick='fixSelection2Free("+parentDomainId+","+domainId+")'>Fix this</a></div>");
                }
            }
            else if(Const.SELECT.equals(info.getType())){
                if(keyfieldHasMultipleNodes>0){
                    out.println("<div class='fieldAnalyzerDiv'> Warning: Single selection field with multiple values. Records affected: "+keyfieldHasMultipleNodes+" <a href='javascript:void(0);' onclick='dedup();'>Fix this</a></div>");
                }
            }
        }
        finally{
            worker.commit();
        }

    }

    if(nodeId>=0){
        BDBWorker worker=new BDBWorker(apname);
        try{
            SDBNode node=worker.getNode(domainId,nodeId);
            if(node!=null) out.println("<div class='fieldAnalyzerDiv' style='font-size:12px;color:#aaa;'>Raw:<input type='text' value='"+node.getValue()+"' style='padding:0;' /><br>Node id:"+node.getId()+"<br>Attr:"+info.getAttributeString()+"<br>LOV:"+info.getLOVString(apname,";")+"</div>");
        }
        finally{
            worker.commit();
        }
    }
    else{
        out.println("<div class='fieldAnalyzerDiv' style='font-size:12px;color:#aaa;'>Raw:N/A<br>Node id:N/A<br>Attr:"+info.getAttributeString()+"<br>LOV:"+info.getLOVString(apname,";")+"</div>");
    }
    //todo for linked field, show if source sheet deleted this


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
