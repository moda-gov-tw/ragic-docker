/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.nui.helper.Encoder;
import org.json.JSONObject;
import org.json.JSONArray;
import com.ragic.s3.util.Const;
import org.json.JSONException;
import java.util.*;
import com.ragic.s3.service.api.APIResultsFull;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.data.mass.create.DomainIdAndValueObj;
import com.ragic.s3.nui.helper.data.mass.create.MassCreate;
import com.ragic.s3.util.SystemTaskProgress;
import com.ragic.s3.postoffice2.RagicMessageV2;
import com.ragic.s3.postoffice2.MessageFacade;

public final class massCreate_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.MessageFacade");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResultsFull");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemTaskProgress");
    _jspx_imports_classes.add("com.ragic.s3.postoffice2.RagicMessageV2");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("org.json.JSONException");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.data.mass.create.MassCreate");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.data.mass.create.DomainIdAndValueObj");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    //load basic info of sheet
    final RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    final User user=(User)session.getAttribute("USER");
    final String apName =request.getParameter("a");
    final String path=request.getParameter("p");
    final int sheetIndex= Encoder.parseInt(request.getParameter("si"));

    MassCreate massCreate = new MassCreate(session , apName ); //大量產生的輔助類別

    //load info of mass creation
    final String strCreateRule = request.getParameter("createRule");
    final String strDefaultDomainIdAndValue = request.getParameter("fieldDefaultValue");
    JSONArray jsonCreateRule = new JSONArray(strCreateRule);
    JSONArray jsonDefaultDomainIdAndValue = new JSONArray(strDefaultDomainIdAndValue);
    //System.out.println(jsonCreateRule);
    //System.out.println(jsonDefaultDomainIdAndValue);


    //取得createRule相關的資訊
    JSONObject CreateRule = null;
    JSONObject result = null;
    String createType = "";
    String dateFormat = "";
    int createRuleDomainId = Const.NODOMAIN;
    try{
        CreateRule = jsonCreateRule.getJSONObject(0);
        createType = CreateRule.getString("createType");
        result = CreateRule.getJSONObject("result");
        dateFormat = CreateRule.optString("dateFormat","");
        createRuleDomainId = jsonDefaultDomainIdAndValue.getJSONObject(0).optInt("field",Const.NODOMAIN);//取得 大量產生的值要放在哪個domainId
    }catch( JSONException e){
        System.out.println("massCreate.jsp 初始資料取得例外");
        System.out.println(e.getMessage());
        massCreate.setJsonOutValue(massCreate.ERROR_OCCUR,"Input Data Error<br />");
        out.print(massCreate.outputJsonOut());
        return;
    }

    //使用者自訂的欄位預設值會放在defaultDomainIdAndValueList裡
    List<DomainIdAndValueObj> defaultDomainIdAndValueList = new ArrayList<>();

    //開始大量產生
    try {
        //產生defaultDomainIdAndValueList
        for(int i = 1; i < jsonDefaultDomainIdAndValue.length() ; i++ ){
            JSONObject obj = jsonDefaultDomainIdAndValue.getJSONObject(i);
            int domainId = obj.optInt("field",Const.NODOMAIN);
            String value = obj.optString("value","");
            defaultDomainIdAndValueList.add( new DomainIdAndValueObj(domainId,value) );
        }

        //根據massCreate設定的規則自動產生
        if( createType.equals(bundle.getString("massCreateCategory_numbericRange")) ){//以數字範圍產生
            //取得數字範圍
            String value1 = result.getString("itemId1") , value2 = result.getString("itemId2");
            int rangeStart = Integer.parseInt(value1.trim()) , rangeEnd = Integer.parseInt(value2.trim());
            //把比較小的放在rangeStart , 比較大的放在rangeEnd
            if( rangeStart > rangeEnd ){
                int t = rangeStart;
                rangeStart = rangeEnd;
                rangeEnd = t;
            }
            //以數字範圍大量產生
            for( int numberValue = rangeStart ; numberValue <= rangeEnd ; numberValue++ ){
                DomainIdAndValueObj DomainIdAndValueObj = new DomainIdAndValueObj(createRuleDomainId,String.valueOf(numberValue) );
                massCreate.createDataToSheet( DomainIdAndValueObj , defaultDomainIdAndValueList , apName, path , sheetIndex );
            }
        }else if( createType.equals(bundle.getString("massCreateCategory_hourlyCreate")) ){//以每小時產生
            String value1 = result.getString("itemId1") , value2 = result.getString("itemId2");
            massCreate.processDateCreate( value1 , value2 , dateFormat , "HOUR" , defaultDomainIdAndValueList , createRuleDomainId , apName, path , sheetIndex );
        }else if( createType.equals(bundle.getString("massCreateCategory_dailyCreate")) ){//以每天產生
            String value1 = result.getString("itemId1") , value2 = result.getString("itemId2");
            massCreate.processDateCreate( value1 , value2 , dateFormat , "DAY" , defaultDomainIdAndValueList , createRuleDomainId , apName, path , sheetIndex );
        }else if( createType.equals(bundle.getString("massCreateCategory_weeklyCreate")) ){//以每週產生
            String value1 = result.getString("itemId1") , value2 = result.getString("itemId2");
            massCreate.processDateCreate( value1 , value2 , dateFormat , "WEEK" , defaultDomainIdAndValueList , createRuleDomainId , apName, path , sheetIndex );
        }else if( createType.equals(bundle.getString("massCreateCategory_monthlyCreate")) ){//以每月產生
            String value1 = result.getString("itemId1") , value2 = result.getString("itemId2");
            massCreate.processDateCreate( value1 , value2 , dateFormat , "MONTH" , defaultDomainIdAndValueList , createRuleDomainId , apName, path , sheetIndex );
        }else if( createType.equals(bundle.getString("massCreateCategory_yearlyCreate")) ){//以每年產生
            String value1 = result.getString("itemId1") , value2 = result.getString("itemId2");
            massCreate.processDateCreate( value1 , value2 , dateFormat , "YEAR" , defaultDomainIdAndValueList , createRuleDomainId , apName, path , sheetIndex );
        }else if( createType.equals(bundle.getString("massCreateCategory_group")) ){//以群組大量產生
            String value1 = result.getString("itemId1"); //取得使用者點選的群組名稱

            String group = value1;
            List<String> userNames = new ArrayList<>();
            Account account = Account.getInstance(apName);

            if( group.equals(bundle.getString("massCreate_AllActiveUsers")) ){ //如果使用者點選的不是群組名，而是點選 所有使用者
                userNames.addAll(account.getActiveUsers());
            }else if( group.equals(bundle.getString("massCreate_AllInternalUsers")) ){ //如果使用者點選的不是群組名，而是點選 所有內部使用者
                userNames.addAll(account.getInternalUsers());
            }else if( group.equals(bundle.getString("massCreate_AllExternalUsers")) ){ //如果使用者點選的不是群組名，而是點選 所有外部使用者
                userNames.addAll(account.getExternalUsers());
            }else{ //使用者點選的是一般的群組名稱
                Map<String,List<String>> groupToUserNames = massCreate.getGroupToUserNames( apName );
                userNames = groupToUserNames.get(group); //取得此群組下的所有user名稱
            }

            //以群組使用者大量產生
            for( String userName : userNames ){
                DomainIdAndValueObj DomainIdAndValueObj = new DomainIdAndValueObj( createRuleDomainId,userName );
                massCreate.createDataToSheet( DomainIdAndValueObj , defaultDomainIdAndValueList , apName, path , sheetIndex );
            }

        }else if( createType.equals(bundle.getString("massCreateCategory_selectOption")) ){//以選擇欄位的選項產生
            String value1 = result.getString("itemId1"); //取得使用者點選的選擇欄位之domainId
            int fieldDomainId = Integer.parseInt(value1);

            List<String> options = Account.getInstance(apName).getLOV(fieldDomainId); //取得選擇欄位的選項值list

            //以選擇欄位的選項大量產生
            for( String option : options ){
                DomainIdAndValueObj DomainIdAndValueObj = new DomainIdAndValueObj( createRuleDomainId,option );
                massCreate.createDataToSheet( DomainIdAndValueObj , defaultDomainIdAndValueList , apName, path , sheetIndex );
            }
        }
        else if( createType.equals(bundle.getString("massCreateCategory_linkSheet")) ){//以表單資料產生
            JSONObject value1 = result.getJSONObject("itemId1");
            String selectedSheetPath = value1.getString("path"); //使用者選擇的表單path
            int selectedSheetIndex = Integer.parseInt(value1.getString("sheetIndex")); //使用者選擇的表單sheetIndex
            int fieldDomainId = Integer.parseInt(value1.getString("field")); //使用者選擇的表單欄位之domainId
            String filterSeq = value1.getString("filter"); //使用者選擇的表單 共通篩選 之序號
            String workingSheetPath = path; //使用者當前所在的表單path
            int workingSheetIndex = sheetIndex; //使用者當前所在的表單sheetIndex

            //System.out.printf("當前工作表單:%s 目標表單:%s\n",workingSheetPath+workingSheetIndex ,selectedSheetPath+selectedSheetIndex );

            //先取得選擇的表單之資料
            APIQuery aq = new APIQuery(apName,selectedSheetPath,selectedSheetIndex);
            aq.setUpdateMode();
            aq.setUser(user);
            //套用共通篩選
            if( filterSeq != null && !filterSeq.isEmpty() && !"-1".equals(filterSeq) ){ //有設定過濾的話
                NUIForm form = AppManager.getListingPage(apName, selectedSheetPath, selectedSheetIndex );
                NUIFilterSet filterSet =  form.getDesignerFilterSet().get( Integer.parseInt(filterSeq) );
                aq.setFilters( filterSet.getFilters() );
            }

            APIResultsFull af = aq.getAPIResultsFull();
            while( af.hasNext() ){ //每個迴圈處理一筆資料 去抓每筆資料的 fieldDomainId 對應的值 ，然後塞到 當前的表單
                APIEntry ae = af.next();
                String fieldValue = ae.getFieldValue(fieldDomainId);
                //把這筆資料的selection title field的欄位值用來在目標表單產生一筆新資料
                DomainIdAndValueObj DomainIdAndValueObj = new DomainIdAndValueObj( createRuleDomainId,fieldValue );
                massCreate.createDataToSheet( DomainIdAndValueObj , defaultDomainIdAndValueList , apName, workingSheetPath , workingSheetIndex );
            }
        }
    }catch ( Exception e ){
        System.out.println("massCreate.jsp 處理資料取得例外");
        System.out.println(e.getMessage());
        e.printStackTrace();
        massCreate.setJsonOutValue(massCreate.ERROR_OCCUR,"Process Data Error<br />");
        out.print(massCreate.outputJsonOut());
        SystemTaskProgress.setStatus(apName, Const.STP_MASSCREATE, "Create Error", -1 , SystemTaskProgress.Status.ERROR );
        return;
    }

    RagicMessageV2 rm = MessageFacade.createMessage(apName, path, sheetIndex,massCreate.getCreatedEntryNodeIds(),user,bundle,false);
    MessageFacade.processOutMessage(rm);
    SystemTaskProgress.setStatus(apName, Const.STP_MASSCREATE, "Mass Create Done", -1 , SystemTaskProgress.Status.DONE );//左下角顯示已完成
    out.print(massCreate.showErrorMsgIfExist());

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
