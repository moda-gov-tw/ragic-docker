/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import java.net.URLDecoder;

public final class vdSelector_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("java.net.URLDecoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.util.HashMap");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    User user = (User) session.getAttribute("USER");
    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    String apname = request.getParameter("a");
    String source_path = request.getParameter("source_path");
    String source_formName = null;
    try {
        source_formName = URLDecoder.decode(request.getParameter("source_fn"), "UTF-8");
    } catch (Exception e) {}
    int source_loadedDomainId = Encoder.parseInt(request.getParameter("source_loadedDomainId"), Const.NODOMAIN); //vd
    int source_linkedDomainId = Encoder.parseInt(request.getParameter("source_linkedDomainId"), Const.NODOMAIN); //stf
    int rootDomainId = Encoder.parseInt(request.getParameter("rootDomainId"), Const.NODOMAIN);
    int loadedDomainId = Encoder.parseInt(request.getParameter("loadedDomainId"), Const.NODOMAIN);
    int linkedDomainId = Encoder.parseInt(request.getParameter("linkedDomainId"), Const.NODOMAIN);
    int x = Encoder.parseInt(request.getParameter("x"), 0);
    int y = Encoder.parseInt(request.getParameter("y"), 0);
    
    if (apname == null || source_path == null || source_formName == null
            || source_formName.length() == 0 || source_loadedDomainId == Const.NODOMAIN) {
        return;
    }

    NUIForm selectedForm = AppManager.getFormPage(apname, source_path, source_formName);
    if (selectedForm == null) return;
    Account account = Account.getInstance(apname);
    if (account == null) return;

    out.print("<a href='javascript:void(0);' onclick='openLinkAndLoad(\"" + source_path + "\",\"" + source_formName
            + "\")' style='line-height:25px;'>" + bundle.getString("openLinkManager") + "</a><br>");
    out.print(bundle.getString("loadFromSheet") + ":<br/>");
    out.print("<select style='width:180px;margin:5px 0 5px 12px;' disabled>");
    out.println("<option rootDomainId='" + selectedForm.getRootDomainId() + "' value='"
            + selectedForm.getFormName() + "'>" + selectedForm.getRealname() + "</option>");
    out.print("</select>");
    
    //[BugId:11079] record loaded relationship of fields in selected source form and those in this target form,
    //later we will use this relationship to determine if fields in selected source form should be displayed in options
    Map<Integer, Integer> loadedRelationship = new HashMap<>(); //source domainId -> target domainId
    List<NUIForm> copiedForms = account.getCopiedNUIForms(rootDomainId);
    for (NUIForm copiedForm : copiedForms) {
        for (CellInfo cInfo : copiedForm.getFields().values()) {
            if (Encoder.parseInt(cInfo.getAttribute("l"), Const.NODOMAIN) == linkedDomainId) {
                loadedRelationship.put(Encoder.parseInt(cInfo.getAttribute("vd"), Const.NODOMAIN), cInfo.getDomainId());
            }
        }
    }
    
    //render fields in chosen form for user to change vd
    out.print("<br/>" + bundle.getString("loadFromField") + ":<br/>");
    out.print("<select id='vdSelParam' name='vd' style='width:180px;margin:5px 0 5px 12px;' onchange='selChange(this);' " + (source_linkedDomainId == Const.NODOMAIN ? "disabled" : "") + ">");
    
    StringBuilder optionsStr = new StringBuilder();
    boolean hasSelected = false;
    Map<CellLocation, CellInfo> fields = selectedForm.getFields();
    for (Map.Entry<CellLocation, CellInfo> e : fields.entrySet()) {
        CellInfo info = e.getValue();
        if (source_linkedDomainId == Const.NODOMAIN && info.getDomainId() != source_loadedDomainId) continue; //[BugId:09699]
        if (!selectedForm.isSingleCell(e.getKey())) continue;
        if (info.getDomainId() == source_linkedDomainId) continue;
        
        //check if this field is loaded by other field that doesn't exist in the target sheet but any other multiple version of it [BugId:11079]
        if (info.getDomainId() == source_loadedDomainId) {} //this field has already been loaded, display it anyway
        else if (loadedRelationship.get(info.getDomainId()) != null) {
            int target_loadedDomainId = loadedRelationship.get(info.getDomainId());
            continue;
        }
        
        boolean isSelected = info.getDomainId() == source_loadedDomainId;
        if (isSelected) hasSelected = true;
        optionsStr.append("<option domainId='" + info.getDomainId() + "' value='" + info.getDomainId() + "'"
                + (info.getDomainId() == source_loadedDomainId ? " selected" : "") + ">" + info.getValue() + "</option>");
    }
    if (!hasSelected) optionsStr.insert(0, "<option selected></option>"); //[BugId:12944] display empty option if vd not exist
    
    out.print(optionsStr + "</select>");

    if (!hasSelected) {
        out.print("<div style='font-weight:400;line-height:1.5;padding-top:8px;color:red;'>" + Encoder.formatMessage(bundle.getString("removeLoadedAttrs"), ""+x, ""+y) + "</div>");
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
