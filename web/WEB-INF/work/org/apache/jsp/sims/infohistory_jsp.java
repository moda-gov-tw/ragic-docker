/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Encoder;
import java.util.Date;
import com.ragic.s3.util.Const;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.NUIForm;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.Files;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.data.bdb.summary.*;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.util.DConst;
import com.ragic.s3.sdb.query.SDBQuerySingleNode;
import com.ragic.s3.service.history.History;
import com.ragic.s3.service.history.formatter.*;
import org.json.JSONObject;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.service.history.HistoryRecord;
import java.util.Set;
import com.ragic.s3.nui.User;
import java.text.SimpleDateFormat;
import com.ragic.s3.nui.RagicBundle;
import java.util.HashMap;
import java.util.Map;
import org.json.JSONException;
import com.ragic.s3.util.ChronologyDate.RagicDateFormatter;

public final class infohistory_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.service.history.formatter");
    _jspx_imports_packages.add("com.ragic.s3.sdb.data.bdb.summary");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuerySingleNode");
    _jspx_imports_classes.add("java.nio.file.Files");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.service.history.History");
    _jspx_imports_classes.add("java.util.HashMap");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("org.json.JSONException");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("java.nio.file.Paths");
    _jspx_imports_classes.add("com.ragic.s3.util.ChronologyDate.RagicDateFormatter");
    _jspx_imports_classes.add("com.ragic.s3.service.history.HistoryRecord");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("java.nio.file.Path");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  String apname = request.getParameter("a");
  User user=(User) session.getAttribute("USER");
  if(user==null) user=new User();
  String path = request.getParameter("path") != null ? request.getParameter("path") : "";
  int sheetIndex = Encoder.parseInt(request.getParameter("si"), Const.NONODE);
  int domainId=Const.NODOMAIN, nodeId=Const.NONODE;
  try {
    domainId=Integer.parseInt(request.getParameter("d"));
  }
  catch (Exception e) {
  }
  try {
    nodeId=Integer.parseInt(request.getParameter("n"));
  }
  catch (Exception e) {
  }
  if (apname==null || domainId==Const.NODOMAIN || nodeId==Const.NONODE) {
    out.println("Data not available.");
    return;
  }

  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  final User fUser=user;
  Set<HistoryRecord> result;
  NUIForm thisForm = null;
  boolean ifArchiveHistoryFileExists = false;

  boolean haveStartTime = (request.getParameter("startTime") != null && request.getParameter("startTime").length() != 0);
  boolean haveFinishTime = (request.getParameter("finishTime") != null && request.getParameter("finishTime").length() != 0);
  final String editorID = (request.getParameter("editor") == null || request.getParameter("editor").length() == 0) ? null : request.getParameter("editor");
  final String fieldID = (request.getParameter("fieldID") == null || request.getParameter("fieldID").length() == 0) ? null : request.getParameter("fieldID");

  if(path != null && path.length() != 0 && sheetIndex != -1) thisForm = AppManager.getFormPage(apname, path, sheetIndex);

  if(thisForm != null) {
//    String thisPathSheet =  path + "_"+ sheetIndex + "_Sheet" + sheetIndex;
//    String pathStr = thisPathSheet.replace("/", "_");
    Path archiveFile = Paths.get("cust", "data", apname, "archive", "history");
    ifArchiveHistoryFileExists = Files.exists(archiveFile);
  }

  //final Map<String, Map<String,String>> m=AppManager.getFormNamesInApplication(apname,user, Const.VIEWER);
  final NUIForm fForm=thisForm;
  BDBWorker worker = new BDBWorker(apname);
  worker.setWhoami("infoHistory.jsp");
  try {
    SDBQuery q = new SDBQueryFull(worker);
    q.go(DConst.HISTORY_TIMESTAMP);
    q.filter(new SDBQuerySingleNode(worker, domainId, nodeId), false);
    if(haveStartTime || haveFinishTime) {
      Long startTimeLong = Encoder.parseLong(request.getParameter("startTime"),0);
      Long finishTimeLong = Encoder.parseLong(request.getParameter("finishTime"),new Date().getTime());

      q.filterRange(String.valueOf(finishTimeLong),String.valueOf(startTimeLong),false,true);
    }
    if(editorID!=null) q.filter(DConst.HISTORY_USERID,editorID);
    q.setReverse(true);
    result= History.retrieveHistory(q, smmaryType -> {
        // Skip notify user change histories. Remove this filter when these summary types are removed.
    	return SummaryType.NOTIFY_USERS_CHANGE != smmaryType &&
    			SummaryType.MASS_UPDATE_NOTIFY_USERS_CHANGE != smmaryType &&
    			SummaryType.MASS_ROLLBACK_NOTIFY_USERS_CHANGE != smmaryType &&
    			SummaryType.IMPORT_NOTIFY_USERS_CHANGE != smmaryType;
    		}, r -> {
      if(r.getPath().length() == 0) {
        // Special processing for [issue id: 04576]
        try {
          JSONObject content = new JSONObject(r.getContent());
          String url = content.optString("url");
          if(url != null) {
            String[] split = url.split("/");
            r.setPath("/" + split[1]);
            r.setPathSheet(r.getPath() + "/" + split[2] + "_Sheet" + split[2]);
          }
        } catch(Exception e) {
        }
      }

      boolean isMatch = true;
      if(fieldID != null){
        try {
          JSONObject content = new JSONObject(r.getContent());
          String contentStr = content.toString();
          if(!contentStr.contains(",\""+fieldID+"\":{") && !contentStr.contains("{\""+fieldID+"\":{")){
            isMatch = false;
          }
        } catch(Exception e){
          isMatch = false;
        }
      }

      if(fForm.checkRight(fUser)<Const.VIEWER) isMatch = false;
      //  if(m.containsKey(r.getPath())){
      //    Map m2=m.get(r.getPath());
      //    if(m2.containsKey(r.getPathSheet().substring(r.getPath().length()+1))) {
      //       return true;
      //    }
      //  }
      return isMatch;
    },0,100000);
  } finally {
    worker.commit();
  }

  RagicDateFormatter rdf = com.ragic.s3.util.Util.getDisplayDateTimeFormat(apname, true, bundle);
  if(result.size()>0){
    Map<String, NUIForm> recordFormMap = new HashMap<>();
    for(HistoryRecord r: result) {
      if(!recordFormMap.containsKey(r.getPathSheet())) {
        String p = r.getPath();
        String sn = r.getPathSheet().replace(r.getPath() + "/", "");
        NUIForm form = AppManager.getFormPage(apname, p, sn);
        if(form != null) recordFormMap.put(r.getPathSheet(), form);
      }

      if("/ragic-setup/5".equals(r.getPathSheet())) continue;
      String loggedTime = rdf.format(bundle.convertTime(new Date(r.getTimestamp())));
      String contentStr = "";

      try{
        JSONObject content = new JSONObject(r.getContent());
        String recordType = content.optString("type");
        if(recordType.startsWith("mass"))
          contentStr = r.processHistoryContentJSON(content, bundle, recordFormMap.get(r.getPathSheet()), nodeId, true, false, user, HistoryRecordFormatType.HTML_HISTORY);
        else
          contentStr = r.processHistoryContentJSON(content, bundle, thisForm == null? recordFormMap.get(r.getPathSheet()): thisForm, nodeId, false, false, user, HistoryRecordFormatType.HTML_HISTORY);
      } catch(JSONException e) {
        contentStr = Encoder.sanitizeExceptionHistoryContent(r.getContent());
      } catch(Exception e) {
        System.out.println("Processing history record JSON failed: " + e.getMessage());
        contentStr = r.getContent();
        e.printStackTrace();
      } finally {
        if(!contentStr.trim().isEmpty()) {
          
      out.write("<div class='historyRecord' onclick='closeUL(this);'>\r\n            <span style='color:#AAAAAA'>");
      out.print(loggedTime);
      out.write("</span>\r\n            ");
      out.print(contentStr);
      out.write("</div>\r\n          ");

        }
      }
    }
    
      out.write("<div style='color:#808080; margin: 5px 0 0 0; cursor:pointer; display:inline-block;' onclick='downloadHistory()'>\r\n              <i class='fa-fw far fa-arrow-circle-down' style='margin: 0 3px 0 0'></i>");
      out.print(bundle.getString("downloadHistory"));
      out.write("</div>\r\n");
 if(ifArchiveHistoryFileExists) { 
      out.write("<span> <i id=\"unarchiveHistoryIcon\" onclick=\"unarchiveHistory();\" class=\"far fa-fw  fa-info-circle\" style=\"cursor:pointer;color:#808080;margin:0 0 0 3px;\"></i></span>\r\n");
 } 

  }
  else{
    out.println(bundle.getString("nohistory"));
    if(ifArchiveHistoryFileExists) {
      
      out.write("<span> <i id=\"unarchiveHistoryIcon\" onclick=\"unarchiveHistory();\" class=\"far fa-fw  fa-info-circle\" style=\"cursor:pointer;color:#808080;margin:0 0 0 3px;\"></i></span>");

    }
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
