/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import org.json.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.service.execute.*;
import com.ragic.s3.service.execute.trigger.*;
import com.ragic.s3.service.workflow.ScriptResponse;
import com.ragic.s3.util.SystemCache;
import com.ragic.s3.util.SystemTaskProgress;
import java.util.*;
import com.ragic.s3.util.Const;
import com.ragic.s3.service.workflow.ScriptLogger;
import com.ragic.s3.service.workflow.ScriptManager;

public final class actionButton_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("org.json");
    _jspx_imports_packages.add("com.ragic.s3.service.execute");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("com.ragic.s3.service.execute.trigger");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ScriptLogger");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ScriptManager");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ScriptResponse");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemTaskProgress");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemCache");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  final String apname=request.getParameter("a"),
         path=request.getParameter("p");
  final HttpSession finalSession=session;
  Integer sheetIndex=-1;
  try{ sheetIndex=Integer.parseInt(request.getParameter("si")); }catch(NumberFormatException e){}
  Sheet sheet=AppManager.getSheetBySheetIndex(apname,path,sheetIndex);
  if(sheet==null) return;
  String buttonId=request.getParameter("bid");
  if(buttonId!=null && buttonId.contains("_")){
    buttonId=buttonId.substring(0,buttonId.indexOf("_"));
  }
  int nodeId=Encoder.parseInt(request.getParameter("nodeId"), Const.NONODE);
  boolean listing=request.getParameter("listing")!=null;
  boolean mobile = request.getParameter("mobile") != null;
  User user=(User)finalSession.getAttribute("USER");
  int secondaryRootDomainId=Encoder.parseInt(request.getParameter("srd"),-1);

  RagicBundle bundle=(RagicBundle) finalSession.getAttribute("RAGIC_BUNDLE");
//  NUIForm form=new NUIForm(apname,path,sheet.getFormName()+"_"+(secondaryRootDomainId==-1?"index":secondaryRootDomainId)+(listing?"_t":"")+".nui",true);
  NUIForm formPage=AppManager.getFormPage(apname,path,sheet.getFormName());
  NUIForm listingPage=AppManager.getListingPage(apname,path,sheet.getFormName());
  NUIForm myForm=listing?listingPage:formPage;
  String sheetScope=formPage.getSheetscope();
  ActionButton actionButton = myForm.getActionButton(buttonId);
  if(actionButton==null){
    out.println("floatWinError('Action button not exist.');");
    return;
  }


  boolean ifAllowed = actionButton.ifUserAllowed(apname,path,sheetIndex,user);
  if(!ifAllowed && !"diageo".equals(apname)) {
    out.println("floatWinError('You do not have access right to execute this action button.');");
    out.println("ctrl.currentController.resetBrowseLeft();ctrl.currentController.load('','',function(){if(mode==BROWSE) {browse._init();browse._focus();}});"); // enable button
    return;
  }

  String buttonScript = "";
  String buttonName = "No name";
  buttonScript = actionButton.getScript();
  buttonName = actionButton.getName();
  String execScript =sheetScope+Encoder.unformatNUIAttr(buttonScript);
  if(execScript.contains("importClass")) execScript ="try {load('nashorn:mozilla_compat.js');} catch (e) {}\n"+ execScript;
  
  ServiceExecuteEngine see=new ServiceExecuteEngine(apname,myForm);
  see.setTrigger(new ActionButtonTrigger(formPage.getApname(), formPage.getPath(), Integer.parseInt(formPage.getSheetIndex()), nodeId, buttonName));

  //make sure only one executed for one session
//  long now=new Date().getTime();
//  String startedTimeLong=SystemCache.getInstance().get("ACTION_BUTTON_START"+apname);
//  if(startedTimeLong!=null){
//    long startedTime=Long.parseLong(startedTimeLong);
//    long diff=(now-startedTime)/(1000*60);
//
//    //started more than 1 minutes ago, reset and execute
//    if(diff>1){
//      SystemCache.getInstance().put("ACTION_BUTTON_START"+apname,""+new Date().getTime());
//    }
//    else{
//      out.println("floatWin(null,'You already have an action button executing "+diff+" minutes ago, please wait for it to finish.')");
//      return;
//    }
//  }
//  else{
//    SystemCache.getInstance().put("ACTION_BUTTON_START"+apname,""+new Date().getTime());
//  }

  boolean ifAsync= execScript.trim().startsWith("//@async");
  //if async processing, remove flag when actually done
  ConfigCommand cc= () -> SystemCache.getInstance().remove("ACTION_BUTTON_START"+apname);

  //actual execute
  ScriptResponse sr=see.executeScript(user,bundle, execScript,ifAsync?cc:null,nodeId);

  //start systemTaskProgress
  if(execScript.contains("db.setSystemTaskProgress")){
    SystemTaskProgress.setStatus(apname, Const.STP_WFSCRIPT, "Initializing...", 0, SystemTaskProgress.Status.PROCESSING);
    out.println("updateSystemTaskProgress();");
  }

  ScriptLogger logger=sr.getLogger();
  if(logger!=null && logger.isToConsole()){
    out.println("debugConsole('"+logger.getOutputString()+"');");
  }

  //remove mutex flag
  if(!ifAsync) SystemCache.getInstance().remove("ACTION_BUTTON_START"+apname);


  if (mobile) {
    JSONObject jo=new JSONObject();
    jo.put("status",sr.getStatus());
    jo.put("message",sr.getMessage());
    jo.put("htmlMessage",sr.getHTMLMessage());
    jo.put("openURL",new JSONArray(sr.getOpenURLs()));
    jo.put("rootNodeId",sr.getRootNodeId());
    out.println(jo.toString());
    return;
  }


  if(sr==null) return;
  //process displayButtonNameAsMessage
  if(sr.isDisplayButtonNameAsMessage()){
      if(sr.getStatus().equals(ScriptResponse.SUCCESS)) {
          sr.insertMessage(bundle.getString("action_execution_success", new Object[] {formPage.getButtonName(Encoder.parseInt(buttonId))}));
      }
  }

  //display return message
  if(sr.getMessages().size()>0 && !sr.getMessage().equals("&nbsp;")){
      StringBuilder resultMsg = new StringBuilder();
      for(String msg: sr.getMessages()) {
          if(resultMsg.length() != 0) resultMsg.append("<br/>");
          resultMsg.append(Encoder.encjs(msg,true));
      }

      switch (sr.getStatus()) {
        case ScriptResponse.ERROR:
        case ScriptResponse.INVALID:
          out.println("floatWinError('"+resultMsg.toString()+"');");
          break;
        case ScriptResponse.DEBUG:
        case ScriptResponse.WARN:
          out.println("floatWinWarning('"+resultMsg.toString()+"');");
          break;
        case ScriptResponse.SUCCESS:
          out.println("floatWinSuccess('"+resultMsg.toString()+"',true);");
          break;
        default:
          out.println("floatWin(null,'"+resultMsg.toString()+"');");
      }
  }
  //reload
  out.println("ctrl.currentController.resetBrowseLeft();ctrl.currentController.load('','',function(){if(mode==BROWSE) {browse._init();browse._focus();}});");

  if(sr.getOpenURLs()!=null){
      List<String> urls = sr.getOpenURLs();
      for(String url: urls) {
        if(sr.isOpenUrlInNewTab()) out.println("window.open('"+url+"');");
        else {
          out.println("location.href='"+url+"';");
          break;
        }
      }
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
