/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.CellInfo;
import com.ragic.s3.nui.CellLocation;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.util.Const;
import org.json.JSONArray;
import org.json.JSONObject;
import java.io.File;
import java.util.Map;

public final class referenceCheckForField_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellLocation");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.nui.CellInfo");
    _jspx_imports_classes.add("org.json.JSONArray");
    _jspx_imports_classes.add("java.io.File");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  int domainId= Encoder.parseInt(request.getParameter("d"));
  RagicBundle bundle=(RagicBundle)session.getAttribute("RAGIC_BUNDLE");
  JSONArray ja =new JSONArray();

  String apname=request.getParameter("a");
  String path=request.getParameter("p");
  int sheetIndex=Encoder.parseInt(request.getParameter("si"));
  NUIForm currentForm= AppManager.getFormPage(apname,path,sheetIndex);
  //probably new sheet
  if(currentForm==null){
    out.println(ja.toString());
    return;
  }
  String myMvpString=path+"|"+currentForm.getFormName();

  if(apname==null || domainId==0){
    out.println(ja.toString());
    return;
  }
  
  boolean isRootDomainOfParentSheet=false;


  //check all nui form pages
  File home=new File(Util.getApEnvPath(apname));
  int count=0;
  for (File dir:home.listFiles()){
    if(!dir.isDirectory() || dir.isHidden() || dir.getName().endsWith(".del")) continue;
    for (File f : dir.listFiles()) {
      String name=f.getName();
      if (name.endsWith(".del")) continue;
      if (name.endsWith(".nui") && !(name.equals(currentForm.getFileName()) && ("/"+dir.getName()).equals(path))){//not the exact same nui

        NUIForm form=new NUIForm(apname, "/"+dir.getName(), name, true);
        if(!form.isInited() || !form.isUsable() || form.getType()==NUIForm.NUI_TABLE) continue;//form page only

        //check how many times did this field appear
        if(form.getField(domainId)!=null) count++;

        //link to parent sheet field on new sheets created from subtable
        if(form.getRootDomainId()==domainId && currentForm.getLevel(domainId)<= Const.SINGLE
                && form.ifSubtableKey(currentForm.getRootDomainId()) //[BugId:09582]
                ){
          JSONObject item=new JSONObject();
          item.put("msg",Encoder.formatMessage(bundle.getString("fieldNoDeleteParentSheet"),"/"+form.getUrl(),form.getRealname()));
          ja.put(item);
          isRootDomainOfParentSheet=true;
        }

        //right hand side fields for link and load tool
        for(CellInfo otherInfo:form.getFields().values()){
          //stf fields for select from other sheets
          if(Encoder.parseInt(otherInfo.getAttribute("stf"))==domainId && myMvpString.equals(otherInfo.getAttribute("mvp"))){
            JSONObject item=new JSONObject();
            item.put("msg",Encoder.formatMessage(bundle.getString("fieldNoDeleteLinkedField"),"/"+form.getUrl(),form.getRealname(),otherInfo.getValue()));
            ja.put(item);
          }
          //value source for loaded fields
          if(Encoder.parseInt(otherInfo.getAttribute("vd"))==domainId){
            CellInfo listenField=form.getField(Encoder.parseInt(otherInfo.getAttribute("l")));
            if(listenField!=null && myMvpString.equals(listenField.getAttribute("mvp"))){
              JSONObject item=new JSONObject();
              item.put("msg",Encoder.formatMessage(bundle.getString("fieldNoDeleteLoadedField"),"/"+form.getUrl(),form.getRealname(),otherInfo.getValue()));
              ja.put(item);
            }
          }

        }
      }
    }
  }
  
  
  JSONObject checkingInfo = currentForm.checkIfSubtableAliasOfParentSheet(domainId);
  boolean isSubtableAliasOfParentSheet = checkingInfo.optBoolean("isSubtableAliasOfParentSheet");
  if (isSubtableAliasOfParentSheet) {
    JSONObject parentFormInfo = checkingInfo.optJSONObject("parentFormInfo");
    String url = parentFormInfo.optString("url");
    String realname = parentFormInfo.optString("realname");
    String subtableName = parentFormInfo.optString("subtableName");
    
    JSONObject item=new JSONObject();
    item.put("msg","<div style='line-height:1.5;'>"+Encoder.formatMessage(bundle.getString("fieldNoDeleteReferenceSubtable"),"/"+url,realname,subtableName)+"</div>");
    ja.put(item);
  }
  
  //[BugId:11786] If this domain id is also a root domain id of its parent sheet, can't delete
  //[BugId:11871] If this domain id is also a subtable alias of its parent sheet, can't delete
  if(count>0 && !isRootDomainOfParentSheet && !isSubtableAliasOfParentSheet){//this field deleted is not the only one, so deleting it should not be a big problem
      out.println("[]");
  }
  else{
    out.println(ja.toString(2));
  }


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
