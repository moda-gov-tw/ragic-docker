/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.report.templates.ragic.dashboard;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.DataHelper;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.output.CellValueDisplayHelper;
import com.ragic.s3.nui.output.CellInfoDisplayInfoRetriever;
import com.ragic.s3.nui.report.DashboardAdder;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.service.api.APIResultsFull;
import com.ragic.s3.service.report.Report;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import java.math.BigDecimal;
import java.math.RoundingMode;
import java.text.DecimalFormat;
import java.util.*;
import java.util.regex.Pattern;

public final class report_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.output.CellValueDisplayHelper");
    _jspx_imports_classes.add("com.ragic.s3.nui.output.CellInfoDisplayInfoRetriever");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("java.text.DecimalFormat");
    _jspx_imports_classes.add("com.ragic.s3.nui.report.DashboardAdder");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResultsFull");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.service.report.Report");
    _jspx_imports_classes.add("java.math.BigDecimal");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.DataHelper");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("java.util.regex.Pattern");
    _jspx_imports_classes.add("java.math.RoundingMode");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  RagicConfig rc= RagicConfig.getInstance();
  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String cdnurl=rc.getProperty("CDN_URL");
  boolean newWindow=request.getParameter("newwin")!=null;
  String fontFamily = request.getParameter("fontFamily");
  double fontSizeScale = Double.parseDouble(request.getParameter("fontSize"));
  String boundaryMargin = request.getParameter("boundaryMargin");

  APIQuery aq=(APIQuery)request.getAttribute("REPORT_APIQUERY");
  String apname = aq.getApname();
  Report report=(Report)session.getAttribute("UNSAVED_REPORT"+aq.getApname());

  HttpSession psSession = (HttpSession) request.getAttribute("psSession");
  if(psSession != null) {
    bundle =(RagicBundle) psSession.getAttribute("RAGIC_BUNDLE");
    if(psSession.getAttribute("UNSAVED_REPORT"+aq.getApname()) != null) {
      report = (Report)psSession.getAttribute("UNSAVED_REPORT"+aq.getApname());
    }
  }


      out.write("<div id=\"reportMain\" style=\"");
      out.print(!fontFamily.equals("")?("font-family: "+fontFamily+";"):"");
      out.print(fontSizeScale != 1?"font-size: "+Math.round(16*fontSizeScale)+"px;":"");
      out.write(" margin: ");
      out.print(boundaryMargin);
      out.write(";\">\r\n\r\n");

  int factDomainId= Encoder.parseInt(report.getValue("fact"), Const.NODOMAIN);
  boolean average="avg".equals(report.getValue("agg"));

  int addSelectionDomainId=Encoder.parseInt(request.getParameter("addsel"), Const.NODOMAIN);
  List<Integer> addSelectionList=(List<Integer>)session.getAttribute("ADDSEL");
  if(addSelectionList==null) addSelectionList= new ArrayList<>();
  if(addSelectionDomainId!=Const.NODOMAIN){
    addSelectionList.add(addSelectionDomainId);
    session.setAttribute("ADDSEL",addSelectionList);
  }


  //find the numeric and selection fields that we should count
  NUIForm form=aq.getForm();
  List<CellInfo> factFields=form.getFactFields(true);
  List<CellInfo> selectionFields=form.getSelectionFields(true);
  List<CellInfo> dateFields=form.getTimeFields(true);
  CellInfo createDate=new CellInfo(bundle.getString("createDate"));
  createDate.setDomainId(DConst.ENTRY_CREATE_DATE);
  createDate.setAttribute("fmt_d","yyyy/MM/dd HH:mm:ss");
  dateFields.add(0,createDate);


  DashboardAdder adder=new DashboardAdder();
  CellInfo factField=form.getField(factDomainId);
  if(factField==null){
    factDomainId = Const.NODOMAIN;
  }
  String factFieldName=factDomainId==Const.NODOMAIN?bundle.getString("numOfEntries"):factField.getValue();
  int factSubtableKey=form.isSingleCell(form.getCellLocation(factDomainId))? 0 : form.getTableHeaderDomain(factDomainId);
  String factFormat=factDomainId==Const.NODOMAIN?"#,###.#":Encoder.unformatNUIAttr(factField.getAttribute("fmt_n"));
  String fact_ts=factDomainId==Const.NODOMAIN?",":factField.getAttribute("fmt_n_ts");
  fact_ts=fact_ts==null?",":Encoder.unformatNUIAttr(fact_ts);
  String fact_ds=factDomainId==Const.NODOMAIN?",":factField.getAttribute("fmt_n_ds");
  fact_ds=fact_ds==null?".":Encoder.unformatNUIAttr(fact_ds);
  boolean ifFactInSubtable=factSubtableKey>0;
  boolean ifFactIsCount=factDomainId==Const.NODOMAIN;

  //add the extra selections
  if(addSelectionList!=null && addSelectionList.size()>0){
    for(int addedSelection:addSelectionList){
      CellInfo info=form.getField(addedSelection);
      selectionFields.add(info);
    }
  }

  //set the fetch domains for better performance
  for(CellInfo info:selectionFields) {
    aq.addFetchDomains(info.getDomainId());
    
    CellLocation infoLocation = form.getCellLocation(info.getDomainId());
    
    //add first cell of subtable to fetchDomains or getAPIResults won't get data in subtable
    if (!form.isSingleCell(infoLocation)) {
      CellLocation firstTableCellLoc = form.getFirstTableCell(infoLocation);
      CellInfo firstTableCell = form.getField(firstTableCellLoc);
      
      if (!aq.getFetchDomains().contains(firstTableCell.getDomainId())) {
        aq.addFetchDomains(firstTableCell.getDomainId());
      }
    }
  }
  for(CellInfo info:factFields) {
    aq.addFetchDomains(info.getDomainId());
    
    CellLocation infoLocation = form.getCellLocation(info.getDomainId());
    
    //add first cell of subtable to fetchDomains
    if (!form.isSingleCell(infoLocation)) {
        CellLocation firstTableCellLoc = form.getFirstTableCell(infoLocation);
        CellInfo firstTableCell = form.getField(firstTableCellLoc);
        
        if (!aq.getFetchDomains().contains(firstTableCell.getDomainId())) {
          aq.addFetchDomains(firstTableCell.getDomainId());
        }
    }
  }
  for(CellInfo info:dateFields) {
    aq.addFetchDomains(info.getDomainId());
    
    CellLocation infoLocation = form.getCellLocation(info.getDomainId());
    
    //add first cell of subtable to fetchDomains
    if (infoLocation != null && !form.isSingleCell(infoLocation)) {
      CellLocation firstTableCellLoc = form.getFirstTableCell(infoLocation);
      CellInfo firstTableCell = form.getField(firstTableCellLoc);
      
      if (!aq.getFetchDomains().contains(firstTableCell.getDomainId())) {
        aq.addFetchDomains(firstTableCell.getDomainId());
      }
    }
  }



  APIResultsFull ar = aq.getAPIResultsFull();
  int entryCount = 0;
  for (APIEntry e : ar) {
    e.setIfTranslateEmailToUserName(true);

    //entry count as fact
    if(ifFactIsCount){
      String factAmount="1";
      for(CellInfo info:factFields){
        if(form.isSingleCell(form.getCellLocation(info.getDomainId()))) {//single field
          String value=e.getFieldValue(info.getDomainId());
          adder.addAsNumber(info.getDomainId(),value,factAmount);
        }
        else{//subtable field
          int subtableKey=form.getTableHeaderDomain(info.getDomainId());
          int subtableSize=e.getSubtableSize(subtableKey);
          for(int sub=0;sub<subtableSize;sub++){
            adder.addAsNumber(info.getDomainId(),e.getSubtableFieldValue(subtableKey,sub,""+info.getDomainId()),factAmount);
          }
        }
      }
      for(CellInfo info:dateFields){
        if(form.isSingleCell(form.getCellLocation(info.getDomainId()))) {//single field
          String value=e.getFieldValue(info.getDomainId());
          if(info.getDomainId()==DConst.ENTRY_CREATE_DATE) value=e.getFieldValue("_create_date");
          adder.addAsDate(info.getDomainId(), value,factAmount);
        }
        else{//subtable field
          int subtableKey=form.getTableHeaderDomain(info.getDomainId());
          int subtableSize=e.getSubtableSize(subtableKey);
          for(int sub=0;sub<subtableSize;sub++){
            adder.addAsDate(info.getDomainId(),e.getSubtableFieldValue(subtableKey,sub,""+info.getDomainId()),factAmount);
          }
        }
      }
      for(CellInfo info:selectionFields){
        if(form.isSingleCell(form.getCellLocation(info.getDomainId()))) {//single field
          if(Const.MSELECT.equals(info.getType())){
            List<String> values=e.getFieldValues(info.getDomainId());
            for(String s:values){
              if(s==null || s.length()==0) {
                s="<span style='color:#ccc;'>"+bundle.getString("empty")+"</span>";
              }
              adder.addAsString(info.getDomainId(),s,factAmount);
            }
          }
          else{
            String value=e.getFieldValue(info.getDomainId());
            if(value==null || value.length()==0){
              value="<span style='color:#ccc;'>"+bundle.getString("empty")+"</span>";
            }
            adder.addAsString(info.getDomainId(),value,factAmount);
          }          }
        else{//subtable field
          int subtableKey=form.getTableHeaderDomain(info.getDomainId());
          int subtableSize=e.getSubtableSize(subtableKey);
          for(int sub=0;sub<subtableSize;sub++){
            String value=e.getSubtableFieldValue(subtableKey,sub,""+info.getDomainId());
            if(value==null || value.length()==0){
              value="<span style='color:#ccc;'>"+bundle.getString("empty")+"</span>";
            }
            adder.addAsString(info.getDomainId(),value,factAmount);
          }
        }

      }
      adder.addCount();
    }
    //subtable field as fact
    else if(ifFactInSubtable){
      for(CellInfo info:factFields){
        if(form.isSingleCell(form.getCellLocation(info.getDomainId()))) {//single field
          String value=e.getFieldValue(info.getDomainId());
          adder.addAsNumber(info.getDomainId(),value,null);
        }
        else{//subtable field
          int subtableKey=form.getTableHeaderDomain(info.getDomainId());
          if(subtableKey>=0 && subtableKey!=factSubtableKey) continue;//skip fields that are not in the same subtable
          int subtableSize=e.getSubtableSize(subtableKey);
          for(int sub=0;sub<subtableSize;sub++){
            adder.addAsNumber(info.getDomainId(),e.getSubtableFieldValue(subtableKey,sub,""+info.getDomainId()),null);
          }
        }
      }
      for(CellInfo info:dateFields){
        int subtableKey=form.getTableHeaderDomain(info.getDomainId());
        if(subtableKey>=0 && subtableKey!=factSubtableKey) continue;//skip fields that are not in the same subtable
        int subtableSize=e.getSubtableSize(subtableKey);
        for(int sub=0;sub<subtableSize;sub++){
          String factAmount=e.getSubtableFieldValue(subtableKey,sub,""+factDomainId);
          String value;
          if(subtableKey>=0){//in subtable
            value=e.getSubtableFieldValue(subtableKey,sub,""+info.getDomainId());
          }
          else{//single field
            value=e.getFieldValue(""+info.getDomainId());
          }
          adder.addAsDate(info.getDomainId(),value,factAmount);
        }
      }
      for(CellInfo info:selectionFields){
        int subtableKey=form.getTableHeaderDomain(info.getDomainId());
        int subtableSize=e.getSubtableSize(subtableKey);
        if(subtableKey>=0 && subtableKey!=factSubtableKey) continue;//skip fields that are not in the same subtable
        for(int sub=0;sub<subtableSize;sub++){
          String factAmount=e.getSubtableFieldValue(subtableKey,sub,""+factDomainId);
          String value;
          if(subtableKey>=0){//in subtable
            value=e.getSubtableFieldValue(subtableKey,sub,""+info.getDomainId());
          }
          else{//single field
            value=e.getFieldValue(""+info.getDomainId());
          }
          if(value==null || value.length()==0){
            value="<span style='color:#ccc;'>"+bundle.getString("empty")+"</span>";
          }
          adder.addAsString(info.getDomainId(),value,factAmount);
        }
      }
      adder.addCount();
    }
    //single field as fact
    else{
      String factAmount=e.getFieldValue(factDomainId);
      for(CellInfo info:factFields){
        String value=e.getFieldValue(info.getDomainId());
        adder.addAsNumber(info.getDomainId(),value,factAmount);
      }
      for(CellInfo info:dateFields){
        if(!form.isSingleCell(form.getCellLocation(info.getDomainId()))) continue;//don't show subtable date fields when fact is not subtable
        String value=e.getFieldValue(info.getDomainId());
        if(info.getDomainId()==DConst.ENTRY_CREATE_DATE) value=e.getFieldValue("_create_date");
        adder.addAsDate(info.getDomainId(), value,factAmount);
      }
      for(CellInfo info:selectionFields){
        if(!form.isSingleCell(form.getCellLocation(info.getDomainId()))) continue;//don't show subtable selection fields when fact is not subtable
        if(Const.MSELECT.equals(info.getType())){
          List<String> values=e.getFieldValues(info.getDomainId());
          for(String s:values){
            if(s==null || s.length()==0) {
              s="<span style='color:#ccc;'>"+bundle.getString("empty")+"</span>";
            }
            adder.addAsString(info.getDomainId(),s,factAmount);
          }
        }
        else{
          String value=e.getFieldValue(info.getDomainId());
          if(value==null || value.length()==0){
            value="<span style='color:#ccc;'>"+bundle.getString("empty")+"</span>";
          }
          adder.addAsString(info.getDomainId(),value,factAmount);
        }
      }
      adder.addCount();
    }
  }

  //output the data accumulated in the Adder
  DecimalFormat dfNum=new DecimalFormat("#,###.##");
  DecimalFormat dfPercent=new DecimalFormat("#,###.##%");
  DecimalFormat dfPercentCSS=new DecimalFormat("#.##");



  /*
   output the top numbers
  */

      out.write("<div domainId=\"0\" style=\"position:relative;float:left;width:200px;margin:12px;\"><div class=\"quickFactName\">");
      out.print(bundle.getString("numOfEntries"));
      out.write("</div><div style=\"font-size:");
      out.print(Math.round(25*fontSizeScale));
      out.write("px;padding:0;\">");
      out.print(dfNum.format(adder.getCount()));
      out.write("</div></div>");


  int lastSubtableKey=Const.NODOMAIN;
  Map<String, String> removedFields = new LinkedHashMap<>(); //(domainId, fieldName)
  
  for(CellInfo info:factFields){
    DashboardAdder.Numbers result=adder.getNumericResult(info.getDomainId());
    if(!ifFactInSubtable && !ifFactIsCount && !form.isSingleCell(form.getCellLocation(info.getDomainId()))) continue;//don't show subtable selection fields when fact is not subtable
    if(result==null) {
      //System.out.println("No result for field:"+info.getValue());
      continue;
    }

    boolean noTotal=report.ifValue("remove",""+info.getDomainId());
    boolean noAvg=report.ifValue("remove",info.getDomainId()+"avg");

    //adding sectioning for subtable
    int currentFactSubtableKey=form.isSingleCell(form.getCellLocation(info.getDomainId())) ? 0 : form.getTableHeaderDomain(info.getDomainId());
    if(currentFactSubtableKey!=lastSubtableKey && currentFactSubtableKey>0){
      lastSubtableKey=currentFactSubtableKey;
      String subtableName=form.getSubtableName(currentFactSubtableKey);

      out.write("<div style='float:left;clear:left;margin:8px 0;width:100%;border-bottom:1px solid #ccc;font-size:12px;color:#CCC;'>");
      out.print(subtableName==null?"":subtableName);
      out.write("</div>");

  }

  String format=Encoder.unformatNUIAttr(info.getAttribute("fmt_n"));
  String ts=info.getAttribute("fmt_n_ts");
  ts=ts==null?",":Encoder.unformatNUIAttr(ts);
  String ds=info.getAttribute("fmt_n_ds");
  ds=ds==null?".":Encoder.unformatNUIAttr(ds);
  String avgFormat=format;
  String[] neg=avgFormat.split(";");
  neg[0]=neg[0].contains(ds)?neg[0] : neg[0]+ds+"##";
  avgFormat=neg.length>1?neg[0]+";"+neg[1] : neg[0];
  DecimalFormat df=dfNum,dfavg=dfPercent;
  try{
    df=new DecimalFormat(format);
    dfavg=new DecimalFormat(avgFormat);
  }catch(Exception e){}

  if(!format.contains("%")){//if contains %, total must have no meaning

      out.write("<div domainId=\"");
      out.print(info.getDomainId());
      out.write("\" style=\"");
      out.print(noTotal?"display:none;":"");
      out.write("position:relative;float:left;width:200px;margin:12px;\"><div class=\"quickFactName\">");
      out.print(info.getValue());
      out.print(bundle.getString("space"));
      out.write("<span style=\"color:#ccc\">(");
      out.print(bundle.getString("total"));
      out.write(")</span></div><div style=\"font-size:25px;padding:0;\">");
      out.print(Encoder.formatNumber(result.getResult().toString(),format,ts,ds));
      out.write("</div></div>");

    if (noTotal) {
      removedFields.put(""+info.getDomainId(), info.getValue()+bundle.getString("space")+"<span style='color:#ccc'>("+bundle.getString("total")+")</span>");
    }
  }

  if(adder.getCount()>0 && result.getCount()>0){

      out.write("<div domainId=\"");
      out.print(info.getDomainId());
      out.write("avg\" style=\"");
      out.print(noAvg?"display:none;":"");
      out.write("position:relative;float:left;width:200px;margin:12px;\"><div class=\"quickFactName\">");
      out.print(info.getValue());
      out.print(bundle.getString("space"));
      out.write("<span style=\"color:#ccc\">(");
      out.print(bundle.getString("average"));
      out.write(")</span></div><div style=\"font-size:25px;padding:0;\">");
      out.print(Encoder.formatNumber(result.getResult().divide(new BigDecimal(result.getCount()==0?adder.getCount():result.getCount()),2, RoundingMode.HALF_UP).toString(),avgFormat,ts,ds));
      out.write("</div></div>");

      if (noAvg) {
        removedFields.put(info.getDomainId()+"avg", info.getValue()+bundle.getString("space")+"<span style='color:#ccc'>("+bundle.getString("average")+")</span>");
      }
    }
  }

  out.println("<div style='float:left;clear:left;margin:8px 0;width:100%;'>&nbsp;</div>");

  /*
   output the categorizations
  */

  List<CellInfo> selectAndDates= new ArrayList<>();
  selectAndDates.addAll(selectionFields);
  selectAndDates.addAll(dateFields);
  for(CellInfo info:selectAndDates){
    DashboardAdder.Types result=adder.getTypesResult(info.getDomainId());
    if(result==null) continue;
    boolean noDim=report.ifValue("remove",""+info.getDomainId());
    if (noDim) {
      removedFields.put(""+info.getDomainId(), info.getValue());
    }


    //adding sectioning for subtable
    int currentFactSubtableKey=form.isSingleCell(form.getCellLocation(info.getDomainId())) ? 0 : form.getTableHeaderDomain(info.getDomainId());
    if(currentFactSubtableKey!=lastSubtableKey && currentFactSubtableKey>0){
      lastSubtableKey=currentFactSubtableKey;
      String subtableName=form.getSubtableName(currentFactSubtableKey);

      out.write("<div style='float:left;clear:left;margin:8px 0;width:100%;border-bottom:1px solid #ccc;font-size:12px;color:#CCC;'>");
      out.print(subtableName==null?"":subtableName);
      out.write("</div>");

  }

  //create the selection of fact
  String options="<select onchange=\"updateConfig('fact',this.options[this.selectedIndex].value,false,true,false,null,'-1');\">";
  options+="<option value='-1'>"+bundle.getString("numOfEntries")+"</option>";
  int _lastSubtableKey=Const.NODOMAIN;
  for(CellInfo _info:factFields){
    //check if this field needs to be displayed in this context
    if(currentFactSubtableKey>0 && form.getTableHeaderDomain(_info.getDomainId())!=currentFactSubtableKey) continue;
    //do grouping
    int _currentFactSubtableKey=form.isSingleCell(form.getCellLocation(_info.getDomainId())) ? Const.NODOMAIN : form.getTableHeaderDomain(_info.getDomainId());
    if(_currentFactSubtableKey!=_lastSubtableKey){
      if(_lastSubtableKey!=Const.NODOMAIN){
        options+="</optgroup>";
      }
      _lastSubtableKey=_currentFactSubtableKey;
      String _subtableName=form.getSubtableName(_currentFactSubtableKey);
      if(_subtableName==null) _subtableName="------";
      options+="<optgroup label=\""+_subtableName+"\">";
    }
    options+="<option value='"+_info.getDomainId()+"' "+(factDomainId==_info.getDomainId()?"selected":"")+">"+_info.getValue()+"</option>";
  }
  if(_lastSubtableKey!=Const.NODOMAIN){
    options+="</optgroup>";
  }
  options+="</select>";
  if(factDomainId>0){
    options+="<select onchange=\"updateConfig('agg',this.options[this.selectedIndex].value,false,true,false,null,'total');\"><option value='total'>"+bundle.getString("total")+"</option><option value='avg' "+(average?"selected":"")+">"+bundle.getString("average")+"</option></select>";
  }


  BigDecimal totalAmount=average?new BigDecimal(0):result.getTotal();
  out.println("<div domainId='"+info.getDomainId()+"' style='"+(noDim?"display:none;":"")+"position:relative;float:left;"+(newWindow?"clear:left;width:100%;":"width:400px;height:250px;overflow:auto;")+"margin:12px;background:#ffffff;border:1px solid #efefef;border-radius:5px;padding:12px;'>");
  out.println("<table class='quicktable' style='"+ (fontSizeScale!=1?"font-size: "+Math.round(16*fontSizeScale)+"px;":"") +"'>");
  out.println("<td class=\"quickFactName\" style=\"margin:0 0 6px 0;\">"+info.getValue()+"</td>");
  out.println("<td class=\"quickFactName\" colspan=\"3\">"+options+"</td>");
  BigDecimal maxValue=result.getMaxValue(average);
  Selection selField;
  BDBWorker worker=new BDBWorker(form.getApname());
  try{selField=DataHelper.createOption(info.getDomainId(), worker);}finally{worker.commit();}
  for(Map.Entry<String,BigDecimal> e:result.getSortedResult(average).entrySet()){
    String type=e.getKey();
    if(selField.isCascaded()){
      for(SDBNode node:selField.getOptions()){
        if(node!=null && node.getValue()!=null && node.getValue().endsWith("|"+type)){
          type=node.getValue();
        }
      }
    }
    if (info != null && (info.getAttribute("fmt_d") != null || info.getAttribute("fmt_n") != null)) {
      CellInfoDisplayInfoRetriever retriever = new CellInfoDisplayInfoRetriever(info, type);
      type = CellValueDisplayHelper.formatDisplayValueAsPlainText(retriever, apname, -1, bundle);
    }
    BigDecimal amount=e.getValue();
    String finalValue = Encoder.processBBCode(type);
    if(finalValue.equals(type)){ //not a bbcode
      finalValue = Encoder.sanitizeHTML(finalValue);
    }

      out.write("<tr>\r\n  <td style=\"");
      out.print(newWindow?"":"white-space: nowrap;");
      out.write('"');
      out.write('>');
      out.print(finalValue);
      out.write("</td>\r\n  <td>");
      out.print(Encoder.formatNumber(amount.toString(),factFormat,fact_ts,fact_ds));
      out.write("</td>\r\n  ");

    if(average){
  
      out.write("<td colspan=\"2\"><div class='rankbar' style='width:");
      out.print(dfPercentCSS.format(maxValue.doubleValue()==0?0:amount.divide(maxValue,2,BigDecimal.ROUND_HALF_UP).multiply(new BigDecimal(200))));
      out.write("px'></div></td>\r\n  ");

  }else{
  
      out.write("<td>");
      out.print(dfPercent.format(totalAmount.intValue()==0?0:amount.divide(totalAmount,2,BigDecimal.ROUND_HALF_UP)));
      out.write("</td>\r\n  <td><div class='rankbar' style='width:");
      out.print(dfPercentCSS.format(totalAmount.intValue()==0?0:amount.divide(totalAmount,2,BigDecimal.ROUND_HALF_UP).multiply(new BigDecimal(200))));
      out.write("px'></div></td>\r\n  ");

    }
  
      out.write("</tr>\r\n");
}
  out.println("</table></div>");
}



      out.write("</div>\r\n<div id=\"removeReportDiv\" onclick=\"removeReportDiv();\" style=\"position:absolute;display:none;cursor:pointer;color:#ccc;\"><i class=\"fa fa-times\"></i></div>\r\n\r\n<div style='float:left;clear:left;margin:8px 0;width:100%;'>&nbsp;</div>\r\n<div id=\"removedDomainIdsContainer\" class=\"printHide\" style=\"clear:left;height:100px;margin:12px;border-radius:5px;padding:5px;");
      out.print(removedFields.size()>0?"":"display:none;");
      out.write("\">\r\n  <h2 style=\"font-weight:700;display:inline-block;\">");
      out.print(bundle.getString("reportHidingFields") );
      out.write("</h2>\r\n  <a href=\"javascript:void(0);\" onclick=\"showSingleTooltip(this,'HELP_REPORT_HIDING_FIELDS',false,true,false,true);\" class=\"tooltipS\" style=\"font-weight:bold;\"><i class=\"fas fa-question\"></i></a>\r\n  <div id=\"removedDomainIds\">\r\n  ");
for(String key:removedFields.keySet()){
      out.write("<div style=\"float:left;cursor:pointer;margin:5px;padding:4px;border:1px solid #efefef;border-radius:5px;\" data-domainid=\"");
      out.print(key );
      out.write("\" onclick=\"addReportDiv(this);\">");
      out.print(removedFields.get(key) );
      out.write("</div>\r\n  ");
}
      out.write("</div>\r\n</div>\r\n<script>\r\n  var currentReportDiv;\r\n  function removeReportDiv(){\r\n    currentReportDiv.style.display='none';\r\n    var domainId=currentReportDiv.getAttribute('domainId');\r\n    updateConfig('remove',domainId,true,false,false,displayDiscardChanges,\"\");\r\n    \r\n    var removedDiv = node('DIV');\r\n    removedDiv.innerHTML = currentReportDiv.querySelector('.quickFactName').innerHTML;\r\n    removedDiv.style.cssFloat = 'left';\r\n    removedDiv.style.cursor = 'pointer';\r\n    removedDiv.style.margin = '5px';\r\n    removedDiv.style.padding = '4px';\r\n    removedDiv.style.border = '1px solid #efefef';\r\n    removedDiv.style.borderRadius = '5px';\r\n    removedDiv.setAttribute('data-domainid', currentReportDiv.getAttribute('domainId'));\r\n    removedDiv.onclick = function(){addReportDiv(this);};\r\n    $('removedDomainIds').appendChild(removedDiv);\r\n    $('removedDomainIdsContainer').style.display = '';\r\n  }\r\n  function reportDivMouseover(event, obj){\r\n    if (event.target.tagName === 'SELECT') return;\r\n    currentReportDiv=obj;\r\n");
      out.write("    $('removeReportDiv').style.display='';\r\n    $('removeReportDiv').style.top='5px';\r\n    $('removeReportDiv').style.right='5px';\r\n    obj.appendChild($('removeReportDiv'));\r\n  }\r\n  function reportDivMouseout(event, obj){\r\n    $('removeReportDiv').style.display='none';\r\n  }\r\n  function addReportDiv(obj) {\r\n    var domainId = obj.getAttribute('data-domainid');\r\n    removeConfig('remove',domainId,true,\"\");\r\n  }\r\n\r\n  afterInitHooks.push(function(){\r\n    //onmouseover=\"reportDivMouseover(this);\"\r\n    for(var i=0;i<$('reportMain').childNodes.length;i++){\r\n      var div=$('reportMain').childNodes[i];\r\n      if(div.nodeType===3) continue;\r\n      div.onmouseover=function(event){reportDivMouseover(event, this);};\r\n      div.onmouseout=function(event){reportDivMouseout(event, this);};\r\n    }\r\n  });\r\n\r\n</script>");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
