/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import org.json.*;
import java.text.MessageFormat;
import java.util.*;

public final class linkDigest_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("org.json");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.text.MessageFormat");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


JSONObject digestResult = new JSONObject();
try {
    String apname = request.getParameter("a");
    String path = request.getParameter("p");
    String sheetIndex = request.getParameter("si");
    
    if (apname == null || path == null || sheetIndex == null) return;
    
    String currentURL = apname + path + "/" + sheetIndex;
    String level3KeyDomainId = request.getParameter("l3");
    boolean isThirdLayerSheet = level3KeyDomainId != null;
    boolean ifListingPage = "true".equals(request.getParameter("listing"));
    NUIForm form;
    if (!isThirdLayerSheet) {
        form = AppManager.getFormPage(apname, path, sheetIndex + "_Sheet" + sheetIndex);
    } else {
        String fileName = sheetIndex + "_Sheet" + sheetIndex + "_" + level3KeyDomainId + ".nui";
        form = new NUIForm(apname, path, fileName, true);
    }
    if(form == null) return;
    
    Account account = Account.getInstance(apname);
    if (account == null) return;
    RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    
    JSONArray digests_linkAndLoad = new JSONArray();
    JSONArray digests_multipleVersion = new JSONArray();
    JSONArray digests_newSheetFromSubtable = new JSONArray();
    JSONArray digests_referenceFromExistingSheet = new JSONArray();
    JSONArray digests_parentSheet = new JSONArray();
    digestResult.put("linkAndLoad", digests_linkAndLoad);
    digestResult.put("multipleVersion", digests_multipleVersion);
    digestResult.put("newSheetFromSubtable", digests_newSheetFromSubtable);
    digestResult.put("referenceFromExistingSheet", digests_referenceFromExistingSheet);
    digestResult.put("parentSheet", digests_parentSheet);
    
    
    //check for link and load
    Map<String,Integer> mvpLinks=new LinkedHashMap<>();
    Map<String,Integer> mvpLinkCounts = new LinkedHashMap<>();
    for (Map.Entry<CellLocation,CellInfo> e: form.getFields().entrySet()) {
        CellLocation loc=e.getKey();
        CellInfo info=e.getValue();
        String mvpLink = info.getAttribute("mvp");
        if(mvpLink!=null){
            mvpLinks.put(mvpLink+"|"+loc.getX()+"|"+loc.getY(),info.getDomainId());
            int count = 0;
            if(mvpLinkCounts.containsKey(mvpLink)) count = mvpLinkCounts.get(mvpLink);
            mvpLinkCounts.put(mvpLink, count + 1);
        }
    }
    
    for (Map.Entry<String, Integer> e : mvpLinks.entrySet()) {
        int linkedFieldId = e.getValue();
        String pickedUrl = e.getKey();
        if (pickedUrl.contains("/ragic-setup")) continue;
    
        String[] urlsec = pickedUrl.split("\\|");
        if(urlsec.length < 4) continue; // Just in case, not gonna happen
        
        String urlPath=urlsec[0];
        
        String urlFormName=urlsec[1];
        String[] urlFormNameSec=urlFormName.split("_");
        String sheetName=account.getSheetName(urlPath+"/"+urlFormNameSec[0]);
        if(sheetName==null) continue;
        int linkNum = mvpLinkCounts.containsKey(urlPath + "|" + urlFormName)? mvpLinkCounts.get(urlPath + "|" + urlFormName): 1;
        if(linkNum > 1) sheetName += "(" + form.getField(linkedFieldId).getValue() + ")";
            
        String specifyForm1LinkField = urlsec[2] + "|" + urlsec[3];
        
        JSONObject digest_linkAndLoad = new JSONObject();
        digest_linkAndLoad.put("urlPath", urlPath);
        digest_linkAndLoad.put("urlFormName", urlFormName);
        digest_linkAndLoad.put("sheetName", Encoder.encjs(sheetName));
        digest_linkAndLoad.put("linkedFieldId", linkedFieldId);
        digest_linkAndLoad.put("specifyForm1LinkField", specifyForm1LinkField);
        digests_linkAndLoad.put(digest_linkAndLoad);
    }
    
    
    //check of duplicate
    List<String> urllist = new ArrayList<>();
    int rootDomainId = form.getRootDomainId();
    if (!isThirdLayerSheet) { // third layer sheet does not generate duplicated sheets
        urllist = Account.getInstance(apname).getCopiedUrlList(rootDomainId);
    }
    
    for (String url : urllist) {
        if (url.equals(currentURL)) continue;
        String sheetName=account.getSheetName(url);
        if(sheetName==null) continue;
        JSONObject digest_multipleVersion = new JSONObject();
        digest_multipleVersion.put("url", url);
        digest_multipleVersion.put("sheetName", Encoder.encjs(sheetName));
        digests_multipleVersion.put(digest_multipleVersion);
    }
    
    
    //check for linked sheet / reference subtable
    Map<CellLocation, Integer> aliasedSubtables = form.getSubtableAliasParent();
    List<String> subtableSheetUrllist;
    Map<Integer, Integer> subtableKeyCounts = new HashMap<>();
    for (Map.Entry<CellLocation, Integer> e : form.getSubtableKey().entrySet()) {
        int subtableKey = e.getValue();
        int count = 0;
        if(subtableKeyCounts.containsKey(subtableKey)) count = subtableKeyCounts.get(subtableKey);
        subtableKeyCounts.put(subtableKey, count + 1);
    }
    
    for (Map.Entry<CellLocation, Integer> e : form.getSubtableKey().entrySet()) {
    
        int subtableKey = e.getValue();
        subtableSheetUrllist = account.getCopiedUrlList(subtableKey);
        if (subtableSheetUrllist.size() == 0) continue;
    
        if (aliasedSubtables.containsKey(e.getKey())) {//reference subtable
            int parentAlias = aliasedSubtables.get(e.getKey());
            for (String url : subtableSheetUrllist) {
    
                String[] urlsec = url.split("/");
                NUIForm linkedForm = AppManager.getFormPage(urlsec[0], "/" + urlsec[1], Encoder.parseInt(urlsec[2]));
                if (linkedForm == null) continue;
                CellInfo softLinkedField = linkedForm.getSingleField(parentAlias);
                if (softLinkedField != null) {
                    int stf = softLinkedField.getStf();
                    if (stf == Const.NODOMAIN) stf = form.getSelectTitleField();
                    String sheetName=account.getSheetName(url);
                    if(sheetName==null) continue;
                    int subtableKeyCount = subtableKeyCounts.containsKey(subtableKey)? subtableKeyCounts.get(subtableKey): 1;
                    if (subtableKeyCount > 1) sheetName += "(" + softLinkedField.getValue() + ")";
                    
                    JSONObject digest_referenceFromExistingSheet = new JSONObject();
                    digest_referenceFromExistingSheet.put("url", url);
                    digest_referenceFromExistingSheet.put("subtableKey", subtableKey);
                    digest_referenceFromExistingSheet.put("parentAlias", parentAlias);
                    digest_referenceFromExistingSheet.put("sheetName", Encoder.encjs(sheetName));
                    digest_referenceFromExistingSheet.put("stf", stf);
                    digests_referenceFromExistingSheet.put(digest_referenceFromExistingSheet);
                }
            }
        } else {
            for (String url : subtableSheetUrllist) {
                String[] urlsec = url.split("/");
                NUIForm linkedForm = AppManager.getFormPage(urlsec[0], "/" + urlsec[1], Encoder.parseInt(urlsec[2]));
                if (linkedForm == null) continue;
//                CellInfo hardLinkedField = linkedForm.getField(form.getKeyField());
//                int stf = Const.NODOMAIN;
//                if (hardLinkedField != null) {
//                    stf = Encoder.parseInt(hardLinkedField.getAttribute("stf"));
//                }
                JSONObject digest_newSheetFromSubtable = new JSONObject();
                digest_newSheetFromSubtable.put("url", url);
                digest_newSheetFromSubtable.put("subtableKey", subtableKey);
                digest_newSheetFromSubtable.put("linkedSheetName", linkedForm.getRealname());
                digest_newSheetFromSubtable.put("stf", form.getKeyField());
                digests_newSheetFromSubtable.put(digest_newSheetFromSubtable);
            }
        }
    }
    
    // check for subtable source
    int sourceDomainId = -1;
    Set<String> queryUrlList = account.getParentRelatedForms(form.getRootDomainId());
    if (!isThirdLayerSheet && !ifListingPage) {
        for (String url : queryUrlList) {
          String sheetName=account.getSheetName(url);
          if(sheetName==null) continue;
          JSONObject digest_parentSheet = new JSONObject();
          digest_parentSheet.put("url", url);
          digest_parentSheet.put("sheetName", Encoder.encjs(sheetName));
          digests_parentSheet.put(digest_parentSheet);
        }
    }
} finally {
    out.println(digestResult);
}

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
