/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import java.util.Collection;
import java.util.Collections;
import java.util.LinkedHashSet;
import java.util.Map;
import java.util.*;
import com.ragic.s3.nui.*;
import org.json.JSONObject;
import java.util.stream.Collectors;
import com.ragic.s3.service.workflow.ApprovalFieldPickerAutoGen;
import com.ragic.s3.service.workflow.SysFieldPickerAutoGen;

public final class listingFieldSelector_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.ApprovalFieldPickerAutoGen");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("java.util.Collection");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.service.workflow.SysFieldPickerAutoGen");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.Collections");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("java.util.LinkedHashSet");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  String apname=request.getParameter("a");
  String path=request.getParameter("p");
  String formName=request.getParameter("fn");
  int linkToDomain=Encoder.parseInt(request.getParameter("ltd"), Const.NODOMAIN);
  int parentAlias=Encoder.parseInt(request.getParameter("pa"), Const.NODOMAIN);
  if(linkToDomain==parentAlias) parentAlias=Const.NODOMAIN;//this is adding reference subtable for deleted subtable in create new sheet from subtable [BugId:
  String callback=request.getParameter("callback");
  String actionName=request.getParameter("actionName");
  boolean noCloseWin=request.getParameter("noClose")!=null;
  boolean filterIntermFields=request.getParameter("filterInterm")!=null;
  boolean ifListingPage=request.getParameter("listing")!=null;
  int generateTempNewSheetSubtableKey=Encoder.parseInt(request.getParameter("gtnssk"), Const.NODOMAIN);
  boolean displayFormPageFieldName = Encoder.parseBoolean(request.getParameter("displayFormPageFieldName"), false);
  User user = (User) session.getAttribute("USER");
  String type = request.getParameter("type");
  boolean ifCheckAdminRight = "xlsx".equals(type) || "csv".equals(type);
  boolean ifNoSysField=Encoder.parseBoolean(request.getParameter("noSysField"), false);

  if(callback==null) callback="insertReferencedSubtable";
  callback = Encoder.encjs(callback); //Prevent XSS
  if(actionName==null) actionName=bundle.getString("insertSubtable");

  boolean ifInsertReferenceSubtable="insertReferencedSubtable".equals(callback);

  //Sheet sheet=AppManager.getSheetBySheetIndex(apname,path,Integer.parseInt(sheetIndex));
  NUIForm formPage= AppManager.getFormPage(apname, path, formName);
  boolean sameFormPage = linkToDomain == Const.NODOMAIN || formPage.getRootDomainId() == linkToDomain;

  if (generateTempNewSheetSubtableKey > 0) {
    formPage = formPage.autoFormGen(generateTempNewSheetSubtableKey, formPage.getRootDomainId(), formPage.getSelectTitleField(), path + "|" + formPage.getFormName());
  }
  NUIForm listingPage= AppManager.getListingPage(apname,path,formName);
  if(request.getParameter("v")!=null){
      listingPage=listingPage.getHistoryForm(request.getParameter("v"));
  }
  if(listingPage==null) return;

  boolean ifSystemAdmin = user.ifApManager(apname, path);

  Collection<CellInfo> listingFields;
  if (generateTempNewSheetSubtableKey > 0) {
    listingFields = Collections.<CellLocation, CellInfo>emptyMap().values();
  } else {
    listingFields = listingPage.getFields().values();
  }
  Collection<CellInfo> formFieldsLeft=new LinkedHashSet<>();

  //check each field in form page if it's already in listing
  for(Map.Entry<CellLocation, CellInfo> e : formPage.getFields().entrySet()){
    CellLocation loc=e.getKey();
    CellInfo info=e.getValue();

    boolean isHideOnFormPage = info.getAttribute("hide") != null;
    /* parentAlias will be used in insert reference subtable */
    //softlinking to linkedToTarget, need to set parentAlias
    if(linkToDomain!=Const.NODOMAIN && formPage.isSingleCell(loc) && //single field
            (linkToDomain==Encoder.parseInt(info.getAttribute("s"),Const.NODOMAIN) ||
             linkToDomain==Encoder.parseInt(info.getAttribute("vdn"),Const.NODOMAIN))
            ) {
      if(parentAlias==Const.NODOMAIN) parentAlias=info.getDomainId();
      continue;
    }
    //removed because we don't know why a field in subtable would matter and a single field can also be in a subtable which we will find the wrong field
//    if(linkToDomain!=Const.NODOMAIN &&
//            (formPage.isFirstTableCell(loc)||formPage.isRestTableCell(loc)) &&  //in a subtable
//            (linkToDomain==Encoder.parseInt(info.getAttribute("s"),Const.NODOMAIN) ||
//                    linkToDomain==Encoder.parseInt(info.getAttribute("vdn"),Const.NODOMAIN))
//            ) {
//      parentAlias=formPage.getTableHeaderDomain(info.getDomainId());
//      continue;
//    }

    //only show single fields for selection
    if(!formPage.isSingleCell(loc)) continue;

    boolean ifHideOnListingPage = false;
    boolean inListing=false;
    for(CellInfo listingInfo:listingFields){
      if(listingInfo.getDomainId()==info.getDomainId() &&
              info.getDomainId()!=linkToDomain &&
              Encoder.parseInt(info.getAttribute("s"))!=linkToDomain) {
        inListing=true;
        ifHideOnListingPage = listingInfo.getAttribute("hide") != null;
        break;
      }
    }

    if(ifCheckAdminRight && !ifSystemAdmin && (isHideOnFormPage && (ifHideOnListingPage || !inListing))) continue;
    if(!inListing) formFieldsLeft.add(info);
  }


    //adding action buttons
    Map<String, ActionButton> actionButtons=formPage.getActionButtons();
    if(actionButtons!=null && !actionButtons.isEmpty() && !ifInsertReferenceSubtable){
        for(Map.Entry<String,ActionButton> e:actionButtons.entrySet()){
            String actionButtonId=e.getKey();
            boolean alreadyAdded=false;
            for(CellInfo listingInfo:listingFields){
                if(listingInfo!=null && actionButtonId.equals(listingInfo.getAttribute("actionButtonId"))){
                    alreadyAdded=true;
                }
            }
            if(alreadyAdded) continue;
            ActionButton button=e.getValue();
            CellInfo info=new CellInfo(button.getName());
            info.setType(Const.TEXT);
            info.setDomainId(Const.DOMAIN_ID_TO_BE_ASSIGNED);

            // [issue#14312]
            JSONObject buttonJSON = button.getJSON();
            String buttonName = buttonJSON.optString("name", "");
            buttonName = buttonName.replace("&", "%26");
            buttonJSON.put("name", buttonName);

            info.setAttribute("actionButton",Encoder.encjs(buttonJSON.toString()));
            info.setAttribute("actionButtonId",actionButtonId);
            formFieldsLeft.add(info);
        }
    }


  // Add approval formula fields and some system fields to formFieldsLeft
  // add sameFormPage means only same form page could add these auto-generated approval formula fields
  if(!Const.NO_APPROVAL.equals(formPage.getApprovalType()) && sameFormPage) {
    Set<CellInfo> autoGenApprovalFormulaListingFields = listingFields.stream().filter(listingField -> listingField.getAttribute("aaf") != null).collect(Collectors.toSet());
    Set<CellInfo> autoGenApprovalFormulaLeftFields = formFieldsLeft.stream().filter(leftField -> leftField.getAttribute("aaf") != null).collect(Collectors.toSet());
    ApprovalFieldPickerAutoGen fpa = new ApprovalFieldPickerAutoGen(apname, bundle);

    //auto-gen approval formula fields
    Set<CellInfo> allAutoApprovalFormulaFields = fpa.genAllFields();

    for(CellInfo info : allAutoApprovalFormulaFields) {
      String formula = info.getAttribute("f");
      if(autoGenApprovalFormulaListingFields.parallelStream().noneMatch(listingField -> listingField.getAttribute("aaf").equals(formula)) &&
              autoGenApprovalFormulaLeftFields.parallelStream().noneMatch(leftField -> leftField.getAttribute("aaf").equals(formula))) {
        formFieldsLeft.add(info);
      }
    }
  }

  boolean createDateInfoInListing = false,
          createUserInfoInListing = false,
          updateDateInfoInListing = false;
  SysFieldPickerAutoGen spa = new SysFieldPickerAutoGen(apname, bundle);

  for(CellInfo listingInfo : listingFields) {
    if(listingInfo.getDomainId() == DConst.ENTRY_CREATE_DATE) createDateInfoInListing = true;
    if(listingInfo.getDomainId() == DConst.ENTRY_ORIGINAL_CREATE_USER) createUserInfoInListing = true;
    if(listingInfo.getDomainId() == DConst.ENTRY_UPDATE_DATE) updateDateInfoInListing = true;

    if(createDateInfoInListing && createUserInfoInListing && updateDateInfoInListing) break;
  }

  if(!createDateInfoInListing) {
    formFieldsLeft.add(spa.genCreateDate());
  }
  if(!createUserInfoInListing) {
//    formFieldsLeft.add(spa.genCreateUser());
  }
  if(!updateDateInfoInListing) {
    formFieldsLeft.add(spa.genLastUpdateDate());
  }

  if(ifNoSysField){
    listingFields.removeIf(e -> e.getDomainId() == DConst.ENTRY_CREATE_DATE);
    listingFields.removeIf(e -> e.getDomainId() == DConst.ENTRY_ORIGINAL_CREATE_USER);
    listingFields.removeIf(e -> e.getDomainId() == DConst.ENTRY_UPDATE_DATE);
    formFieldsLeft.removeIf(e -> e.getDomainId() == DConst.ENTRY_CREATE_DATE);
    formFieldsLeft.removeIf(e -> e.getDomainId() == DConst.ENTRY_ORIGINAL_CREATE_USER);
    formFieldsLeft.removeIf(e -> e.getDomainId() == DConst.ENTRY_UPDATE_DATE);
  }

  //display warning if there's no backlink to the current designing page on the page that you're adding as a subtable of this page
  if(ifInsertReferenceSubtable && linkToDomain!=Const.NODOMAIN){
    //check for hardlink or softlink(s,vdn)
    boolean backlinkFound=false;
    for(CellInfo info : formPage.getFields().values()){
      if(info.getDomainId()==linkToDomain ||
              Encoder.parseInt(info.getAttribute("s"))==linkToDomain ||
              Encoder.parseInt(info.getAttribute("vdn"))==linkToDomain){
        backlinkFound=true;
        break;
      }
    }
    if(!backlinkFound){
      String source=request.getParameter("source");
      out.println("<div style='margin:0 0 12px 0;color:#08c;'><p style='margin:0 0 5px 0;'>The sheet that you are linking to has no field linked to this form.</p>" +
              "<p>You will need to add a linked field on <b>"+formPage.getRealname()+"</b>, so when user creates a new <b>"+formPage.getRealname()+"</b> they can choose a <b>"+source+"</b>. " +
              "This way the new <b>"+formPage.getRealname()+"</b> will be shown in this subtable.</p></div>");
      return;
    }
  }


      out.write("<table id=\"listingFieldSelectorTable\">\r\n  <tr>\r\n    <td style=\"font-weight:400;font-size:13px;color:#222;padding:0 0 0 10px;vertical-align: top;\">");
      out.print(bundle.getString("availableFields"));
      out.write("</td>\r\n    <td></td>\r\n    <td style=\"font-weight:400;font-size:13px;color:#222;padding:0 0 0 10px;vertical-align: top;\">");
      out.print(bundle.getString("selectedFields"));
      out.write("</td>\r\n  </tr>\r\n  <tr>\r\n    <td style=\"vertical-align:top;\">\r\n      <div><input type=\"text\" id=\"fieldFilter1\" onkeyup=\"fieldFilter(this.value,$('formFieldOptions'));\" style=\"width:100%;border-radius:5px;border:1px solid #CCC;\" onfocus=\"ctrl.outOfNUI=true;\" onblur=\"ctrl.outOfNUI=false;\" /><i class=\"fas fa-search\" style=\"margin:0 0 12px -20px;color:#aaa;\"></i></div>\r\n      <select id=\"formFieldOptions\" multiple=\"multiple\" style=\"height:100%;\" size=\"10\">\r\n      ");

        for(CellInfo info:formFieldsLeft){
          //added subtable should not have a field domain id same as the keyfield of the form
          if(linkToDomain != Const.NODOMAIN && info.getDomainId() == linkToDomain) continue;
          if(filterIntermFields && info.getAttribute("interm")!=null) continue;

          CellInfo formPageInfo=formPage.getField(info.getDomainId());
          String displayedFieldName = displayFormPageFieldName? (formPageInfo == null? info.getValue():formPageInfo.getValue()):info.getValue();
          displayedFieldName = Encoder.htmlEscape(displayedFieldName); //[BugId:14422]
          String displayOption=(info.isActionButton()?"["+bundle.getString("actionButton")+"] ":"")+displayedFieldName;
          int cascadedDomainId = Account.getInstance(apname).getLOVS2RecNoAlias(info.getDomainId());//[BugId:09553]use getLOVS2RecNoAlias instead of getLOVS2Rec

          int domainId = info.getDomainId();
          String value = info.isActionButton() ? "" : String.valueOf(domainId);
          out.println("<option " + (domainId == Const.NODOMAIN ? "style='background-color:#ffe7e7;' " : info.getAttribute("sysField") != null ? "style='background-color:#e8e8e8ab;' " :"") + "value=\""+value+"\" attr=\""+info.getAttributeStringForCopy(sameFormPage)+"\" ondblclick=\"if (this.closest('select').id === 'formFieldOptions') listingFieldSelector(); else listingFieldSelector(true);" + callback + "();\"" +
                       (info.isSelection()?"lov=\""+Encoder.encjs(info.getLOVString(apname,"`"))+"\"":"")
                       +(cascadedDomainId != Const.NODOMAIN ? " cascaded=\"" + cascadedDomainId + "\"" : "")
                       +" it=\""+info.getType()+"\" fieldName=\""+displayedFieldName+"\">"+displayOption+"</option>");
        }
      
      out.write("</select>\r\n    </td>\r\n    <td>\r\n      <div onclick=\"listingFieldSelector();");
      out.print(callback);
      out.write("();\" class=\"topButtonWhole topButtonWholeLight\" style=\"padding:3px 10px;\"><i class=\"fa fa-caret-right\" style=\"font-size:15px;\"></i></div>\r\n      <div onclick=\"listingFieldSelector(true);");
      out.print(callback);
      out.write("();\" class=\"topButtonWhole topButtonWholeLight\" style=\"padding:3px 10px;clear:left;\"><i class=\"fa fa-caret-left\" style=\"font-size:15px;\"></i></div>\r\n    </td>\r\n    <td style=\"vertical-align:top;\">\r\n      <div><input type=\"text\" id=\"fieldFilter2\" style=\"width:100%;border-radius:5px;border:1px solid #CCC;\" onkeyup=\"fieldFilter(this.value,$('listingFieldOptions'));\" onfocus=\"ctrl.outOfNUI=true;\" onblur=\"ctrl.outOfNUI=false;\" /><i class=\"fas fa-search\" style=\"margin:0 0 12px -20px;color:#aaa;\"></i></div>\r\n      <select id=\"listingFieldOptions\" multiple=\"multiple\" size=\"10\">\r\n      ");

        for(CellInfo info:listingFields){
          int domainId = info.getDomainId();
          if(info.getDomainId()==linkToDomain ||
                  linkToDomain!=Const.NODOMAIN && Encoder.parseInt(info.getAttribute("s"),Const.NODOMAIN)==linkToDomain ||
                  info.getDomainId()==parentAlias
          ) {
              continue;
          }

          if(ifCheckAdminRight && !ifSystemAdmin) {
            boolean hideOnListingPage = info.getAttribute("hide") != null;

            CellInfo formField = formPage.getField(info.getDomainId());
            boolean hideOnFormPage = formField == null || formField.getAttribute("hide") != null;
            if(hideOnFormPage && hideOnListingPage) continue;
          }

          if(filterIntermFields && info.getAttribute("interm")!=null) continue;

          CellInfo formPageInfo = formPage.getField(info.getDomainId());
          String displayedFieldName = displayFormPageFieldName ? (formPageInfo == null ? info.getValue() : formPageInfo.getValue()) : info.getValue();
          displayedFieldName = Encoder.htmlEscape(displayedFieldName);
          String displayOption=(info.isActionButton()?"["+bundle.getString("actionButton")+"] ":"")+displayedFieldName;

          int cascadedDomainId = Account.getInstance(apname).getLOVS2RecNoAlias(info.getDomainId());
          String attr = info.getAttributeStringForCopy(sameFormPage).replaceAll("\"", "&quot;"); // [issue #15234]
          out.println("<option value=\""+info.getDomainId()+"\" attr=\""+attr+
                  "\" ondblclick=\"if (this.closest('select').id === 'formFieldOptions') listingFieldSelector(); else listingFieldSelector(true);" + callback + "();\""+
                      (info.isSelection()?"lov=\""+Encoder.encjs(info.getLOVString(apname,"`"))+"\"":"")
                      + (domainId == Const.NODOMAIN ? "style='background-color:#ffe7e7;' " : info.getAttribute("sysField") != null ? "style='background-color:#e8e8e8ab;' " :"")
                      +(cascadedDomainId != Const.NODOMAIN ? " cascaded=\"" + cascadedDomainId + "\"" : "")
                      +" it=\""+info.getType()+"\" fieldName=\""+displayedFieldName+"\">"+displayOption+"</option>");
        }
      
      out.write("</select>\r\n    </td>\r\n    <td>\r\n      <div onclick=\"listingFieldSelectorMove();");
      out.print(callback);
      out.write("();\" class=\"topButtonWhole topButtonWholeLight\" style=\"padding:3px 8px;\"><i class=\"fa fa-caret-up\" style=\"font-size:15px;\"></i></div>\r\n      <div onclick=\"listingFieldSelectorMove(true);");
      out.print(callback);
      out.write("();\" class=\"topButtonWhole topButtonWholeLight\" style=\"padding:3px 8px;clear:left;\"><i class=\"fa fa-caret-down\" style=\"font-size:15px;\"></i></div>\r\n    </td>\r\n  </tr>\r\n</table>\r\n<input type=\"hidden\" id=\"parentAlias\" value=\"");
      out.print(parentAlias);
      out.write("\" />\r\n<input type=\"hidden\" id=\"keyField\" value=\"");
      out.print(formPage.getKeyField());
      out.write("\" />\r\n");
 if(!noCloseWin){ 
      out.write("<input id='insertListingFields' type=\"button\" onclick=\"");
      out.print(callback);
      out.write("();floatWinClose();\" value=\"");
      out.print(actionName);
      out.write("\"/>\r\n<input type=\"button\" onclick=\"floatWinClose();\" value=\"");
      out.print(bundle.getString("cancel"));
      out.write("\" />\r\n");
 } 
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
