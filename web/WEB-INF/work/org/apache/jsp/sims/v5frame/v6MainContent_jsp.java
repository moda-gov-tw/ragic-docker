/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims.v5frame;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.util.*;
import java.util.*;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.*;
import org.json.JSONObject;
import com.ragic.s3.nui.helper.Encoder;
import java.time.temporal.TemporalAccessor;
import java.text.SimpleDateFormat;

public final class v6MainContent_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("java.time.temporal.TemporalAccessor");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  RequestParam rp=(RequestParam) request.getAttribute("REQ");
  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  User user=(User)session.getAttribute("USER");

  if("/start".equals(rp.getPath()) && !Util.isSandboxApname(rp.getApname()) && (user==null || !user.isValid())){
    out.print("<script>location='/sims/reg/login.jsp?a="+rp.getApname()+"';</script>");
    return;
  }

  if(rp.getSheetIndex() != -1) {
    NUIForm form = AppManager.getFormPage(rp.getApname(), rp.getPath(), rp.getSheetIndex());
    if(form != null) {
    String partnerTemplateId = form.getPartnerTemplateId();
    boolean ifPaidApp = form.isIfPaidApp();
    if(ifPaidApp) {
      HomeConfig homeConfig = Account.getInstance(rp.getApname()).getHomeConfig();
      JSONObject templateInfo = homeConfig.getPartnerTemplateInfo(partnerTemplateId);
      boolean dataErr = false;
      boolean expired = false;
      if(templateInfo == null) {
        dataErr = true;
      } else {
        JSONObject paidInfo = templateInfo.optJSONObject("PaymentInfo");
        if(paidInfo == null) {
          dataErr = true;
        } else {
          String expireDateStr = paidInfo.optString("expiredDate");
          SimpleDateFormat spd = new SimpleDateFormat("yyyy/MM/dd HH:mm:ss");
          Date expireDate = spd.parse(expireDateStr);
          Date now = new Date();
          expired = expireDate.getTime() - now.getTime() < 0;
        }

        if(expired || dataErr) {
          request.setAttribute("ifExpired", expired);
          request.setAttribute("ifDataErr", dataErr);
          request.setAttribute("partnerTemplateId", form.getPartnerTemplateId());
            
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "v6PartnerTemplateExpired.jsp", out, false);

            return;
        }
      }
    }
//    if (partnerTemplateId != null && !partnerTemplateId.isEmpty()) {
//      HomeConfig homeConfig = Account.getInstance(rp.getApname()).getHomeConfig();
//      JSONObject templateInfo = homeConfig.getPartnerTemplateInfo(partnerTemplateId);
//      if (templateInfo != null) {
//        JSONObject paidInfo = templateInfo.optJSONObject("PaymentInfo");
//      paidInfo.opS？
//      }
//    }
  }
  }
  AccountPaymentStatus aps=rp.getAccountPaymentStatus();
  boolean expired=aps.ifExpired() && !"/ragic-setup".equals(rp.getPath());
  String invalidMessage=(String)request.getAttribute("INVALID_REQ");
  boolean invalid=invalidMessage!=null;
  //boolean ifExternalUser=request.getAttribute("EXTERNAL_USER_ACCESS_INTERNAL_PAGE")!=null;
  boolean ifOwner=user.ifApManager(rp.getApname(),rp.getPath());
  String actualJSP=rp.getActualJSP(ifOwner);
  //if("/sims/v5Module/home.jsp".equals(actualJSP)) actualJSP="/sims/v5Module/homeOld.jsp";
  String tab=rp.getTabName(ifOwner);
  List<String> expiredStillAccessTabs= new ArrayList<>();
  expiredStillAccessTabs.add(Const.TAB_DATA);
  expiredStillAccessTabs.add(Const.TAB_ADMIN);
  expiredStillAccessTabs.add(Const.TAB_CONFIGSETUP);

  if(rp.isArchivedDB() && !("/data".equals(rp.getPath()) && request.getParameter("backup")!=null)){//allow backup and restore for shelved account
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "v6ArchivedDB.jsp", out, false);

    return;
  }

  if (!user.ifApManager(rp.getApname())) {
      AbstractMap.SimpleEntry<Boolean, String> checkResult = CompanyRestrictionChecker.checkCompanyRestriction(request, rp.getApname(), bundle);
	  if (!checkResult.getKey()) {

      out.write("<div style='font-size:15px;margin:50px;'>");
      out.print(checkResult.getValue());
      out.write("</div>\r\n");

	      return;
	  }
  }

  boolean concurrentUserWaiting=SessionRegistry.getInstance().ifConcurrentUserWait(session,rp.getApname());
  if(concurrentUserWaiting){
    //try kicking idle
    User kicked=SessionRegistry.getInstance().kickIdleConcurrent(rp.getApname());
    //kicked people, and the new user allowed is me
    if(kicked!=null && kicked.getUserId().equalsIgnoreCase(user.getUserId())) {
      new Log("log/concurrent.log").setIfDisplayTime(true).log(rp.getApname()+" kicked "+kicked.getUserId()+" during load "+user.getUserId(),Log.INFO);
    }
    //nobody kicked, block
    else{
      Queue waiting=SessionRegistry.getInstance().getWaitingQueue(rp.getApname());
      if(waiting!=null && !waiting.isEmpty()){
        new Log("log/concurrent.log").setIfDisplayTime(true).log(rp.getApname()+" "+waiting.size()+" users waiting:"+user.getUserId(),Log.INFO);
      }
      else{
        new Log("log/concurrent.log").setIfDisplayTime(true).log(rp.getApname()+" 1st user waiting:"+user.getUserId(),Log.INFO);
      }
      
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "v6ConcurrentWaiting.jsp", out, false);

      return;
    }
  }

  if(invalid){
    
      out.write("<div style='font-size:15px;margin:50px 50px 200px 50px;'>\r\n    ");
      out.print(invalidMessage);
      out.write("</div>\r\n    ");

  }

  if(expired && !expiredStillAccessTabs.contains(tab)){
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "v6Expired.jsp", out, false);

  }

  if(actualJSP!=null){
    if(!invalid){
      if(!expired || expiredStillAccessTabs.contains(tab)){
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, actualJSP, out, false);
}
    }
  }
  else{
    request.setAttribute("HAS_NUI",true);
    if(expired || invalid) out.println("<div style='display:none;'>");
    
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "v6NUIHeader.jsp", out, false);
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "v6NUIContainer.jsp", out, false);

    if(expired || invalid) out.println("</div>");
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
