/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.BackupDB;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.UserManager;
import com.ragic.s3.util.SystemTaskProgress;
import com.ragic.s3.util.Const;
import com.ragic.s3.sdb.search.FTIndex;
import org.json.JSONArray;
import org.json.JSONObject;
import com.ragic.s3.nui.helper.URLCaller;
import java.io.File;
import java.util.Set;
import java.util.HashSet;
import com.ragic.s3.nui.RagicConfig;
import com.ragic.s3.util.SystemCache;
import com.ragic.s3.util.Log;
import com.ragic.s3.service.schedule2.BackgroundDBCleaner;
import java.util.Date;

public final class shelf_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("java.util.Set");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemTaskProgress");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("java.util.HashSet");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.BackupDB");
    _jspx_imports_classes.add("com.ragic.s3.sdb.search.FTIndex");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.URLCaller");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserManager");
    _jspx_imports_classes.add("com.ragic.s3.service.schedule2.BackgroundDBCleaner");
    _jspx_imports_classes.add("org.json.JSONArray");
    _jspx_imports_classes.add("com.ragic.s3.util.SystemCache");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



    final String apname=request.getParameter("a");
    final User user=(User)session.getAttribute("USER");
    final String sessionKey="SHELFING_"+apname;
    final HttpSession fSession=session;


    String action=request.getParameter("action");
    if("shelf".equals(action) && apname!=null){
        if(user==null || !UserManager.ifRagicSupport(user.getUserId())) return;
        if(session.getAttribute(sessionKey)!=null) return;
        session.setAttribute(sessionKey,new Date());
        Log log=new Log("log/shelf.log").setIfDisplayTime(true);
        log.info("Manual shelf "+apname+" start.");
        BackupDB.shelf(apname);
        log.info("Manual shelf "+apname+" complete.");
        session.removeAttribute(sessionKey);
    }
    else if("unshelf".equals(action) && apname!=null){
        if(session.getAttribute(sessionKey)!=null) return;
        session.setAttribute(sessionKey,new Date());
        Thread t=new Thread(new Runnable() {
            @Override
            public void run() {
                Log log=new Log("log/shelf.log").setIfDisplayTime(true);
                log.info("Unshelf "+apname+" start.");
                SystemTaskProgress.setStatus(apname,
                        Const.STP_UNSHELF,
                        "Retreiving account data from archive...",
                        -1,
                        SystemTaskProgress.Status.PROCESSING
                );
                BackupDB.unshelf(apname);
                SystemTaskProgress.setStatus(apname,
                        Const.STP_UNSHELF,
                        "Finished restoring account data. Reload this page to access your account.",
                        -1,
                        SystemTaskProgress.Status.DONE
                );
                log.info("Unshelf "+apname+" complete.");
                fSession.removeAttribute(sessionKey);
            }
        });
        t.setName("Unshelf "+apname);
        t.start();
    }
    else if("purge".equals(action) && apname!=null){
        if(user==null || !UserManager.ifRagicSupport(user.getUserId())) return;
        if(session.getAttribute(sessionKey)!=null) return;
        session.setAttribute(sessionKey,new Date());
        Log log=new Log("log/shelf.log").setIfDisplayTime(true);
        log.info("Purge "+apname+" start.");
        BackupDB.shelf(apname);
        log.info("Purge "+apname+" shelf done, start unshelf.");
        BackupDB.unshelf(apname);
        log.info("Purge "+apname+" unshelf done, start index.");
        FTIndex.index(apname);
        log.info("Purge "+apname+" ends.");
        session.removeAttribute(sessionKey);
    }
    else if("autoshelf".equals(action)){
        if(user==null || !UserManager.ifRagicSupport(user.getUserId())) return;
        JSONObject jo= URLCaller.get("https://accounts.ragic.com/api/internal/proxy_shelfList.jsp?fromServer="+ RagicConfig.getInstance().getProperty("SERVER_NAME"));
        if(jo==null){
            out.println("No list retrieved.");
            return;
        }
        JSONArray ja=jo.optJSONArray("data");
        if(ja==null){
            out.println("No data in the list.");
            return;
        }
        Set<String> toShelf=new HashSet<>();
        for(int i=0;i<ja.length();i++){
            toShelf.add(ja.optString(i));
        }
        if(SystemCache.getInstance().get("AUTO_SHELF_EXEC")!=null){
            out.println("Already running.");
            return;
        }
        else{
            SystemCache.getInstance().put("AUTO_SHELF_EXEC","true");
            Thread t=new Thread(new Runnable() {
                @Override
                public void run() {
                    int count=0;
                    Log log=new Log("log/shelf.log").setIfDisplayTime(true);
                    for(String shelfApname:toShelf){
                        if(shelfApname==null || shelfApname.isEmpty()) continue;
                        if(shelfApname.contains("_deleted_")){
                            log.info("Delete "+shelfApname+" start.");
                            File dir=new File("cust/data/"+shelfApname);
                            if(dir.exists() && dir.isDirectory()){
                                dir.delete();
                            }
                            dir=new File("cust/def/app/"+shelfApname);
                            if(dir.exists() && dir.isDirectory()){
                                dir.delete();
                            }
                            log.info("Delete "+shelfApname+" complete.");
                        }
                        else{
                            log.info("Shelf "+shelfApname+" start.");
                            BackgroundDBCleaner.getInstance().deleteUnlinkedFiles(shelfApname);
                            BackupDB.shelf(shelfApname);
                            log.info("Shelf "+shelfApname+" complete.");
                        }
                        count++;
                        Thread.currentThread().setName("Auto shelved "+count);
                    }
                    SystemCache.getInstance().remove("AUTO_SHELF_EXEC");
                }
            });
            t.setName("Auto shelved 0");
            t.start();
            out.print("Start shelfing <b>"+jo.getInt("size")+"</b> accounts.");
        }
    }
    else{
        out.println("No action.");
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
