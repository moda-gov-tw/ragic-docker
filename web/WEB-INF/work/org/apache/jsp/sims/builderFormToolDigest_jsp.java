/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:42 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.sims;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.NUIForm;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.AppManager;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.nui.rules.FormRules;
import org.json.*;
import java.util.Map;
import com.ragic.s3.nui.ActionButton;

public final class builderFormToolDigest_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("org.json");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.AppManager");
    _jspx_imports_classes.add("com.ragic.s3.nui.ActionButton");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIForm");
    _jspx_imports_classes.add("com.ragic.s3.nui.rules.FormRules");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


JSONObject digestResult = new JSONObject();
try {
	String apname=request.getParameter("a");
	String path=request.getParameter("p");
	int sheetIndex= Encoder.parseInt(request.getParameter("si"), -1);
	boolean ifListingPage = Encoder.parseBoolean(request.getParameter("listing"), false);
	
	if(apname==null || path==null || sheetIndex==-1) return;

	NUIForm form = null;
	if(!ifListingPage) form = AppManager.getFormPage(apname, path, sheetIndex);
	else form = AppManager.getListingPage(apname, path, sheetIndex);

	if(form == null) return;
    
    JSONArray digests_convertRecord = new JSONArray();
    JSONArray digests_entryUpdater = new JSONArray();
    JSONArray digests_fieldUpdater = new JSONArray();
    JSONArray digests_sendCustomMail = new JSONArray();
    JSONArray digests_mergedButton = new JSONArray();
    digestResult.put("digests_convertRecord", digests_convertRecord);
    digestResult.put("digests_entryUpdater", digests_entryUpdater);
    digestResult.put("digests_fieldUpdater", digests_fieldUpdater);
    digestResult.put("digests_sendCustomMail", digests_sendCustomMail);
    digestResult.put("digests_mergedButton", digests_mergedButton);
    
    Map<String, ActionButton> actionButtons = form.getActionButtons();
    for(Map.Entry<String, ActionButton> e:actionButtons.entrySet()){
        ActionButton button=e.getValue();
        String seq = String.valueOf(button.getSeq());
        String name = button.getName();
        if(name == null) continue;
        String action=button.getAction();
        if(action == null) continue;
        String type=button.getType();
        if(type == null || !"B".equals(type)) continue;

        if (action.startsWith("_RAGIC_AUTOGEN_CR_")) {
        	String func = action.indexOf("(") == -1? null: action.substring(0, action.indexOf("("));
            if(func == null) continue;
            
            JSONObject digest_convertRecord = new JSONObject();
            String actionId = func.replace("_RAGIC_AUTOGEN_CR_", "");
            digest_convertRecord.put("name", Encoder.encjs(name));
            digest_convertRecord.put("actionId", actionId);
            digest_convertRecord.put("buttonSeq", seq);
            
            digests_convertRecord.put(digest_convertRecord);
        } else if (action.startsWith("_RAGIC_AUTOGEN_ENTRY_UPDATER_") && !action.contains("_REVERSE")) {
        	String func = action.indexOf("(") == -1? null: action.substring(0, action.indexOf("("));
            if(func == null) continue;
            
        	JSONObject digest_entryUpdater = new JSONObject();
            String actionId = func.replace("_RAGIC_AUTOGEN_ENTRY_UPDATER_", "");
            digest_entryUpdater.put("name", Encoder.encjs(name));
            digest_entryUpdater.put("actionId", actionId);
            digest_entryUpdater.put("buttonSeq", seq);
            
            digests_entryUpdater.put(digest_entryUpdater);
        } else if(action.startsWith("_RAGIC_AUTOGEN_FIELD_UPDATER_")){
        	String func = action.indexOf("(") == -1? null: action.substring(0, action.indexOf("("));
            if(func == null) continue;
            
            JSONObject digest_fieldUpdater = new JSONObject();
            String actionId = func.replace("_RAGIC_AUTOGEN_FIELD_UPDATER_", "");
            digest_fieldUpdater.put("name", Encoder.encjs(name));
            digest_fieldUpdater.put("actionId", actionId);
            digest_fieldUpdater.put("buttonSeq", seq);
            
            digests_fieldUpdater.put(digest_fieldUpdater);
        } else if(action.startsWith("_RAGIC_AUTOGEN_SEND_CUSTOM_MAIL_")) {
            String func = action.indexOf("(") == -1? null: action.substring(0, action.indexOf("("));
            if(func == null) continue;
            
            JSONObject param = Util.getRagicAutoGeneratedActionParam(form.getSheetscope(), func);
            if(param == null) continue;
            String templateKey = param.optString("key", null);
            if(templateKey == null) continue;
            
            JSONObject digest_sendCustomMail = new JSONObject();
            
            digest_sendCustomMail.put("name", Encoder.encjs(name));
            digest_sendCustomMail.put("templateKey", templateKey);
            digest_sendCustomMail.put("buttonSeq", seq);
            
            digests_sendCustomMail.put(digest_sendCustomMail);
        } else if(action.startsWith("_RAGIC_AUTOGEN_MERGED_BUTTON_")) {
            JSONObject digest_mergedButton = new JSONObject();
          
            digest_mergedButton.put("name", Encoder.encjs(name));
            digest_mergedButton.put("buttonSeq", seq);
          
            digests_mergedButton.put(digest_mergedButton);
        }
    }
    
    int interactiveRulesNum = new FormRules(form).getRules().size();
    digestResult.put("interactiveRulesNum", interactiveRulesNum);
    
    int totalButtonNum = actionButtons.size();
    digestResult.put("totalButtonNum", totalButtonNum);
} finally {
    out.println(digestResult);
}
/* 
String apname=request.getParameter("a");
String path=request.getParameter("p");
int sheetIndex= -1;
try {
    sheetIndex = Integer.parseInt(request.getParameter("si"));
} catch(Exception e) {
}
boolean ifListingPage = false;
try {
    ifListingPage = Boolean.parseBoolean(request.getParameter("listing"));
} catch(Exception e) {
}

if(apname==null || path==null || sheetIndex==-1) return;
RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
User user = (User) session.getAttribute("USER");

NUIForm form = null;
if(!ifListingPage) form = AppManager.getFormPage(apname, path, sheetIndex);
else form = AppManager.getListingPage(apname, path, sheetIndex);

if(form == null) return;

//[issue id: 03940]
Map<String, JSONObject> actionButtons = form.getActionButtons();
String entryCopierDesc="";
String entryUpdaterDesc="";
String fieldUpdaterDesc="";
String sendCustomEmailButtonDesc="";
String combineButtonsDesc="";
int entryCopierCount=0;
int entryUpdaterCount=0;
int fieldUpdaterCount=0;
int sendCustomEmailButtonCount=0;
int combineButtonsCount=0;
for(Map.Entry<String, JSONObject> e:actionButtons.entrySet()){
  String seq=e.getKey();
  JSONObject button=e.getValue();
  String name=button.optString("name", null);
  if(name == null) continue;
  String action=button.optString("action", null);
  if(action == null) continue;
  String type=button.optString("type", null);
  if(type == null) continue;

  if (action.startsWith("_RAGIC_AUTOGEN_CR_") && "B".equals(type)) {
      if (entryCopierCount > 0) entryCopierDesc += ", ";
      entryCopierDesc += "<a href='javascript:void(0);' onclick='cancelEventBubble(event);copyRecordSettingEdit(event," + seq + "," + action.substring(18, 23) + ");'>" + Encoder.encjs(name) + "</a>";
      entryCopierCount++;
  } else if (action.startsWith("_RAGIC_AUTOGEN_ENTRY_UPDATER_") &&
          !action.contains("_REVERSE") &&
          "B".equals(type)) {
      if (entryUpdaterCount > 0) entryUpdaterDesc += ", ";
      String func = action.substring(0, action.indexOf("("));
      String actionId = func.replace("_RAGIC_AUTOGEN_ENTRY_UPDATER_", "");
      entryUpdaterDesc += "<a href='javascript:void(0);' onclick='cancelEventBubble(event);updateRecordSettingEdit(event, " + seq + ", " + actionId + ");'>" + Encoder.encjs(name) + "</a>";
      entryUpdaterCount++;
  } else if(action.startsWith("_RAGIC_AUTOGEN_FIELD_UPDATER_") &&
          !action.contains("_REVERSE") &&
          "B".equals(type)){
      if(fieldUpdaterCount>0) fieldUpdaterDesc+=", ";
      String func = action.substring(0, action.indexOf("("));
      String actionId = func.replace("_RAGIC_AUTOGEN_FIELD_UPDATER_", "");
      fieldUpdaterDesc+="<a href='javascript:void(0);' style='color: #999999;font-weight:bold;white-space:normal;' onclick='cancelEventBubble(event);updateFieldSettingEdit(event, " + seq + ", " + actionId + ");'>"+Encoder.encjs(name)+"</a>";
      fieldUpdaterCount++;
  } else if(action.startsWith("_RAGIC_AUTOGEN_SEND_CUSTOM_MAIL_") &&
          "B".equals(type)) {
      String func = action.indexOf("(")==-1?null:action.substring(0, action.indexOf("("));
      JSONObject param = Util.getRagicAutoGeneratedActionParam(form.getSheetscope(), func);
      if(param != null) {
          String templateKey = param.optString("key", null);
          if(templateKey != null) {
              if(sendCustomEmailButtonCount>0) sendCustomEmailButtonDesc += ", ";
              sendCustomEmailButtonDesc+="<a href='javascript:void(0);' style='color: #999999;font-weight:bold;white-space:normal;' onclick='cancelEventBubble(event);builderModeEmailTemplateEditors.actionButton.open(" + seq + ", " + templateKey + ");'>"+Encoder.encjs(name)+"</a>";
              sendCustomEmailButtonCount++;
          }
      }
  } else if(action.startsWith("_RAGIC_AUTOGEN_MERGED_BUTTON_") &&
          "B".equals(type)) {
      if(combineButtonsCount>0) combineButtonsDesc += ", ";
      combineButtonsDesc+="<a href='javascript:void(0);' style='color: #999999;font-weight:bold;white-space:normal;' onclick='cancelEventBubble(event);setMergedActionButton(" + seq + ");'>"+Encoder.encjs(name)+"</a>";
      combineButtonsCount++;
  }
}


if (entryCopierCount > 0) {
    entryCopierDesc = bundle.getString("linkDigest_convertRecords", new Object[] {entryCopierCount, entryCopierCount > 1 ? "s" : ""}) + "<br/>" + entryCopierDesc;
} else {
    entryCopierDesc = bundle.getString("linkDigest_convertRecords_noButtons");
}
out.println("if($('linkToolDesc6')) $('linkToolDesc6').innerHTML=\"" + entryCopierDesc + "\";");

if (entryUpdaterCount > 0) {
    entryUpdaterDesc = bundle.getString("linkDigest_updateEntries", new Object[] {entryUpdaterCount, entryUpdaterCount > 1 ? "s" : ""}) + "<br/>" + entryUpdaterDesc;
} else {
    entryUpdaterDesc = bundle.getString("linkDigest_updateEntries_noButtons");
}
out.println("if($('linkToolDesc7')) $('linkToolDesc7').innerHTML=\"" + entryUpdaterDesc + "\";");

fieldUpdaterDesc = fieldUpdaterCount == 0? bundle.getString("fieldUpdaterDigestNo"):
    bundle.getString("fieldUpdaterDigest", new Object[] {fieldUpdaterCount, fieldUpdaterCount > 1? "s":""}) + "<br>" + fieldUpdaterDesc;
out.println("if($('fieldUpdaterDesc')) $('fieldUpdaterDesc').innerHTML=\""+ fieldUpdaterDesc +"\";");

sendCustomEmailButtonDesc = sendCustomEmailButtonCount == 0? bundle.getString("sendCustomEmailButton_digest_no"):
    bundle.getString("sendCustomEmailButton_digest", new Object[] {sendCustomEmailButtonCount, sendCustomEmailButtonCount > 1? "s":""}) + "<br>" + sendCustomEmailButtonDesc;
out.println("if($('sendCustomEmailButtonDesc')) $('sendCustomEmailButtonDesc').innerHTML=\""+ sendCustomEmailButtonDesc +"\";");
if(form.getActionButtons().size() > 1) {
    combineButtonsDesc = combineButtonsCount == 0? bundle.getString("combineButtons_digest_no"):
        bundle.getString("combineButtons_digest", new Object[] {combineButtonsCount, combineButtonsCount > 1? "s":""}) + "<br>" + combineButtonsDesc;
    out.println("if($('combineButtonsDesc')) $('combineButtonsDesc').innerHTML=\""+ combineButtonsDesc +"\";");
} else {
    out.println("$('customButtonsDiv2').style.display=\"none\";");
}

int rulesNum = new FormRules(form).getRules().size();
String interactiveRuleDesc = rulesNum == 0? bundle.getString("conditionalFormattingDigestNo"): bundle.getString("conditionalFormattingDigest", new Object[] {rulesNum, "s"});
out.println("if($('interactiveRuleDesc')) $('interactiveRuleDesc').innerHTML=\"" + interactiveRuleDesc + "\";");
 */

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
