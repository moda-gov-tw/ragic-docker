/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.taoyuan;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.NUIFilter;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.apache.poi.ss.usermodel.Sheet;
import org.apache.poi.ss.util.CellRangeAddress;
import org.apache.poi.xssf.streaming.SXSSFSheet;
import org.apache.poi.xssf.streaming.SXSSFWorkbook;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;
import java.io.FileInputStream;
import java.io.IOException;
import java.io.InputStream;
import java.net.URLEncoder;
import java.util.ArrayList;
import java.util.Collections;
import java.util.LinkedList;
import java.util.List;

public final class generateQuestionHitRateReport_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


    final String[] REPORT_NAME = {"各議員模擬議題蒐集暨命中率分析表", "各議員模擬議題蒐集暨命中率概況表", "各局處模擬議題蒐集暨命中率概況表"};

    final int SESSION_FID = 1003274;                    // 屆次
    final int REPORT_SHEET_INDEX_FID = 1003283;         // 提報表單 index

    final int QUESTION_MEMBER_SUBTABLE_KEY = 1003278;   // 議員子表格
    final int MAIN_OFFICE_FID = 1003282;                // 主責局處
    final int QUESTION_MEMBER_FID = 1003275;            // 議員
    final int QUESTION_COUNT_FID = 1003276;             // 質詢題數

    final int[] NUMBER_OF_CELL_PER_ROW = {8, 6, 7};

    String apname;
    String whichSession;
    int reportSheetIndex;



    private void createHitRateReportByCollectionOfficeReportSheet(SXSSFWorkbook workbook, Sheet templateSheet, String office, List<String> questionMemberSubtable) {
        SXSSFSheet reportSheet = (SXSSFSheet) workbook.createSheet(office);
        Row templateHeaderRow = templateSheet.getRow(0);
        Row templateTitleRow = templateSheet.getRow(1);
        Row templateContentRow = templateSheet.getRow(2);
        Row templateTotalRow = templateSheet.getRow(3);

        Row headerRow = reportSheet.createRow(0);
        Row titleRow = reportSheet.createRow(1);
        int numberOfCellPerRow = NUMBER_OF_CELL_PER_ROW[2];
        for (int i = 0; i < numberOfCellPerRow; i++) {
            headerRow.createCell(i).setCellStyle(templateHeaderRow.getCell(i).getCellStyle());
            Cell cell = titleRow.createCell(i);
            cell.setCellValue(templateTitleRow.getCell(i).getStringCellValue());
            cell.setCellStyle(templateTitleRow.getCell(i).getCellStyle());
            reportSheet.setColumnWidth(i, templateSheet.getColumnWidth(i));
        }
        reportSheet.addMergedRegion(new CellRangeAddress(0, 0, 0, numberOfCellPerRow - 1));
        headerRow.setHeight(templateHeaderRow.getHeight());
        headerRow.getCell(0).setCellValue("桃園市議會" + whichSession + "定期會" + office + REPORT_NAME[2].substring(3));

        int rowCount = 1;

        for (String qmString : questionMemberSubtable) {
            String qmName = qmString.split("\\|")[1];
            int numberB = getQuestionCount(office, qmName, false);
            if (numberB == 0) continue;
            int numberA = getQuestionCount(null, qmName, false);
            int numberC = getQuestionCount(null, qmName, true);
            int numberD = getQuestionCount(office, qmName, true);
            Row row = reportSheet.createRow(++rowCount);
            for (int i = 0; i < numberOfCellPerRow; i++) {
                Cell newCell = row.createCell(i);
                CellStyle style = templateContentRow.getCell(i).getCellStyle();
                newCell.setCellStyle(style);
            }
            row.getCell(0).setCellValue(qmName);
            row.getCell(1).setCellValue(numberA);
            row.getCell(2).setCellValue(numberB);
            if (numberA > 0) row.getCell(3).setCellValue((double) numberB / numberA);
            row.getCell(4).setCellValue(numberC);
            row.getCell(5).setCellValue(numberD);
            if (numberC > 0) row.getCell(6).setCellValue((double) numberD / numberC);
        }
        if (rowCount > 1) {
            Row row = reportSheet.getRow(rowCount);
            for (int i = 0; i < numberOfCellPerRow; i++) {
                row.getCell(i).setCellStyle(templateTotalRow.getCell(i).getCellStyle());
            }
        } else {
            Row row = reportSheet.createRow(2);
            for (int i = 0; i < numberOfCellPerRow; i++) {
                row.createCell(i).setCellStyle(templateTotalRow.getCell(i).getCellStyle());
            }
        }
    }

    private void createHitRateReportByQuestionMemberReportSheet(SXSSFWorkbook workbook, Sheet templateSheet, String qmString, List<String> allOffices) {
        String qmName = qmString.split("\\|")[1];
        SXSSFSheet reportSheet = (SXSSFSheet) workbook.createSheet(qmName);
        Row templateHeaderRow = templateSheet.getRow(0);
        Row templateTitleRow = templateSheet.getRow(1);
        Row templateContentRow = templateSheet.getRow(2);
        Row templateTotalRow = templateSheet.getRow(3);

        Row headerRow = reportSheet.createRow(0);
        Row titleRow = reportSheet.createRow(1);
        int numberOfCellPerRow = NUMBER_OF_CELL_PER_ROW[1];
        for (int i = 0; i < numberOfCellPerRow; i++) {
            headerRow.createCell(i).setCellStyle(templateHeaderRow.getCell(i).getCellStyle());
            Cell cell = titleRow.createCell(i);
            cell.setCellValue(templateTitleRow.getCell(i).getStringCellValue());
            cell.setCellStyle(templateTitleRow.getCell(i).getCellStyle());
            reportSheet.setColumnWidth(i, templateSheet.getColumnWidth(i));
        }
        reportSheet.addMergedRegion(new CellRangeAddress(0, 0, 0, numberOfCellPerRow - 1));
        headerRow.setHeight(templateHeaderRow.getHeight());
        headerRow.getCell(0).setCellValue("桃園市議會" + whichSession + "定期會" + qmName + REPORT_NAME[2].substring(3));

        int numberA = Encoder.parseInt(qmString.split("\\|")[2]);
        int totalB = 0, totalC = 0, rowCount = 2;
        for (String office : allOffices) {
            int numberB = getQuestionCount(office, qmName, false);
            if (numberB == 0) continue;
            int numberC = getQuestionCount(office, qmName, true);
            Row row = reportSheet.createRow(rowCount++);
            for (int i = 0; i < numberOfCellPerRow; i++) {
                Cell newCell = row.createCell(i);
                CellStyle style = templateContentRow.getCell(i).getCellStyle();
                newCell.setCellStyle(style);
            }
            totalB += numberB;
            totalC += numberC;

            row.getCell(1).setCellValue(office);
            row.getCell(2).setCellValue(numberB);
            row.getCell(3).setCellValue(numberC);
            if (numberA > 0) row.getCell(4).setCellValue((double) numberC / numberA);
            if (numberB > 0) row.getCell(5).setCellValue((double) numberC / numberB);
        }
        Row row = reportSheet.createRow(rowCount);
        for (int i = 0; i < numberOfCellPerRow; i++) {
            Cell newCell = row.createCell(i);
            newCell.setCellStyle(templateTotalRow.getCell(i).getCellStyle());
        }
        reportSheet.addMergedRegion(new CellRangeAddress(rowCount, rowCount, 0, 1));
        reportSheet.addMergedRegion(new CellRangeAddress(2, rowCount - 1, 0, 0));
        reportSheet.getRow(2).getCell(0).setCellValue(numberA);
        row.getCell(0).setCellValue(templateTotalRow.getCell(0).getStringCellValue());
        row.getCell(2).setCellValue(totalB);
        row.getCell(3).setCellValue(totalC);
        if (numberA > 0) row.getCell(4).setCellValue((double) totalC / numberA);
        row.getCell(5).setCellValue((double) totalC / totalB);
    }

    private void createHitRateReportSheet(SXSSFWorkbook workbook, Sheet templateSheet, List<String> questionMemberSubtable) {
        SXSSFSheet reportSheet = (SXSSFSheet) workbook.createSheet("命中率分析表");
        Row templateHeaderRow = templateSheet.getRow(0);
        Row templateTitleRow = templateSheet.getRow(1);
        Row templateContentRow = templateSheet.getRow(2);
        Row templateTotalRow = templateSheet.getRow(3);

        Row headerRow = reportSheet.createRow(0);
        Row titleRow = reportSheet.createRow(1);
        int numberOfCellPerRow = NUMBER_OF_CELL_PER_ROW[0];
        for (int i = 0; i < numberOfCellPerRow; i++) {
            headerRow.createCell(i).setCellStyle(templateHeaderRow.getCell(i).getCellStyle());
            Cell cell = titleRow.createCell(i);
            cell.setCellValue(templateTitleRow.getCell(i).getStringCellValue());
            cell.setCellStyle(templateTitleRow.getCell(i).getCellStyle());
            reportSheet.setColumnWidth(i, templateSheet.getColumnWidth(i));
        }
        reportSheet.addMergedRegion(new CellRangeAddress(0, 0, 0, numberOfCellPerRow - 1));
        headerRow.setHeight(templateHeaderRow.getHeight());
        headerRow.getCell(0).setCellValue("桃園市議會" + whichSession + "定期會" + REPORT_NAME[0]);


        int totalA = 0, totalB = 0, totalC = 0, rowCount = 2;
        for (String qmString : questionMemberSubtable) {
            String[] officeMemberCount = qmString.split("\\|");
            Row row = reportSheet.createRow(rowCount++);
            for (int i = 0; i < numberOfCellPerRow; i++) {
                Cell newCell = row.createCell(i);
                CellStyle style = templateContentRow.getCell(i).getCellStyle();
                if (i == 1) style.setWrapText(true);
                newCell.setCellStyle(style);
            }
            String mainOffice = officeMemberCount[0];
            String questionMember = officeMemberCount[1];
            String questionCount = officeMemberCount[2];

            int numberA = Encoder.parseInt(questionCount);
            int numberB = getQuestionCount(null, questionMember, false);
            int numberC = getQuestionCount(null, questionMember, true);

            totalA += numberA;
            totalB += numberB;
            totalC += numberC;

            row.getCell(0).setCellValue(rowCount - 2);
            row.getCell(1).setCellValue(mainOffice);
            row.getCell(2).setCellValue(questionMember);
            row.getCell(3).setCellValue(numberA);
            row.getCell(4).setCellValue(numberB);
            row.getCell(5).setCellValue(numberC);
            if (numberA > 0) row.getCell(6).setCellValue((double) numberC / numberA);
            if (numberB > 0) row.getCell(7).setCellValue((double) numberC / numberB);
        }
        Row row = reportSheet.createRow(rowCount);
        for (int i = 0; i < numberOfCellPerRow; i++) {
            Cell newCell = row.createCell(i);
            newCell.setCellStyle(templateTotalRow.getCell(i).getCellStyle());
        }
        reportSheet.addMergedRegion(new CellRangeAddress(rowCount, rowCount, 0, 2));
        row.getCell(0).setCellValue(templateTotalRow.getCell(0).getStringCellValue());
        row.getCell(3).setCellValue(totalA);
        row.getCell(4).setCellValue(totalB);
        row.getCell(5).setCellValue(totalC);
        row.getCell(6).setCellValue((double) totalC / totalA);
        row.getCell(7).setCellValue((double) totalC / totalB);
    }

    private int getQuestionCount(String reportOffice, String questionMember, boolean isHit) {
        List<String> filters = new LinkedList<>();
        filters.add(1002875 + "|" + NUIFilter.OPERAND_EQ + "|" + questionMember);
        if (reportOffice != null) {
            filters.add(1002937 + "|" + NUIFilter.OPERAND_EQ + "|" + reportOffice);
        }
        if (isHit) {
            filters.add(1002902 + "|" + NUIFilter.OPERAND_EQ + "|" + "命中");
        }
        APIQuery query = new APIQuery(apname, "/forms", reportSheetIndex);
        query.setFiltersStringV2(filters.toArray(new String[filters.size()]));
        return query.getAPIResults().getData().size();
    }

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("org.apache.poi.ss.util.CellRangeAddress");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIFilter");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Sheet");
    _jspx_imports_classes.add("java.io.IOException");
    _jspx_imports_classes.add("java.io.FileInputStream");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("java.util.ArrayList");
    _jspx_imports_classes.add("org.apache.poi.xssf.streaming.SXSSFWorkbook");
    _jspx_imports_classes.add("org.apache.poi.xssf.usermodel.XSSFWorkbook");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("java.net.URLEncoder");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Cell");
    _jspx_imports_classes.add("org.apache.poi.xssf.streaming.SXSSFSheet");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Row");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.CellStyle");
    _jspx_imports_classes.add("java.util.LinkedList");
    _jspx_imports_classes.add("java.util.Collections");
    _jspx_imports_classes.add("java.io.InputStream");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    apname = request.getParameter("a");
    String typeNumber = request.getParameter("type");
    String nodeId = request.getParameter("id");
    if (apname == null || typeNumber == null || nodeId == null) return;
    int type = Encoder.parseInt(typeNumber);
    XSSFWorkbook template;
    Sheet templateSheet;
    try {
        InputStream fs = new FileInputStream("web/cust/taoyuan/template/" + REPORT_NAME[type] + ".xlsx");
        template = new XSSFWorkbook(fs);
        templateSheet = template.getSheetAt(0);
    } catch (IOException e) {
        e.printStackTrace();
        return;
    }
    APIQuery query = new APIQuery(apname, "/forms4", 17);    // 議員質詢題數統計
    query.setIfEidNaming(true);
    APIEntry entry = query.getAPIEntry(Encoder.parseInt(nodeId));
    reportSheetIndex = Encoder.parseInt(entry.getFieldValue(REPORT_SHEET_INDEX_FID));
    whichSession = entry.getFieldValue(SESSION_FID);
    int questionMemberSubtableSize = entry.getSubtableSize(QUESTION_MEMBER_SUBTABLE_KEY);
    List<String> questionMemberSubtable = new ArrayList<>();
    List<String> allOffices = new ArrayList<>();
    for (int i = 0; i < questionMemberSubtableSize; i++) {
        String mainOffices = entry.getSubtableFieldValue(QUESTION_MEMBER_SUBTABLE_KEY, i, MAIN_OFFICE_FID + "");
        questionMemberSubtable.add(mainOffices + "|" + entry.getSubtableFieldValue(QUESTION_MEMBER_SUBTABLE_KEY, i, QUESTION_MEMBER_FID + "") + "|" + entry.getSubtableFieldValue(QUESTION_MEMBER_SUBTABLE_KEY, i, QUESTION_COUNT_FID + ""));
        for (String office : mainOffices.split("、")) {
            if (!allOffices.contains(office)) {
                allOffices.add(office);
            }
        }
    }
    Collections.sort(questionMemberSubtable);

    SXSSFWorkbook workbook = new SXSSFWorkbook(template);
    switch (type) {
        case 0:
            createHitRateReportSheet(workbook, templateSheet, questionMemberSubtable);
            break;
        case 1:
            for (String qmString : questionMemberSubtable) {
                createHitRateReportByQuestionMemberReportSheet(workbook, templateSheet, qmString, allOffices);
            }
            break;
        case 2:
            for (String office : allOffices) {
                createHitRateReportByCollectionOfficeReportSheet(workbook, templateSheet, office, questionMemberSubtable);
            }
            break;
    }

    response.setCharacterEncoding("UTF-8");
    response.setContentType("application/ms-excel");
    response.setHeader("Content-Disposition", "attachment;filename*=UTF-8''" + URLEncoder.encode(REPORT_NAME[type], "UTF-8") + ".xlsx");
    out.clear();
    out = pageContext.pushBody();
    ServletOutputStream output = response.getOutputStream();
    workbook.removeSheetAt(0);      // delete the template sheet from workbook
    workbook.write(output);
    output.flush();
    output.close();

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
