/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.diageo;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.RagicConfig;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.S3Helper;
import com.ragic.s3.sdb.data.bdb.BDBSmartWorker;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.service.notification.Notification;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import com.ragic.s3.util.DES;
import org.apache.poi.ss.usermodel.*;
import java.io.FileInputStream;
import java.io.InputStream;
import java.net.URLEncoder;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Map;

public final class checkAll_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("org.apache.poi.ss.usermodel");
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.service.notification.Notification");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBSmartWorker");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("java.util.HashMap");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("java.io.FileInputStream");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.ArrayList");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("java.net.URLEncoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.S3Helper");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("com.ragic.s3.util.DES");
    _jspx_imports_classes.add("java.io.InputStream");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;

      out.write("<!DOCTYPE>\r\n<html>\r\n<head>\r\n    <meta http-equiv=\"Content-Type\" content=\"text/html; charset=UTF-8\">\r\n    <title>Check all the things</title>\r\n</head>\r\n<body>\r\n\r\n");

    final String AP_NAME = "diageo";
    final String AREA;
    final String TEAM_PRODUCT;
    final int YEAR;
    final String MONTH;
    int MONTH_NUM;
    final int DAYS = 31;

    final int ROW_SPACE = 35;		//Modified by Jean
    final int COLUMN_SPACE = 9;		//Modified by Jean

    //field of upload schedule page
    final int F_UP_ROOT_NUMBER = 1000115; //root
    final int F_YEAR = 1000128;
    final int F_MONTH = 1000116;
    final int F_UP_UPLOADER = 1000117;
    final int F_FILE = 1000118;
    final int F_STATUS = 1000119;
    final int F_AREA = 1000126;
    final int F_CHANNEL = 1000127;
    final int F_UP_CHECKED = 1000148;
    final int F_UP_SL_ROOT = 1000121;
    final int F_UP_SL_NAME = 1000122;
    final int F_UP_SL_STATUS = 1000123;

    //field of Store page "S"
    final int F_S_STORE = 1000057;
    final int F_S_TYPE = 100058;

    // field of PG page "PG"
    final int F_PG_NUMBER = 1000062;
    final int F_PG_CHNAME = 1000063;
    final int F_PG_NICKNAME = 1000064;

    //field of product page "PD"
    final int F_PD_ABBR_NAME = 1000223;
    final int F_PD_FULL_NAME = 1000224;


    final String V_CHECKED = "已完成";
    final String V_HAS_TYPO = "有錯誤";

    //field of user page
    final int USER_ID = DConst.USER_USERID_EMAIL;
    final int USER_GROUP = DConst.USER_GROUP;
    final int USER_STATUS = DConst.USER_STATUS;
    final int USER_NAME = DConst.USER_NAME;

    final int MAIL_TYPE = Const.NOTIFY_WORKFLOW;

    //error message list
    ArrayList<String> errorList = new ArrayList<String>();
    
    
    //get node id
    String nodeId = request.getParameter("no");
    int intNodeId = Integer.parseInt(nodeId);
    User user = (User) session.getAttribute("USER");
    //declare dbd workers
    BDBSmartWorker worker = new BDBSmartWorker(AP_NAME);
    
    //check if the user is the uploader, if so, importing is allowed.
    if (!user.getUserName(AP_NAME).equalsIgnoreCase(worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_UP_UPLOADER))) {
        out.println("上傳者才能檢查班表");
        return;

    }


    //open th uploaded file
    String file = worker.getFieldValue(F_UP_ROOT_NUMBER, Integer.parseInt(nodeId), F_FILE);
    if (file.isEmpty()) {
        out.println("沒有上傳檔案！");
        return;
    }
    if (file.endsWith("xlsx")) {
        out.println("檔案格式不正確！檔案須為Excel97~2003格式(.xls)");
        return;
    }

    InputStream inp;
    String s3Name = RagicConfig.getInstance().getProperty("SERVER_NAME")+"/"+AP_NAME+"/"+file;
    if('@'==file.charAt(8) && S3Helper.isValidFile(Const.S3_BUCKET_UPLOAD, s3Name)){
        inp = S3Helper.getURLPresigned(Const.S3_BUCKET_UPLOAD, s3Name).openStream();
    } else {
        inp = new FileInputStream("cust/data/" + AP_NAME + "/upload/" + file);
    }
    Workbook wb = WorkbookFactory.create(inp);
    Sheet sheet1 = wb.getSheetAt(wb.getActiveSheetIndex());
    
    Row row;
    Cell cell;
    
    //get YEAR
    row = sheet1.getRow(2);
    cell = row.getCell(0);    
    if(cell.getCellType() != CellType.NUMERIC){
        YEAR = Integer.parseInt(cell.getStringCellValue());
    } else {
        YEAR = (int) cell.getNumericCellValue();
    }
    //get MONTH
    row = sheet1.getRow(2);
    cell = row.getCell(1);
    MONTH = cell.getRichStringCellValue().getString();


    //transfer month to number form
    if (MONTH.equalsIgnoreCase("JAN"))
        MONTH_NUM = 1;
    else if (MONTH.equalsIgnoreCase("FEB"))
        MONTH_NUM = 2;
    else if (MONTH.equalsIgnoreCase("MAR"))
        MONTH_NUM = 3;
    else if (MONTH.equalsIgnoreCase("APR"))
        MONTH_NUM = 4;
    else if (MONTH.equalsIgnoreCase("MAY"))
        MONTH_NUM = 5;
    else if (MONTH.equalsIgnoreCase("JUN"))
        MONTH_NUM = 6;
    else if (MONTH.equalsIgnoreCase("JUL"))
        MONTH_NUM = 7;
    else if (MONTH.equalsIgnoreCase("AUG"))
        MONTH_NUM = 8;
    else if (MONTH.equalsIgnoreCase("SEP"))
        MONTH_NUM = 9;
    else if (MONTH.equalsIgnoreCase("OCT"))
        MONTH_NUM = 10;
    else if (MONTH.equalsIgnoreCase("NOV"))
        MONTH_NUM = 11;
    else if (MONTH.equalsIgnoreCase("DEC"))
        MONTH_NUM = 12;
    else
        MONTH_NUM = -1;

    //get field values
    String v_area = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_AREA);
    String v_channel = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_CHANNEL);
    String v_year = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_YEAR);
    String v_month = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_MONTH);

    //check if dates match
    if ((Integer.parseInt(v_year) != YEAR) || (Integer.parseInt(v_month) != MONTH_NUM)) {
        errorList.add("資料中的年月與表單所選年月不符，請檢查後再重新上傳");
    }
    
    // check restaurants, PGs
    int dayForST;
    int currentRowIndexForST = 3;
    Row currentRowForST;
    int currentColumnIndexForST = 0;
    ArrayList<String> FakeStores = new ArrayList<String>();
    ArrayList<String> FakePG = new ArrayList<String>();
    boolean storeExists = false;
    boolean PGExists;
    do {
        do {
            dayForST = 1;
            for (int r = currentRowIndexForST; ; r++) {
				storeExists = false;
                Row rit = sheet1.getRow(r);
                Cell cit=null;
                try{
                    cit = rit.getCell(currentColumnIndexForST);
                } catch(Exception e){
                    System.out.println(r);
                }
                if (cit == null) break; //break if out of data range
                Cell storeCell = rit.getCell(currentColumnIndexForST + 2); // move to store name column
				if (storeCell!=null && storeCell.getCellType() == CellType.STRING) {
                    /**
                    for (String s : storeList) {
                        if (storeCell.getStringCellValue().contains("請假") || 
                            s.equalsIgnoreCase(storeCell.getStringCellValue()))
                            storeExists = true;
                    }

                    if (!storeExists) {
                        FakeStores.add(storeCell.getStringCellValue());
                    }
                    **/
                }

                Cell PGCell = rit.getCell(currentColumnIndexForST + 7);
				PGExists = false;
                if (PGCell!=null && PGCell.getCellType() == CellType.STRING) {
                    /**
                    String[] PGs = PGCell.getStringCellValue().split("/");
                    for (String PG : PGs) {
                        for (String p : PGList) {
                            if (p.equalsIgnoreCase(PG))
                                PGExists = true;
                        }
                        if (!PGExists)
                            FakePG.add(PG);
                    }
                    **/
                }

                dayForST++;
                               
                if (dayForST > DAYS) break;
            }
            currentRowForST = sheet1.getRow(currentRowIndexForST);
            currentColumnIndexForST += COLUMN_SPACE;

        } while (currentColumnIndexForST<45);

        currentColumnIndexForST = 0;
        currentRowIndexForST += ROW_SPACE;
        currentRowForST = sheet1.getRow(currentRowIndexForST);
        if (currentRowForST == null) {
            break;
        }
    } while (currentColumnIndexForST<45);
	    //check if all stores in the file exist in the database
    if (FakeStores.size() > 0) {
        for (int i = 0; i < FakeStores.size(); i++) {
            errorList.add("店家「" + FakeStores.get(i) + "」不存在資料庫中！請檢查是否輸入錯誤");
        }
    }
    //check if all PGs in the file exist in the database
    if (FakePG.size() > 0) {
        for (int i = 0; i < FakePG.size(); i++) {
            errorList.add("PG「" + FakePG.get(i) + "」不存在資料庫中！請檢查是否輸入錯誤");
        }
    
    }
    
    if (errorList.size() == 0) {
        worker.setFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_UP_CHECKED, V_CHECKED);
        out.println("班表沒有錯誤");
		
		
        //get email list of those sales in the check list
        BDBWorker saleWorker = new BDBWorker(AP_NAME);
        ArrayList<Integer> nodeIdList = new ArrayList<Integer>();
        ArrayList<String> saleIdList = new ArrayList<String>();
        Map<String, String> saleMap = new HashMap<String, String>();
        try {
            SDBQueryFull saleQ = new SDBQueryFull(saleWorker);
            SDBQueryFull saleQ2 = new SDBQueryFull(saleWorker);
            saleQ.go(F_UP_ROOT_NUMBER);
            nodeIdList.add(intNodeId);
            saleQ.filter(nodeIdList);
            SDBNode rootNode;
            while ((rootNode = saleQ.next()) != null) {

                saleQ.go(F_UP_SL_ROOT);
                SDBNode node;
                while ((node = saleQ.next()) != null) {

                    saleIdList.add(saleQ.dip(F_UP_SL_NAME));
                    saleQ2.go(USER_ID);
                    saleQ2.filter(saleQ.dip(F_UP_SL_NAME));
                    saleQ2.next();
                    saleMap.put(saleQ.dip(F_UP_SL_NAME), saleQ2.dip(USER_NAME));
                    System.out.println(saleQ.dip(F_UP_SL_NAME) + "   " + saleQ2.dip(USER_NAME));
                    saleQ2.goback();
                }
                saleQ.goback();
            }


        } finally {
            saleWorker.commit();
        }

        //send emails to sales in check list
        String subject = v_year + "年" + v_month + "月" + v_area + v_channel + "的排班表已上傳，請確認。";
        String fileUrl = "https://" + request.getServerName() + "/sims/file.jsp?a=" + AP_NAME + "&f=" + Encoder.encodeURIComponent(file);

        String confirmUrl = "https://" + request.getServerName() + "/cust/" + AP_NAME + "/saleConfirm.jsp?a=" + AP_NAME + "&p=/1_pg&si=6&bid=1&nodeId=" + nodeId + "&email=";
        String initMessage = v_year + "年" + v_month + "月" + v_area + v_channel + "的排班表已上傳，請點選進入瀏覽以及確認排班表<br/>";
        String message;


        String saleName, encodeEmail = "";

        for (String saleId : saleIdList) {
            saleName = saleMap.get(saleId);
            encodeEmail = new DES().encrypt(saleId);

            //message = "Hi " + saleName + ",<br/>" + initMessage + "<a href='" + fileUrl + "'>瀏覽班表</a><br />" + "<a href='" + confirmUrl + URLEncoder.encode(encodeEmail, "UTF-8") + "'>確認班表</a><br />";
            
            message ="<img src='https://"+request.getServerName()+"/cust/diageo/mailHeader.jpg' />"
               + "<div style='line-height:200%;margin-left:50px'>"
               + "Hi "+saleName+",<br/>" + initMessage + "<a href='" + fileUrl + "'>瀏覽班表</a><br />" + "<a href='" + confirmUrl + URLEncoder.encode(encodeEmail, "UTF-8") + "'>確認班表</a><br />"
               + "</div>"			   
               + "<img src='https://"+request.getServerName() + "/cust/diageo/mailFooter.jpg' />"
			   + "<div style='line-height:150%;font-size:12px;margin-top:10px;margin-left:10px'>如無法看到連結內容，可直接複製以下網址，至瀏覽器網址列貼上即可前往<br/>"
			   + "瀏覽班表網址："+fileUrl+"<br />"
			   + "確認班表網址："+confirmUrl + URLEncoder.encode(encodeEmail, "UTF-8")+"<br />"
			   +"</div>";
            Notification.createNotification(AP_NAME, saleId, subject, message, fileUrl + encodeEmail, MAIL_TYPE);
        }


        out.println("系統已寄信通知sales確認表單!");


    } else {

        for (int i = 0; i < errorList.size(); i++) {
            out.println(errorList.get(i));
        }
        worker.setFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_UP_CHECKED, V_HAS_TYPO);
    }
    
    

      out.write("<script type=\"text/javascript\">\r\n\r\n</script>\r\n\r\n</body>\r\n</html>");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
