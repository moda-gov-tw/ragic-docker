/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.diageo;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.RagicConfig;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.S3Helper;
import com.ragic.s3.sdb.data.bdb.BDBSmartWorker;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.util.Const;
import org.apache.poi.ss.usermodel.*;
import java.io.FileInputStream;
import java.io.InputStream;

public final class uploadSchedule_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("org.apache.poi.ss.usermodel");
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicConfig");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.S3Helper");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBSmartWorker");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("java.io.FileInputStream");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.io.InputStream");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    final String AP_NAME = "diageo";
    final int YEAR;
    final String MONTH;
    int MONTH_NUM;
    final int DAYS = 31;
    final int PARAM_NUM = 14;

    final int ROW_SPACE = 35;
    final int COLUMN_SPACE = 9;

    //field of planned schedule page
    final int F_PL_ROOT_NUMBER = 1000091; // root
    final int F_PL_YEAR = 1000129;
    final int F_PL_MONTH = 1000146;
    final int F_PL_DAY = 1000147;
    final int F_WEEKDAY = 1000092;
    final int F_STORE = 1000321;
    final int F_TARGET = 1000053;   //目標業績
    final int F_PRODUCT = 1000355;  //主推商品
    
    final int F_TIME = 1000111;
    final int F_HOURS = 1000112;
    final int F_PG = 1000113;
    final int F_SALE = 1000114;
    final int F_PL_AREA = 1000124;
    final int F_PL_CHANNEL = 1000125;

    //field of upload schedule page
    final int F_UP_ROOT_NUMBER = 1000115; //root
    final int F_YEAR = 1000128;
    final int F_MONTH = 1000116;
    final int F_UP_UPLOADER = 1000117;
    final int F_FILE = 1000118;
    final int F_STATUS = 1000119;
    final int F_AREA = 1000126;
    final int F_CHANNEL = 1000127;

    //field of Store page "S"
    final int F_S_STORE = 1000057;
    final int F_S_TYPE = 100058;

    //get node id
    String nodeId = request.getParameter("no");
    int intNodeId = Integer.parseInt(nodeId);

    //declare dbd workers
    BDBSmartWorker worker = new BDBSmartWorker(AP_NAME);

    User user = (User) session.getAttribute("USER");
    if(!user.getUserName(AP_NAME).equalsIgnoreCase( worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_UP_UPLOADER))){
        out.println("匯入終止：上傳者才能匯入班表");
        return;

    }

    //check the status
    final String V_ALL_CONFIRMED = "全數確認";
    if (!worker.getFieldValue(F_UP_ROOT_NUMBER, Integer.parseInt(nodeId), F_STATUS).equals(V_ALL_CONFIRMED)) {
        out.println("匯入終止：班表狀態未確認，無法匯入班表");
        return;
    }

    //open the uploaded file
    String file = worker.getFieldValue(F_UP_ROOT_NUMBER, Integer.parseInt(nodeId), F_FILE);
    if(file.isEmpty()){
        out.println("匯入終止：請選擇上傳檔案！");
        return;
    }
    InputStream inp;
    String s3Name = RagicConfig.getInstance().getProperty("SERVER_NAME")+"/"+AP_NAME+"/"+file;
    if('@'==file.charAt(8) && S3Helper.isValidFile(Const.S3_BUCKET_UPLOAD, s3Name)){
        inp = S3Helper.getURLPresigned(Const.S3_BUCKET_UPLOAD, s3Name).openStream();
    } else {
        inp = new FileInputStream("cust/data/" + AP_NAME + "/upload/" + file);
    }
    Workbook wb = WorkbookFactory.create(inp);
    Sheet sheet1 = wb.getSheetAt(wb.getActiveSheetIndex());

    Row row;
    Cell cell;
       
    //get YEAR
    row = sheet1.getRow(2);
    cell = row.getCell(0);
    YEAR = (int) cell.getNumericCellValue();
    //get MONTH
    cell = row.getCell(1);
    MONTH = cell.getRichStringCellValue().getString();

    //transfer month to number form
    if (MONTH.equalsIgnoreCase("JAN"))
        MONTH_NUM = 1;
    else if (MONTH.equalsIgnoreCase("FEB"))
        MONTH_NUM = 2;
    else if (MONTH.equalsIgnoreCase("MAR"))
        MONTH_NUM = 3;
    else if (MONTH.equalsIgnoreCase("APR"))
        MONTH_NUM = 4;
    else if (MONTH.equalsIgnoreCase("MAY"))
        MONTH_NUM = 5;
    else if (MONTH.equalsIgnoreCase("JUN"))
        MONTH_NUM = 6;
    else if (MONTH.equalsIgnoreCase("JUL"))
        MONTH_NUM = 7;
    else if (MONTH.equalsIgnoreCase("AUG"))
        MONTH_NUM = 8;
    else if (MONTH.equalsIgnoreCase("SEP"))
        MONTH_NUM = 9;
    else if (MONTH.equalsIgnoreCase("OCT"))
        MONTH_NUM = 10;
    else if (MONTH.equalsIgnoreCase("NOV"))
        MONTH_NUM = 11;
    else if (MONTH.equalsIgnoreCase("DEC"))
        MONTH_NUM = 12;
    else
        MONTH_NUM = -1;

    //get field values
    String v_area = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_AREA);
    String v_channel = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_CHANNEL);
    String v_year = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_YEAR);
    String v_month = worker.getFieldValue(F_UP_ROOT_NUMBER, intNodeId, F_MONTH);

    //check if dates match
    if ((Integer.parseInt(v_year) != YEAR) || (Integer.parseInt(v_month) != MONTH_NUM)) {
        out.println("匯入終止：上傳檔案中的年月與表單上年月不符，請檢查後再匯入");
        return;
    }

    /** 不檢查店家
    //get the List of name of all customers    
    ArrayList<String> storeList = new ArrayList<String>();
    BDBWorker worker3 = new BDBWorker(AP_NAME);
    try {
        SDBQueryFull sdbQuery = new SDBQueryFull(worker3);
        sdbQuery.go(F_S_STORE);
        List<SDBNode> nodes = sdbQuery.getList();
        for (SDBNode n : nodes) {
            storeList.add(n.getValue());
        }
    } finally {
        worker3.commit();
    }
    **/

    //check clients
    int dayForST;
    int currentRowIndexForST = 3;
    Row currentRowForST;
    int currentColumnIndexForST = 0;
    
    /** 不檢查店家
    ArrayList<String> FakeStores = new ArrayList<String>();
    boolean storeExists;
    do {
        do {
            dayForST = 1;
            for (int r = currentRowIndexForST; ; r++) {
                storeExists = false;
                Row rit = sheet1.getRow(r);
                Cell cit = rit.getCell(currentColumnIndexForST);
                if (cit == null) break; //break if out of data range
                Cell storeCell = rit.getCell(currentColumnIndexForST + 2); // move to store name column
                if (storeCell.getCellType() == Cell.CELL_TYPE_STRING) {
                    
                    for (String s : storeList) {
                        if (storeCell.getStringCellValue().contains("請假") || 
                            s.equalsIgnoreCase(storeCell.getStringCellValue()))
                            storeExists = true;
                    }
                    if (!storeExists) {
                        FakeStores.add(storeCell.getStringCellValue());
                    }
                    
                }

                dayForST++;

                if (dayForST > DAYS) break;
            }
            currentRowForST = sheet1.getRow(currentRowIndexForST);
            currentColumnIndexForST += COLUMN_SPACE;            
        } while (currentRowForST.getCell(currentColumnIndexForST).getCellType() != Cell.CELL_TYPE_BLANK);
        
        currentColumnIndexForST = 0;
        currentRowIndexForST += ROW_SPACE;
        currentRowForST = sheet1.getRow(currentRowIndexForST);
        if (currentRowForST == null) {
            break;
        }        
    } while (currentRowForST.getCell(currentColumnIndexForST).getCellType() != Cell.CELL_TYPE_BLANK);


    //check if all stores in the file exist in the database
    if (FakeStores.size()>0) {
        for(int i = 0 ; i<FakeStores.size(); i++){
            out.println("上傳終止：因為店家“"+FakeStores.get(i)+"”不存在資料庫中！請檢查是否輸入錯誤");
        }
        return;
    }
	**/
        
	BDBWorker worker2 = new BDBWorker(AP_NAME);
    //delete data of the same month
    try {
        SDBQueryFull q = new SDBQueryFull(worker2);
        q.go(F_PL_ROOT_NUMBER);
        q.filter(F_PL_AREA, v_area);
        q.filter(F_PL_CHANNEL, v_channel);
        q.filter(F_PL_MONTH, v_month);
        q.filter(F_PL_YEAR, v_year);
        SDBNode node;
        while ((node = q.next()) != null) {
            worker2.delNode(F_PL_ROOT_NUMBER, node.getId());
        }
    } finally {
        worker2.commit();
    }

    //parameters of worker.addRecord()
    int[] fieldId = {F_PL_ROOT_NUMBER, F_PL_YEAR, F_PL_MONTH, F_PL_DAY, F_PL_AREA, F_PL_CHANNEL, F_WEEKDAY, F_STORE, F_PRODUCT, F_TIME, F_TARGET, F_HOURS, F_PG, F_SALE};
    boolean[] ifMerge = {false, false, false, false, false, false, false, true, false, false, false, true, false, false};

    //add each record to database
    int day;
    String[] recordValue = new String[PARAM_NUM];
    boolean validData = true;

    int currentRowIndex = 3;
    Row currentRow;
    int currentColumnIndex = 0;
    int recordCount = 0;
    do {
        do {
            day = 1;
            for (int r = currentRowIndex; ; r++) {
                Row rit = sheet1.getRow(r);
                Cell cit = rit.getCell(currentColumnIndex);
                if (cit == null) break;
                recordValue[0] = "R-"+YEAR+"-"+MONTH_NUM+"-"+(recordCount + 1) + "";
                recordValue[1] = YEAR + "";
                recordValue[2] = MONTH_NUM + "";
                recordValue[3] = day + "";
                recordValue[4] = v_area;
                recordValue[5] = v_channel;

                int c_idx = currentColumnIndex;
                for (int i = c_idx + 1; i < c_idx + 9; i++) {                    
                    switch (rit.getCell(i).getCellType()) {                        
                        case BLANK:
                            if(i==(c_idx + 3)){   //主推商品如果空白仍可匯入
                                recordValue[8]="";
                                continue;
                            }
                            if(i==(c_idx + 5)){   //目標業績如果空白仍可匯入
                                recordValue[10]="";
                                continue;
                            }
                            if(i==(c_idx + 8)){   //經銷商如果空白仍可匯入
                                recordValue[13]="";
                                continue;
                            }
                            validData = false;
                            break;
                        case NUMERIC:
                            recordValue[i - c_idx + 5] = Integer.toString((int) rit.getCell(i).getNumericCellValue());
                            continue;
                        case STRING:
                            recordValue[i - c_idx + 5] = rit.getCell(i).getRichStringCellValue().getString();
                            continue;                            
                        default:
                            continue;
                    }                    
                }

                if (validData == true) {
                    worker.addRecord(fieldId, recordValue, ifMerge);
                    recordCount++;
                }
                day++;
                validData = true;

                if (day > DAYS) break;
            }
            currentRow = sheet1.getRow(currentRowIndex);
            currentColumnIndex += COLUMN_SPACE;            

        } while (currentColumnIndex<45);

        currentColumnIndex = 0;
        currentRowIndex += ROW_SPACE;
        currentRow = sheet1.getRow(currentRowIndex);

        if (currentRow == null) {
            break;
        }
    } while (currentColumnIndex<45);

    out.println(v_year+"年"+v_month+"月班表上傳成功！");
    out.println("共上傳"+recordCount+"筆排班資料。");


    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
