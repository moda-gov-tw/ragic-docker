/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.typdcic;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.NUIFilter;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.output.CustomServlet;
import com.ragic.s3.nui.output.FileServlet;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.service.api.APIResults;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.Zip;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.apache.poi.ss.usermodel.Sheet;
import org.apache.poi.xssf.streaming.SXSSFSheet;
import org.apache.poi.xssf.streaming.SXSSFWorkbook;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;
import java.io.*;
import java.nio.file.Files;
import java.text.SimpleDateFormat;
import java.util.Calendar;
import java.util.Iterator;
import java.util.zip.Adler32;
import java.util.zip.CheckedOutputStream;
import java.util.zip.ZipOutputStream;
import com.ragic.s3.nui.helper.Util;
import java.nio.file.StandardCopyOption;

public final class generateReceiptReport_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {



    final String REPORT_NAME = "收據報署表";

    final int CASE_NUMBER_FIELD = 1000393;          // 案件管制編號
    final int PERPETRATOR_FIELD = 1000404;          // 行為人
    final int ID_NUMBER_FIELD = 1000405;            // 身分證字號
    final int AMOUNT_TO_BE_PAID_FIELD = 1000691;    // 待繳納金額
    final int AMOUNT_ALREADY_PAID_FIELD = 1000720;  // 已繳納金額
    final int IMPLEMENTATION_UNIT_FIELD = 1000651;  // 執行股別

    final int RECEIPT_SUBTABLE_KEY = 1000728;       // 收據報署子表格
    final int RECEIPT_NUMBER_FIELD = 1000662;       // 收據編號
    final int RECEIPT_IMAGE_FIELD = 1000712;        // 收據圖檔
    final int RECEIPT_DATE_FIELD = 1000781;         // 繳納日

    final int NUMBER_OF_CELL_PER_ROW = 16;

    final String[] ALL_IMPLEMENTATION_UNITS = {"忠", "孝", "仁", "愛", "信", "義", "和", "平", "甲", "乙", "丙", "丁", "戊", "己", "辛", "禮", "廉"};

    String apname;




    private APIEntry getAPIEntryByCaseNumber(String caseNumber) {
        APIQuery query = new APIQuery(apname, "/test", 2);      // 裁處總冊
        query.setIfEidNaming(true);
        query.setFiltersStringV2(new String[]{CASE_NUMBER_FIELD + "|" + NUIFilter.OPERAND_EQ + "|" + caseNumber});
        return query.getAPIResults().iterator(query.getForm()).next();
    }


  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("java.io");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.zip.ZipOutputStream");
    _jspx_imports_classes.add("com.ragic.s3.nui.NUIFilter");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.util.Zip");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.nui.output.CustomServlet");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResults");
    _jspx_imports_classes.add("java.nio.file.StandardCopyOption");
    _jspx_imports_classes.add("org.apache.poi.xssf.streaming.SXSSFWorkbook");
    _jspx_imports_classes.add("org.apache.poi.xssf.usermodel.XSSFWorkbook");
    _jspx_imports_classes.add("java.util.Calendar");
    _jspx_imports_classes.add("java.util.zip.Adler32");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Cell");
    _jspx_imports_classes.add("org.apache.poi.xssf.streaming.SXSSFSheet");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Sheet");
    _jspx_imports_classes.add("java.util.Iterator");
    _jspx_imports_classes.add("java.nio.file.Files");
    _jspx_imports_classes.add("com.ragic.s3.nui.output.FileServlet");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("java.util.zip.CheckedOutputStream");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.Row");
    _jspx_imports_classes.add("org.apache.poi.ss.usermodel.CellStyle");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    apname = request.getParameter("a");

    File dir = new File(REPORT_NAME);
    if (!dir.exists() && !dir.mkdir()) {
        return;
    }

    Calendar calendar = Calendar.getInstance();
    SimpleDateFormat sdf = new SimpleDateFormat("yyyy/MM/dd");
    String todayString = sdf.format(calendar.getTime());
    calendar.add(Calendar.DATE, -45);
    String dateBeforeSevenDays = sdf.format(calendar.getTime());

    for (String implementationUnit : ALL_IMPLEMENTATION_UNITS) {
        String unitFolder = REPORT_NAME + "/" + implementationUnit + "股";
        new File(unitFolder).mkdir();
        XSSFWorkbook template;
        try {
            InputStream fs = new FileInputStream("web/cust/typdcic/template/" + REPORT_NAME + ".xlsx");
            template = new XSSFWorkbook(fs);
        } catch (IOException e) {
            e.printStackTrace();
            return;
        }
        SXSSFWorkbook workbook = new SXSSFWorkbook(template);
        SXSSFSheet reportSheet = (SXSSFSheet) workbook.getSheetAt(0);
        Sheet templateSheet = template.getSheetAt(1);
        workbook.removeSheetAt(1);
        Row templateTitleRow = templateSheet.getRow(0);
        Row templateContentRow = templateSheet.getRow(1);
        Row titleRow = reportSheet.createRow(0);
        CellStyle titleCellStyle = templateTitleRow.getCell(0).getCellStyle();
        for (int i = 0; i < NUMBER_OF_CELL_PER_ROW; i++) {
            Cell cell = titleRow.createCell(i);
            cell.setCellValue(templateTitleRow.getCell(i).getStringCellValue());
            cell.setCellStyle(titleCellStyle);
            reportSheet.setColumnWidth(i, templateSheet.getColumnWidth(i));
        }
        String lastCaseNumber = null;
        APIQuery query = new APIQuery(apname, "/test", 38);      // 收據報署
        query.setIfEidNaming(true);
        query.setFiltersStringV2(new String[]{
                IMPLEMENTATION_UNIT_FIELD + "|" + NUIFilter.OPERAND_EQ + "|" + implementationUnit,
                RECEIPT_DATE_FIELD + "|" + NUIFilter.OPERAND_GTE + "|" + dateBeforeSevenDays,
                RECEIPT_DATE_FIELD + "|" + NUIFilter.OPERAND_LT + "|" + todayString});
        query.addFilter(1000418,NUIFilter.OPERAND_SORT_ASC,"x");

        int rowCount = 0;
        APIResults results = query.getAPIResults();

        for (Iterator<APIEntry> it = results.iterator(query.getForm()); it.hasNext(); ) {

            String caseNumber = it.next().getFieldValue(1000418);
            if (lastCaseNumber == null || !lastCaseNumber.equals(caseNumber)) {
                lastCaseNumber = caseNumber;
                APIEntry entry = getAPIEntryByCaseNumber(caseNumber);
                int receiptSubtableSize = entry.getSubtableSize(RECEIPT_SUBTABLE_KEY);
                Row row = null;
                for (int receiptIndex = 0; receiptIndex < receiptSubtableSize; receiptIndex++) {
                    int cellIndex = receiptIndex % 10;
                    if (cellIndex == 0) {
                        row = reportSheet.createRow(++rowCount);
                        for (int i = 0; i < NUMBER_OF_CELL_PER_ROW; i++) {
                            Cell newCell = row.createCell(i);
                            newCell.setCellStyle(templateContentRow.getCell(i).getCellStyle());
                        }
                        row.getCell(0).setCellValue(entry.getFieldValue(CASE_NUMBER_FIELD));
                        row.getCell(1).setCellValue(entry.getFieldValue(PERPETRATOR_FIELD));
                        row.getCell(2).setCellValue(entry.getFieldValue(ID_NUMBER_FIELD));
                        row.getCell(3).setCellValue(Encoder.parseInt(entry.getFieldValue(AMOUNT_TO_BE_PAID_FIELD)));
                        row.getCell(4).setCellValue(Encoder.parseInt(entry.getFieldValue(AMOUNT_ALREADY_PAID_FIELD)));
                        row.getCell(5).setCellValue(entry.getFieldValue(IMPLEMENTATION_UNIT_FIELD));
                    }
                    String receiptNumber = entry.getSubtableFieldValue(RECEIPT_SUBTABLE_KEY, receiptIndex, RECEIPT_NUMBER_FIELD + "");
                    String receiptImageName = entry.getSubtableFieldValue(RECEIPT_SUBTABLE_KEY, receiptIndex, RECEIPT_IMAGE_FIELD + "");
                    row.getCell(6 + cellIndex).setCellValue(receiptNumber);
                    File receiptImage = new File("cust/data/" + apname + "/upload/" + receiptImageName);
                    File destination = new File(unitFolder + "/" + receiptNumber + ".png");
                    Files.copy(receiptImage.toPath(), destination.toPath(), StandardCopyOption.REPLACE_EXISTING);
                }
            }
        }

        File file = new File(REPORT_NAME + "/" + implementationUnit + "股.xlsx");
        FileOutputStream fileOut = new FileOutputStream(file);
        workbook.write(fileOut);
        fileOut.close();
    }

    response.setCharacterEncoding("UTF-8");
    response.setContentType("application/zip");
    response.setHeader("Content-Disposition", FileServlet.getContentDisposition("attachment", REPORT_NAME + "(" + todayString + ").zip", request));

    Zip zip = new Zip();
    ServletOutputStream os = response.getOutputStream();

    CheckedOutputStream checksum = new CheckedOutputStream(os, new Adler32());
    ZipOutputStream zout = new ZipOutputStream(new BufferedOutputStream(checksum));
    zip.zipDir(REPORT_NAME, zout);
    zout.flush();
    zout.close();

    Util.deleteDirectory(dir);

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
