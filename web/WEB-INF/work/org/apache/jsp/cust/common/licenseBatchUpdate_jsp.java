/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.common;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.UserManager;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.util.Const;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.util.DConst;
import com.ragic.s3.sdb.query.SDBNode;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import java.util.List;
import java.util.Arrays;
import java.util.ArrayList;
import com.ragic.s3.nui.Account;
import com.ragic.s3.nui.AccountPaymentStatus;
import java.util.Date;
import java.text.SimpleDateFormat;
import java.util.stream.Collectors;
import com.ragic.s3.sdb.query.SDBQuery;
import com.ragic.s3.sdb.query.SDBQueryFull;
import com.ragic.s3.proxy.ProxyUpdater;

public final class licenseBatchUpdate_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.Arrays");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQuery");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.proxy.ProxyUpdater");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("java.util.stream.Collectors");
    _jspx_imports_classes.add("java.util.ArrayList");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.Account");
    _jspx_imports_classes.add("com.ragic.s3.nui.AccountPaymentStatus");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("java.util.List");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBNode");
    _jspx_imports_classes.add("com.ragic.s3.sdb.query.SDBQueryFull");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserManager");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html;charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    User user=(User)session.getAttribute("USER");
    RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
    if(user==null || !UserManager.ifRagicSupport(user.getUserId())) {
        out.println("No access right.");
        return;
    }
    
    int ni= Encoder.parseInt(request.getParameter("id"), Const.NONODE);
    if(ni < 0) {
        out.println("No such record.");
        return;
    }

    APIQuery aq=new APIQuery(Const.AP_TEAMRAGIC,"/sales",14);
    aq.setUpdateMode();
    APIEntry e = aq.getAPIEntry(ni);

    int batchNameField = 1002111;
    int licenseEdidtionField=1002104;
    int qtyField=1002105;
    int expireDateField=1002106;
    int emailsField=1002107;
    int resultsField=1002108;
    int execDate=1002109;

    String lastResult = e.getFieldValue(resultsField);
    if(lastResult!=null && !lastResult.isEmpty()){
        out.println("Already executed, please create new batch.");
        return;
    }

    String edition = e.getFieldValue(licenseEdidtionField);
    String emailStr = e.getFieldValue(emailsField);
    SimpleDateFormat sdf = new SimpleDateFormat("yyyy/MM/dd");
    Date expireDate = sdf.parse(e.getFieldValue(expireDateField));
    int qty = Encoder.parseInt(e.getFieldValue(qtyField), 1);
    List<String> emails = Arrays.asList(emailStr.split("\n"));
    List<String> apnames = new ArrayList<>();

    emails = emails.parallelStream().map(String::trim).collect(Collectors.toList());

    final List<String> result=new ArrayList<>();
    emails.forEach(email -> {
        BDBWorker worker = new BDBWorker(Const.AP_RAGIC);
        try{
            SDBQuery q = new SDBQueryFull(worker);
            q.go(DConst.ROOT_PRECONFIRM_EMAIL);
            q.filterRegex("(?i)^" + email + "$");
            SDBNode node = q.next();
            if(node != null) {
                int emailNodeId = node.getId();
                SDBNode apNode = worker.getLinkedNode(DConst.ROOT_PRECONFIRM_EMAIL, emailNodeId, DConst.ROOT_APNAME);
                String apname = apNode.getValue();

                SDBNode confirmNode = worker.getLinkedNode(DConst.ROOT_APNAME, apNode.getId(), DConst.ROOT_AP_CONFIRM_STATUS);
                boolean confirmed = false;

                if(confirmNode != null) {
                    if("OK".equals(confirmNode.getValue())) {
                        confirmed = true;
                    }
                }

                apnames.add(apname);
                result.add(email+" : " + apname + (confirmed ? "" : "  Not confirmed"));

            }
            else{
                result.add(email+" : No account registered.");
            }
        }
        finally{
            worker.commit();
        }
    });

    result.add("=============================================");

    apnames.forEach(apname -> {
        Account acc = Account.getInstance(apname);
        AccountPaymentStatus aps= acc.getPaymentStatus(true);
        aps.setSubscribeToDate(expireDate);
        if(qty > 1) aps.modifyExtraUser(qty - 1);
        if(edition != null) aps.modifyPlan(edition);

        result.add(apname+" set to "+edition+"/"+qty+" ("+Encoder.formatAsStandardDate(expireDate,false)+")");
    });

    //save the execution log
    e.setFieldValue(resultsField,Encoder.collectionToString(result,"\n"));
    e.setFieldValue(execDate,Encoder.formatAsStandardDate(new Date(),false));
    e.save(user,bundle);
    e.lock();



    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
