/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.rserial;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import java.util.*;
import java.util.regex.*;
import java.lang.reflect.*;
import java.net.*;
import java.text.*;
import com.ragic.s3.sdb.data.*;
import com.ragic.s3.sdb.data.bdb.*;
import com.ragic.s3.sdb.query.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.*;
import com.ragic.s3.util.*;
import com.ragic.s3.service.api.*;
import com.ragic.s3.service.schedule2.*;
import com.ragic.s3.service.schedule2.handler.*;
import com.ragic.s3.service.workflow.*;
import org.json.*;

public final class generateSerialNumber_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


public static final String APNAME = "rserial";
public static final String PATH = "/sndb";

public static final int SHEET_INDEX_BATCH = 2;
public static final int DID_BATCH_ID = 1000010;
public static final int DID_BATCH_GENERATED_DATE = 1000011;
public static final int DID_BATCH_GENERATED_USER = 1000012;
public static final int SUBKEY_BATCH = 1000017;
public static final int DID_BATCH_LICENSE_TYPE = 1000013;
public static final int DID_BATCH_NUM_OF_USER = 1000014;
public static final int DID_BATCH_QTY = 1000015;

public static final int LENGTH_SERIAL_NUMBER= 25;
public static final int SHEET_INDEX_SERIAL_NUMBER = 1;
public static final int DID_SERIAL_NUMBER_CODE = 1000002;
public static final int DID_SERIAL_NUMBER_PLAN = 1000003;
public static final int DID_SERIAL_NUMBER_NUM_OF_USER = 1000004;
public static final int DID_SERIAL_NUMBER_STATUS = 1000005;
public static final int DID_SERIAL_NUMBER_BATCH_ID = 1000006;
public static final int DID_SERIAL_GENERATED_TIME = 1000007;


public static String genSerialNumber(int n) {
    char[] codes = new char[n];
    int c = 'A';
    int r1 = 0;
    for (int i = 0; i < n; i++) {
        r1 = (int) (Math.random() * 2);
        switch (r1) {
        case 0:
            c = '0' + (int) (Math.random() * 10);
            break;
        case 1:
            c = 'A' + (int) (Math.random() * 26);
            break;
        }
        codes[i] = (char) c;
    }
    return new String(codes);
}

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("java.lang.reflect");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("com.ragic.s3.service.schedule2.handler");
    _jspx_imports_packages.add("com.ragic.s3.sdb.data");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.text");
    _jspx_imports_packages.add("java.util.regex");
    _jspx_imports_packages.add("java.net");
    _jspx_imports_packages.add("com.ragic.s3.sdb.query");
    _jspx_imports_packages.add("org.json");
    _jspx_imports_packages.add("com.ragic.s3.service.schedule2");
    _jspx_imports_packages.add("com.ragic.s3.service.workflow");
    _jspx_imports_packages.add("com.ragic.s3.nui.helper");
    _jspx_imports_packages.add("com.ragic.s3.sdb.data.bdb");
    _jspx_imports_packages.add("com.ragic.s3.service.api");
    _jspx_imports_classes = null;
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


Log logger = new Log().setIfDisplayLevel(true).setIfDisplayTime(true).setLevel(Log.ERROR);

User user = (User) session.getAttribute("USER");
RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");

int root_nid_batch = Integer.parseInt(request.getParameter("nid"));
logger.debug("root_nid_batch = " + root_nid_batch);

APIQuery q_batch = new APIQuery(APNAME, PATH, SHEET_INDEX_BATCH);
if(q_batch.getForm() == null) return;
q_batch.setUpdateMode();

APIQuery q_serailNumber = new APIQuery(APNAME, PATH, SHEET_INDEX_SERIAL_NUMBER);
if(q_serailNumber.getForm() == null) return;
q_serailNumber.setUpdateMode();

APIEntry e_batch = q_batch.getAPIEntry(root_nid_batch);
if(e_batch == null) return;


String batch_id = e_batch.getFieldValue(DID_BATCH_ID);
if(batch_id == null || batch_id.trim().length() == 0) return;

String generatedDate_batch = e_batch.getFieldValue(DID_BATCH_GENERATED_DATE);
if(generatedDate_batch != null && generatedDate_batch.trim().length() != 0) {
    out.println("Serial numbers of this batch is already generated!");
    return;
}

final Map<String, Map<String, Integer>> map_licenses = new HashMap<String, Map<String, Integer>>();
for(int i = 0; i <  e_batch.getSubtableSize(SUBKEY_BATCH); i++) {
	String license_type = e_batch.getSubtableFieldValue(SUBKEY_BATCH, i, DID_BATCH_LICENSE_TYPE + "");
    if(license_type == null || license_type.trim().length() == 0) continue;
    logger.debug("License Type = " + license_type);
    
	String num_of_user = e_batch.getSubtableFieldValue(SUBKEY_BATCH, i, DID_BATCH_NUM_OF_USER + "");
	if(num_of_user == null || num_of_user.trim().length() == 0) continue;
	logger.debug("Num of User = " + num_of_user);
    
	int qty = 0;
    try {
        qty = Integer.parseInt(e_batch.getSubtableFieldValue(SUBKEY_BATCH, i, DID_BATCH_QTY + ""));
    } catch(Exception e) {}
    if(qty == 0) continue;
    logger.debug("QTY=" + qty);
    
    Map<String, Integer> map_type = map_licenses.get(license_type);
    if(map_type == null) {
        map_type = new HashMap<String, Integer>();
        map_licenses.put(license_type, map_type);
    }
    
    if(map_type.containsKey(num_of_user)) {
        map_type.put(num_of_user, map_type.get(num_of_user) + qty);
    } else {
        map_type.put(num_of_user, qty);
    }
}


SimpleDateFormat sdf = new SimpleDateFormat("yyyy/MM/dd");
generatedDate_batch = sdf.format(new Date());
e_batch.setFieldValue(DID_BATCH_GENERATED_DATE, generatedDate_batch);
e_batch.setFieldValue(DID_BATCH_GENERATED_USER, user.getUserId());
e_batch.save(user, bundle);

sdf = new SimpleDateFormat("yyyy/MM/dd HH:mm:ss");
BDBWorker worker = new BDBWorker(APNAME);

try {
    for(Map.Entry<String, Map<String, Integer>> e_licenses: map_licenses.entrySet()) {
        String license = e_licenses.getKey();
        Map<String, Integer> map_num_of_user = e_licenses.getValue();
        for(Map.Entry<String, Integer> e_num_of_user: map_num_of_user.entrySet()) {
            String num_of_user = e_num_of_user.getKey();
            int qty = e_num_of_user.getValue();
            out.println("=====================<br/>");
            out.println("license:     " + license + "<br/>");
            out.println("num of user: " + num_of_user + "<br/>");
            out.println("qty:         " + qty + "<br/>");
            
            int successCount = qty;
            int failCount = 0;
            for(int i = 0; i < qty; i++) {
                int nid_serial_number = Const.NONODE;
                String serialNumber = null;
                while(true) {
                    serialNumber = genSerialNumber(LENGTH_SERIAL_NUMBER);
                    nid_serial_number = worker.getNodeId(DID_SERIAL_NUMBER_CODE, serialNumber);
                    if(nid_serial_number == Const.NONODE) break;
                }
                logger.info("Serial " + (i+1) + ": " + serialNumber);
                
                APIEntry newEntry = q_serailNumber.insertAPIEntry();
                newEntry.setFieldValue(DID_SERIAL_NUMBER_CODE, serialNumber);
                newEntry.setFieldValue(DID_SERIAL_NUMBER_PLAN, license);
                newEntry.setFieldValue(DID_SERIAL_NUMBER_NUM_OF_USER, num_of_user);
                newEntry.setFieldValue(DID_SERIAL_NUMBER_STATUS, "Valid");
                newEntry.setFieldValue(DID_SERIAL_NUMBER_BATCH_ID, batch_id);
                newEntry.setFieldValue(DID_SERIAL_GENERATED_TIME, sdf.format(new Date()));
                ScriptResponse result = newEntry.save(user, bundle);
                if(!result.ifExec()) {
                	successCount--;
                	failCount++;
                    out.println("error: " + result.getMessage());
                }
            }
            out.println("success:     " + successCount + "<br/>");
            out.println("failed:      " + failCount + "<br/>");
            
        }
    }
} finally {
    worker.commit();
}



    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
