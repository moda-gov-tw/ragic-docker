/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.cust.qbank;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.service.api.APIEntry;
import com.ragic.s3.service.api.APIQuery;
import com.ragic.s3.service.api.APIResults;
import org.apache.poi.xssf.usermodel.XSSFCell;
import org.apache.poi.xssf.usermodel.XSSFRow;
import org.apache.poi.xssf.usermodel.XSSFSheet;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;
import java.io.File;
import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.text.SimpleDateFormat;
import java.util.*;

public final class generateOutstandingBalanceReport_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {


    // A: 撥款單位  B: 案件類別  C: 經銷商
    String[] TYPE = {"撥款單位", "案件類別", "經銷商"};

    final int SHEET_INDEX_A = 7;
    final int SHEET_INDEX_B = 2;
    final int SHEET_INDEX_C = 1;

    final int CLASS_FILED_A = 1000274;
    final int CLASS_FILED_B = 1000163;
    final int CLASS_FILED_C = 1000144;

    final int QUERY_FIELD_A = 1000272;
    final int QUERY_FIELD_B = 1000104;
    final int QUERY_FIELD_C = 1000087;

    final int OUTSTANDING_BALANCE = 1000396;
    final int COLLECTION_STAGE = 1000530;

    final int CELL_PER_ROW = 11;

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("org.apache.poi.xssf.usermodel.XSSFWorkbook");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIQuery");
    _jspx_imports_classes.add("org.apache.poi.xssf.usermodel.XSSFSheet");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("java.io.FileOutputStream");
    _jspx_imports_classes.add("java.text.SimpleDateFormat");
    _jspx_imports_classes.add("java.io.FileInputStream");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIEntry");
    _jspx_imports_classes.add("org.apache.poi.xssf.usermodel.XSSFCell");
    _jspx_imports_classes.add("java.io.File");
    _jspx_imports_classes.add("com.ragic.s3.service.api.APIResults");
    _jspx_imports_classes.add("org.apache.poi.xssf.usermodel.XSSFRow");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html;charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


    String apname = request.getParameter("a");
    String type = request.getParameter("type");
    String time = request.getParameter("time");
    if (apname == null || type == null || time == null) return;

    int sheetIndex, classField, queryField;
    String reportType;
    switch (type) {
        case "A":
            sheetIndex = SHEET_INDEX_A;
            classField = CLASS_FILED_A;
            queryField = QUERY_FIELD_A;
            reportType = TYPE[0];
            break;
        case "B":
            sheetIndex = SHEET_INDEX_B;
            classField = CLASS_FILED_B;
            queryField = QUERY_FIELD_B;
            reportType = TYPE[1];
            break;
        case "C":
            sheetIndex = SHEET_INDEX_C;
            classField = CLASS_FILED_C;
            queryField = QUERY_FIELD_C;
            reportType = TYPE[2];
            break;
        default:
            return;
    }
    APIQuery classQuery = new APIQuery(apname, "/forms2", sheetIndex);
    classQuery.setIfEidNaming(true);
    Iterator<APIEntry> classIterator = classQuery.getAPIResults().iterator(classQuery.getForm());
    List<String> classNames = new ArrayList<>();
    while (classIterator.hasNext()) {
        APIEntry e = classIterator.next();
        String name = e.getFieldValue(classField);
        classNames.add(name);
    }

    String[] collectionStages = {"N", "M0", "M1", "M2", "M3", "M4", "M5", "M6", "ChargeOff"};
    int[][] caseCount = new int[classNames.size()][];
    for (int i = caseCount.length - 1; i >= 0; i--) {
        caseCount[i] = new int[collectionStages.length];
        for (int j = collectionStages.length - 1; j >= 0; j--) {
            caseCount[i][j] = 0;
        }
    }
    int[] caseStageCount = new int[collectionStages.length];
    for (int i = caseStageCount.length - 1; i >= 0; i--) {
        caseStageCount[i] = 0;
    }
    int caseCountTotal = 0;
    int[][] balanceTotal = new int[classNames.size()][];
    for (int i = caseCount.length - 1; i >= 0; i--) {
        balanceTotal[i] = new int[collectionStages.length];
        for (int j = collectionStages.length - 1; j >= 0; j--) {
            balanceTotal[i][j] = 0;
        }
    }
    int[] balanceStageTotal = new int[collectionStages.length];
    for (int i = balanceStageTotal.length - 1; i >= 0; i--) {
        balanceStageTotal[i] = 0;
    }
    int outstandingBalance = 0;
    APIQuery caseQuery = new APIQuery(apname, "/forms5", 23);
    caseQuery.setIfEidNaming(true);
    APIResults caseResults = null;
    int pageSize = 1000;
    while (caseResults == null || caseResults.hasMore()) {
        caseQuery.resetData();
        caseQuery.setLimitSize(pageSize);
        caseQuery.setLimitFrom(caseCountTotal);
        caseResults = caseQuery.getAPIResults();
        Iterator<APIEntry> caseIterator = caseResults.iterator(caseQuery.getForm());
        while (caseIterator.hasNext()) {
            APIEntry e = caseIterator.next();
            String className = e.getFieldValue(queryField);
            String collectionStage = e.getFieldValue(COLLECTION_STAGE);
            if (className == null) continue;
            if (collectionStage == null || collectionStage.length() == 0) {
                collectionStage = "N";
            }
            int classIndex = classNames.indexOf(className);
            int stageIndex = Arrays.asList(collectionStages).indexOf(collectionStage);
            if (classIndex < 0 || stageIndex < 0) continue;
            caseCount[classIndex][stageIndex]++;
            caseStageCount[stageIndex]++;
            caseCountTotal++;
            int balance = Encoder.parseInt(e.getFieldValue(OUTSTANDING_BALANCE));
            balanceTotal[classIndex][stageIndex] += balance;
            balanceStageTotal[stageIndex] += balance;
            outstandingBalance += balance;
        }
    }
    FileInputStream fs = new FileInputStream("web/cust/qbank/outstandingBalanceReportTemplate.xlsx");
    XSSFWorkbook workbook = new XSSFWorkbook(fs);
    XSSFSheet sheet = workbook.getSheetAt(0);
    XSSFCell reportTypeCell = sheet.getRow(0).getCell(0);
    reportTypeCell.setCellValue(reportType);
    XSSFRow caseStageCountRow = sheet.getRow(4);
    XSSFRow balanceStageTotalRow = sheet.getRow(5);
    XSSFRow overdueRatioStageRow = sheet.getRow(6);
    for (int stageIndex = 1; stageIndex < collectionStages.length; stageIndex++) {
        caseStageCountRow.getCell(stageIndex + 1).setCellValue(caseStageCount[stageIndex]);
        balanceStageTotalRow.getCell(stageIndex + 1).setCellValue(balanceStageTotal[stageIndex]);
        overdueRatioStageRow.getCell(stageIndex + 1).setCellValue((double) balanceStageTotal[stageIndex] / outstandingBalance);
    }
    sheet.getRow(8).getCell(1).setCellValue(caseCountTotal);
    sheet.getRow(9).getCell(1).setCellValue(outstandingBalance);
    XSSFRow[] topDataRow = new XSSFRow[3];
    for (int classIndex = 0; classIndex < classNames.size(); classIndex++) {
        XSSFRow[] dataRow = new XSSFRow[3];
        if (classIndex > 0) {
            sheet.shiftRows(classIndex * 3 + 1, classIndex * 3 + 6, 3, true, true);
            for (int rowIndex = 0; rowIndex < 3; rowIndex++) {
                dataRow[rowIndex] = sheet.createRow(classIndex * 3 + rowIndex + 1);
                for (int cellIndex = 0; cellIndex < CELL_PER_ROW; cellIndex++) {
                    XSSFCell cell = dataRow[rowIndex].createCell(cellIndex);
                    cell.setCellStyle(topDataRow[rowIndex].getCell(cellIndex).getCellStyle());
                    if (cellIndex == 1) {
                        cell.setCellValue(topDataRow[rowIndex].getCell(cellIndex).getStringCellValue());
                    }
                }
            }
            sheet.addMergedRegion(new org.apache.poi.ss.util.CellRangeAddress(classIndex * 3 + 1, classIndex * 3 + 3, 0, 0));
        } else {
            for (int rowIndex = 0; rowIndex < 3; rowIndex++) {
                dataRow[rowIndex] = sheet.getRow(rowIndex + 1);
                topDataRow[rowIndex] = dataRow[rowIndex];
            }
        }
        dataRow[0].getCell(0).setCellValue(classNames.get(classIndex));
        for (int stageIndex = 1; stageIndex < collectionStages.length; stageIndex++) {
            dataRow[0].getCell(stageIndex + 1).setCellValue(caseCount[classIndex][stageIndex]);
            dataRow[1].getCell(stageIndex + 1).setCellValue(balanceTotal[classIndex][stageIndex]);
            dataRow[2].getCell(stageIndex + 1).setCellValue((double) balanceTotal[classIndex][stageIndex] / outstandingBalance);
        }
    }
    for (int rowIndex = 1; rowIndex <= classNames.size() * 3 + 3; rowIndex++) {
        sheet.getRow(rowIndex).getCell(CELL_PER_ROW - 1).setCellFormula("SUM(C" + (rowIndex + 1) + ":J" + (rowIndex + 1) + ")");
    }
    sheet.setForceFormulaRecalculation(true);
    SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
    Calendar cal = Calendar.getInstance();
    if (time.equals("after")) {
        cal.add(Calendar.DATE, 1);
    }
    String dateString = sdf.format(cal.getTime()) + "_" + (time.equals("before") ? "2200" : "0800");
    String filename = "TCJ_OB_report_" + type + "(" + dateString + ").xlsx";
    File outputFile = new File("cust/data/" + apname + "/upload/ob_report/" + filename);
    File outputFolder = outputFile.getParentFile();
    if ((outputFolder.exists() || outputFolder.mkdir()) && outputFile.createNewFile()) {
        FileOutputStream fos = new FileOutputStream(outputFile);
        workbook.write(fos);
        fos.flush();
        fos.close();
    }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
