/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.google;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.sdb.data.bdb.BDBSmartWorker;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.DConst;
import com.ragic.s3.util.Util;
import org.json.JSONObject;
import com.ragic.s3.nui.helper.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.util.Log;

public final class oauth2_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.nui.helper");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.util.Util");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBSmartWorker");
    _jspx_imports_classes.add("com.ragic.s3.util.DConst");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.Log");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  String from = request.getParameter("from");
  String email = null, name=null;
  RagicConfig rc = RagicConfig.getInstance();

  String code=request.getParameter("code");
  if(code==null) {
    String redir="https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=135474885933-kekj6o1uelgid10si6mhak2aq1185pnh.apps.googleusercontent.com&redirect_uri=https%3A%2F%2F"+rc.getProperty("SERVER_NAME")+"/google/oauth2.jsp?from=google&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email";
    out.println("<script>location.href='"+redir+"';</script>");
    return;
  }

  String client_id = "135474885933-kekj6o1uelgid10si6mhak2aq1185pnh.apps.googleusercontent.com";
  String apiSecret = "z5qFpuERhtaeKHhvEQ7E5hA-";
  String redirectUrl = "https://"+rc.getProperty("SERVER_NAME")+"/google/oauth2.jsp?from=google";
  //String scope = "https://www.googleapis.com/auth/userinfo.email";

  String postBody="code="+ code+"&client_id="+client_id+"&client_secret="+apiSecret+"&redirect_uri="+redirectUrl+"&grant_type=authorization_code";
  JSONObject jo=URLCaller.post("https://www.googleapis.com/oauth2/v4/token",postBody);
  if(jo==null){
    Thread.sleep(2000);
    jo=URLCaller.post("https://www.googleapis.com/oauth2/v4/token",postBody);
  }
  String accessToken=jo.optString("access_token");
  if(accessToken!=null && !accessToken.isEmpty()){
    jo=URLCaller.get("https://www.googleapis.com/oauth2/v1/userinfo?alt=json&access_token="+accessToken);
    while(jo==null){
      Thread.sleep(2000);
      jo=URLCaller.get("https://www.googleapis.com/oauth2/v1/userinfo?alt=json&access_token="+accessToken);
    }
    if(jo==null){}
  }

  email = jo.optString("email");
  name = jo.optString("name");

  if(email==null || email.isEmpty()){
    out.println("no open id email in your request");
    return;
  }
  session.removeAttribute("OPENID_MANAGER");
  session.removeAttribute("OPENID_ASSOCIATION");
  session.removeAttribute("OPENID_TYPE");

  User user= RemoteAccountManager.main_userInfo(email);//new LoginManager().local_login(email);
  boolean apnameFound=false;
  if(user==null){
//    System.out.println("open id user not found:"+email+" -> creating user.");
    //check if any apname in the same domain
    BDBSmartWorker bbs=new BDBSmartWorker(Const.AP_RAGIC);
    String domain=(String)session.getAttribute("GOOGLEAPPS_DOMAIN");
    if(domain!=null){
      String apname=bbs.getFieldValue(DConst.ROOT_GOOGLEAPPS_DOMAIN,domain,DConst.ROOT_APNAME);
      if(apname!=null && apname.length()>0){
        UserManager.createUser(apname, email, "", Encoder.genPassword(10));
        apnameFound=true;
      }
    }
//    System.out.println("open id create user done for "+email);

    if(apnameFound){
      user=new LoginManager().local_login(email);
    }
    else{
      RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");
      session.setAttribute("OPENID_NEW_USER",email);
      session.setAttribute("OPENID_NEW_USER_NAME",name);
      response.sendRedirect("/intl/"+bundle.getString("langv5")+"/openid-register");
      return;
    }
  }

  String apname=(String)session.getAttribute("AP");
  session.removeAttribute("AP");
  if(user!=null){
    user.setLoginType(Const.LOGINTYPE_OPENID);

    // if no apname and user authenticated Ap size is 1, get apname which for get denied loginType
    if((apname==null||apname.isEmpty())&&user.getAuthenticatedApnamesSize()==1)apname=user.getDefaultApname();


    if(apname!=null&&!apname.isEmpty()&&!user.getUserId().endsWith("@ragic.com")) {
      AccountSetting accountSetting = Account.getInstance(apname).getAppSetting();

      //Check loginType allowed
      if (accountSetting.getDeniedLoginType().contains(user.getLoginType())) {
        RagicBundle bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
        String notDeniedLoginTypeText=accountSetting.getNotDeniedLoginTypeText(bundle);
        if(notDeniedLoginTypeText.isEmpty()){
          out.print(bundle.getString("incorrectLoginDeniedLoginType_noAllow",new Object[]{apname}));
        }else {
          out.print(bundle.getString("incorrectLoginDeniedLoginType",new Object[]{apname,notDeniedLoginTypeText}));
        }
        new Log("log/login.log").setIfDisplayTime(true).info("[" + apname + "][" + user.getUserId() + "] use denied loginType:" + user.getLoginType());

        return;
      }
    }


    //for future cookie log in
    Util.setCookieSession(Const.userId, user.getUserId(), response);
    Util.setCookieSession(Const.sigature, user.getHash(), response, RagicConfig.getInstance().ifProperty("SSL_ENABLED"));

    session.setAttribute("USER", user);
    //update NUIFORMSET
    NUIFormSetInfo.setUser(session, user);
    boolean openId_resetPwd = session.getAttribute("OPENID_RESET_PWD") == null ? false : (boolean) session.getAttribute("OPENID_RESET_PWD");

    String referrer = (String) session.getAttribute("LOGIN_REFERRER");
    if (openId_resetPwd) {
      response.sendRedirect("/sims/reg/openId_resetPwd.jsp");
    } else if (referrer != null && referrer.length() > 0) {
      referrer=Encoder.unencjs(referrer);
      if (referrer.contains("webview") || referrer.contains("embed"))
        response.sendRedirect("/sims/webview/googleLoginSuccess.jsp");
      else response.sendRedirect(referrer);
    } else response.sendRedirect("/");



  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
