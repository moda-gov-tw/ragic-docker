/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.intl.en;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.*;
import com.ragic.s3.nui.helper.*;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.util.*;
import com.ragic.s3.util.doc.DocCache;
import com.ragic.s3.util.doc.article.Article;
import java.util.regex.Matcher;
import java.util.regex.Pattern;
import java.util.*;
import org.json.JSONArray;
import org.json.JSONObject;

public final class doc_005fsearch_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("java.util");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("com.ragic.s3.nui.helper");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("java.util.regex.Matcher");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("com.ragic.s3.util.doc.DocCache");
    _jspx_imports_classes.add("java.util.regex.Pattern");
    _jspx_imports_classes.add("org.json.JSONArray");
    _jspx_imports_classes.add("com.ragic.s3.util.doc.article.Article");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  RagicBundle bundle = (RagicBundle) request.getAttribute("RAGIC_BUNDLE");
  if (bundle == null) {
    bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  }
  User user=(User) session.getAttribute("USER");
  String qt=request.getParameter("q");
  long timestamp=Encoder.parseLong(request.getParameter("t"));
  if(qt==null) return;
  //qt=Encoder.sanitizeHTML(qt);

  //with lowercase, AND OR will not be processed
  qt=qt.trim().toLowerCase();
  qt=qt.replaceAll(" and "," AND ");
  qt=qt.replaceAll(" or "," OR ");
//  qt=qt.trim();
  if(qt.length()==0) return;
  String qtProcessed=qt.replaceAll("\"","");
  qtProcessed=qtProcessed.replaceAll(" ","|");
  
  String originalUrl = (String)request.getAttribute("ORIGINAL_URI");
  String langv5 = request.getParameter("lang")!=null ? request.getParameter("lang") : bundle.getString("langv5");
  String searchfrom = request.getParameter("searchfrom")!=null ? request.getParameter("searchfrom") : "doc";
  boolean searchAll = request.getParameter("all") != null;

  Set<String> searchScopes = new LinkedHashSet<>();
  searchScopes.add(searchfrom);
  if (searchAll) {
    searchScopes.add("course");
    searchScopes.add("kb");
    searchScopes.add("doc");
  }
  
  Set<JSONObject> searchedArticles = new LinkedHashSet<>();
  
  for (String docname : searchScopes) {
    DocCache docCache = DocCache.getInstance(docname, langv5);
    Collection<Article> doc = docCache.getAllArticles().values();

    int count=0;
    synchronized(doc){
      for(Article a:doc){
        count++;
        if(count>10000) break;//fixing some strange infinite loop that results in CPU 100%

        if(!a.isPublic()) continue;
        if (a.getTitle().toLowerCase().contains(qt)){
          if (a.getContent()!=null && a.getContent().contains("{{ragic_doc_skip}}")) continue;

          JSONObject jo = new JSONObject();
          jo.put("nodeId", a.getNodeId());
          jo.put("title", a.getTitle());
          jo.put("description", a.getDescription());
          jo.put("docname", docname);

          String url = "/intl/" + langv5 + "/" +
                      ("kb".equals(docname) || "user".equals(docname) || "api".equals(docname) ? "doc-" + docname : docname) +
                      "/" + a.getNodeId() + "/" + Util.createSEOUrl(a.getTitle(),null);
          jo.put("url", url);
          
          searchedArticles.add(jo);
        }
      }
    }
  }
  
  
  if (request.getParameter("realtime") != null) {
    JSONObject result = new JSONObject();
    JSONArray ja = new JSONArray();
    result.put("i", ja);
    
    for (JSONObject articleInfo : searchedArticles) {
      JSONObject jo = new JSONObject();
      
      String content = articleInfo.optString("title");
      String highlightedContent = content;
      String highlighted = qtProcessed;
      
      //edited from Encoder.highlightContent
      try{
        //remove query syntax which is also regular expression
        //highlighted=highlighted.replaceAll("\\+","");
        //highlighted=" "+highlighted+"|^"+highlighted;
        //System.out.println("Encoder.highlightContent, highlighted:"+highlighted);

        Pattern p = Pattern.compile(highlighted, Pattern.CASE_INSENSITIVE);
        Matcher m = p.matcher(content);
        StringBuffer sb = new StringBuffer();
        while (m.find()) {
          m.appendReplacement(sb, "<span class='match'>" + Matcher.quoteReplacement(m.group()) + "</span>");
        }
        m.appendTail(sb);
        highlightedContent = sb.toString();
      }
      catch(java.util.regex.PatternSyntaxException e){
        //do nothing, just a bad highlight pattern
      }
      catch(Exception e){
        ExceptionManager.getInstance().processException(e);
      }
      
      //jo.put("name", Encoder.highlightContent(a.getTitle(), qtProcessed, "match"));
      jo.put("name", highlightedContent);
      jo.put("url", articleInfo.optString("url"));
      ja.put(jo);
    }
    
    result.put("ts", timestamp);//echo timestamp
    out.print(result.toString(2));
    return;
  }

      out.write("<div class=\"kbBlock\">\r\n  <div class=\"searchResultsContainer\">\r\n    <div class=\"searchResultsCount\">\r\n      <i class=\"far fa-search\" style=\"margin-right:16px;\"></i>");
      out.print(bundle.getString("Doc_SearchResultsTitle", new Object[] {""+searchedArticles.size(), Encoder.encjs(qt)}) );
      out.write("</div>\r\n    \r\n    ");

      int DESCRIPTION_MAX_LENGTH = 150;
      String lastDoc = searchfrom;
      for (JSONObject articleInfo : searchedArticles) {
        String title = articleInfo.optString("title");
        String url = articleInfo.optString("url");
        String description = Encoder.removeHTML(articleInfo.optString("description"));
        String docname = articleInfo.optString("docname");
        boolean moreDesc = false;
        if (description.length() > DESCRIPTION_MAX_LENGTH) {
          description = description.substring(0, DESCRIPTION_MAX_LENGTH);
          moreDesc = true;

          // language specific feature
          // not to cut off words for English
          if ("en".equals(langv5)) {
            // find last word
            int lastSpace = description.lastIndexOf(" ");
            if (lastSpace > 0) description = description.substring(0, lastSpace);
          }
          
          if (moreDesc) description += "...";
        }
        
        if (!"".equals(lastDoc) && !docname.equals(lastDoc)) {
          String docTitle = "";
          if ("kb".equals(docname)) docTitle = bundle.getString("template_docKb");
          else if ("course".equals(docname)) docTitle = bundle.getString("template_course");
          else if ("doc".equals(docname)) docTitle = bundle.getString("template_doc");
          out.println("<div class='searchDivider' style='font-size:18px;font-weight:700;color:#555;border-bottom:1px solid #555;padding:18px 0 6px 0;'>" + docTitle + "</div>");
        }
        lastDoc = docname;
    
      out.write("<a href=\"");
      out.print(url);
      out.write("\" target=\"_blank\" class=\"briefArticleContainer\">\r\n      <div class=\"titleContainer\" style=\"display:flex;align-items:baseline;font-weight:700;\">\r\n        <i class=\"titleIcon fal fa-file-alt\"></i>\r\n        <h2 class=\"title\">");
      out.print(title);
      out.write("</h2>\r\n      </div>\r\n      <div class=\"briefArticleContent\">");
      out.print(description);
      out.write("</div>\r\n      <div style=\"padding:0 30px;\"><span class=\"fakeLink\">");
      out.print(bundle.getString("continueReading"));
      out.write("</span></div>\r\n    </a>\r\n    ");
 } 
      out.write("</div>\r\n</div>\r\n\r\n");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
