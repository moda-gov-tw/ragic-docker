/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.intl.en;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.helper.LoginManager;
import com.ragic.s3.nui.helper.UserRegister;
import com.ragic.s3.nui.helper.UserRegisterResponse;
import com.ragic.s3.sdb.data.bdb.BDBMgr;
import com.ragic.s3.sdb.data.bdb.BDBSmartWorker;
import com.ragic.s3.sdb.data.bdb.BDBWorker;
import com.ragic.s3.nui.*;
import com.ragic.s3.util.*;
import com.ragic.s3.nui.helper.Encoder;
import java.util.Date;

public final class initExec_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("com.ragic.s3.nui");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("com.ragic.s3.util");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBMgr");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserRegisterResponse");
    _jspx_imports_classes.add("java.util.Date");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.LoginManager");
    _jspx_imports_classes.add("com.ragic.s3.sdb.data.bdb.BDBSmartWorker");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.UserRegister");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;


  //if(!RagicConfig.getInstance().ifProperty("NEED_INIT")) return;
  RagicBundle bundle=(RagicBundle) session.getAttribute("RAGIC_BUNDLE");

  //create default account (even though cust/def might have already exist)
  String email=request.getParameter("e");
  String pwd=request.getParameter("p");
  String SMTP_SERVER=request.getParameter("SMTP_SERVER");
  String SMTP_SENDER_AUTH_USER=request.getParameter("SMTP_SENDER_AUTH_USER");
  String SMTP_SENDER_AUTH_PWD=request.getParameter("SMTP_SENDER_AUTH_PWD");

  String SMTP_SSL_ENABLE=request.getParameter("SMTP_SSL_ENABLE")==null?"off":request.getParameter("SMTP_SSL_ENABLE");
  String SMTP_PORT=request.getParameter("SMTP_PORT");
  String SMTP_SSL_PORT=request.getParameter("SMTP_SSL_PORT");
  String SMTP_SENDER_PERSONAL=request.getParameter("SMTP_SENDER_PERSONAL");
  String SMTP_SENDER_EMAIL=request.getParameter("SMTP_SENDER_EMAIL");
  String DISTR_DBBACKUP_PATH=request.getParameter("DISTR_DBBACKUP_PATH");
  String DISTR_PRESERVED_NUM=request.getParameter("DISTR_PRESERVED_NUM");

  if(SystemCache.getInstance().get("DISTR_INIT_STARTED")!=null){
    out.println("<script>window.setTimeout(\"location='/"+Const.AP_DEFAULT+"/home';\",3000);</script>");
    return;
  }

  Log log=new Log("log/initExec.log");
  log.info(Encoder.formatAsStandardDate(new Date(),true));

  log.info("=================Start initExec log");
  SystemCache.getInstance().put("DISTR_INIT_STARTED","true");
  //register account
  UserRegister ur=new UserRegister();
  UserRegisterRequest req=new UserRegisterRequest(Const.AP_DEFAULT,email,pwd,Account.PROF,email,bundle);//[BugId:14038]
  log.info("Start register and confirm");
  UserRegisterResponse urr=ur.registerAndConfirm(req);
  log.info("Done register and confirm, start initLocalDB");
  ur.initLocalDB(Const.AP_DEFAULT,email,urr,bundle.getTimezoneOffset(),bundle.getString("langv5"),request.getRemoteAddr());
  log.info("InitLocalDB complete.");
  if(urr!=null && urr.ifSuccess()){
    log.info("urr:"+urr.ifSuccess()+",ap="+urr.getApnameResponse()+",confirm="+urr.getConfirmResponse()+",email="+urr.getEmailResponse()+",pwd="+urr.getPwdResponse()+",reg="+urr.getRegisterResponse());
    log.info("info:"+urr.getApname()+","+urr.getEmail()+","+urr.getMessage());
    log.info("urrbool:"+urr.isAccountAlreadyActivated()+","+
            urr.isAccountPatternWrong()+","+
            urr.isAccountAlreadyActivated()+","+
            urr.isAccountRegistered()+","+
            urr.isEmailEmpty()+","+
            urr.isEmailPatternWrong()+","+
            urr.isEmailRegistered()+","+
            urr.isInvalidConfirmCode()+","+
            urr.isInvalidConfirmPwd()+","+
            urr.isInvalidEdition()+","+
            urr.isPasswordTooShort()+","+
            urr.isRegisterDisabled()+","+
            urr.isRegistrationFailed()
    );

    log.info("Updating Ragic Config....");
    RagicConfig rc = RagicConfig.getInstance();
    //set as the administrator email
    rc.setProperty("ADMINISTRATOR_EMAIL",email);
    //set default redirect
    rc.setProperty("ROOT_REDIRECT","/"+Const.AP_DEFAULT+"/home");
    //init complete
    rc.setProperty("NEED_INIT","false");
    //set register disabled after the first registration
    rc.setProperty("REGISTER_DISABLED","true");

    rc.setProperty("SMTP_SERVER",SMTP_SERVER);
    rc.setProperty("SMTP_SENDER_AUTH_USER",SMTP_SENDER_AUTH_USER);
    rc.setProperty("SMTP_SENDER_AUTH_PWD",SMTP_SENDER_AUTH_PWD);
    rc.setProperty("SMTP_SSL_ENABLE",(SMTP_SSL_ENABLE.equals("on")?"true":"false"));
    rc.setProperty("SMTP_PORT",SMTP_PORT);
    rc.setProperty("SMTP_SSL_PORT",SMTP_SSL_PORT);
    rc.setProperty("SMTP_SENDER_PERSONAL",SMTP_SENDER_PERSONAL);
    rc.setProperty("SMTP_SENDER_EMAIL",SMTP_SENDER_EMAIL);
    rc.setProperty("DISTR_DBBACKUP_PATH", DISTR_DBBACKUP_PATH);
    rc.setProperty("DISTR_PRESERVED_NUM",DISTR_PRESERVED_NUM);

    //save
    rc.saveChanges();
    log.info("Ragic Config updated.");

    //enable external user
    log.info("Enabling external user...");
    String emailDomain="example.com";
    try{
      emailDomain=email.split("@")[1];
    }
    catch (Exception e){}
    String apname=Const.AP_DEFAULT;
    String domain=emailDomain;

    BDBMgr.getInstance(apname).setProperty(Const.DBPROP_INTERNAL_DOMAIN, domain);
    BDBMgr.getInstance(apname).setProperty(Const.DBPROP_INTERNAL_DOMAIN_REQ_APPROVER,"support@ragic.com");
    log.info("External user enabled.");

    //make sure the local database has the group X-User
    BDBWorker worker=new BDBWorker(apname);
    int xuserNodeId=-1;
    try{
      xuserNodeId=worker.addNode(DConst.USER_GROUP,"X-User",true);
    }
    finally{
      worker.commit();
    }
    BDBSmartWorker bbs=new BDBSmartWorker(apname);
    bbs.setFieldValue(DConst.USER_GROUP,xuserNodeId, DConst.USER_GROUP_DESC,"Default group for external users.");

    log.info("External user groups added.");

    //log in user
    User user=new LoginManager().local_login(email);
    if(user!=null) session.setAttribute("USER",user);

    log.info("First user logged in.");

    Util.resetDB(Const.AP_DEFAULT,null,false);

    log.info("Reset db done.");

    
      out.write("<script>location='/");
      out.print(Const.AP_DEFAULT);
      out.write("/home';</script>");

  }
  else{
    
      out.write("<div style=\"width:500px;margin:40px auto;text-align:center;font-size:16px;\">");
      out.print(urr.getMessage());
      out.write("</div>\r\n      <script>window.setTimeout(\"location='/intl/");
      out.print(bundle.getString("langv5"));
      out.write("/init';\",3000);</script>\r\n    ");

    SystemCache.getInstance().remove("DISTR_INIT_STARTED");
  }

    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
