/*
 * Generated by the Jasper component of Apache Tomcat
 * Version: JspC/ApacheTomcat8
 * Generated at: 2022-11-10 01:41:41 UTC
 * Note: The last modified time of this file was set to
 *       the last modified time of the source file after
 *       generation to assist with modification tracking.
 */
package org.apache.jsp.intl.en;

import javax.servlet.*;
import javax.servlet.http.*;
import javax.servlet.jsp.*;
import com.ragic.s3.nui.RagicBundle;
import com.ragic.s3.nui.User;
import com.ragic.s3.nui.helper.Encoder;
import com.ragic.s3.nui.helper.Util;
import com.ragic.s3.util.Const;
import com.ragic.s3.util.doc.DocCache;
import com.ragic.s3.util.doc.article.Article;
import com.ragic.s3.util.doc.article.CourseArticle;
import com.ragic.s3.util.doc.type.ArticleType;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Locale;
import org.json.JSONArray;
import org.json.JSONException;
import org.json.JSONObject;

public final class course_jsp extends org.apache.jasper.runtime.HttpJspBase
    implements org.apache.jasper.runtime.JspSourceDependent,
                 org.apache.jasper.runtime.JspSourceImports {

 public JSONObject courseObject(CourseArticle course) throws JSONException {
  JSONObject jo = new JSONObject();
  jo.put("nodeId", course.getNodeId());
  jo.put("courseName", course.getTitle());
  jo.put("howLong", course.getHowLong());
  jo.put("description", course.getDescription());
  jo.put("content", course.getContent());
  return jo;
}

  private static final javax.servlet.jsp.JspFactory _jspxFactory =
          javax.servlet.jsp.JspFactory.getDefaultFactory();

  private static java.util.Map<java.lang.String,java.lang.Long> _jspx_dependants;

  private static final java.util.Set<java.lang.String> _jspx_imports_packages;

  private static final java.util.Set<java.lang.String> _jspx_imports_classes;

  static {
    _jspx_imports_packages = new java.util.HashSet<>();
    _jspx_imports_packages.add("javax.servlet");
    _jspx_imports_packages.add("javax.servlet.http");
    _jspx_imports_packages.add("javax.servlet.jsp");
    _jspx_imports_classes = new java.util.HashSet<>();
    _jspx_imports_classes.add("com.ragic.s3.nui.User");
    _jspx_imports_classes.add("com.ragic.s3.util.Const");
    _jspx_imports_classes.add("com.ragic.s3.util.doc.article.Article");
    _jspx_imports_classes.add("java.util.LinkedHashMap");
    _jspx_imports_classes.add("com.ragic.s3.util.doc.article.CourseArticle");
    _jspx_imports_classes.add("com.ragic.s3.util.doc.type.ArticleType");
    _jspx_imports_classes.add("org.json.JSONException");
    _jspx_imports_classes.add("org.json.JSONObject");
    _jspx_imports_classes.add("com.ragic.s3.nui.RagicBundle");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Util");
    _jspx_imports_classes.add("java.util.Locale");
    _jspx_imports_classes.add("java.util.Map");
    _jspx_imports_classes.add("com.ragic.s3.nui.helper.Encoder");
    _jspx_imports_classes.add("com.ragic.s3.util.doc.DocCache");
    _jspx_imports_classes.add("org.json.JSONArray");
  }

  private volatile javax.el.ExpressionFactory _el_expressionfactory;
  private volatile org.apache.tomcat.InstanceManager _jsp_instancemanager;

  public java.util.Map<java.lang.String,java.lang.Long> getDependants() {
    return _jspx_dependants;
  }

  public java.util.Set<java.lang.String> getPackageImports() {
    return _jspx_imports_packages;
  }

  public java.util.Set<java.lang.String> getClassImports() {
    return _jspx_imports_classes;
  }

  public javax.el.ExpressionFactory _jsp_getExpressionFactory() {
    if (_el_expressionfactory == null) {
      synchronized (this) {
        if (_el_expressionfactory == null) {
          _el_expressionfactory = _jspxFactory.getJspApplicationContext(getServletConfig().getServletContext()).getExpressionFactory();
        }
      }
    }
    return _el_expressionfactory;
  }

  public org.apache.tomcat.InstanceManager _jsp_getInstanceManager() {
    if (_jsp_instancemanager == null) {
      synchronized (this) {
        if (_jsp_instancemanager == null) {
          _jsp_instancemanager = org.apache.jasper.runtime.InstanceManagerFactory.getInstanceManager(getServletConfig());
        }
      }
    }
    return _jsp_instancemanager;
  }

  public void _jspInit() {
  }

  public void _jspDestroy() {
  }

  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)
      throws java.io.IOException, javax.servlet.ServletException {

    final java.lang.String _jspx_method = request.getMethod();
    if (!"GET".equals(_jspx_method) && !"POST".equals(_jspx_method) && !"HEAD".equals(_jspx_method) && !javax.servlet.DispatcherType.ERROR.equals(request.getDispatcherType())) {
      response.sendError(HttpServletResponse.SC_METHOD_NOT_ALLOWED, "JSPs only permit GET POST or HEAD");
      return;
    }

    final javax.servlet.jsp.PageContext pageContext;
    javax.servlet.http.HttpSession session = null;
    final javax.servlet.ServletContext application;
    final javax.servlet.ServletConfig config;
    javax.servlet.jsp.JspWriter out = null;
    final java.lang.Object page = this;
    javax.servlet.jsp.JspWriter _jspx_out = null;
    javax.servlet.jsp.PageContext _jspx_page_context = null;


    try {
      response.setContentType("text/html; charset=UTF-8");
      pageContext = _jspxFactory.getPageContext(this, request, response,
      			null, true, 8192, true);
      _jspx_page_context = pageContext;
      application = pageContext.getServletContext();
      config = pageContext.getServletConfig();
      session = pageContext.getSession();
      out = pageContext.getOut();
      _jspx_out = out;



  RagicBundle bundle = (RagicBundle) request.getAttribute("RAGIC_BUNDLE");
  if (bundle == null) {
    bundle = (RagicBundle) session.getAttribute("RAGIC_BUNDLE");
  }
  User user = (User) session.getAttribute("USER");

  String originalUrl = (String) request.getAttribute("ORIGINAL_URI");
  String queryTerm = request.getParameter("q");
  String searchFrom = request.getParameter("searchFrom"); //if users search from start page, we direct the page to course
  String langv5 = bundle.getString("langv5");

  if (queryTerm != null) {
  
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "doc_search.jsp" + "?" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("searchfrom", request.getCharacterEncoding())+ "=" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode(String.valueOf(searchFrom!=null?searchFrom:"course"), request.getCharacterEncoding()), out, false);

    return;
  }


  Map<String,String> majorTypeOptions = new LinkedHashMap<>();
  Map<String,String> useCasesTypeOptions = new LinkedHashMap<>();

  for (int i = 0; i < 3; i++) {
    String msgKey = "course_majorType" + (i + 1);
    String enType = Const.BUNDLE_EN.getString(msgKey);
    majorTypeOptions.put(enType,bundle.getString(msgKey));
  }
  for (int i = 0; i < 15; i++) {
    String msgKey = "course_useCase" + (i + 1);
    String enType = Const.BUNDLE_EN.getString(msgKey);
    useCasesTypeOptions.put(enType, bundle.getString(msgKey));
  }
  
  
  int id = -1;
  String majorTypeQuery = "", useCaseQuery = "";
  String pageTitle = "";
  String[] urlsec = originalUrl.split("/");
  if (urlsec.length > 4) {
    id = Encoder.parseInt(urlsec[4], -1);
    
    if (id < 0) {
      majorTypeQuery = Util.decodeSEOUrl(urlsec[4]);
      if (majorTypeQuery.equalsIgnoreCase("use cases")) {
        if (urlsec.length > 5) useCaseQuery = Util.decodeSEOUrl(urlsec[5]);
        if (useCaseQuery.isEmpty()) majorTypeQuery = "required courses";
      }
    }
  }
  
  if (id < 0 &&
          !majorTypeQuery.equalsIgnoreCase("required courses") &&
          !majorTypeQuery.equalsIgnoreCase("elective courses") &&
          !majorTypeQuery.equalsIgnoreCase("use cases")) {
    majorTypeQuery = "required courses";
  }
  
  JSONObject courseArticleJo = null;
  JSONArray courseList = null;

  DocCache docCache = DocCache.getInstance("course", langv5);

  if (id >= 0) {
    //specified article id
    id = Encoder.parseInt(urlsec[4], -1);

    Article article = docCache.getArticle(id);
    if(article.getArticleType() != ArticleType.COURSE) return;
    CourseArticle course = (CourseArticle)article;
    if (course.isPublic()) {
      courseArticleJo = courseObject(course);
      courseArticleJo.put("majorType", course.getMajorType());
      courseArticleJo.put("useCaseType", course.getUseCaseType());
      request.setAttribute("course", courseArticleJo);
    }
  }
  else {
    courseList = new JSONArray();
    for(Article article : docCache.getAricleCellection()){
      if(article.getArticleType() != ArticleType.COURSE) continue;
      CourseArticle course = (CourseArticle) article;
      if(!course.isPublic()) continue;
      if(!course.getMajorType().toLowerCase(Locale.ROOT).contains(majorTypeQuery.toLowerCase(Locale.ROOT))) continue;
      if (useCaseQuery.length() > 0 &&
              !course.getUseCaseType().toLowerCase(Locale.ROOT).contains(useCaseQuery.toLowerCase(Locale.ROOT))) continue;
      JSONObject jo = courseObject(course);
      jo.put("coverImg", course.getCoverImg());
      courseList.put(jo);
    }
    request.setAttribute("courseList", courseList);
  }

      out.write("<div class=\"kbBlock preload\">\r\n  <div class=\"kbContainer\" style=\"display:flex;border:1px solid #d0d0d0;\">\r\n    <div class=\"kbFloatingBarsIcon\" onclick=\"showKbSidebar();\" id=\"showKbSidebarIcon\" style=\"\"><i class=\"fas fa-bars\" style=\"font-size:20px;\"></i></div>\r\n    <div class=\"kbFloatingBarsIcon\" onclick=\"hideKbSidebar();\" id=\"hideKbSidebarIcon\" style=\"display:none;\"><i class=\"fas fa-times\" style=\"font-size:20px;\"></i></div>\r\n    <!-- sidebar -->\r\n    <ul class=\"kbSidebar majorTypesWrapper\" id=\"kbSidebar\" onclick=\"toggleKbSidebarCategory(event);\">\r\n      ");

        for (Map.Entry<String, String> entry : majorTypeOptions.entrySet()) {
          String majorKey = entry.getKey();
          String majorTypeTrans = entry.getValue();
          boolean isMajorTypeSelected = majorTypeQuery.length() > 0 && majorKey.toLowerCase().contains(majorTypeQuery) ||
                      courseArticleJo != null && courseArticleJo.optString("majorType").contains(majorKey);
          
          if (majorKey.equals("Required Courses") || majorKey.equals("Elective Courses")) {
            String url = "/intl/" + langv5 + "/course/" + Util.createSEOUrl(majorKey.toLowerCase(), null);
            out.println("<li class='majorType' style='padding-left:20px;'><a href='" + url + 
                    "' class='typeText" + (isMajorTypeSelected ? " selectedArticle" : "") + "'>" + majorTypeTrans + "</a>");
            
            if (isMajorTypeSelected) {
              if (courseArticleJo == null) pageTitle = majorTypeTrans;
            }
          }
          else if (majorKey.equals("Use Cases")) {
            out.println("<li class='majorType'><i class='fas fa-chevron-right" + (isMajorTypeSelected?" down":"") +
                    "'></i><span class='typeText" + (isMajorTypeSelected && courseArticleJo != null ? " selectedArticle" : "") + "'>" + majorTypeTrans + "</span>");
            
            out.println("<ul class='articlesWrapper useCasesWrapper' style='padding-top:5px;padding-left:20px;" + (isMajorTypeSelected?"":"display:none;") + "'>");
            for (Map.Entry<String, String> useCase : useCasesTypeOptions.entrySet()) {
              String useCaseTrans = useCase.getValue();
              String useCaseEn = useCase.getKey();
              boolean isUseCaseSelected = useCaseQuery.length() > 0 && useCaseEn.toLowerCase().contains(useCaseQuery) && courseArticleJo == null;
              String url = "/intl/" + langv5 + "/course/" + Util.createSEOUrl(majorKey.toLowerCase(), null) + "/" + Util.createSEOUrl(useCaseEn.toLowerCase(), null);
              out.println("<li><a href='" + url + "'" + (isUseCaseSelected ? " class='selectedArticle'" : "") + ">" + useCaseTrans + "</a></li>");
              
              if (isUseCaseSelected && courseArticleJo == null) {
                pageTitle = useCaseTrans;
              }
            }
            out.println("</ul></li>");
          }
        }
        
        String articleContainerClass = courseList != null ? " courseListContainer" : "";
        if (courseList != null) articleContainerClass = " courseListContainer";
        
        request.setCharacterEncoding("UTF-8");
      
      out.write("</ul>\r\n    \r\n    <!-- article content -->\r\n    <div id=\"kbArticleContainer\" class=\"kbArticleContainer");
      out.print(articleContainerClass);
      out.write("\">\r\n      ");
      org.apache.jasper.runtime.JspRuntimeLibrary.include(request, response, "course_content.jsp" + "?" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("id", request.getCharacterEncoding())+ "=" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode(String.valueOf(id), request.getCharacterEncoding()) + "&" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode("title", request.getCharacterEncoding())+ "=" + org.apache.jasper.runtime.JspRuntimeLibrary.URLEncode(String.valueOf(pageTitle), request.getCharacterEncoding()), out, false);
      out.write("</div>\r\n    \r\n    ");
 if (courseArticleJo != null) { 
      out.write("<!-- table of contents (copy from docSidebar by js) -->\r\n    <div id=\"kbArticleTocContainer\" class=\"kbArticleTocContainer\">\r\n      <ul id=\"kbArticleToc\">\r\n      </ul>\r\n    </div>\r\n    ");
 } 
      out.write("</div>\r\n</div>\r\n\r\n\r\n<script>\r\nfunction toggleKbSidebarCategory(event) {\r\n  const target = event.target;\r\n  if (!target.classList.contains('fa-chevron-right') && !target.classList.contains('typeText') &&\r\n          !target.classList.contains('majorType') || target.tagName === 'A') {\r\n    return;\r\n  }\r\n\r\n  const parent = target.classList.contains('majorType') ? target : target.parentElement;\r\n  const arrowIcon = target.classList.contains('fa-chevron-right') ? target : parent.querySelector('.fa-chevron-right');\r\n  if (!arrowIcon) return;\r\n  let listElement = parent.querySelector('ul');\r\n  if (arrowIcon.classList.contains('down')) {\r\n    arrowIcon.classList.remove('down');\r\n    if (listElement) listElement.style.display = 'none';\r\n  }\r\n  else {\r\n    arrowIcon.classList.add('down');\r\n    if (listElement) listElement.style.display = '';\r\n  }\r\n}\r\n\r\nwindow.addEventListener('load', function() {\r\n  const bodyWidth = document.body.clientWidth || document.documentElement.clientWidth;\r\n  if (bodyWidth > 800) {\r\n    createTocOfArticle();\r\n");
      out.write("  }\r\n  \r\n  const preloadElements = document.querySelectorAll('.preload');\r\n  preloadElements.forEach(function(ele) {\r\n    ele.classList.remove('preload');\r\n  });\r\n  \r\n});\r\n\r\n\r\n</script>\r\n");
    } catch (java.lang.Throwable t) {
      if (!(t instanceof javax.servlet.jsp.SkipPageException)){
        out = _jspx_out;
        if (out != null && out.getBufferSize() != 0)
          try {
            if (response.isCommitted()) {
              out.flush();
            } else {
              out.clearBuffer();
            }
          } catch (java.io.IOException e) {}
        if (_jspx_page_context != null) _jspx_page_context.handlePageException(t);
        else throw new ServletException(t);
      }
    } finally {
      _jspxFactory.releasePageContext(_jspx_page_context);
    }
  }
}
